# نقشه‌راه اصلاحات ریفرش — وضعیت کارها

> این فایل رو داخل پروژه نگه دار (پیشنهاد: `ROADMAP.md` توی ریشه‌ی پروژه، کنار `package.json`).
> هر بار که با هوش مصنوعی (من یا هرکس دیگه) کار جدید شروع می‌کنی، این فایل رو اول بفرست تا لازم نباشه کل چت قبلی رو توضیح بدی.

## ⚠️ یادداشت ادغام (این دور)
فایل همکار جدا از فایل من پیش رفته بود — همکار مستقل چندتا از همین آیتم‌های لیست جدید رو خودش پیاده کرده بود (با جزئیات پیاده‌سازی متفاوت)، در حالی که من هم موازی روشون کار کرده بودم. این دو تا رو با دقت ادغام کردم؛ جاهایی که همکار زودتر/کامل‌تر پیاده کرده بود، نسخه‌ی خودش نگه داشته شد و کار من روش اضافه شد (نه جایگزین). موارد مهم همکار که تازه توی این فایل دیدم:
- سیستم «هاید از کاتالوگ» (اسم فیلدش `hiddenFromCatalog` هست، نه اونی که من اول انتخاب کرده بودم — یکی شد)
- کولپس/اکسپند سشن‌های قفل‌شده‌ی متریال (فیچر جدید، توی رودمپ نبود)
- فیلتر چندانتخابی وضعیت محصول (موجود/تسویه‌نشده/تسویه‌شده) توی کاتالوگ
- بج هدیه با آیکون (به‌جای ایموجی)
- فیکس: پرتی فقط برای متریال مساحتی/فرش حساب بشه

**آخرین آپدیت:** ۱۴۰۵/۰۴/۳۰ — تقویم گرافیکی، سیستم «نوع محصول» (کلاینت+SQL)، و ۷ فیکس سریع دیگه؛ لیست بزرگ جدید هم اضافه شد (پایین صفحه)

## 🎨 راهنمای رنگ همکارهای این دور (۴ نشست موازی)
🟡 = Ash &nbsp;|&nbsp; 🟠 = Logy &nbsp;|&nbsp; 🟣 = Wall &nbsp;|&nbsp; ⚫ = Ref
هرکس قبل از شروع یه آیتم، اگه رنگ دیگه‌ای جلوش نبود، با رنگ خودش علامتش می‌زنه تا دوباره‌کاری نشه. آیتم‌های کامل‌شده با ✅ + رنگ نشون داده می‌شن.

**لاگ نشست Logy 🟠 (۱۴۰۵/۰۵/۰۴):** بخش ۱۱ (رجیستر شدن مودال گالری/مشتری/کارت‌ویزیت برای بلاک‌کردن سوایپ) کامل شد. بخش ۸: دو زیرآیتم فیلتر «برگرده» بررسی شد و معلوم شد از قبل کامل بودن (فقط تیک نخورده بودن)؛ زیرآیتم پاپ‌آپ‌های خارج‌از-کادر رو عمداً دست نزدم چون بزرگ‌تر از یه فیکس سبکه (جزئیات توی خودِ بخش ۸).

**لاگ نشست Logy 🟠 (نشست بعدی):** بخش ۳ (تأیید خروج) رو شروع کرده بودم ولی قبل از push دیدم Wall 🟣 نسخه‌ی کامل‌تری (با فیکس Back سخت‌افزاری هم) رو زودتر push کرده بود، پس کار خودم رو کنار گذاشتم و نسخه‌ی Wall موند. همین برای دو زیرآیتم بخش ۵ (حذف نوشته‌ی ۱۰۰٪ / جابه‌جایی دکمه‌های پرتی) با Ref ⚫ هم پیش اومد. بعدش بخش ۶ (یکپارچه‌سازی تمپلیت فاکتور) رو بررسی کردم — از قبل کامل یکپارچه بود، تیک خورد.

**لاگ نشست Logy 🟠 (این نشست):** یه دور روی آخر فایل (دامپ خام گزارش کاربر) گشتم دنبال آیتم‌های بازِ کم‌ریسک. فرمت دسته‌بندی فرش (نام دقیق‌ترش پایین همین فایل، بخش «دسته‌بندی محصولات بر اساس فرش») کامل شد. بقیه‌ی آیتم‌های باز رو **عمداً دست نزدم** چون هرکدوم یکی از این‌هاست: نیاز به اسکرین‌شات/عکس مرجعی داره که توی این نشست برام نیومد (مثلاً جهت مایتر ۱D، ارتفاع دکمه‌های فیلتر/اکسپند)، مستقیم روی منطق حسابداری زنده اثر می‌ذاره و بدون تست واقعی رو گوشی ریسکش بالاست (مثل پرتی معلق/قفل، apply چندبچ همزمان)، یا از قبل به‌صراحت «سنگین/بررسی‌نشده» یا claim‌شده توسط رنگ دیگه (Wall) علامت خورده بود. اگه عکس مرجع این موارد رو داری، بفرست تا نشست بعد روشون کار کنم.

## ⚠️ گزارش ادغام (خیلی مهم، حتماً بخون)

۴ فایل zip از همکارهای مختلف اومد که رو هم کار کرده بودن (App.jsx توی هر ۴تاشون بود!). این‌کارو کردم:

- **`woodCuttingCore.js`, `NestingVisualizer.jsx`, `WoodCuttingTab.jsx`**: از همکار «ب» گرفته شد (بدون تعارض، تنها کسی بود که روشون کار کرده بود)
- **`dataModels.js`, `utils/swipeTabs.js`, `server.ts`, `utils/backButton.js`, `utils/formNav.js`, `utils/syncManager.js`**: از همکار «د» گرفته شد (کارش کامل‌تر/جدیدتر از همکار «ج» بود که دقیقاً همون کارو با جزئیات کمتر انجام داده بود)
- **`App.jsx`, `MaterialTab.jsx`, `ProductTab.jsx`, `SyncTab.jsx`**: پایه از همکار «د» گرفته شد (کامل‌ترین)، فیکس پسورد (`2744`+`7539`) از همکار «ب» روش پورت شد
- **`InvoicePrint.jsx`, `GalleryTab.jsx`**: نسخه‌ی **خودم** (از نشست قبلی) نگه داشته شد چون جدیدتر از هرکدوم از همکارها بود؛ فقط یه تیکه‌ی مستقل از همکار «د» (اتصال دکمه‌ی Back به بستن پنل ویرایش مشتری در GalleryTab) روش اضافه شد
- **`package.json`**: `@capacitor/app` (برای دکمه‌ی Back) اضافه شد

### ⛔ چیزی که عمداً merge نکردم (نیاز به نشست جدا داره)
همکار «الف» (که همون همکار «ب» بود ولی یه قدم جلوتر) یه فیچر بزرگ اضافه کرده بود: **پیش‌نمایش گرافیکی رنگی اکسل** (بخش ۳۵) + ۳ شیت جدید توی اکسپورت معمولی (تجهیزات/لینک‌های کارگاه/کارت‌ویزیت‌ها، بخش ۳۴). خودش توی رودمپش نوشته بود «تست واقعی نشده». چون این یه تابع حدود ۲۵۰ خطیه که به‌شدت با نسخه‌ی «د» تداخل داره (که پایه‌مون شد)، و ریسک خراب‌کردن ادغام رو داشت، فعلاً کنار گذاشتمش. **اگه می‌خوای این فیچر رو هم برگردونیم، بگو تا با احتیاط جدا اضافه‌ش کنم.**


**فایل‌های اصلی پروژه که دستشون درازه:** `App.jsx`, `MaterialTab.jsx`, `ProductTab.jsx`, `AccountingTab.jsx`, `InvoicePrint.jsx`, `GalleryTab.jsx`, `WoodCuttingTab.jsx`, `woodCuttingCore.js`, `NestingVisualizer.jsx`, `nativeSave.js`, `AuthContext.jsx`, `firebase.js`, `main.jsx`, `capacitor.config.ts`

---

## ✅ کارهای کامل‌شده (قبل از این لیست بزرگ)
- ~~باگ نمایش مایتر (شکل ذوذنقه‌ای اشتباه) در تب برش چوب~~ → `woodCuttingCore.js`, `WoodCuttingTab.jsx`
- ~~دکمه‌های حذف فاکتور/بازگشت محصول در گالری کار نمی‌کرد (raw `confirm()` در اپ موبایل کار نمی‌کنه)~~ → `GalleryTab.jsx`
- ~~ذخیره‌ی عکس/PDF/اکسل/JSON در APK کار نمی‌کرد (نیاز به `@capacitor/filesystem` + `@capacitor/share`)~~ → `nativeSave.js`, `App.jsx`, `ProductTab.jsx`, `InvoicePrint.jsx`, `WoodCuttingTab.jsx`
- ~~نمایش ۱D/۲D روی موبایل بزرگ‌تر از صفحه بود / نیاز به اسکرول~~ → `WoodCuttingTab.jsx` (`FitToWidth`), `NestingVisualizer.jsx`
- ~~فونت توی APK یه‌واحد کوچیک‌تر از پریویو بود~~ → `main.jsx` (`@capacitor/text-zoom`)
- ~~Google Sign-In خطای `invalid_request` می‌داد~~ → `AuthContext.jsx`, `firebase.js`, `capacitor.config.ts` (تبدیل به `@capacitor-firebase/authentication`)
- ~~راه‌اندازی Capacitor/Gradle/JDK/Firebase از صفر برای گرفتن APK~~ → مستندسازی شده، پروژه appId نهایی: `com.refarsh.studio`

---

## 🔧 لیست بزرگ فعلی (۱۰ بخش) — وضعیت هرکدوم

### ۱) متریال (بولک/پرتی/کش/قفل) — **✅ تکمیل شد**
فایل‌ها: `App.jsx`, `MaterialTab.jsx`
- [x] باگ اصلی: نوع «فرش» درصد انتخابی کاربر رو نادیده می‌گرفت و از مساحت خام بازمحاسبه می‌کرد → در `materialPendingUsage` (App.jsx) درست شد
- [x] چک‌باکس «پرتی حساب شود» فقط برای بچ رسمی نشون داده می‌شد → الان برای هر متریال مساحتی/فرش (با یا بدون بچ) هست
- [x] منطق توزیع پرتی برای حالت بدون‌بچ اصلاً وصل نبود → با مفهوم «بچ ضمنی» در `areaBatchCostByProduct` وصل شد
- [x] کش: انلینک→ری‌لینک درصد قبلی رو حفظ کنه → `material.lastAllocationPcts`
- [x] سقف ۱۰۰٪: نباید بشه بیشتر از موجودی خرج کرد → اعتبارسنجی در `bulkApplyMaterial`
- [x] آیتم قفل‌شده دست‌نخورده بمونه → چک شد، از قبل درست بوده
- [x] **باقی‌مونده:** فیلد «تعداد» توی ویرایش مستقیم متریال نیست — تکمیل شد؛ بلوک «موجودی فعلی (تعداد)» که قبلاً فقط برای نسبتی/ابزار بود، حالا برای مساحتی/فرش هم نمایش داده می‌شه (چون `totalQty`/`remainingQty` روی این انواع هم استفاده می‌شه، طبق منطق قفل/آزادسازی توی `App.jsx`)
- [x] «پیش‌فرض تعداد بولک باید کل باشه» — **✅ باگ واقعی پیدا و فیکس شد ⚫ (Ref).** درصد پیش‌فرض (۱۰۰٪) از قبل درست بود، ولی خودِ فیلد «تعداد» که این رو به یه عدد ملموس ترجمه می‌کنه، مبناش `material.totalQty` (کل تاریخیِ همیشه‌خریداری‌شده) بود، نه `remainingQty` (چیزی که *الان* واقعاً در دسترسه). برای متریالی که بخشی‌ش قبلاً مصرف/قفل شده بود، عدد پیش‌فرض/حداکثرِ «تعداد» بیشتر از واقعیت نشون داده می‌شد. الان از `remainingQty` (وقتی موجود باشه) استفاده می‌کنه

### ۲) رفتار فیلدهای عددی پیش‌فرض (کلیک=پاک‌شدن) — **✅ تکمیل شد**
فایل: `ProductTab.jsx`
- [x] فیلد «درصد فرش لینک‌شده» (`fabricCoveragePct`): باگ `0 || 100` بود — پاک‌کردن فوراً برمی‌گشت رو ۱۰۰٪. اصلاح شد، الان می‌شه پاک کرد و عدد جدید نوشت
- [x] فیلد «تعداد مصرفی» (`consumedCount`) هم همین باگ رو داشت (`|| 1`)، اصلاح شد
- بررسی شد: `MaterialTab.jsx` و `WoodCuttingTab.jsx` این باگ رو نداشتن، جای دیگه‌ای تکرار نشده بود

### ۳) تخفیف ۱۰۰٪ = هدیه — **بخش محاسباتی ✅ / بخش نمایش فاکتور ⏳ (جزو بخش ۲۴-۲۶)**
فایل‌ها: `ProductTab.jsx`, `AccountingTab.jsx`
- [x] باگ اصلی پیدا شد: `p.discountedPrice || p.salePrice` — چون ۰ در جاوااسکریپت falsy است، وقتی تخفیف ۱۰۰٪ بود (discountedPrice=0)، به‌جای صفر، قیمت کامل نمایش داده می‌شد. با `??` جایگزین شد (۲ جا در ProductTab.jsx)
- [x] همین باگ دقیقاً در `AccountingTab.jsx` (فیلتر لیست فاکتورها، خط ۱۳۳۱) هم بود، اصلاح شد
- [x] باکس صورتی «🎁 هدیه» در وضعیت تسویه‌ی فاکتور — بررسی شد، از قبل کامل توی `InvoiceTemplate.jsx` بود (باکس صورتی `#fce4ec` دقیقاً توی ستون وضعیت، برای `discountPct >= 100`)؛ `InvoicePrint.jsx` هم همینو داخلش رندر می‌کنه (wrapper مودال چاپه، خودش جدول نداره)
- ⚠️ نکته: قسمت «۱۰۰ رو قبول نمی‌کنه، ۹۹ رو قبول می‌کنه» رو نتونستم توی کد پیدا کنم (هیچ `maxLength` یا محدودیتی روی فیلد درصد تخفیف نیست) — با اصلاح باگ بالا شاید خودش حل شده باشه؛ اگه بازم دیدی، یه اسکرین‌شات از لحظه‌ی تایپ‌کردن بفرست

### ۴) حسابداری تخفیف رو حساب نمی‌کرد — **✅ تکمیل شد**
فایل: `App.jsx`
- [x] پیدا شد: در چند جای کلیدی (`revenueSold`, `totalOutstanding`, `settledOnlyRevenue`, نمودار ماهانه `monthlyMap`) درآمد از روی `salePrice` خام حساب می‌شد، نه قیمت نهایی بعد از تخفیف. یک تابع مشترک `finalPriceOf(p)` اضافه شد که همه‌جا از `discountedPrice` (اگه ست شده) استفاده می‌کنه
- [x] همین باگ در ۳ جای `AccountingTab.jsx` (لیست فاکتورها و جزئیات هرکدوم) هم بود، اصلاح شد
- توجه: `projectedRevenue`/`projectedCost` (برای محصولات هنوز فروخته‌نشده) عمداً دست‌نخورده موند چون تخفیف هنوز نهایی نشده

### ۵) کارت ویزیت QR کد — **✅ تکمیل شد**
فایل: `BusinessCardModal.jsx`
- [x] QR کد الان قابل لمس/زوم است — به همون سیستم Lightbox که برای عکس‌های آرشیو از قبل بود وصل شد
- [x] (اضافه دیدم و درست کردم) شماره واتساپ توی همین صفحه فاصله‌گذاری نداشت و بدون RTL درست نمایش داده می‌شد — اصلاح شد

### ۶) متن فاکتورها (پرانتز/جنسیت/انبار/فروش مستقیم) — **✅ تکمیل شد**
فایل: `InvoiceTemplate.jsx`
- [x] بررسی شد: این بخش (فرمت گالری/مشتری/فروش‌مستقیم با جنسیت درست) از قبل توسط اون یکی نفر پیاده‌سازی شده بود، فقط عبارت‌ها رو دقیق‌تر با خواسته‌ی تو تنظیم کردم: «همکار محترم» → «همکار گرامی»، و پرانتز گالری از `(به مدیریت جناب آقای ...)` به فرمت دقیق `(جناب آقای/سرکار خانم نام مالک)` ساده شد
- [x] ⚠️ **یک باگ ساختاری جدی و از قبل موجود پیدا و رفع شد:** یک `}` اضافی کل تابع کامپوننت `InvoiceTemplate` رو زودتر از موقع می‌بست — یعنی این فایل اصلاً build نمی‌شد و کل صفحه‌ی فاکتور با خطا کرش می‌کرد. این ربطی به کار من نداشت، از تغییرات ناتمام قبلی مونده بود

### ۷) دکمه‌های فاکتور در سبد خرید گالری + دکمه Share تکراری — **✅ تکمیل شد**
فایل: `ProductTab.jsx`, `InvoicePrint.jsx`
- [x] دکمه‌ی «پیش‌نمایش فاکتور» توی سبد خرید فقط `mode==="sale"` نشون داده می‌شد؛ این شرط برداشته شد و توی سبد ارجاع‌به‌گالری هم نشون داده می‌شه
- [x] فیلد `isAvailableInGallery` که موقع ساخت invoiceData برای همین حالت جا افتاده بود اضافه شد (وگرنه آیتم‌های ارجاعی به‌جای «موجود در گالری» به‌اشتباه «فروخته‌شده» نشون داده می‌شدن)
- [x] دکمه‌ی Share کنار ذخیره‌عکس/PDF حذف شد

### ۸) گوگل سینک کار نمی‌کنه + حذف کل دیتابیس کار نمی‌کنه — **✅ ریشه‌یابی و اصلاح شد**
فایل‌ها: `SyncTab.jsx`, `App.jsx`، `server.ts`/بک‌اند، `utils/syncManager.js`، `dataModels.js`
- [x] **علت اصلی پیدا شد:** سرور (`server.ts`) از قبل یک endpoint کامل و درست داشت (`POST /api/sync`، با احراز هویت توکن Firebase و fallback خودکار به یک دیتابیس محلی JSON وقتی Firestore روی پروژه provision نشده)، ولی کلاینت (`syncManager.js`) هیچ‌وقت این مسیر رو صدا نمی‌زد — به‌جاش مستقیم از مرورگر به Firestore وصل می‌شد، بدون هیچ fallback ای. اگه Firestore پروژه فعال نبود (خیلی محتمل)، این یعنی سینک همیشه ساکت fail می‌کرد.
- [x] `performSynchronization` در `syncManager.js` بازنویسی شد تا واقعاً `POST /api/sync` رو با هدر `Authorization: Bearer <token>` صدا بزنه (دقیقاً همون endpoint که `/api/sync/status` هم قبلاً تستش می‌کرد)
- [x] دکمه‌ی «حذف کل دیتابیس»: هم باگ منطقی داشت (بعد از ریست، `setData` فقط ۵ از ۸ فیلد دیتای برنامه رو خالی می‌کرد — `myBusinessCard`, `businessCards`, `woodCuttingSessions` دست‌نخورده/`undefined` می‌موند و می‌تونست باعث کرش بشه)، هم مسیر اشتباه (حذف دستی از Firestore کلاینت) به‌جای endpoint آماده‌ی `/api/sync/reset` استفاده می‌کرد. هر دو اصلاح شد؛ `dataModels.js` هم یک تابع `getDefaultData()` اکسپورت جدید داره که همه‌جا برای ریست کامل استفاده می‌شه
- ⚠️ **نیاز به تست واقعی:** این اصلاح بر اساس ریشه‌یابی کد انجام شده (سرور بدون دیتابیس واقعی/توکن قابل اجرا نبود اینجا) — لطفاً یک‌بار روی گوشی/APK با لاگین گوگل تست کن؛ اگه بازم پیام خطا داد، متن دقیق پیام قرمزی که زیر دکمه‌ی سینک میاد رو بفرست (الان از قبل نمایش داده می‌شه، فقط قبلاً کسی بهش دقت نکرده بود)

### ۹) رفتار عمومی تب‌ها (اسکرول بالا، پوزیشن باز شدن) — **✅ تکمیل شد**
فایل: `App.jsx` + `utils/scrollToTop.js` (جدید) + `MaterialTab.jsx`, `GalleryTab.jsx`, `ProductTab.jsx`, `AccountingTab.jsx`, `InvoicesTab.jsx`
- [x] منطق اسکرول-به-بالا (که قبلاً فقط روی سوییچ تب اصلی کار می‌کرد) به یک تابع مشترک `scrollAppToTop()` منتقل شد
- [x] حالا علاوه بر سوییچ تب، باز/بسته‌شدن پنل مدیریت هم اسکرول رو صفر می‌کنه
- [x] اکسپندشدن ردیف‌ها (متریال، محصول، فاکتور در گالری/حسابداری/تب فاکتورها) و باز شدن پنل «لیست فاکتورهای مشتری» در گالری هم حالا هنگام باز شدن (نه بسته شدن) اسکرول رو به بالا می‌برن

### ۱۰) تاریخ/شماره تماس فاکتور + تقویم شمسی — **✅ تکمیل شد**
فایل‌ها: `InvoicePrint.jsx`, `InvoiceTemplate.jsx`, `AccountingTab.jsx`, `InvoicesTab.jsx`
- [x] بررسی شد: تقویم شمسی گرافیکی (`JalaliDatePicker`, سه‌تا dropdown روز/ماه/سال) از قبل همه‌جا (فاکتور، بچ متریال، فرم‌های ویرایش) جایگزین فیلد دستی شده بود؛ هیچ فیلد ۳تایی متنی دیگه‌ای توی کد پیدا نشد
- [x] **باگ واقعی پیدا و اصلاح شد:** توی فرم ویرایش فاکتور (هم `AccountingTab.jsx` هم `InvoicesTab.jsx`)، وقتی شماره تلفن خریدار رو برای یک مشتریِ از قبل ثبت‌شده عوض می‌کردی، توی فاکتور چاپی/پیش‌نمایش دوباره شماره‌ی قدیمیِ ثبت‌شده روی پروفایل مشتری نشون داده می‌شد (اولویت اشتباه بین دیتای خودِ فاکتور و دیتای مشتری). اولویت برعکس شد؛ الان شماره‌/آدرس/جنسیتِ ثبت‌شده روی خودِ فاکتور اولویت داره، و اگه مشتری از قبل موجود بود، رکورد مشتری هم با شماره‌ی جدید آپدیت می‌شه تا فاکتورهای بعدی هم سینک بمونن

### 🔑 رمز پنل مدیریت
- [x] هم `2744` هم `7539` قبول می‌شه — بررسی شد، از قبل توی `VALID_PIN_CODES` هردو بودن
- [x] (پرسیده شد) امکان باز کردن پنل با اثرانگشت/فیس‌آی‌دی هم خوبه ولی پیش‌نیازش نامشخصه — **✅ توسط Logy پیاده‌سازی شد** (`@aparajita/capacitor-biometric-auth`)؛ متن دکمه هم این نشست به «ورود با اثرانگشت» (بدون «/فیس») ساده‌تر شد

---

## 🆕 دسته‌ی جدید فیچرها (پیام بزرگ آخر)

### ۱۱) متریال — بچ‌های چندگانه‌ی کامل — **✅ کامل شد (این نشست + نشست قبل)**
فایل: `MaterialTab.jsx`, `App.jsx`, `dataModels.js`
- [x] هر متریال بتونه چند بچ ثبت‌شده داشته باشه، هر بچ مستقل قابل ویرایش/حذف: قبلاً این فقط برای نوع «مساحتی» (`batches`) و «خطی» (`sticks`) درست کار می‌کرد؛ برای «نسبتی»/«ابزار»/«فرش» فقط یه لیست فقط‌خواندنی (`تاریخچه خرید`) بود که می‌شد بهش خرید اضافه کرد ولی هیچ ردیفی قابل ویرایش/حذف مستقل نبود. الان همون لیست به یه کارت اکسپند/کولپس‌شونده تبدیل شده — هر خرید (`procurement`) با تپ باز می‌شه (مبلغ/تعداد/تاریخ قابل ویرایش) و دکمه‌ی حذف مستقل داره؛ ویرایش/حذف هرکدوم به‌درستی روی استخر `totalCost`/`remainingCost`/`totalQty`/`remainingQty` متریال هم اثر می‌ذاره (فقط delta همون ردیف، نه کل استخر)
- [x] هر بچ بسته به نوع متریال، فیلد دیتیل مخصوص بگیره — مساحتی/فرش: ابعاد؛ خطی: متراژ+تعداد؛ نسبتی/ابزار: تعداد
- [x] اولین بچ = همون خریدی که موقع افزودن اولیه‌ی متریال ثبت می‌شه — برای مساحتی/خطی از قبل بود، برای فرش هم همین نشست اضافه شد؛ اون خرید اولیه هم `id` می‌گیره و بلافاصله قابل ویرایش/حذفه
- [x] لیست بچ‌ها قابل اکسپند/کولپس، با دکمه‌ی ویرایش/حذف جلوی هرکدوم — همه‌ی انواع
- [x] فرم تاریخ بچ با «تاریخچه خرید» یکی شد — هر ردیف تاریخچه‌ی خرید حالا خودش یک بچ کامل با تاریخ مستقل قابل‌ویرایشه
- **این نشست: نوع «فرش» هم به بچ‌های چندگانه (مثل مساحتی) تبدیل شد.** موقع بررسی متوجه شدم موتور اصلی حسابداری (`bulkApplyMaterial` و `areaBatchCostByProduct` توی `App.jsx`) از قبل جاهای مهم `mat.type === "area" || mat.type === "fabric"` رو یکسان حساب می‌کردن (احتمالاً کار یه نشست/همکار قبلی که تکمیل نشده بود) — فقط UI توی `MaterialTab.jsx` هنوز فرش رو به بچ محدود نمی‌کرد. کاری که کردم:
  - `isAreaType` توی صفحه‌ی بولک فرش رو هم شامل شد → انتخاب بچ موقع تخصیص به محصول برای فرش هم فعال شد
  - فرم «افزودن بچ» (با عرض/ارتفاع) برای فرش هم نشون داده می‌شه (به‌جای فرم ساده‌ی مبلغ/تعداد قبلی)
  - محاسبه‌ی `total`/`consumedCost`/`lockedCost` توی کارت متریال برای فرش هم مثل مساحتی از روی مجموع بچ‌ها حساب می‌شه
  - فرمول قدیمی درصد-بر-اساس-ابعاد-تکی فرش (`mat.dimW×mat.dimH`) فقط برای لاین‌آیتم‌هایی که به بچ وصل *نیستن* باقی موند (سازگاری کامل با محصولاتی که قبلاً بدون بچ به فرش وصل شده بودن)
  - متریال فرش تازه‌ساز هم از حالا یک بچ اولیه (با همون ابعاد ثبت‌شده) می‌گیره
- ⚠️ **یه مورد کوچیک دست‌نخورده موند:** توی `ProductTab.jsx` (لینک مستقیم متریال به یک محصول، مسیر جدا از صفحه‌ی بولک) درصد خودکار فرش هنوز از `linkedMat.dimW×dimH` (بُعد اولیه‌ی متریال) حساب می‌شه، نه از مجموع بچ‌ها — یعنی اگه بعداً بچ جدیدی با ابعاد متفاوت به یه فرش اضافه کنی، این عدد نمایشی (فقط نمایشی، روی حسابداری واقعی اثر نداره) ممکنه به‌روز نباشه. عمداً دست نزدم چون این مسیر جدا از موتور بولک/قفل‌شدنه و ریسکش با فایده‌اش الان نمی‌خوند؛ بگو اگه می‌خوای این‌ام هماهنگ بشه
- 🔴 **حتماً قبل از اعتماد کامل، با چند فرش واقعی که چند بار خرید/بچ دارن تست کن** — این تغییر مستقیم روی حسابداری قفل‌شدن پول اثر می‌ذاره

### ۱۲) صفحه‌ی بولک متریال — بازطراحی چیدمان — **✅ بررسی شد، از قبل درست بود**
فایل: `MaterialTab.jsx`
- [x] لیست محصولات قابل‌انتخاب باید بالای لیست «محصولات انتخاب‌شده» بیاد — چک کردم توی `BulkApplyMaterialPage`: بخش «محصولات موجود کارگاه برای افزودن» از قبل بالاتر از «محصولات در لیست تخصیص» رندر می‌شه
- [x] ترتیب لیست: قفل‌شده‌ها، بعد محصولات موجود در کارگاه، بعد لیست تازه‌تخصیص‌داده‌شده — ترتیب فعلی دقیقاً همینه: بخش «محصولات قفل‌شده (سشن‌های اکسپند/کولپس‌شونده)» → «محصولات موجود کارگاه» → «محصولات در لیست تخصیص» (که خودش اسلایدر درصد + دکمه‌ی حذف `X` جلوی هر محصول داره)
- [x] کنار سرچ محصولات، دکمه فیلتر بر اساس دسته‌بندی «فرش» (چندانتخابی) — از قبل پیاده‌سازی شده (دکمه‌ی «فرش» کنار جستجو، چندانتخابی با `fabricFilter`)
- این سه مورد ظاهراً توی یکی از merge های قبلی (احتمالاً همون کاری که همکار روی «کولپس/اکسپند سشن‌های قفل‌شده‌ی متریال» کرده بود) از قبل انجام شده بودن، فقط چک‌باکس‌های این فایل آپدیت نشده بود؛ کد تغییری نکرد، فقط تأیید شد

### ۱۳) همگام‌سازی — پوشه‌ی تصاویر — **✅ ریشه‌یابی و اصلاح شد**
فایل‌ها: `SyncTab.jsx`, `ProductTab.jsx`
- [x] **علت واقعی «خودکار کپی نشدن عکس» پیدا شد:** توی `ProductTab.jsx`، آپلود عکس محصول یک `Authorization: Bearer <token>` می‌فرستاد که از یک کلید `localStorage` به اسم `"refarsh_token"` می‌خوند — این کلید هیچ‌جای دیگه‌ی برنامه ست نمی‌شد (همیشه `null` بود)! یعنی هر آپلود عکس همیشه با خطای ۴۰۱ رد می‌شد و بی‌صدا می‌رفت روی حالت شکست (فقط اسم فایل ذخیره می‌شد، نه خودِ عکس — که باعث می‌شد عکس برای همیشه شکسته/نمایش‌ناپذیر بمونه). الان از توکن واقعی و تازه‌ی Firebase (همون که تب سینک استفاده می‌کنه) استفاده می‌کنه، و اگه بازم آپلود به سرور شکست بخوره، به‌جای فقط اسم فایل، خودِ عکس (فشرده‌شده) داخل دیتای برنامه ذخیره می‌شه تا حداقل آفلاین هم درست دیده بشه
- [x] **دکمه‌ی «پوشه تصاویر»:** علتش اینه که انتخاب گرافیکی پوشه (`webkitdirectory`) روی اندروید/اپ نصب‌شده اصلاً پشتیبانی نمی‌شه (فقط توی کروم دسکتاپ کار می‌کنه) — قبلاً وقتی روی گوشی می‌زدی هیچ اتفاقی نمی‌افتاد. الان اگه دستگاه ازش پشتیبانی نکنه، پیام واضح نشون می‌ده که مسیر رو دستی توی کادر کنارش (که از قبل وجود داشت و کار می‌کنه) وارد کنی

### ۱۴) گالری — کرش در اکسپند — **✅ ریشه‌یابی و اصلاح شد**
فایل‌ها: `App.jsx`, `ProductTab.jsx`, `BusinessCardModal.jsx` + `utils/imageCompress.js` (جدید)
- [x] **علت واقعی کرش پیدا شد:** وقتی از لیست محصولات (بدون باز کردن فرم ویرایش) روی محصول عکس/آیکون گذاشته می‌شد، عکسِ خامِ دوربین گوشی (که معمولاً ۵-۱۰ مگابایته) بدون هیچ فشرده‌سازی مستقیم به‌صورت base64 داخل دیتای برنامه ذخیره می‌شد. وقتی گالری‌ای که اون محصول توش بود اکسپند می‌شد، همه‌ی این عکس‌های خیلی سنگین با هم توی یک لیست رندر می‌شدن → روی اندروید (حافظه محدود WebView) کرش می‌کرد؛ از طرفی چون ذخیره‌سازی اصلی برنامه localStorage است (سقف ~۵-۱۰ مگابایت)، همین چندتا عکس خام می‌تونست کل ظرفیت رو پر کنه
- [x] یک تابع مشترک `compressImageFile()` ساخته شد که هر عکس رو قبل از ذخیره به حداکثر ۱۲۸۰ پیکسل و JPEG با کیفیت ۰.۷۵ فشرده می‌کنه (حجم از چند مگابایت به چند صد کیلوبایت می‌رسه، بدون افت محسوس کیفیت نمایش). این تابع روی هر سه نقطه‌ای که عکس خام ذخیره می‌شد وصل شد: آیکون سریع از لیست محصولات (`App.jsx`)، عکس داخل فرم ویرایش محصول در حالت آفلاین (`ProductTab.jsx`)، و عکس‌های کارت‌ویزیت (`BusinessCardModal.jsx` — کد QR از این قاعده مستثناست چون فشرده‌سازی JPEG می‌تونه غیرقابل‌اسکن بشه)
- [x] یک تکه کد یتیم/مرده هم توی `GalleryTab.jsx` پیدا و حذف شد: یک نسخه‌ی قدیمی و فراموش‌شده از منطق فیلتر که به متغیرهایی (`setExportFilters`, `isGallery`) اشاره می‌کرد که اصلاً توی اون بخش از کد وجود نداشتن — اگه یه روز اشتباهی صدا زده می‌شد، همون‌جا کرش می‌کرد

### ۱۵) محصول — آیکون به‌عنوان اولین عکس — **✅ تکمیل شد**
فایل: `App.jsx`
- [x] همون تابعی که آیکون سریع از لیست محصولات رو ست می‌کرد (`handleImageUpload` در `App.jsx`) قبلاً فقط فیلد `image` رو عوض می‌کرد و آرایه‌ی `images` (که فرم ویرایش محصول ازش برای نمایش/حذف عکس‌ها استفاده می‌کنه) دست‌نخورده می‌موند — یعنی آیکونی که از لیست گذاشته می‌شد نه توی ویرایش محصول دیده می‌شد، نه قابل حذف بود. الان همزمان به اول آرایه‌ی `images` هم اضافه می‌شه، دقیقاً مثل وقتی از خودِ فرم ویرایش عکس اضافه می‌شه

### ۱۶) گالری — فیلتر وضعیت (موجود/تسویه‌شده/تسویه‌نشده) — **✅ تکمیل شد**
فایل: `GalleryTab.jsx`
- [x] فیلترها از حالت چک‌باکس مستقل (که می‌شد همه‌شون رو هم‌زمان خاموش کرد و هیچی نمونه) به یک گروه تب انتخابی تبدیل شدن: همیشه دقیقاً یکی از «همه / موجود / تسویه‌نشده / تسویه‌شده» فعاله و با زدن هرکدوم، بقیه خودکار غیرفعال می‌شن

### ۱۷) متریال — جابه‌جایی دکمه‌های بولک/ویرایش/حذف — **✅ تکمیل شد**
فایل: `MaterialTab.jsx`
- [x] موقع اکسپند متریال، دکمه‌های بولک/ویرایش/حذف بالای لیست محصولات لینک‌شده منتقل شد

### ۱۸) کاتالوگ — گرید مربعی و ریسپانسیو — **✅ تکمیل شد**
فایل: `ProductTab.jsx` (`CatalogTab`)
- [x] تایل‌ها مربع شدن (`aspect-ratio: 1/1` به‌جای ارتفاع ثابت ۱۳۰px، عکس با `object-fit: cover` بدون افت کیفیت/کشیدگی)
- [x] گرید ریسپانسیو شد: موبایل (حالت portrait) = ۲تایی، landscape = ۳تایی، صفحه‌ی بزرگ/کامپیوتر (≥1280px) = نهایتاً ۴تایی

### ۱۹) برش ۱D — جهت مایتر یک‌طرف اشتباهه (مهم، عکس پیوست شده) — **بخشی تموم شد**
فایل: `woodCuttingCore.js` / `WoodCuttingTab.jsx`
- [ ] محاسبه‌ی عدد درسته، ولی سمت کجِ برش‌های یک‌طرفه باید رو به سمت دیگه (سمتی که باید صاف/راست بمونه، نه برعکس) کشیده بشه — **هنوز انجام نشده:** توی این پیام فایل عکسی که گفته بودی پیوست شده رو دریافت نکردم (فقط `ROADMAP.md` و زیپ پروژه اومد)، و این باگ کاملاً به جهت بصری وابسته‌ست، نه یه باگ عددی مشخص توی کد. اگه دوباره عکسو بفرستی (یا دقیق‌تر بگی کدوم لبه باید کج باشه)، انجامش می‌دم — **آپدیت کاربر:** فعلاً ولش کن، عکس نمی‌فرستم، اولویت پایین
- [x] موقع Save (تصویر خروجی)، برش‌های کج به اشتباه صاف کشیده می‌شدن → **علت پیدا شد:** کتابخونه‌ی `html2canvas` اصلاً از CSS `clip-path: polygon(...)` پشتیبانی نمی‌کنه، برای همین لحظه‌ی گرفتن عکس، مایترها صاف افتاده بودن. راه‌حل: درست قبل از گرفتن عکس، هر تکه‌ی مایترشده موقتاً با یه SVG واقعی (با همون زاویه، اندازه‌گیری‌شده از پیکسل واقعی روی صفحه) جایگزین می‌شه، بعد از گرفتن عکس هم فوراً به حالت اول برمی‌گرده — یعنی نمایش زنده دست‌نخورده می‌مونه و فقط خروجی عکس درست می‌شه
- [x] **باگ «سبزها بعد از قرمز/آبی می‌شینن و ۲.۵ سانت کنار لبه‌ی خام هدر می‌ره» — پیدا و اصلاح شد** (`optimizeCutting` در `woodCuttingCore.js`): وقتی چوب تازه باز می‌شه، هر دو لبه‌ی اون تکه به لبه‌ی خامِ چوب می‌خوره. تکه‌ی تک‌مایتر (سبز) یا بدون‌مایتر (قرمز) می‌تونه سمت *صافش* رو به این لبه بچسبونه (رایگان)، ولی تکه‌ی دومایتر (آبی) همیشه یه‌ضخامت اضافه از همون لبه هدر می‌ده چون هیچ سمت صافی نداره. قبلاً انتخاب اینکه کدوم تکه چوب تازه رو باز کنه فقط بر اساس طول (نزولی) بود، بدون توجه به این تفاوت. الان: صف تکه‌ها به یه لیست قابل‌تغییر تبدیل شد؛ درست قبل از باز کردن چوب جدید، اگه تکه‌ی فعلی دومایتر (آبی) بود و توی صف یه تکه‌ی تک‌مایتر/بدون‌مایتر منتظره که خودش هم جای دیگه‌ای جا نمی‌شه، اون یکی جلو می‌افته و چوب رو باز می‌کنه (این ۲.۵ سانت رو ذخیره می‌کنه)، و تکه‌ی آبی برای دور بعد برمی‌گرده. تست شبیه‌سازی‌شده با دقیقاً همون ترکیب چوب ۵ عکس (۱×۹۵ بدون‌مایتر + ۴×۱۹.۵ تک‌مایتر + ۲×۴۹ دومایتر، روی چوب ۳۰۰سانتی): مصرف واقعی چوب از ۲۷۰.۶ به ۲۶۳.۱ سانت کاهش پیدا کرد (تقریباً ۷.۵ سانت کمتر مصرف، از جمله همون ۲.۵ سانت لبه‌ی خام + جفت‌شدن بهتر باقی تکه‌ها)
- [x] **باگ «بین دو سبزِ کنار هم مثلث پرتی» — این مورد رو با تست شبیه‌سازی‌شده بررسی کردم و توی کد فعلی (قبل از این اصلاح هم) پیدا نکردم:** منطق `getPieceCostInBin` از قبل هر دو حالت (سمتِ صاف کنار سمتِ صاف = رایگان، یا سمتِ کج کنار سمتِ کج با جهت آینه‌ای = اتصال V بدون مثلث) رو به‌درستی امتحان و ارزون‌ترین رو انتخاب می‌کنه؛ توی شبیه‌سازی هیچ‌کدوم از این تراکنش‌ها مثلث پرتی تولید نکرد. اگه بعد از این اصلاح بازم توی اپ واقعی (بعد از build جدید) این مثلث رو دیدی، لطفاً همون عکس رو دوباره بفرست تا با دیتای دقیق خودش بررسی کنم — ممکنه از یه build قدیمی‌تر بوده باشه
- [x] **باگ «یه تکه‌ی ناهم‌نوع وسط بلوک تکه‌های هم‌نوع (مثلاً وسط چند دومایتر) گیر می‌افته و پرتی اضافه می‌سازه» — پیدا و اصلاح شد (Logy 🟠):** الگوریتم قبلی حریصانه بود و بعد از چیدن یه تکه هیچ‌وقت برنمی‌گشت جای اون رو عوض کنه، پس گاهی یه تکه (مثلاً تک‌مایتر) بین دو تکه‌ی دومایتر گیر می‌افتاد و زنجیره‌ی جفت‌شدن رایگان دومایترها رو قطع می‌کرد. یه پاس بازچینی محلی اضافه شد (`reorderBinForLessWaste` در `optimizeCutting`): بعد از چیدن حریصانه‌ی هر چوب، هر تکه‌ش تک‌تک امتحان می‌شه بره ابتدا یا انتهای همون چوب، و فقط اگه واقعاً پرتی کمتر بشه (و هنوز جا بشه) جایگزین می‌شه. تست شبیه‌سازی‌شده (۲ تکه‌ی دومایتر ۹۰+۴۵ب+یه تک‌مایتر ۵۵ روی چوب ۲۰۰): قبل از فیکس تک‌مایتر وسط می‌نشست، بعد از فیکس رفت لبه و پرتی از حالت وسط‌نشین کمتر شد — دقیقاً همون الگوی عکسی که کاربر فرستاد (تکه‌ی وسط با آبی قبل/بعدش)
- 🔴 لطفاً با build جدید دوباره چک کن، خصوصاً همون سناریوی عکس؛ اگه بازم یه تکه بی‌دلیل وسط بلوک هم‌نوع نشست (نه لبه)، عکس دقیق بفرست

### ۲۰) برش ۲D — ظاهر و رنگ‌بندی — **✅ تکمیل شد**
فایل: `NestingVisualizer.jsx`
- [x] پس‌زمینه‌ی پنل تیره‌تر شد (`#d8d8d8` → `#2a2a2a`)، رنگ آبی‌روشن ثابت قبلی (`#00CED1`) به‌روزتر شد (`#38BDF8`)
- [x] هر برش رنگ متفاوت می‌گیره: یه پالت ۱۲رنگ ملایم و مجزا (بدون قرمز، چون قرمز برای خطا/همپوشانی رزرو شده) تعریف شد و بر اساس اندازه‌ی هر تکه (طول×عرض) یه رنگ ثابت بهش اختصاص داده می‌شه — یعنی تکه‌های هم‌اندازه همیشه هم‌رنگن، برای اینکه چندرنگی شلوغ/گیج‌کننده نشه

### ۲۱) برش — بهینه‌سازی الگوریتم (دستی + دستگاه MDF) — **بخشی انجام شد، ادامه در بخش ۴۱** — **بخشی انجام شد**
فایل: `woodCuttingCore.js`
- [x] هر دو حالت الان با روش «اضافه‌کردن قاب جدید یک‌جا» کار می‌کنن، نه بازمحاسبه‌ی کامل برای بهینه‌ترین چینش — **این نشست حل شد:** به‌جای فقط یه حریصانه‌ی شلف‌بندی، الان یه موتور guillotine (مستطیل آزاد واقعی، نه نوار ثابت) اضافه شد و ۹ ترکیب مختلف (۳ ترتیب چیدمان × هم شلف‌بندی هم guillotine با ۲ جهت تقسیم) امتحان و بهترین‌شون (کمترین صفحه، بعد کمترین هدررفت) نگه داشته می‌شه — جزئیات کامل توی بخش ۴۱ پایین‌تر نوشته شده. حل *دقیقاً* بهینه (bin-packing ریاضی) همچنان یه مسئله‌ی NP-hard جداست که هیچ الگوریتمی تضمینش نمی‌ده، ولی این چند-استراتژی در عمل نتیجه‌ی خیلی بهتری می‌ده
- [x] برش دستی: ترتیب پرشدن برعکس شد — قبلاً اول طول چوب پر می‌شد بعد عرض؛ الان اول عرض (Y) پر می‌شه، بعد در طول (X) جلو می‌ره (`nestBackingPanelsManual`, فرمول امتیازدهی کاندیدها از `y*100000+x` به `x*100000+y` عوض شد)
- [ ] برش دستگاه (MDF): بررسی شد — منطق فعلی (مرتب‌سازی بزرگ‌به‌کوچیک قبل از چیدن) از قبل درسته؛ چون خودتون گفتید «درسته ولی بهینه نیست» و مورد مشخص/باگ خاصی اعلام نشده بود، دست نزدم تا چیزی خراب نشه — اگه یه سناریوی مشخص (چند تکه با این ابعاد) داری که بد چیده می‌شه، بفرست تا دقیق روش کار کنم — **آپدیت کاربر:** فعلاً ولش کن، اولویت پایین

### ۲۲) برش — ترسیم نیم‌دایره چرخش ۹۰ درجه — **✅ تکمیل شد**
فایل: `woodCuttingCore.js`, `NestingVisualizer.jsx`
- [x] **علت پیدا شد:** خودِ رسم (`NestingVisualizer.jsx`) از قبل برای هر دو حالت چرخیده/نچرخیده‌ی نیم‌دایره درست کار می‌کرد، ولی الگوریتم‌های چیدمان (`nestBackingPanelsManual` و حالت «دستگاه» در `nestBackingPanels`) صراحتاً نیم‌دایره‌ها رو از چرخش ۹۰ درجه مستثنا کرده بودن (`!item.isSemiCircle`) — یعنی نیم‌دایره هیچ‌وقت فرصت نمی‌کرد بچرخه تا بهتر جا بشه، برخلاف مستطیل‌های معمولی. این استثنا برداشته شد؛ الان نیم‌دایره هم مثل بقیه‌ی تکه‌ها امتحان می‌شه چرخیده/نچرخیده کدوم بهتر جا می‌شه

### ۲۳) برش دستگاه — خط نقطه‌چین برش سرتاسری — **✅ تکمیل شد**
فایل: `NestingVisualizer.jsx`, `WoodCuttingTab.jsx`
- [x] برای هر مرز بین ردیف‌ها (شلف‌ها) — که همون برش‌های اول و سرتاسری دستگاه هستن — یه خط نقطه‌چین کم‌رنگ از این سر پنل تا اون سر کشیده می‌شه
- [x] این خط فقط برای حالت «برش دستگاه» فعاله (نه دستی)، و فقط بین ردیف‌ها؛ برش‌های کوچیک‌تر بین تکه‌های داخل یک ردیف سرتاسری کشیده نمی‌شن که شلوغ نشه

### ۲۴) فاکتور — فوتر، هدر، متن‌ها — **✅ تکمیل شد**
فایل: `InvoiceTemplate.jsx`
- [x] فوتر: پرانتز معکوس «(آپ‌سایکلینگ(» اصلاح شد
- [x] هدر: QR کد به سمت چپ منتقل شد (قبلاً به‌خاطر چیدمان RTL راست بود)
- [x] فوتر: از قبل فقط فیلدهای پرشده (تلفن/اینستا/سایت/آدرس) نمایش داده می‌شن، چیزی برای اصلاح نبود؛ فیلد جدای واتساپ توی این فایل استفاده نمی‌شه (فقط phone/instagram/website)، پس تکرار واتساپ‌وتلفن اینجا موضوعیت نداشت
- [x] وضعیت «موجود» گالری از «(خالی)» به «موجود در گالری» عوض شد

### ۲۵) فاکتور — بخش جمع حساب (گالری vs مشتری) — **✅ تکمیل شد**
فایل: `InvoiceTemplate.jsx`
- [x] **باگ اصلی («جمع اقلام فروش‌رفته همیشه خالیه») پیدا شد:** چهار جای مختلف کد (`AccountingTab.jsx`, `InvoicesTab.jsx` ۲بار, `GalleryTab.jsx` ۲بار) هرکدوم شکل متفاوتی از `totals` می‌ساختن (یکی `totals.raw`، یکی `totals.total`، یکی اصلاً `totals.available` نداشت) ولی `InvoiceTemplate.jsx` فقط یک فرمت خاص رو می‌خوند — برای همین اغلب مقدارها خالی/نادرست بود
- [x] راه‌حل: به‌جای اعتماد به `totals` (که فرمتش هرجا فرق می‌کرد)، حالا `InvoiceTemplate.jsx` خودش مستقیم از روی آرایه‌ی `items` (که همه‌جا فیلدهای یکسان `finalPrice`/`originalPrice`/`isSettled`/`isAvailableInGallery` رو داره) همه‌چیز رو حساب می‌کنه — قابل‌اعتمادتر و همیشه یکسان
- [x] دو چیدمان جدا برای گالری و مشتری عادی، دقیقاً مطابق فرمتی که خواسته بودی، پیاده شد

### ۲۶) فاکتور — نمایش تخفیف در ردیف کالا — **✅ تکمیل شد**
فایل: `InvoiceTemplate.jsx`
- [x] از یک برچسب رنگی ساده به یک کادر آبی‌روشن با حاشیه تبدیل شد: درصد بالا، مبلغ تخفیف پایین، کنار قیمت خط‌خورده

### ۲۷) فرم‌ها — دکمه Enter = فیلد بعدی — **✅ تکمیل شد**
فایل‌ها: `utils/formNav.js`, `ProductTab.jsx`
- [x] `handleEnterNavigate` روی فرم افزودن/ویرایش محصول و پنل سبد خرید (`data-enter-nav`) وصل شده

### ۲۸) انتخاب مشتری/گالری — تجربه‌ی بهتر جستجو — **✅ تکمیل شد**
فایل: `ProductTab.jsx`
- [x] به‌جای باز شدن خودکار کیبورد وقتی پنل انتخاب مشتری/گالری باز می‌شه — بررسی شد، از قبل حل شده بود (`SmartLocationSelect` توی `ProductTab.jsx`، بدون autoFocus)
- [x] سبد خرید کاتالوگ: اگه گالری‌های زیادی (۵۰+) اضافه بشه، کل صفحه دراز می‌شه — بررسی کردم: پیکر گالری مقصد توی `BasketPanel` و فیلترهای بالای کاتالوگ از قبل اسکرول‌محدود بودن (`maxHeight`+`overflowY`)؛ یه لیست دیگه (دکمه‌های «انتقال به گالری» داخل کارت محصول) واقعاً نامحدود بود و رشد می‌کرد — همونو محدود و اسکرول‌شونده کردم

### ۲۹) متریال — اکسپند بچ (تقویم + یکپارچگی) — **✅ تکمیل شد**
فایل: `MaterialTab.jsx`
- [x] فرم ورود مبلغ بچ با تقویم ۳فیلدی جا تنگه — باید جمع بشه با «تاریخچه خرید» (هر بچ خودش تاریخ می‌گیره) — با فیچر چندبچی بخش ۱۱ حل شد
- [x] لیست بچ‌ها اکسپند/کولپس‌شونده با دکمه‌ی ویرایش/حذف مجزا برای هرکدوم — با فیچر چندبچی بخش ۱۱ حل شد (الان برای همه‌ی انواع متریال، نه فقط مساحتی)

### ۳۰) فاکتور — عکس محصول — **✅ (از قبل موجود بود)**
فایل: `InvoiceTemplate.jsx`
- [x] بررسی شد: از قبل پیاده‌سازی شده بود (عکس محصول یا «بدون عکس» placeholder)، نیاز به تغییر نداشت

### ۳۱) تخفیف ۱۰۰٪ = هدیه — **✅ تکمیل شد**
فایل‌ها: `InvoiceTemplate.jsx`
- [x] باکس صورتی با نماد یونیکد 🎁 (با variation selector متنی، نه حالت اموجی رنگی) + «هدیه» اضافه شد، جایگزین وضعیت عادی می‌شه
- [x] چون فرمول جدید بخش ۲۵ مبلغ نهایی/پرداختی رو مستقیم از `finalPrice` هر آیتم حساب می‌کنه، و هدیه `finalPrice=0` داره، خودکار در «پرداختی» صفر و در «تخفیف» به‌طور کامل لحاظ می‌شه — نیازی به کد جدا نبود

### ۳۲) رَدپای تغییرات (Audit Log) کامل‌تر — **✅ تکمیل شد**
فایل: `SyncTab.jsx`, `App.jsx`, `utils/syncManager.js`, `server.ts`
- [x] بررسی شد: از قبل کامل بود — `syncManager.js` برای هر ساخت/ویرایش/حذف روی ۶ نوع داده (محصولات، متریال، مشتریان، تجهیزات، لینک‌های کارگاه، کارت‌ویزیت‌ها) یه رکورد با فیلد `date` می‌سازه، و `SyncTab.jsx` هم توی نمایش لاگ همون تاریخ رو نشون می‌ده

### ۳۳) دکمه Back گوشی / سوایپ لبه — **✅ تکمیل شد**
فایل: سطح اپ (`App.jsx` + Capacitor)
- [x] Back داخل اپ رفتار منطقی داره: پنجره‌ی باز رو می‌بنده یا برمی‌گرده صفحه‌ی قبل — بررسی شد، از قبل کامل پیاده‌سازی شده بود (`utils/backButton.js` + `App.jsx`)
- [x] یک‌بار Back در صفحه‌ی اصلی → توست «برای خروج دوباره بک بزنید»؛ دوبار پشت‌سرهم (کمتر از ۲ ثانیه) → خروج از اپ — از قبل پیاده بود
- [x] اگه داخل یک فیلد در حال تایپ هستی و Back زدی، فقط کیبورد بسته بشه (نه کل اپ/پنجره) — این یکی واقعاً جا افتاده بود، اضافه شد

### ۳۴) خروجی اکسل/JSON — کامل‌تر — **✅ بررسی و تأیید شد (از قبل کامل بوده)**
فایل: `App.jsx`
- [x] بررسی شد: `handleExportExcel` از قبل شیت‌های جدا برای همه‌چیز داره — محصولات، متریال‌ها، **جلسات برش بهینه‌ی ذخیره‌شده** (`جلسات برش` با تمام frames/stickRows/panelRows + Raw JSON کامل)، مشتریان/گالری‌ها، **کارت‌ویزیت‌ها** (خودم + همکارها)، تجهیزات، لینک‌های کارگاه، و ردپای تغییرات
- [x] `handleExportJson` هم چون از `{ ...data, ... }` می‌سازه، خودکار و همیشه کامل همه‌چیزو شامل می‌شه (از جمله هر فیلد جدیدی که بعداً به `data` اضافه بشه)، نیازی به تغییر نداشت

### ۳۵) خروجی اکسل — صفحه‌ی پیش‌نمایش گرافیکی — **✅ انجام شد (به‌عنوان فایل جدا)**
فایل‌ها: `App.jsx` (`handleExportPreviewExcel`), `SyncTab.jsx`, `package.json`
- ⚠️ **تصمیم فنی مهم:** کتابخونه‌ی فعلی اکسل (`xlsx`/SheetJS نسخه‌ی رایگان) نمی‌تونه رنگ پس‌زمینه‌ی سلول بنویسه (این قابلیت فقط توی نسخه‌ی Pro پولیشه). چون رنگی بودن جزو اصل درخواست بود، کتابخونه‌ی `exceljs` (رایگان و متن‌باز، پشتیبانی کامل از رنگ/استایل) فقط برای همین یک گزارش اضافه شد؛ بقیه‌ی سیستم اکسپورت دست‌نخورده موند
- [x] یه دکمه‌ی جدا («پیش‌نمایش گرافیکی اکسل») توی تب Sync اضافه شد — فایل کاملاً جدا از «خروجی اکسل کامل»، هرگز import نمی‌شه، هر بار از نو ساخته می‌شه
- [x] ستون‌ها: کد/نام/ابعاد ثابت، هزینه/قیمت فروش، بعد یک ستون برای هر گالری، بعد ستون بدهکار
- [x] ریزهزینه‌های هر محصول به‌صورت زیرردیف با outline level ۱ (اکسپند/کولپس بومی اکسل، با کلیک روی علامت + کنار شماره‌ی ردیف باز می‌شه)
- [x] گروه‌بندی بر اساس دسته‌ی محصول (فرش): ردیف عنوان دسته با جمع هزینه و جمع قیمت فروش، رنگش از پالت ۱۲‌تایی (بدون قرمز)
- [x] رنگ سلول محصول توی ستون هر گالری: رنگ خود گالری (موجود) / مشکی (تسویه‌شده) / قرمز (تسویه‌نشده، با مبلغ بدهی هم توی ستون بدهکار تکرار می‌شه)
- [x] بعد از جمع کل، برای هر گالری یه بلوک ۳ ردیفی: جمع موجودی، جمع تسویه‌شده، جمع بدهکاری
- [x] با یه اسکریپت Node مجزا (بیرون از مرورگر) تک‌تک متدهای ExcelJS استفاده‌شده (رنگ، merge، outline، writeBuffer) تست شد و بدون خطا کار کرد
- ⚠️ **نکته:** چون هیچ‌وقت فایل نمونه‌ای که قبلاً بهش اشاره شده بود رو ندیدم، این پیاده‌سازی بر اساس متن دقیق خواسته‌هاست، نه یک الگوی تصویری. اگه چیدمان دقیقش با تصور شما فرق داره، فایل نمونه یا یه اسکرین‌شات از خروجی فعلی بفرست تا تنظیمش کنم



## فایل‌های مورد نیاز که هنوز آپلود نشدن
`SyncTab.jsx`, `GlobalHeader.jsx` (یا هرجا کارت‌ویزیت/QR ساخته می‌شه), و هر فایلی که تقویم شمسی/فیلد تاریخ توشه (احتمالاً `dataModels.js` یا یک کامپوننت `PersianDatePicker` مشترک).

## 🔴 گزارش دقیق باگ چیدمان (از عکس‌های واقعی) — برای همکار

این بخش رو کامل و فنی نوشتم که مستقیم قابل پیاده‌سازی باشه، چون خودِ الگوریتم پیچیده‌ست و ریسک شکستنش بالاست.

### مشکل ۲D (نستینگ پنل) — علت ریشه‌ای

فایل: `src/woodCuttingCore.js`, تابع `nestBackingPanelsManual`

**رفتار فعلی الگوریتم (خط ۹۹ تا ۱۷۰ تقریباً):**
1. قطعات از بزرگ به کوچیک مرتب می‌شن (`expanded.sort`, خط ۹۵)
2. برای هر قطعه، حلقه می‌زنه روی صفحات **از قبل بازشده** (`for sIdx < sheets.length`) و توی هرکدوم دنبال یه «نقطه‌ی کاندید» (گوشه‌ی آزاد) می‌گرده که قطعه توش جا بشه
3. **اگه توی هیچ صفحه‌ی بازشده جا نشد، یه صفحه‌ی کاملاً جدید باز می‌کنه** و قطعه رو گوشه‌ی بالا-چپش می‌ذاره

**دقیقاً همین قدم ۳ باعث مشکلیه که توی عکس دیدی:** وقتی قطعه‌ی بعدی (مثلاً چهارمین نیم‌دایره‌ی ۹۰D) توی صفحات فعلی (با اون چیدمان خاص گوشه‌ای که تا اون لحظه دارن) جا نمی‌شه، الگوریتم **بدون این‌که امتحان کنه شاید با چیدمان دیگه‌ای (نه لزوماً گوشه‌ی بعدی) جا بشه**، مستقیم می‌ره سراغ صفحه‌ی بعد. نتیجه: صفحه‌ی ۳ با فضای خالی زیاد (زیر مربع‌های آبی، سمت راست بالا) نصفه‌کاره می‌مونه، و صفحه‌ی ۴ فقط با یک قطعه‌ی تنها (۳۸D) باز می‌شه — دقیقاً چیزی که تو گفتی «یه‌دونه الکی گذاشته برای صفحه‌ی چهارم».

**چرا این اتفاق می‌افته، فنی‌تر:** روش «نقطه‌ی کاندید» (candidate points — دو گوشه‌ی جدید بعد از هر قرارگیری، خط ۱۵۵-۱۵۸) یک روش استاندارد و شناخته‌شده‌ست (skyline/corner-point heuristic)، ولی **greedy و بدون نگاه به جلو (no look-ahead)** عمل می‌کنه:
- فقط چک می‌کنه «این قطعه‌ی خاص، همین الان، توی این نقاط کاندید جا می‌شه یا نه»
- امتحان نمی‌کنه که اگه ترتیب قرارگیری قطعات قبلی رو کمی عوض کنه (مثلاً یه مربع آبی رو یه‌جای دیگه بذاره)، شاید نیم‌دایره‌ی بعدی هم جا بشه
- امتحان نمی‌کنه که قبل از باز کردن صفحه‌ی جدید، شاید قطعه‌ی *کوچیک‌تری* از لیست باقی‌مونده (نه لزوماً قطعه‌ی فعلی در نوبت) توی فضای خالی باقی‌مونده جا بشه — همینه که باعث می‌شه صفحه‌ی ۴ فقط با یه ۳۸D تنها باز بشه، در حالی که شاید اون ۳۸D توی فضای خالی صفحه‌ی ۳ (کنار مربع‌های آبی) جا می‌شد

**راه‌حل پیشنهادی (بدون rewrite کامل):**
قبل از باز کردن صفحه‌ی جدید (خط ~۱۷۵ به بعد، جایی که `sheets.push(...)` صدا زده می‌شه)، یک پاس اضافه اجرا بشه: کل لیست `expanded` باقی‌مونده (نه فقط قطعه‌ی فعلی) رو به‌ترتیب از کوچیک به بزرگ چک کنه ببینه کدوم‌شون توی فضای خالی *صفحات موجود* جا می‌شن، اول اون‌ها رو جا بده، بعد اگه هنوز قطعه‌ی اصلی (که باعث این چک شد) جا نشد، صفحه‌ی جدید باز کنه. این «best-fit با یک پاس تکمیلی» نسبت به rewrite کامل به bin-packing واقعی (که NP-hard و پرریسکه) خیلی امن‌تره.

### مشکل ۱D (ترتیب مایتر) — مثال دقیق «چوب ۵» از عکس

فایل: همون `woodCuttingCore.js`، تابع مربوط به برش ۱بعدی (`nestFrameSticks` یا مشابه — باید پیدا بشه)

توی عکس چوب ۵ (باقیمانده ۲۹.۴ سانت): ترتیب فعلی چپ‌به‌راست: `95(قرمز) | 19.5×4(سبز) | 49(آبی) | 49(آبی)`

**قرمز = بدون مایتر (لبه‌ی صاف چوب خام)، سبز = یک‌طرف مایتر، آبی = دو‌طرف مایتر.**

مشکل: بین قرمز (بدون مایتر) و اولین سبز (یک‌طرف مایتر)، یه اتصال «صاف↔کج» پیش میاد که نیاز به یه کرف/هدررفت اضافه داره (چون نمی‌شه یه خط برش هم صاف باشه هم کج). اگه ترتیب اینجوری می‌بود: `سبز(با سمت صافش رو به همون اول چوب) | سبز | سبز | سبز | آبی | آبی | قرمز`، اونجا سبزها می‌تونستن سمت *صاف*شون رو مستقیم به لبه‌ی خام چوب بچسبونن (بدون نیاز به کرف اضافه، چون خودِ لبه‌ی چوب از اول صافه)، و ۲ تا ۲.۵ سانت (دقیقاً یه ضخامت چوب) کمتر هدر بره.

**بدتر از اون: بین دو تا سبزِ کنار هم، یه مثلث دور‌ریز اضافه هست** — یعنی وقتی دو قطعه‌ی «یک‌طرف مایتر» کنار هم قرار می‌گیرن، سمت مایتر‌دارشون باید رو به هم باشه (تا مثل یه اتصال V شکل جفت بشن، بدون هدررفت مثلثی)، ولی الان جهت چرخش (`flipped`) این‌طوری انتخاب نمی‌شه.

**راه‌حل پیشنهادی:** قبل از قرار دادن هر قطعه‌ی «یک‌طرف مایتر» کنار قطعه‌ی قبلی، الگوریتم باید:
1. اگه قطعه‌ی قبلی «بدون مایتر» (لبه‌ی خام چوب) بود → این قطعه باید سمت **صافش** رو به همون لبه بچسبونه (نه سمت مایتردارش)
2. اگه قطعه‌ی قبلی هم «یک‌طرف مایتر» بود → جهت (`flipped`) این قطعه باید طوری انتخاب بشه که سمت مایتردارش دقیقاً آینه‌ی سمت مایتردار قطعه‌ی قبلی باشه (تشکیل یه اتصال V بدون فاصله)
3. فقط وقتی این دو تا امکان‌پذیر نبود (مثلاً وسط چوب، بین دو قطعه‌ی نامرتبط)، اونجا کرف/هدررفت معمولی حساب بشه

⚠️ **نکته‌ی مهم برای همکار:** این دو تا مشکل (۲D و ۱D) از قبل هم توی رودمپ به‌عنوان «الگوریتم greedy، نیاز به نشست جدا» علامت‌گذاری شده بودن — این بخش فقط توضیح فنی و مثال دقیق‌تری از همون مشکلاست تا پیاده‌سازیش راحت‌تر باشه، نه یه باگ جدید.


هر بخش که کامل و تست شد، این فایل رو آپدیت کن (تیک بزن ✅) و نسخه‌ی جدید فایل‌های تغییریافته رو جایگزین کن. توی چت بعدی، فقط همین `ROADMAP.md` + فایل‌های مربوط به همون بخش رو بفرست، نه کل پروژه رو.

## ⚠️ نکته درباره‌ی حجم لیست فعلی (۳۵ آیتم)
این لیست الان خیلی بزرگه — نمی‌شه همه‌شو یک‌جا و باکیفیت انجام داد. ترتیب پیشنهادی برای ادامه (بر اساس اثر و ریسک):
1. تموم‌کردن بخش ۱ (متریال) — نیمه‌کاره مونده
2. بخش ۳۱ (هدیه/تخفیف ۱۰۰٪) چون به بخش ۴ و ۲۵/۲۶ (فاکتور و حسابداری) وصله، بهتره باهم
3. بخش ۶، ۲۴، ۲۵، ۲۶، ۳۰ (همه مربوط به متن/ظاهر فاکتور) — یک‌جا انجام بشه چون همه یه فایلن (`InvoicePrint.jsx`)
4. بخش ۱۹-۲۳ (برش ۱D/۲D) — پیچیده و حساسه، جدا و با دقت
5. بخش ۳۵ (اکسل پیش‌نمایش) — بزرگ‌ترین تک‌آیتم، بهتره تنها انجام بشه
6. بقیه به ترتیب دلخواه

هر وقت خواستی ادامه بدیم، بگو کدوم شماره رو می‌خوای اول.

---

## 🆕🆕 دور جدید تست (پیام با ۹ عکس + گزارش مو‌به‌مو) — ۱۴۰۵/۰۴/۲۷

### انجام‌شده در همین دور:
- [x] **۳۶) کپی متن فاکتور (`handleCopyText` در `InvoicePrint.jsx`)**: یک نسخه‌ی کاملاً جدا و باگ‌دار از منطق اسم خریدار داشت (پرانتز تکراری «(با مدیریت...)»، و برای انبار/بی‌نام می‌نوشت «آقای مشتری گرامی» به‌جای «فروش مستقیم: مشتری گرامی»). یک تابع مشترک `buildBuyerDisplayName` ساختم که دقیقاً منطق درستِ `InvoiceTemplate.jsx` رو تکرار می‌کنه و هم `handleCopyText` هم `handleShare` رو بهش وصل کردم.
- [x] **۳۷) دکمه‌ی Share اضافه/خراب در پیش‌نمایش فاکتور**: طبق خواسته‌ی اصلی (بخش ۷ قدیم) حذف شد — چون Save PDF/عکس از قبل خودش شیت اشتراک‌گذاری بومی رو باز می‌کنه.
- [x] **۳۸) فیلترهای گالری (موجود/تسویه‌شده/تسویه‌نشده)**: به چندگزینه‌ای با منطق هوشمند «همه» تبدیل شد — هر دکمه جدا تاگل می‌شه، دکمه‌ی «همه» یک‌جا همه رو روشن می‌کنه، اگه تک‌تک همه‌ی گزینه‌ها دستی روشن بشن خودکار به حالت «همه» جمع می‌شه (دکمه‌های تکی از هایلایت در میان)، و نمی‌شه با تاگل همه رو خاموش کرد (حداقل یکی همیشه فعاله).
- [x] **۳۹) نمایش تخفیف در لیست اکسپند گالری**: هر ۳ بخش (موجود/تسویه‌نشده/تسویه‌شده) فقط `salePrice` خام رو نشون می‌دادن. یک `renderPriceCell` مشترک اضافه شد که وقتی تخفیف واقعی خورده، قیمت اصلی خط‌خورده کوچیک بالا و قیمت نهایی پایین نشون می‌ده.

### هنوز باز/جدید — نیاز به کار دارن:

- [x] **۴۰) برش ۱D — Save هنوز درست نیست (طبق عکس‌های جدید)** ✅ **تأیید شد توسط کاربر: درست شده.**
- [x] **بخشی از ۴۱ انجام شد — «صفحه‌ی جدید فقط با یه نیم‌دایره‌ی تنها باز می‌شه در حالی که صفحه‌ی قبلی هنوز جا داشت» — پیدا و اصلاح شد** (`nestBackingPanels` در `woodCuttingCore.js`، حالت «دستگاه»): همون راه‌حلی که قبلاً توی این فایل (بخش «گزارش دقیق باگ چیدمان») پیشنهاد داده بودم رو پیاده کردم: درست قبل از باز کردن صفحه‌ی جدید، یه پاس اضافه روی کل صف باقی‌مونده (از کوچیک به بزرگ) اجرا می‌شه که هر تکه‌ای که توی فضای خالی صفحات *موجود* جا می‌شه رو زودتر جا می‌ده، قبل از اینکه صفحه‌ی تازه باز بشه. تست شبیه‌سازی‌شده با ترکیبی شبیه عکس (۳ نیم‌دایره‌ی ۹۰، ۳ مربع ۴۸×۵۱، و ۶ نیم‌دایره‌ی ۳۸ روی صفحه‌ی ۱۵۰×۲۰۰): قبلاً همه‌ی نیم‌دایره‌های ۳۸ توی یه صفحه‌ی جدا می‌رفتن، الان ۵ تاشون کنار همون مربع‌ها جا می‌شن و فقط ۱ دونه صفحه‌ی جدید لازم داره
- [x] **باقی‌مونده از ۴۱ — این نشست انجام شد (طبق درخواست، الگوریتم واقعی nesting):** به‌جای فقط شلف‌بندی حریصانه، یه موتور دوم و قوی‌تر (`runGuillotinePacking` در `woodCuttingCore.js`) اضافه شد که فضای خالی هر صفحه رو به‌صورت لیست مستطیل آزاد نگه می‌داره (نه نوار افقی با ارتفاع ثابت)؛ هر تکه توی *بهترین* مستطیل آزاد (کمترین فضای هدررفته، از بین همه‌ی مستطیل‌های همه‌ی صفحات، نه فقط اولین جای مناسب) جا می‌شه و باقی‌مونده‌ش با یه برش کامل (guillotine — هماهنگ با برش واقعی دستگاه) به دو مستطیل جدید تقسیم می‌شه. این با ۲ نوع جهت تقسیم (نوار کوتاه‌تر جدا / نوار بلندتر جدا) و ۳ ترتیب چیدمان مختلف (شامل یه ترتیب جدید که تکه‌های هم‌بعد رو کنار هم می‌ذاره — دقیقاً برای سناریوی «۲ تکه‌ی ۲۳ + ۱ تکه‌ی ۲۱») امتحان می‌شه، جمعاً با ۳ تای شلف‌بندی قدیمی = ۹ تلاش مختلف، و هرکدوم که کمتر صفحه/هدررفت داشت انتخاب می‌شه. تست شد: دقیقاً همون سناریوی مثال (۲×۲۳ + ۱×۲۱ کنار چند تکه‌ی دیگه روی صفحه‌ی ۱۵۰×۲۰۰) با موفقیت تکه‌های هم‌بعد رو کنار هم گذاشت؛ سناریوی قبلی نیم‌دایره‌ها (که قبلاً به یه صفحه‌ی جدا نیاز داشت) الان توی یه صفحه جا شد؛ تست عملکرد با ۴۰۰ تکه هم حدود ۳۰۰ میلی‌ثانیه طول کشید (قابل قبول). **صادقانه بگم:** این یه حل *دقیقاً بهینه* نیست (bin-packing واقعاً NP-hard هست و هیچ الگوریتمی تضمین «بهترین ممکن» نمی‌ده) — ولی چند استراتژی واقعی رو امتحان و بهترین‌شون رو نگه می‌داره، که در عمل نتیجه‌ش خیلی بهتر از قبله. لطفاً با چند سناریوی واقعی خودتون تست کنید و اگه یه مورد مشخص هنوز بد چیده می‌شه، دقیق بفرستید
- [x] برش دستگاه (MDF) — بخشی از بخش ۲۱ هم همین نشست حل شد (همون موتور guillotine بالا رو استفاده می‌کنه، چون یه تابع مشترکه)
- [x] **برش ۱D هم همین نشست بهتر شد:** `optimizeCutting` هم الان به‌جای یه ترتیب پردازش تکه‌ها، ۳ تا امتحان می‌کنه (ترتیب قبلی، طول‌محور خالص، و دومایتری‌ها-اول) و بهترین (کمترین چوب، بعد کمترین هدررفت) رو نگه می‌داره. نمی‌تونه نتیجه رو بدتر کنه (چون ترتیب قبلی هم جزو گزینه‌هاست)، فقط می‌تونه بهتر بشه یا یکی بمونه
- [x] **۴۲) اسکرول‌به‌بالا موقع اکسپند — ✅ تکمیل شد**: `scrollAppToTop()` از هر ۵ جایی که با اکسپند/کولپس یک ردیف صدا زده می‌شد (`MaterialTab.jsx`, `ProductTab.jsx`, `AccountingTab.jsx` ×۲, `InvoicesTab.jsx`, `GalleryTab.jsx`) حذف شد؛ فقط برای سوییچ تب اصلی، باز/بسته‌شدن پنل مدیریت، و باز کردن پنل تمام‌صفحه‌ی لیست فاکتورها باقی موند.
- [x] **۴۳) دکمه‌ی GoToTop اشتباه ظاهر می‌شه — ✅ تکمیل شد**: یک تاخیر ۱۸۰ میلی‌ثانیه‌ای قبل از نمایش اضافه شد (مخفی‌شدن هنوز فوریه) تا لرزش/بازگشت‌فنری موقتِ کشیدن صفحه‌ی غیرقابل‌اسکرول باعث ظاهرشدن نشه؛ پنجره‌ی مخفی‌ماندن موقع سوییچ تب هم از ۲۰۰ به ۷۰۰ میلی‌ثانیه افزایش یافت (هم‌زمان با آخرین تلاش تاخیری `scrollAppToTop`).
- [x] **۴۴) دکمه‌ی + قرمز در تب گالری — ✅ تکمیل شد**: پیدا شد — سایز ۵۴px و آیکون ۲۴ بود، در حالی که تب محصولات/متریال ۵۲px و آیکون ۲۲ دارن. یکی شد.

- [x] **۴۵) بازطراحی کامل صفحه‌ی «بولک متریال» — ✅ تکمیل شد** (`MaterialTab.jsx`):
  - [x] دو چک‌باکس ناهماهنگ به یک دکمه‌ی دوحالته («باقی بماند» / «پرتی شود») تبدیل شدن، برای هر ۴ نوع مساحتی/فرش/خطی/نسبتی
  - [x] پیش‌فرض نحوه‌ی توزیع برای مساحتی/فرش/خطی/نسبتی روی «نسبت مساحت» تنظیم شد؛ فقط نوع ابزار/ثابت با توزیع مساوی شروع می‌شه
  - [x] بررسی شد: نسبت مساحت از قبل درست از روی مساحت واقعی محصولات حساب می‌شد
  - [x] عدد درصد تکراری بالای اسلایدر حذف شد
  - [x] اسم محصول در لیست تخصیص از قبل نمایش داده می‌شد
  - [x] ترتیب پنل (قفل‌شده‌ها → محصولات قابل‌افزودن → لیست تخصیص) طبق بخش ۱۲ قبلاً انجام شده بود
  - [x] فیلد «تعداد» در ویرایش مستقیم متریال نوع ابزار/نسبتی (تکمیل بخش ۱): بخش «موجودی فعلی (تعداد)» با دو فیلد قابل‌اصلاح (مجموع کل / باقیمانده) اضافه شد

- [x] **۴۶) ایمپورت/اکسپورت اکسل ناقصه — ✅ بررسی و تکمیل شد**: نوع متریال و دسته‌بندی از قبل درست ایمپورت/اکسپورت می‌شدن. خلأ واقعی: «فرش متصل» و «پیش‌نویس بودن» محصول توی شیت محصولات نبودن — دو ستون جدید در انتهای شیت (برای سازگاری با فایل‌های قدیمی) اضافه شد، هم اکسپورت هم ایمپورت.

- [x] **۴۷) تخفیف ۱۰۰٪ به‌عنوان هدیه — ✅ تکمیل شد**: باکس صورتی 🎁 الان توی همه‌جا هست: `InvoiceTemplate.jsx`, `ProductTab.jsx` (لیست + فرم ویرایش), `GalleryTab.jsx` (لیست اکسپند), `AccountingTab.jsx` (۲ جای فاکتور), و یک باگ محاسباتی مشابه (چک نادرست `discountedPrice > 0` که آیتم هدیه رو از مسیر اصلی رد می‌کرد) توی `InvoicesTab.jsx` هم پیدا و رفع شد.

- [x] **۴۸) تقویم شمسی گرافیکی — بررسی شد، از قبل کامل انجام شده بود**: `JalaliDatePicker.jsx` از قبل دقیقاً همین‌جوریه — یه دکمه‌ی کوچیک که تاریخ انتخاب‌شده رو نشون می‌ده، با لمس یه پاپ‌آپ تقویم گرافیکی مینیمال (گرید روز/ماه/سال با دکمه‌ی «امروز») باز می‌شه، هیچ‌جا سه فیلد بزرگ جدا نیست. توی ۵ فایل (`MaterialTab`, `ProductTab`, `AccountingTab`, `InvoicesTab`, `GalleryTab`) هم استفاده می‌شه. (نکته‌ی جدا: «یکی‌شدن فرم تاریخ بچ با تاریخچه‌ی خرید» که توی بخش چندبچی مطرح شده، یه فیچر ساختاری بزرگ‌تره و جدا از این مورده — هنوز باز)

- [x] **۴۹) شکل دایره‌ی کامل («O») برای محصولات — ✅ تکمیل شد**
فایل‌ها: `mathCore.js`, `ProductTab.jsx`, `WoodCuttingTab.jsx`, `woodCuttingCore.js`, `NestingVisualizer.jsx`
  - [x] توی فرم محصول، دکمه‌ی ابعاد حالا ۳ حالته‌ست: کلیک اصلی مستطیل↔گرد رو سوییچ می‌کنه، و وقتی روی حالت گرد (D) هستیم یک دکمه‌ی کوچیک «O» کنارش ظاهر می‌شه که بین نیم‌دایره و دایره‌ی کامل جابه‌جا می‌کنه (فرمت ذخیره‌سازی: `50O`، فیلد `shape: "circle"`)
  - [x] `getProductArea`/`dimsArea` برای شکل دایره حالا از فرمول کامل πr² استفاده می‌کنن (نه فرمول نیم‌دایره‌ی قدیمی)؛ یک تابع جدید `getProductPerimeter()` هم اضافه شد (مستطیل/نیم‌دایره/دایره‌ی کامل) برای هر جایی که بعداً به محیط نیاز باشه
  - [x] توی تب برش، وقتی از لیست محصولات یک محصول با شکل دایره انتخاب بشه، فریم خودکار حالت O می‌گیره (نه D)
  - [x] برش ۱D برای فریم‌های دایره‌ای عمداً هیچ قطعه‌ای تولید نمی‌کنه (طبیعیه، پیاده‌سازی نشد چون معنی نداره)؛ برش ۲D براش یک مربع کامل (نه نصفه‌مربع نیم‌دایره) رزرو می‌کنه
  - [x] `NestingVisualizer.jsx` برای O یک مربع نقطه‌چین با دایره‌ی کامل داخلش می‌کشه، دقیقاً طبق خواسته

- [x] **۵۰) انتخاب مشتری/گالری/فرش — کیبورد خودکار باز نشه — ✅ تکمیل شد**: مشتری/گالری از قبل (بخش ۲۸) درست بود. فیلد انتخاب فرش هم از یک اینپوت متنی (که لمسش همیشه کیبورد باز می‌کرد) به همون کامپوننت پنل قابل‌اسکرول با سرچ داخلی تبدیل شد.

- [x] **۵۱) نوع/دسته‌بندی محصول — فیچر جدید بزرگ — ✅ کامل شد**:
  - [x] کنار دکمه‌ی «فرش» در فرم محصول، دکمه‌ی «نوع محصول» اضافه شد: با لمس یه لیست کوچیک از انواع ثبت‌شده باز می‌شه، هرکدوم قابل ویرایش/حذف (اگه محصولی از اون نوع داشته باشه، حذف نمی‌شه)، + فیلد «نوع جدید» ته لیست. محصول جدید خودکار روی آخرین نوع انتخاب‌شده (ذخیره‌شده توی مرورگر) پیش‌فرض می‌شه
  - [x] `productTypes` تا `ProductEditor` رد شد (`App.jsx` → `ProductTab.jsx` → `ProductEditor`)؛ ذخیره/ایمپورت/اکسپورت اکسل از قبل حاضر بود
  - [x] دکمه‌ی فیلتر نوع توی تب محصولات و کاتالوگ — هردوجا اضافه شد، دقیقاً کنار ردیف دکمه‌های همه/موجود/تسویه‌شده (سمت چپشون)
  - [x] ⚠️ باگ «نوع فرش خودش پاک می‌شه» — بررسی کردم، از قبل (یه سشن قبلی) پیدا و فیکس شده بود (`fabricLineItem` fallback توی `ProductTab.jsx`)

### فایل‌های جدیدی که برای دور بعد لازمه (اگه هنوز نفرستادی):
`GlobalHeader.jsx` (برای دکمه‌ی + گالری)، `JalaliDatePicker.jsx` (برای بازطراحی تقویم)، هر فایلی که «نوع محصول» توش تعریف/فیلتر می‌شه.

---

## ✅ این دور تکمیل شد

- **بخش ۴۸ (تقویم گرافیکی)**: `JalaliDatePicker.jsx` کاملاً از صفر بازنویسی شد — از ۳ دراپ‌داون بزرگ به یک دکمه‌ی تک (نمایش تاریخ فرمت‌شده) تبدیل شد که با لمس، یک تقویم گرافیکی مینیمال (گرید ۷ستونه، ناوبری ماه، دکمه‌ی «امروز») باز می‌کنه. Props بیرونی (`value`, `onChange`, `allowEmpty`) عوض نشده، پس همه‌جا که ازش استفاده می‌شد بدون تغییر کار می‌کنه.

- **بخش ۴ + ۲ (فیچر «نوع محصول») — ساخته شد، هم کلاینت هم SQL**:
  - `dataModels.js`: `emptyProductType()` + `data.productTypes[]` اضافه شد
  - `src/db/schema.ts`: جدول جدید `product_types` + ستون `productTypeId` روی جدول `products` — **⚠️ این یعنی باید یک migration دیتابیس اجرا کنی، پایین توضیح دادم چطور**
  - `server.ts`: همگام‌سازی کامل (upsert/fetch/delete) برای `productTypes` مثل بقیه‌ی لیست‌ها
  - `App.jsx`: ستون «نوع محصول» به اکسپورت/ایمپورت اکسل اضافه شد (فعلاً فقط با نام تطبیق می‌ده؛ اگه نوعی از قبل با اون اسم نساخته باشی، ایمپورت اون ستون رو نادیده می‌گیره — باید اول نوع رو با UI بسازی)
  - JSON بکاپ کامل خودکار همه‌چیو شامل می‌شه (نیازی به تغییر نداشت)
  - ⏳ **باقی‌مونده از این فیچر**: خودِ UI (دکمه‌ی «نوع» کنار دکمه‌ی فرش در فرم محصول با لیست/افزودن/ویرایش/حذف، و دکمه‌ی فیلتر نوع در تب محصولات+کاتالوگ) هنوز ساخته نشده — نیاز به یه نشست جدا داره چون باید prop جدید از `App.jsx` تا `ProductEditor` رد بشه (چند لایه)

- **بخش‌های کوچیک و سریع دیگه**:
  - [x] رنگ دکمه‌ی GoToTop از قرمز به مشکی تغییر کرد و جاش عوض شد تا رو دکمه‌ی + نیفته (بالای اون قرار گرفت)
  - [x] تب حسابداری: باز شدن ماشین‌حساب خودکار اسکرول به بالا می‌کنه
  - [x] دکمه‌ی «تسویه نشد» (لغو تسویه‌ی محصول) به «لغو تسویه» تغییر نام داد
  - [x] باگ محاسباتی بولک متریال (وقتی ۱۰۰٪ می‌ذاری ولی جمع واقعی ۹۸/۹۹/۱۰۱٪ از آب درمیاد): پیدا شد — توزیع «نسبت مساحت» فقط سهم هر واحد رو گرد می‌کرد، نه جمع کل رو. با روش «باقی‌مانده‌ی بزرگ‌تر» (largest remainder) بازنویسی شد که جمع کل همیشه دقیقاً ۱۰۰٪ بشه.

---

## 🆕 لیست بزرگ جدید (پیام آخر) — بخشی تکمیل شد

### متریال
- [x] دکمه‌ی «هاید/آنهاید همه» — همکار مستقل یه نسخه (یه دکمه‌ی سراسری بالای لیست فیلترشده) پیاده کرده بود، همونو نگه داشتم
- [x] باگ آنلاک-آنهاید-نکردن: توی `runLockUnlockPass` (`App.jsx`)، آنلاک کردن یه بچ از طریق دکمه‌ی قفل/باز (نه از مسیر پول عادی) `hidden` رو ریست نمی‌کرد — فیکس شد
- [x] متریال‌های هایدشده حالا توی هر دسته به ته لیست منتقل می‌شن (زیر موجودها)
- [x] وقتی متریالی از بولک جدا/حذف می‌شه، دکمه‌ی لاک/آنلاک باید یه بج قرمز فعال نشون بده، و درصد/نوار/قیمت آزادشده باید هم برای استفاده‌ی مجدد سینک بشه (نه فقط درصد آزاد بشه) — **✅ بررسی شد، از قبل کامل پیاده‌سازی شده بود ⚫ (Ref)** — نوار آبی «در انتظار آزادسازی» + بج قرمز رو دکمه‌ی رفرش + refund به remainingCost، همه از قبل بودن؛ جزئیات کامل در «۲) گزارش کاربر با اسکرین‌شات»
- [x] وقتی حالت توزیع (مساوی/نسبت‌مساحت) انتخاب می‌شه، این تنظیم روی خودِ لاین‌آیتم (`distributionMode`) ذخیره می‌شه و دفعه‌ی بعد که همون متریال رو ویرایش می‌کنی، به‌جای پیش‌فرض «دستی»، همون حالت قبلی یادآوری می‌شه

### محصولات / کاتالوگ
- [x] دکمه‌ی «هاید از کاتالوگ» کنار هر محصول — همکار مستقل خودش این‌و کامل پیاده کرده بود (فیلد `hiddenFromCatalog`)، همون نسخه نگه داشته شد
- [x] ترتیب دسته‌بندی‌ها بر اساس کمترین کد محصول — همکار این‌و توی `App.jsx` (سطح `groupedProducts`) درست کرده بود؛ نسخه‌ی من هم توی `ProductTab.jsx` بود، هردو سازگارن و تداخلی ندارن
- [x] ردیف اول اکسپند محصول (هزینه/قیمت/سود) ثابت بمونه بالا، فقط ریزهزینه‌ها کولپس بشن — انجام شد؛ ریزهزینه‌ها الان خودشون یه تاگل جدا و پیش‌فرض بسته دارن
- [x] ردیف دکمه‌ها (گالری/وضعیت فروش/ادیت/حذف/جدید) بالای ریزهزینه‌ها بیاد — ترتیب جدید: خلاصه (هزینه/سود) → دکمه‌ها → ریزهزینه‌های کولپس‌شونده

### رفرش/Undo
- [x] ⚠️ **باگ مهم — فیکس شد** (`App.jsx`، `runLockUnlockPass` + تابع جدید `reverseOperation`): علتش همون بود که قبلاً پیدا شده بود — `undoSnapshotRef` یه اسنپ‌شات از *کل* state می‌گرفت، پس Undo همیشه کل پاس (چندین قفل با هم) رو برمی‌گردوند. الان به‌جای اسنپ‌شات کامل، `runLockUnlockPass` هر آیتمی که قفل/آزاد می‌کنه رو جدا (با کد کافی برای برگردوندنش: مبلغ/مقدار دقیقاً کسرشده از استخر متریال، دلتای چوب‌های مصرف‌شده، فیلدهای قبلی همون ردیف، و پرچم «همین آیتم بود که بچ رو تکمیل کرد» برای موارد بچ‌محور) توی یه لیست عملیات (`operations`) ثبت می‌کنه. دابل‌تپ (`handleUndoRefresh`) فقط **آخرین** عملیات این لیست رو برمی‌گردونه (نه همه رو با هم)، و دابل‌تپ‌های پشت‌سرهم می‌تونن یکی‌یکی عقب‌تر برن. از نظر ریاضی درسته چون هر آیتم مبلغش رو مستقل از بقیه‌ی آیتم‌های همون پاس، از استخر *قبل از پاس* حساب می‌کنه — پس واگرد یه آیتم روی بقیه اثر نمی‌ذاره. با شبیه‌سازی جدا (خارج از اپ، با دیتای واقعی‌نما) تست شد: قفل هم‌زمانِ ۳ آیتم مختلف (دو تا استخری + یه چوب)، بعد واگرد فقط آخرین‌ی‌شون → بقیه دست‌نخورده موندن و عدد استخر متریال دقیق برگشت؛ همین‌طور برای بچ‌های مساحتی (وقتی دومین آیتم بچ رو تکامل می‌کنه، واگرد فقط همون آیتم پرچم `locked` بچ رو هم درست به `false` برمی‌گردونه، نه بیشتر)، آزادسازی (Undo یه Unlock دوباره قفلش می‌کنه)، و قفل/آزادسازی یه‌جای کل یه بچ (از صف `pendingBulkChanges`) که عمداً یه واحد اتمیک موند (چون خودش از اول یه کلیک صریح روی یه بچ خاصه)
- 🔴 چون این بخش حساسه (مبلغ/موجودی واقعی)، لطفاً قبل از اعتماد کامل، با چند سناریوی واقعی توی خودِ اپ تست کنید: چند آیتم مختلف رو با هم قفل کنید، بعد چندبار دابل‌تپ بزنید و ببینید هر بار فقط یکی عقب می‌ره و بقیه دست‌نخورده می‌مونن

### چیدمان کلی
- [x] گپ کوچیک بین هدر داخلی هر تب و نوار تب‌های اصلی — با اسکرین‌شات پیدا شد: هدر داخلی چسبان (`محصولات`/`متریال`/`برش`) یه `padding-top: 24px` اضافی داشت که با `paddingTop:12` خودِ `<main>` جمع می‌شد و فاصله‌ی قابل‌توجه ایجاد می‌کرد؛ توی هر ۳ فایل (`ProductTab.jsx` ۲ جا، `MaterialTab.jsx`، `WoodCuttingTab.jsx`) به `8px` کم شد

### سورت
- [x] تپ دوم روی همون گزینه‌ی فعال (توی منوی بازشو) جهت رو برعکس می‌کنه (▲/▼) — توی هر ۴ کپی دکمه‌ی سورت (محصولات/کاتالوگ/متریال/گالری) و منطق مرتب‌سازیشون
- [x] 🔍 یه چیز جالب: دکمه‌ی سورت گالری از قبل کدش نوشته شده بود ولی هیچ‌جا رندر نمی‌شد (پراپ‌هاش حتی توی امضای تابع نبودن) — وصلش کردم؛ الان بالای لیست گالری/مشتری دیده می‌شه و واقعاً مرتب می‌کنه (بر اساس تعداد کالا یا مانده‌حساب هم کار می‌کنه، نه فقط تاریخ/الفبا)
- [x] دکمه‌ی جدا «گروه‌بندی‌شده» که وقتی خاموشه دسته‌بندی‌ها هاید بشن — اضافه شد؛ توی متریال و محصولات (کنار دکمه‌ی سورت)، پیش‌فرض روشن، حالت انتخابی توی مرورگر ذخیره می‌شه

### تجربه‌ی تایپ/فرم
- [x] اسکرول خودکار فیلد فعال بالای کیبورد — یه یوتیل جدید (`utils/keyboardScroll.js`) اضافه شد که سراسر اپ کار می‌کنه (بدون دستکاری تک‌تک فرم‌ها): با فوکوس روی هر input/textarea بعد از یه تاخیر کوتاه (برای انیمیشن کیبورد) فیلد رو وسط صفحه می‌بره، و وقتی کیبورد کامل بسته شد موقعیت اسکرول قبلی برمی‌گرده

### تب برش
- [x] دکمه‌ی قرمز ذخیره مثل بقیه‌ی تب‌ها، با منطق: هر دو نستینگ (۱D/۲D) رو اگه هردو بودن ذخیره کنه، وگرنه هرکدوم بود — دکمه‌ی «ذخیره تصویر» جدید کنار «ذخیره جلسه» اضافه شد (دکمه‌های تکی قبلی هرکدوم هم دست‌نخورده موندن، برای وقتی فقط یکیشون لازمه)
- [x] موقع سیو، دقیقاً همون UI که دیده می‌شه (نه یه نسخه‌ی متفاوت) ذخیره بشه — با فیکس بالا (حذف `FitToWidth` و گرفتن مستقیم عکس از `div` واقعی) همین مشکل هم حل شد، چون دیگه تریک تبدیل موقت قبل از عکس‌گیری وجود نداره
- [x] پس‌زمینه‌ی ۲D به همون رنگ تیره‌ی ۱D تغییر کرد (نه سفید) — `NestingVisualizer.jsx`
- [x] وقتی برش‌های ۱D زیاد و اعداد ناخوانا می‌شن، چوب‌ها دراز (قابل‌اسکرول افقی) بشن — **این نشست تکمیل شد:** `PlankRow` از حالت flex-تناسبی-توی-عرض-ثابت (که هر چقدر برش زیاد می‌شد همه‌چیز فشرده‌تر می‌شد) به عرض واقعی پیکسلی (`PLANK_PX_PER_CM = 8px` به‌ازای هر سانت، با حداقل ۲۲px برای هر تکه) تغییر کرد؛ یعنی چوب بلندتر = ردیف پهن‌تر، نه فشرده‌تر. ردیف‌های ۱D الان توی یه container با `overflowX: auto` رندر می‌شن، پس وقتی عرض واقعی از صفحه بیشتره، به‌جای کوچیک‌شدن، به‌صورت افقی اسکرول می‌شه. برای JPG هم دیگه نیازی به تریک «موقتاً fit کن، عکس بگیر، برگردون» نبود — چون `ref` گرفتن مستقیم از عکس‌گیری (`html2canvas`) الان روی خودِ `div` داخلی (`width:max-content`) نصب شده نه روی wrapper اسکرول‌شونده‌ی بیرونی، پس تصویر ذخیره‌شده همیشه عرض کامل واقعی رو می‌گیره (نه فقط بخش دیده‌شده‌ی صفحه)، دقیقاً هماهنگ با چیزی که می‌بینی. کامپوننت قدیمی `FitToWidth` (که اصلاً استفاده نمی‌شد) حذف شد
- 🔴 لطفاً با چند برش واقعی که چوب‌هاش خیلی درازن تست کن (هم نمایش، هم ذخیره‌ی JPG) تا مطمئن بشیم اسکرول و عکس هردو درست دراومدن

---

## 🆕🆕 دور جدید — گزارش مستقیم شما (بدون عکس، فقط توضیح) — این نشست

### ۱) باگ Undo — رفرش رو کامل بازنشانی می‌کرد، نه فقط قفل‌های آزادشده — **✅ فیکس شد**
فایل: `App.jsx`
گزارش شما: «الان دکمه‌ی Undo رو که می‌زنم، نه‌تنها قفل‌های آزادشده رو باز می‌کنه تا متریال به باقیمانده برگرده، بلکه هرچی متریال بعد از آخرین قفل ویرایش یا افزوده شده رو هم حذف می‌کنه، در حالی که فقط باید متریال آزادشده رو به باقیمانده برگردونه»
- **علت واقعی:** موقع هر پاس قفل/آزادسازی، برای Undo از کل `data.materials`/`data.products` یه اسنپ‌شات کامل (JSON deep-clone) گرفته می‌شد. موقع Undo، کل این اسنپ‌شات جایگزین state فعلی می‌شد — یعنی هر متریال/محصولی که بین اون پاس و لحظه‌ی Undo اضافه یا ویرایش شده بود، با نسخه‌ی قدیمی (که قبل از اون تغییرات بود) بازنویسی و عملاً ناپدید می‌شد
- **فیکس:** من یه نسخه (بازگردانی فیلد-به-فیلد فقط همون پاس) نوشته بودم، ولی موقع merge کردن با کاری که همکارتون هم‌زمان روش کار می‌کرد، معلوم شد نسخه‌ی همکارتون کامل‌تره — به‌جای اسنپ‌شات کامل، `runLockUnlockPass` حالا هر عملیات (هر قفل/آزادسازی تکی) رو جدا توی یه آرایه‌ی `operations` ثبت می‌کنه (`undoOperationsRef`، یه پشته/stack). هر بار Undo زده بشه، فقط **آخرین عملیات** پشته (`reverseOperation`) برمی‌گرده و از پشته حذف می‌شه — نه کل پاس با هم. این هم مشکل شما رو حل می‌کنه (چیز دیگه‌ای پاک نمی‌شه) و هم دقیق‌تره: اگه یه پاس چند قفل هم‌زمان زده باشه، می‌تونی با چند بار Undo پشت‌سرهم یکی‌یکی عقبشون بزنی، نه همه رو با هم
- 🔴 حتماً تست کن: یه چیزی رو قفل/آزاد کن، بعدش (قبل از Undo) یه متریال جدید اضافه کن یا یکی رو ویرایش کن، بعد Undo بزن — باید فقط قفل/آزادسازی برگرده، متریال جدید/ویرایش‌شده باید سرجاش بمونه

### ۲) رفرش (کلیک، نه Hold) قیمت محصولات درصدی رو آپدیت نمی‌کرد — **✅ فیکس شد**
فایل: `App.jsx`
- کلیک ساده روی دکمه‌ی رفرش (`handleQuickRefresh`) فقط داده رو از حافظه دوباره می‌خوند، ولی قیمت فروش محصولاتی که با «درصد سود» (`profitPct`) قیمت‌گذاری شدن رو بازمحاسبه نمی‌کرد؛ اگه قیمت متریالی کم/زیاد شده بود، قیمت این محصولات قدیمی می‌موند
- تابع مشترک جدید `syncPercentPricedProducts(materials, products)` اضافه شد که برای هر محصول با `profitPct` غیر-خالی، `totalCost` رو از روی هزینه‌ی زنده‌ی متریال‌ها بازمحاسبه می‌کنه و `salePrice`/`discountedPrice` رو آپدیت می‌کنه (محصولات با قیمت دستی دست‌نخورده می‌مونن). این تابع الان توی کلیک رفرش صدا زده می‌شه

### ۴) ویرایش متریال — قیمت محصولات درصدی باید فوری (نه فقط موقع رفرش) آپدیت بشه — **✅ فیکس شد**
فایل: `App.jsx`
- همون `syncPercentPricedProducts` حالا توی همه‌ی توابعی که مستقیماً قیمت/موجودی متریال رو عوض می‌کنن هم صدا زده می‌شه: `addMaterialPurchase`, `updateProcurement`, `deleteProcurement`, `addBatch`, `updateBatch`, `deleteBatch` — یعنی همون لحظه‌ای که خرید/بچ جدید ثبت، ویرایش یا حذف بشه، قیمت محصولات درصدی هم در همون setData آپدیت می‌شه، بدون نیاز به کلیک رفرش

### ۳) فرش — درصد خودکار تا کلیک روی «خودکار» صفر بود و زنده نبود — **✅ فیکس شد**
فایل: `ProductTab.jsx`
گزارش شما: «در قسمت ادیت/افزودن محصول وقتی دسته‌بندی فرش انتخاب شده... تا وقتی یکبار روی خودکار کلیک نکنم عددش صفر نشون می‌ده و لایو پردازش نشده»
- **علت:** درصد خودکار فقط لحظه‌ی کلیک روی دکمه‌ی «خودکار» روی `li.pct` نوشته می‌شد. تا اون کلیک، `li.pct` خالی بود و چون فرمول هزینه (`resolveCost`) فقط وقتی `li.pct != null` بود چیزی حساب می‌کرد، هزینه صفر نمایش داده می‌شد؛ حتی بعد از کلیک هم اگه ابعاد محصول یا متریال عوض می‌شد، عدد قدیمی (stale) می‌موند
- **فیکس:** `resolveCost` الان برای لاین‌آیتم‌های فرش با حالت «خودکار» (`useAutoPct !== false`)، درصد رو همیشه زنده از روی ابعاد فعلی محصول و متریال حساب می‌کنه، نه از روی `li.pct` ذخیره‌شده — نیازی به کلیک اولیه نیست و با هر تغییر دیم/متریال زنده آپدیت می‌شه

### ۵) پیش‌فرض‌های درصد سود در افزودن/ویرایش محصول — **✅ فیکس شد**
فایل: `ProductTab.jsx`
- از `[30, 50, 70, 100, 150]` به `[70, 100, 150, 250, 300]` تغییر کرد

### ۶) بولک متریال — محصول قفل‌شده دوباره قابل‌افزودن نباشه، فقط قابل‌ویرایش — **بررسی شد، از قبل درست بود**
شما گفتید این مورد از قبل درست کار می‌کنه؛ کد رو هم چک کردم، تأیید می‌شه

### ۷) کد تکراری محصول موقع ثبت خطا نمی‌داد — **✅ فیکس شد**
فایل: `ProductTab.jsx`
گزارش شما: دو محصول اشتباهی یه کد یکسان گرفتن و هردو ثبت شدن
- موقع ذخیره‌ی محصول، الان قبل از ثبت چک می‌شه که کد وارد‌شده با کد هیچ محصول دیگه‌ای (بجز خودش موقع ویرایش) تکراری نباشه؛ اگه بود، ذخیره متوقف می‌شه، فیلد کد قرمز می‌شه و پیام خطا نشون داده می‌شه

---

## 📋 راهنمای npm/Capacitor برای این نشست
این نشست **پکیج جدیدی اضافه نشد** (فقط کد JS/JSX و اسکیمای دیتابیس تغییر کرد) — پس نیازی به `npm install` پکیج جدید نیست، فقط:
```
npm run build
npx cap sync android

```

⚠️ **ولی یک migration دیتابیس لازمه** (چون جدول `product_types` و ستون `productTypeId` به `schema.ts` اضافه شد). اگه از Drizzle Kit استفاده می‌کنی:
```
npx drizzle-kit push
```
این دستور رو توی همون پوشه‌ای بزن که `drizzle.config.ts`ت هست، با دیتابیس واقعی‌ات وصل باش. اگه مطمئن نیستی این دستور رو داری یا نه، بگو کمکت کنم پیدا کنی.

### دکمه‌ی Scroll-to-top — ✅ (اضافه شد توسط همکار + تکمیل شد)
فایل‌ها: `ScrollToTopButton.jsx` (جدید، توسط همکار)، `GlobalHeader.jsx`، `App.jsx`
- [x] دکمه‌ی شناور، فقط وقتی صفحه واقعاً قابل‌اسکرول و پایین‌تر از بالا هست نمایش داده می‌شه
- [x] وقتی هر مودال/پنلی (افزودن/ویرایش، بولک، سبد خرید، فاکتور و…) باز باشه، دکمه هاید می‌شه (`isAnyModalOpen`)
- [x] موقع سوییچ بین تب‌ها، دکمه یه لحظه هاید می‌شه و بعد بر اساس اسکرول واقعی دوباره تصمیم می‌گیره
- [x] رنگش از قرمز/خاکستری تیره به یه استایل شیشه‌ای بی‌رنگ‌تر (`rgba(255,255,255,0.06)`) تغییر کرد طبق خواسته
- [x] موقعیتش (`bottom:82`) دقیقاً بالای دکمه‌ی شناور «+» (`bottom:20`, ارتفاع ۵۲px) با ۱۰px فاصله‌ست
- [x] **باقی‌مونده‌ای که جا افتاده بود، اضافه شد:** خودِ سوییچ‌کردن بین تب‌ها (هم نوار اصلی هم تب‌های پنل مدیریت) قبلاً اسکرول رو به بالا برنمی‌گردوند؛ الان `scrollAppToTop()` روی هردو تب‌ زن صدا زده می‌شه

### سوالات باز قبلی — پاسخ
- **فیس‌آیدی/اثرانگشت برای باز کردن پنل:** این نیاز به یه پلاگین بومی Capacitor (مثل `capacitor-native-biometric`) و تنظیم مجوز اندروید داره که فقط روی گوشی واقعی قابل تست‌ه، نه توی کد به‌تنهایی. اگه می‌خوایش، باید پلاگین رو نصب کنیم و بعد حتماً خودتون روی گوشی تستش کنید — بگید اگه بریم سراغش
- **«پیش‌فرض تعداد بولک باید کل باشه»:** همه‌جای منطقی که بررسی کردم از قبل «کل»/۱۰۰٪ پیش‌فرضه. اگه هنوز یه‌جا می‌بینیش که اشتباهه، دقیقاً بگو کدوم صفحه/دکمه رو باز می‌کنی تا اونجا رو پیدا کنم

## 🆕🆕🆕 دور جدید — فیچر «تعداد» برای محصولات ستی + نرمالایز ابعاد — این نشست

### ۸) فیلد «تعداد» برای محصولات ستی — **✅ اضافه شد**
فایل‌ها: `ProductTab.jsx`, `mathCore.js`, `dataModels.js`, `db/schema.ts`, `server.ts`
گزارش شما: بعضی محصولات ست هستن (مثلاً یه محصول = ۴ تا)؛ قیمت برای کل ست ثبت می‌شه، ولی مصرف متریال (فرش/پنل/نسبت مساحتی بولک) باید qty×(طول×عرض) حساب بشه؛ محصول جدا فروخته نمی‌شه، فقط یه شناسه/کد داره
- فیلد «تعداد» کنار ابعاد (توی فرم افزودن/ویرایش محصول) اضافه شد؛ پیش‌فرض ۱
- `getProductArea` (تابع مرکزی مساحت محصول در `mathCore.js`) حالا نتیجه رو در `qty` ضرب می‌کنه — این تابع دقیقاً همون‌جاییه که فرش، پنل مساحتی و نسبت‌مساحتی بولک همه ازش استفاده می‌کنن، پس بدون نیاز به تغییر جدا در هرکدوم، مصرف متریال خودکار qty برابر حساب می‌شه
- نمایش: توی لیست محصول، تب کاتالوگ، سبد خرید، بزرگنمایی عکس، و متن‌های فاکتور/چاپ — اگه qty=۱ چیزی اضافه نشون داده نمی‌شه؛ اگه بیشتر بود، «(۴عدد)» کنار ابعاد نشون داده می‌شه
- ⚠️ ستون `qty` به جدول `products` توی `schema.ts` اضافه شد (و `server.ts` هم برای ذخیره/خوندنش آپدیت شد) — یعنی **این یکی هم نیاز به همون migration دیتابیس داره** (`npx drizzle-kit push`، مثل بخش ۵۱)

### ۹) نرمالایز خودکار ترتیب ابعاد (طول همیشه بزرگ‌تر) — **✅ اضافه شد**
فایل‌ها: `ProductTab.jsx`, `mathCore.js`
گزارش شما: ممکنه کاربر عدد کوچیک‌تر رو اول بزنه (عرض جای طول)؛ می‌خواید همیشه عدد بزرگ‌تر اول نمایش داده بشه، از چپ به راست
- موقع ذخیره‌ی محصول (برای شکل مستطیلی، نه دایره/نیم‌دایره)، اگه عرض از طول بزرگ‌تر بود، خودکار جابه‌جا می‌شن — یعنی `dimW`/`dims` ذخیره‌شده همیشه با عدد بزرگ‌تر اول‌ه
- برای دیتای قدیمی که ممکنه از قبل برعکس ذخیره شده باشه، یه تابع نمایشی جدا (`formatProductDims`) اضافه شد که همه‌جا (لیست محصول، کاتالوگ، بزرگنمایی عکس، سبد خرید، فاکتور/چاپ در `ProductTab.jsx`/`AccountingTab.jsx`/`GalleryTab.jsx`/`InvoicesTab.jsx`) به‌جای خوندن مستقیم `p.dims`، همیشه با عدد بزرگ‌تر اول نمایش می‌ده — حتی برای محصولات قدیمی که خودِ دیتاشون هنوز برعکسه
- 🔴 لطفاً چک کن: یه محصول جدید با عرض بزرگ‌تر از طول بساز و سیو کن — باید بعد از سیو swap بشه؛ یه محصول قدیمی که می‌دونی برعکسه رو هم چک کن که نمایشش درست شده باشه

## 🆕🆕🆕 دور جدید — دفترچه‌ی بزرگ گزارش‌ها (این نشست، مستقیم از پیام کاربر + عکس‌ها)

⚠️ این بخش صرفاً مستندسازیه (طبق درخواست، سریع و دقیق نوشته شده تا برای همکارها فرستاده بشه).
🔵 = همین الان دارم روش کار می‌کنم (Claude) — لطفاً همکار AI روی همین آیتم‌ها هم‌زمان کار نکنه تا تداخل نشه

### تب محصولات و تب متریال — دکمه‌های بالای لیست
- [x] دکمه‌ی «دسته‌بندی» جدا حذف شد؛ منطقش (groupedView) دست‌نخورده موند، فقط از داخل منوی sort کنترل می‌شه
- [x] دکمه‌ی sort: با کلیک ساده باز می‌شه، با کلیک دوباره روی دکمه یا هرجای دیگه‌ی صفحه بسته می‌شه؛ انتخاب یه گزینه دیگه بسته‌ش نمی‌کنه، می‌شه پشت‌سرهم چندبار سوییچ کرد
- [x] بالای لیست sort (محصولات و متریال): «دسته‌بندی‌شده» و «یکجا (لیست ساده)» با یه خط جداکننده از بقیه‌ی حالت‌های sort
- [x] دکمه‌ی «انواع» بین دکمه‌ی موقعیت محصول و دکمه‌ی sort جابه‌جا شد (هم تب محصولات هم کاتالوگ)؛ فقط آیکونه، با tooltip اسم نوع/«همه‌ی انواع»
- [x] تب متریال: دکمه‌ی چشم بین فیلد جستجو و دکمه‌ی «وضعیت موجودی» اضافه شد (toggle نمایش آیتم‌های تمام‌شده/هایدشده)

### تب برش
- [x] دکمه‌ی «ذخیره تصویر» که الان بالا (بین ذخیره‌جلسه و بارگذاری) هست رو حذف کن؛ به‌جاش پایین صفحه بذار (مثل دکمه‌ی قرمز + شناور)، شکل دکمه شناور قرمز با آیکون ذخیره‌ی عکس — **حل شد** (`WoodCuttingTab.jsx`): از نوار بالا حذف شد، الان یه دکمه‌ی گرد قرمز شناور پایین صفحه (کنار دکمه‌ی + اصلی، سمت چپ) هست
- [x] برش ۱D: همه‌ی چوب‌های اصلی (که روشون برش می‌خوره) توی ترسیم باید هم‌سایز باشن و از کادر (عرض صفحه) بیرون نزنن — مگر وقتی عدد روی تکه‌های برش‌خورده جا نشه؛ اون‌وقت فقط در همون حد که عدد جا بشه همه‌شون با هم یه‌کم بزرگ‌تر و قابل‌اسکرول بشن (نه خیلی زیاد) — انجام شد: یه مقیاس مشترک (`plankScale`) حساب می‌شه که پرکارترین ردیف رو دقیقاً توی عرض دیده‌شده جا بده؛ همه‌ی ردیف‌ها همین یه مقیاس رو می‌گیرن (نه هرکدوم مستقل)، پس یکدست به‌نظر می‌رسن. فقط وقتی حتی حداقل سایز خوانا (۱۶px) هم جا نشه، مقیاس قفل می‌شه و اسکرول افقی جای کوچیک‌تر شدن بیشتر رو می‌گیره
  - 🔴 لطفاً تست کن: با تعداد و طول‌های مختلف چوب، ببین ردیف‌ها یکدست به‌نظر می‌رسن و بی‌دلیل اسکرول‌بار نمیاد (فقط وقتی واقعاً لازمه)
  - (این نشست هم موازی روی همین آیتم کار می‌کرد با یه رویکرد متفاوت (flex-grow بدون اندازه‌گیری container) — موقع merge، رویکرد `plankScale` بالا نگه داشته شد چون واقعاً عرض container رو اندازه می‌گیره و دقیق‌تره؛ فقط یه تکه کد اضافه‌ی نیمه‌کاره از تلاش موازی حذف شد)
- [x] برش ۲D: اگه موقع رسم یه شکل، متن داخلش (مثلاً «۲۰×۳۰») جا نشه، اون متن ۹۰ درجه بچرخه و نوشته بشه — **حل شد** (`NestingVisualizer.jsx`): اگه به حالت عادی جا نشه ولی چرخیده جا بشه، با `transform="rotate(90 ...)"` چرخیده نمایش داده می‌شه
- [x] دکمه‌ی نوع «O» (دایره) کنار «D» (نیم‌دایره) نیست — باید اضافه بشه که بتونه دایره‌ی کامل بکشه — **حل شد** (`WoodCuttingTab.jsx`): دکمه‌ی O اضافه شد؛ بقیه‌ی پایپ‌لاین (اندازه‌گیری قطر×قطر، حذف از ۱D، رسم دایره‌ی کامل توی ۲D) از قبل توی کد آماده بود، فقط دکمه‌ی خودش جا افتاده بود
- [x] دایره توی برش ۱D معنی نداره (چوبه، خطیه)، ولی توی ترسیم ۲D باید به شکل یه مربع نقطه‌چین که یه دایره داخلشه نمایش داده بشه — **بررسی شد، از قبل درست پیاده‌سازی شده بود** (`NestingVisualizer.jsx` خط ۸۵-۹۹)؛ فقط دکمه‌ی فعال‌سازیش (O، بالا) جا افتاده بود که الان اضافه شد
- [x] ⚠️ باگ قدیمی «چوب اولی که سبز شده طرف ترسیمش برعکسه» — **پیدا و فیکس شد** (`WoodCuttingTab.jsx`، `PlankRow`): علتش اصلاً به الگوریتم مایتر مربوط نبود، به جهت رندر بود — ردیف چوب‌ها هیچ `direction` صریحی نداشت، پس از `dir="rtl"` کل اپ ارث می‌برد و ترتیب تکه‌ها (flex) کلاً آینه می‌شد، درحالی‌که فرمول‌های `clip-path` توی کد فرض چیدمان چپ‌به‌راست (LTR) رو داشتن. همین باعث می‌شد سمت «صاف»/«کج» هر تکه با تکه‌ی کناریش جفت نشه (و حتی بلوک هدررفت آخر چوب، که با `border-radius`/`border-left` مخصوص «انتهای راست» طراحی شده بود، عملاً سمت چپ نمایش داده می‌شد). فیکس: `direction: "ltr"` روی خودِ ردیف تکه‌ها اضافه شد (فقط همین یک `div`، نه کل صفحه)
  - 🔴 لطفاً با یه سشن واقعی که چند تکه‌ی سبز/آبی/قرمز کنار هم داره تست کن

### تب گالری
- [x] فیلد جستجو اضافه بشه (دقیقاً از روی ظاهر جستجوی تب محصولات کپی بشه) — **حل شد** (`GalleryTab.jsx`)
- [x] بین فیلد جستجو و دکمه‌ی sort این تب، یه دکمه‌ی چندحالته (مثل دکمه‌ی «موقعیت محصول») اضافه بشه با گزینه‌های: «همه»، «بدهکار» (فقط گالری‌های بدهکار)، «دارای موجودی» (گالری‌هایی که موجودی دارن) — با قابلیت چند-انتخابی هم باشه — **حل شد**: کامپوننت جدید `GalleryStatusFilter` با چک‌باکس‌های چند-انتخابی
- [x] سبد خرید، دکمه‌ی «تایید فروش» (با فاکتور کنارش) و دکمه‌ی «ارجاع به گالری» (با فاکتور کنارش) باید به‌هم چسبیده بمونن و موقع اسکرول از دید خارج نشن (شناور/چسبان بمونن) — **حل شد** (`ProductTab.jsx`): ردیف دکمه‌های پرینت/تأیید الان `position:sticky` نسبت به همون باکس اسکرول‌شونده‌ست، پس همیشه پایین دیده می‌شه، حتی وقتی لیست کالاها بلنده

### افزودن/ویرایش محصول 🔵
- [x] فیلد «نوع محصول» به خط بالا، سمت راست (اول، قبل از انتخاب فرش) منتقل بشه؛ نیازی نیست این‌قدر بزرگ باشه — نوع معمولاً خیلی کوتاهه (یه کلمه یا دو کلمه) — **✅ انجام شد ⚫ (Ref)** (`ProductTab.jsx`): از پایین فرم (بعد از «انتخاب فرش») به همون خط بالا (کنار «نام محصول» و «کد») منتقل شد؛ اولین فیلد سمت راستِ همون ردیفه. دکمه‌ی بسته الان کوچیکه (عرض ۷۶px، مثل عرض فیلد «کد») و متن بلند رو با «…» کوتاه می‌کنه؛ پاپ‌آپ لیست کامل انواع (با امکان اضافه/ویرایش/حذف) دست‌نخورده مونده، فقط عریض‌تر (۲۰۰px) باز می‌شه که خوانا بمونه

### فاکتور / پرینت (تب گالری)
- [x] هنوز توی فاکتور تب گالری، تخفیف و هدیه نشون داده نمی‌شه — **حل شد** (`GalleryTab.jsx`): مسیر «چاپ موارد انتخاب‌شده» (فاکتور ادغامی) آیتم‌ها رو بدون map کردن به شکل موردنیاز `InvoiceTemplate` (`finalPrice`/`originalPrice`/`discountPct`) پاس می‌داد؛ الان دقیقاً مثل مسیر «چاپ تکی» map می‌شن
- [x] از تب گالری وقتی مستقیم دکمه‌ی پرینت زده می‌شه، پرانتز اول نام مالک گالری دوبار نوشته می‌شه — **حل شد**: `customer.name` از قبل با پرانتز ساخته می‌شد، و `InvoiceTemplate.jsx` دوباره از روی `galleryOwnerName` همون پرانتز رو اضافه می‌کرد → دوبار. الان فقط یه‌بار اضافه می‌شه
- [x] بک‌گراند ترسیم فاکتور قبلاً وجود داشت و الان حذف شده — **حل شد (Ash 🟡، این نشست هم تایید کرد):** `public/assets/images/invoice-header.jpg` و `invoice-watermark.jpg` الان واقعاً توی ریپو commit شدن (قبلاً نبودن، همون چیزی که این بخش می‌گفت). Wall 🟣 این نشست هم یه باگ مرتبط رو پیدا و فیکس کرد: چون `InvoicePrint.jsx` برای PDF/عکس یه کپی از فاکتور می‌سازه، عکس‌های کپی‌شده باید دوباره لود بشن؛ کد قبلی فقط ۱۵۰ms ثابت صبر می‌کرد که رو گوشی‌های کندتر/بار اول کافی نبود — همین «بعضی‌وقتا میاد بعضی‌وقتا نه» بود. الان واقعاً منتظر لود عکس‌ها می‌مونه (جزئیات کامل توی «دسته‌ی کارهای سطحی جدید» بالاتر)
- [x] QRcode بالا و مشخصات پایین (فوتر) توی فاکتور رسم نمی‌شن — **علت پیدا و حل شد (باگ مهمی که کل تب گالری رو می‌گرفت):** `App.jsx` به گالری‌تب فیلد `businessCard={data?.businessCard}` می‌داد که اصلاً وجود نداره (تایپو)؛ فیلد درست `myBusinessCard`ه که بقیه‌ی تب‌ها ازش استفاده می‌کردن. یعنی از گالری، QR و اطلاعات تماس همیشه خالی می‌موند. هر ۳ جای این باگ (رندر اصلی گالری در `App.jsx`، و داخل `MgmtTabs` هم گالری هم حسابداریش که اصلاً `businessCard` پاس نمی‌داد) درست شد
- [x] کلاً تمپلیت فاکتور بین تب‌ها فرق داره — **✅ ریشه‌ش حذف شد (Wall 🟣، طبق دستور صریح کاربر):** واقعاً ۲ مسیر کاملاً موازی برای رندر تب‌ها وجود داشت — (۱) رندر مستقیم و **بدون هیچ رمزی** توی `App.jsx` (پشت `activeTab`)، و (۲) `MgmtTabs`/`ManagementPanelModal` که از قبل پشت `PinScreen` (رمز ۴رقمی) قفل بود. کاربر مشخص کرد: باید فقط مسیر پشتِ رمز باقی بمونه، مسیر اول باید کاملاً حذف بشه. **کاری که شد:** توی `App.jsx`، `<main>` که قبلاً بسته به `activeTab` هرکدوم از Product/Material/WoodCutting/Gallery/Accounting/Invoices تب رو بدون رمز رندر می‌کرد، الان همیشه فقط `CatalogTab` رو نشون می‌ده — اون شش‌تا کامپوننت از اونجا کاملاً حذف شدن. توی `GlobalHeader.jsx` هم نوار ناوبری‌ای که این تب‌های بدون‌رمز رو نشون می‌داد (`{!isCatalog && <nav>...}`) کاملاً حذف شد. از این به بعد، تنها راه رسیدن به محصولات/متریال/برش/گالری/حسابداری/فاکتورها، دکمه‌ی «پنل مدیریت» توی تب کاتالوگ + رمز ۴رقمیه (`PinScreen`، کد فعلی: همونی که از قبل تنظیم بود، عوضش نکردم)
- 🔴 لطفاً حتماً تست کن: مطمئن شو دیگه هیچ راهی (نه دکمه، نه اسکرول، نه هیچی) جز دکمه‌ی «پنل مدیریت» + رمز به بخش‌های مدیریتی نیست، و کاتالوگ خودش (نمایش محصولات به مشتری) درست کار می‌کنه

### قیمت‌گذاری و تخفیف
- [x] تب محصولات، قسمت جلوی محصول (قیمت): اگه تخفیف خورده، عدد نباید سبز باشه — **حل شد** (`ProductTab.jsx`): رنگ قیمت نهایی بعد از تخفیف از سبز (`#5fd180`) به همون سفید بقیه‌ی قیمت‌ها (`#F5F0EB`) عوض شد
- [x] تب کاتالوگ: تخفیف‌ها اصلاً نمایش داده نمی‌شن — **حل شد** (`ProductTab.jsx`، `CatalogTab`): همون الگوی نمایش تخفیف تب محصولات (قیمت خط‌خورده + کادر درصد + قیمت نهایی، یا برچسب «هدیه» برای ۱۰۰٪) به کارت‌های کاتالوگ هم اضافه شد

### تب متریال — هاید و لیست محصولات لینک‌شده
- [x] دکمه‌ی «هاید کلی» نباید همه رو هاید کنه — **حل شد** (`MaterialTab.jsx`): دکمه‌ی سراسری «هاید همه/آنهاید همه» کاملاً حذف شد. الان هر بخش (فرش/خطی/مساحتی/...) خودش هرچی که یا دستی هاید شده یا کاملاً تموم شده رو با هم جمع می‌کنه، پشت یه دکمه‌ی «X آیتم مخفی شده» (با آیکون چشم) قایم می‌کنه، و فقط با زدن خودِ اون دکمه نمایان می‌شن
- ⚠️ نکته برای هماهنگی: این دو مورد بالا با 🔵 (نشون‌دهنده‌ی «کس دیگه‌ای داره روش کار می‌کنه») علامت خورده بودن، ولی چون این نشست عملاً پیاده‌سازی و تست‌شون تموم شده بود، نسخه‌ی انجام‌شده نگه داشته شد — لطفاً اگه همکار دیگه‌ای هم موازی روی همین دوتا کار کرده، هماهنگ بشید که دوباره‌کاری نشه
- [x] ریز لیست محصولات لینک‌شده به یه متریال باید کولپس‌شده باشه با قابلیت اکسپند (مثل ریز‌هزینه‌ها در تب محصولات) — **حل شد** (`MaterialTab.jsx`): الان پشت یه دکمه با تعداد + فلش کولپس/اکسپند قایمه، پیش‌فرض بسته

### فیلترها جابه‌جا شدن — قبلاً 🔵 بود، دیگه معتبر نیست
- [x] تب محصولات باید فیلترش این باشه: همه / موجود / تسویه‌شده / تسویه‌نشده — **تکراریه، قبلاً بررسی و تأیید شده** (پایین‌تر در «تب حسابداری/بخش ۸»: «تب محصولات: فیلتر برگرده به...»)
- [x] تب کاتالوگ باید فیلترش این باشه: همه / موجود / فروخته‌شده — **تکراریه، قبلاً بررسی و تأیید شده**
(الان جابه‌جا شدن — باید برگردن سرجاشون)

### بخش بولک (تخصیص متریال) — **✅ همه‌ی ۵ مورد این نشست تکمیل شد ⚫ (Ref)**
- [x] دکمه‌ی «پرتی شود» و «باقی بماند» هنوز کوچیک نشدن — کوچیک‌تر شدن (padding و fontSize کمتر)
- [x] این دو دکمه باید بالای عدد «درصد قابل‌استفاده‌ی متریال» در بخش مربوطه قرار بگیرن (الان جاشون درست نیست) — منتقل شدن به درست بالای بخش «درصد مورد استفاده از باقیمانده فعال» (قبلاً پایین‌تر، بعد از «انتخاب بچ»، بودن)
- [x] عدد ورودیِ «تعداد» متریال‌های این متریال انتخاب‌شده قابل ست‌کردن (تایپ‌کردن) نیست — باید اول خط و قبل از نوار درصد باشه — **علتش پیدا شد: این فیلد اصلاً وجود نداشت.** یه state (`quantitySelected`) و محاسبه‌ش (`quantityFraction`) از قبل توی کد بودن ولی هیچ input ای براشون رندر نمی‌شد و هیچ‌جا استفاده نمی‌شدن (کد مرده/نیمه‌کاره). الان یه فیلد «تعداد از کل X» درست بالای نوار درصد اضافه شد (فقط برای متریال‌های شمارشی: نسبتی/ثابت)، کاملاً تایپ‌پذیره، و مستقیم به همون `allocatedRemainingPct` (منبع حقیقتِ باقی محاسبات) وصله — تایپ عدد تعداد بلافاصله درصد رو هم به‌روز می‌کنه (بدون دو منبع حقیقت جدا)
- [x] زیر عدد درصد متریال مصرفی نوشته‌ی پیش‌فرض «۱۰۰٪» هست — این نوشته کلاً برداشته بشه — حذف شد
- [x] محصولاتی که به متریال در بولک لینک می‌شن خیلی بد چیده شدن — الان توی ۲ خطن؛ باید توی یه خط باشه: اول کد محصول + نام، بعدش ابعاد، و در امتداد همون خط نوار درصد + عدد درصد روبروش + ضربدر (×) برای حذف — لیاوت همیشه یه‌خطی شد (قبلاً وقتی اسلایدر فعال بود ۲ خطی می‌شد)؛ دقیقاً به ترتیب خواسته‌شده: کد+نام (کشیده با حذف اضافی)، ابعاد، نوار درصد، عدد درصد، ×
- 🔴 لطفاً تست کن: یه متریال «ثابت» یا «نسبتی» رو باز کن و فیلد «تعداد» رو تغییر بده، ببین درصد و مبلغ درست آپدیت می‌شن

### Undo قفل
- [x] دکمه‌ی Undo قفل (دابل‌کلیک) باید فقط آخرین وضعیت قفل رو بردار و معلق کنه — هر چند بار دابل‌کلیک زده بشه، قفل‌های قبلی‌تر رو یکی‌یکی باز کنه — **✅ بررسی شد، دقیقاً همینه 🟡 (Ash):** خط ۴۲۶ همین فایل (بخش «رفرش/Undo») این دقیقاً هم پیاده‌سازی هم شبیه‌سازی‌شده تست شده — `operations` لیست جداگانه‌ی هر عملیات قفل/آزادسازیه، دابل‌تپ فقط آخرین موردش رو برمی‌گردونه، دابل‌تپ‌های پشت‌سرهم یکی‌یکی عقب‌تر می‌رن. تکراریه، کاری لازم نبود

### تقویم (همه‌جا: متریال جدید/ویرایش، محصول جدید/ویرایش، بچ جدید) — **✅ همه‌ی موارد این نشست حل شد**
فایل: `JalaliDatePicker.jsx` (کامپوننت مشترکه — همه‌ی تقویم‌های اپ از همینه، پس یه‌جا فیکس شد و همه‌جا اثر داره)
- [x] موقع باز شدن، تقویم از کادر بیرون می‌زد — علتش این بود که پاپ‌آپ نسبت به دکمه‌ی تریگر موضع‌گیری می‌شد (`position:absolute`) و اگه فیلد نزدیک لبه‌ی صفحه بود، تقویم بیرون می‌زد. الان `position:fixed` + همیشه دقیقاً وسط صفحه (با یه بک‌گراند نیمه‌شفاف پشتش که با کلیک روش هم بسته می‌شه)
- [x] دکمه‌ی بسته‌ی تقویم خیلی بزرگ بود — الان خیلی کوچیک‌تره و طبق درخواست فقط روز+ماه رو نشون می‌ده (مثلاً «۲۵ تیر»، بدون سال)، هر بار که تاریخ عوض بشه آپدیت می‌شه
- [x] روی «ماه» بزنی، لیست ماه‌ها باز می‌شه؛ روی «سال» بزنی، لیست سال‌ها (از ۱۰ سال قبل تا ۵ سال بعد) باز می‌شه — قبلاً این‌ها فقط متن ثابت بودن، اصلاً کلیک‌پذیر نبودن
- [x] جهت دکمه‌های ‹ › برعکس بود — الان جهت خودِ فلش با کاری که انجام می‌ده یکی شده (فلش راست = جلو/بعد، فلش چپ = عقب/قبل)
- 🔴 لطفاً روی گوشی چک کن: بازکردن تقویم از چند جای مختلف اپ، وسط صفحه بودنش، فشرده‌بودن دکمه، کارکردن ماه/سال، و جهت درست فلش‌ها

### اسکرول بین تب‌ها
- [x] تب کاتالوگ: اگه به طرفین اسکرول بشه، الان به‌اشتباه به یه نسخه‌ی متفاوت پنل مدیریتی وصل می‌شه — **✅ حل شد**: کاتالوگ کاملاً از `MAIN_TAB_ORDER` حذف شد، سوایپ توش دیگه هیچ اثری نداره. (فیلتر «همه/موجود/فروخته‌شده» توی کاتالوگ خودش دکمه‌ست، سوایپ روی خودِ فیلترها اصلاً قرار نبوده کار کنه)
- [x] تب‌های مدیریتی: اسکرول به طرفین دیگه وقتی یه پنجره‌ی دیگه (سبد خرید، ویرایش/افزودن محصول، ویرایش/افزودن متریال، بولک متریال) باز باشه، تب‌های اصلی رو عوض نمی‌کنه — یه رجیستری سراسری جدید ساختم (`utils/modalRegistry.jsx`) که هر پنجره‌ای (حتی اگه استیتش محلی/داخل خودِ کامپوننتش باشه، نیازی به lift کردن نبود) با یه هوک (`useRegisterOpenModal`) خودش رو معرفی می‌کنه؛ سوایپ هم توسط این تعداد غیرفعال می‌شه (`useSwipeTabNav` یه پارامتر `disabled` جدید گرفت)
  - ⚠️ **بخش «بین صفحات/بخش‌های همون پنجره‌ی باز بچرخه» هنوز پیاده نشده** — چون این نیاز به این داره که هر پنجره (فرم محصول، فرم متریال، سبد خرید و…) بخش‌های داخلیش رو به‌عنوان «صفحات» قابل سوایپ تعریف کنه، که یه طراحی جدا برای هرکدوم می‌خواد؛ فعلاً فقط جلوی تداخل با تب‌های اصلی گرفته شده (که مشکل اصلی/آزاردهنده بود)
- [x] همین قانون برای ویرایش/افزودن گالری و مشتری هم باید باشه (بین بخش‌های خودِ گالری/مشتری سوییچ کنه) — **بررسی شد 🟠 (Logy):** توی `CustomerEditor` (`GalleryTab.jsx`) چیزی که شبیه «دو بخش» به‌نظر می‌رسه (دکمه‌های «گالری»/«مشتری» بالای فرم) در واقع یه **سوییچِ نوع رکورد** هست، نه دو صفحه‌ی قابل‌مرور — با کلیک روی هرکدوم، `customer.kind` عوض می‌شه و حتی فیلد نام جابه‌جا/پاک می‌شه (چون گالری و مشتری مدل داده‌ی متفاوتی دارن). سوایپ گذاشتن روش خطرناکه: یه سوایپ تصادفی (مثلاً موقع اسکرول) می‌تونه بی‌خبر یه مشتری واقعی رو به گالری تبدیل کنه و اسمش پاک بشه. **تصمیم: سوایپ اینجا عمداً اضافه نشد** — این یه انتخاب دودویی مخرب‌پذیره، نه یه ناوبری بی‌خطر بین صفحات
- [x] همین‌طور برای کارت ویزیت — **✅ انجام شد 🟠 (Logy):** برخلاف گالری/مشتری، این‌جا («ریفرش» / «همکاران») واقعاً دو **نمای غیرمخرب** جدان (نه یه سوییچ نوع داده). با `useSwipeTabNav`/`useTabSlideClass` (همون هوک تب‌های اصلی) بین‌شون سوایپ چپ/راست اضافه شد، با انیمیشن اسلاید مشابه بقیه‌ی جاهای اپ. وقتی `hidePartners` باشه (فقط نمای خودِ کارت) سوایپ غیرفعاله چون چیزی برای سوییچ نیست
- 🔴 لطفاً تست کن: کارت‌ویزیت (پنل مدیریت → کارت‌ویزیت) رو باز کن، بین «ریفرش» و «همکاران» سوایپ کن ببین جابه‌جا می‌شه و انیمیشن درست داره
- ⚠️ فرم محصول/متریال/سبد خرید هنوز باقی مونده (بزرگ‌تر و نیاز به بررسی جدا برای هرکدوم داره که واقعاً بخش‌های داخلیشون قابل‌سوایپ امنی هستن یا نه، مثل تجربه‌ی بالا)

### حسابداری
- [x] توی جمع کل، لیست فروخته‌شده‌ها، لیست فاکتورها، و هرجای دیگه‌ای که حسابداری می‌شه: محصولات با تخفیف باید درست (با قیمت تخفیف‌خورده) حساب بشن — **بررسی شد:** جمع کل بالا (`سود خالص قطعی`، `درآمد فروش`) توی `App.jsx` از قبل با `finalPriceOf` (که `discountedPrice` رو در نظر می‌گیره) درست حساب می‌شد. باگ واقعی توی کامپوننت `DrillDown` (ریز جزئیات هر بخش، توی `AccountingTab.jsx`) بود — اونجا قیمت/سود خام (`p.salePrice`) نمایش داده می‌شد، بدون تخفیف. **حل شد**
- [x] محصولات هدیه نباید به‌عنوان سود با قیمت اصلی ثبت بشن — باید جدا، به‌عنوان «هدیه» ثبت بشن — **حل شد**: یه بخش جدید و جدا «هدیه داده شده» (با رنگ صورتی، جمع هزینه به‌جای سود) اضافه شد؛ آیتم‌های هدیه (تخفیف ۱۰۰٪) از لیست‌های تسویه‌شده/تسویه‌نشده جدا شدن و توی این بخش جداگونه‌ن، دیگه به‌عنوان سود شمرده نمی‌شن
- [x] ریز بخش «فروخته‌شده‌ها» باید ۲ تا اکسپند/کولپس جدا داشته باشه: یکی «فروخته‌شده‌ها»، یکی «هدیه‌ها» — **حل شد** (همون تغییر بالا)
- [x] ترتیب نهایی بخش‌ها: فروخته‌شده تسویه‌شده، فروخته‌شده تسویه‌نشده، هدیه داده‌شده (جدید)، بعدش موجودی انبار، و بقیه... — **حل شد**، دقیقاً همین ترتیب رعایت شد
- [x] نمودار سود ماهانه باید قابلیت رفتن به سال‌های قبل رو هم داشته باشه (و داده‌ی سال‌های قبل هم باید ثبت/نگه‌داری بشه) — **حل شد** (`AccountingTab.jsx`، `MonthlyChart`): بررسی کردم، لایه‌ی داده (`monthlyProfit` توی `App.jsx`) از قبل همه‌ی سال‌های موجود توی تاریخ فروش رو جمع می‌کرد (محدود به سال جاری نبود) — فقط خودِ نمودار UI ناوبری سال نداشت. الان یه انتخاب‌گر سال (فلش قبلی/بعدی + عدد سال) بالای نمودار اضافه شد که فقط بین سال‌هایی که واقعاً داده دارن حرکت می‌کنه؛ پیش‌فرض جدیدترین سال باز می‌شه

### ابعاد محصول (طول×عرض)
- [x] وقتی طول و عرض برعکس وارد بشه (عرض عدد بزرگ‌تر باشه)، الان خودکار درستش می‌کنه ولی دقیقاً برعکسِ چیزی که باید بشه — **رفع ابهام شد ⚫ (Ref):** کاربر با اسکرین‌شات دقیق تأیید کرد منظورش همون «بزرگ اول، کوچیک دوم» با همون لیبل «طول×عرض»‌ه (نه برعکس‌کردن خودِ لیبل به «عرض×طول»)؛ فیکسش پایین‌تر در «۲) گزارش کاربر با اسکرین‌شات» ثبت شده — مهاجرت داده‌های قدیمی هم اضافه شد

### باگ اجرا (AI Studio) — ✅ همین بگ توی این ریپو هم بود، فیکس شد
- [x] چک کردم: بله، دقیقاً همین مشکل توی این ریپو هم بود — `MgmtTabs` (توی `App.jsx`، همون بخشی که پنل مدیریت PIN-قفل‌شده رو می‌سازه) به `updateProcurement`/`deleteProcurement` رفرنس می‌داد ولی این دوتا نه توی لیست props خودش تعریف شده بودن، نه از جایی که `<MgmtTabs>` صدا زده می‌شه پاس داده می‌شدن. با باز کردن تب متریال (چون تب مساحتی/فرش/نسبتی چندبچی حالا از procurements استفاده می‌کنه) این کرش پیش می‌اومد. فیکس شد — هردو حالا هم توی props تعریف شدن هم درست پاس داده می‌شن. **این یه باگ واقعی و مستقل بود، نه اینکه فقط توی AI Studio باشه** — پس اگه توی APK هم تست کرده بودید همین کرش رو می‌دیدید

### پیش‌نیازهای فیس‌آیدی / اثرانگشت (طبق درخواست، جمع‌بندی نهایی)
برای باز کردن پنل با اثرانگشت/فیس‌آیدی، این‌ها لازمه (هنوز نصب/اضافه نشده):
- پکیج: `@capawesome-team/capacitor-biometrics` (فعال‌ترین و کامل‌ترین پلاگین Capacitor برای این کار در حال حاضر؛ جایگزین‌های دیگه مثل `capacitor-native-biometric` یا `@aparajita/capacitor-biometric-auth` هم هستن ولی این یکی به‌روزتر و مستندتره)
- نصب: `npm install @capawesome-team/capacitor-biometrics` سپس `npx cap sync`
- **iOS:** یه کلید `NSFaceIDUsageDescription` توی `Info.plist` اضافه بشه (رشته‌ی توضیح چرا اپ به FaceID نیاز داره) — بدون این کلید اپ روی گوشی‌های FaceID‌دار کرش می‌کنه
- **اندروید:** مجوز `<uses-permission android:name="android.permission.USE_BIOMETRIC" />` به `AndroidManifest.xml` اضافه بشه
- بعد از نصب و اضافه‌کردن مجوزها، حتماً باید روی گوشی واقعی (نه شبیه‌ساز/مرورگر) تست بشه، چون این پلاگین توی مرورگر فقط یه پیاده‌سازی ساختگی (dummy) داره

🟢 = من (Claude، از طریق گیت‌هاب) دارم روی این بخش‌ها کار می‌کنم — همکار AI لطفاً روی این‌ها هم‌زمان کار نکنه:
- تب محصولات/متریال — دکمه‌های بالای لیست (sort/group/انواع/چشم) — **✅ بخشی تکمیل شد (Claude دیگه، هم‌زمان)**: دکمه‌ی sort الان با کلیک ساده باز می‌شه (نه هولد) و با انتخاب بسته نمی‌شه؛ دکمه‌ی «دسته‌بندی/یکجا» از تولبار حذف و به بالای پاپ‌آپ sort منتقل شد؛ دکمه‌ی هاید متریال از «هاید همه»ی مخرب به یه toggle نمایش (reveal) غیرمخرب تبدیل شد که فقط موارد دستی‌هایدشده/تمام‌شده رو نشون/مخفی می‌کنه، با یه کادر «X آیتم مخفی شده» به‌جای دکمه‌ی مخرب قبلی. دکمه‌ی «انواع» هنوز جابه‌جا نشده
- تب برش (جای دکمه‌ی ذخیره تصویر، سایز چوب‌های ۱D، چرخش متن ۲D، دکمه‌ی O، باگ مایتر)
- تب گالری (جستجو، فیلتر چندحالته، دکمه‌های چسبان سبد خرید)
- اسکرول بین تب‌ها (سوییچ افقی)
- حسابداری (تخفیف/هدیه)
- **اضافه (Claude دیگه، هم‌زمان، خارج از این لیست claim):** رنگ قیمت تخفیف‌خورده در لیست محصولات از سبز به سفید تغییر کرد؛ نمایش تخفیف (خط‌خورده + درصد + بج هدیه) به تب کاتالوگ هم اضافه شد که قبلاً اصلاً نداشت؛ فیلتر تب محصولات و تب کاتالوگ که جابه‌جا شده بودن (محصولات باید موجود/تسویه‌شده/تسویه‌نشده داشته باشه، کاتالوگ باید همه/موجود/فروخته‌شده داشته باشه) برگردوندن سرجاشون

## 🆕🆕🆕 دور جدید ۲ — همون الگو (مستقیم از پیام کاربر + عکس‌ها)

⚠️ هماهنگی: طبق قرارداد بالا (🔵ها)، این‌ها رو ندید — لطفاً کسی موازی روی این آیتم‌های تازه کار نکنه تا هماهنگ بشه.

### فیلترها (چندجا)
- [x] «موقع باز کردن فیلترها در تمام تب‌ها جاشون درست نیست — یا خودکار صفحه رو اسکرول می‌کنن یا بیرون باز می‌شن» — این دقیقاً همون مشکلیه که برای تقویم حل شد (پاپ‌آپ‌های `position:absolute` نسبت به دکمه‌ی نزدیک لبه‌ی صفحه بیرون می‌زدن). **تقویم حل شد** (بخش بالا). بقیه‌ی فیلترهای دراپ‌داون (sort، دسته‌بندی، وضعیت و…) — **✅ بررسی شد، الان کاملاً حل شده 🟡 (Ash):** این همون بخش ۸ (بازسازی پاپ‌آپ‌های خارج‌از-کادر)ه که 🟣 Wall به‌طور جدا کامل کرده — یه کامپوننت مشترک `FilterPopup.jsx` ساخته شده و الان `GalleryTab.jsx`/`MaterialTab.jsx`/`ProductTab.jsx` هر سه ازش استفاده می‌کنن؛ تکراریه، کاری لازم نبود

### تقویم — ادامه
- [x] برای انتخاب سال، به‌جای تایل‌تایل، لیست از ۱۴۰۰ به بعد با اسکرول — **در نسخه‌ی فعلی، بازه‌ی سال‌ها ۱۰ سال قبل تا ۵ سال بعد از سال درحال‌نمایشه (نه ثابت از ۱۴۰۰)**؛ اگه دقیقاً از ۱۴۰۰ ثابت می‌خوای (نه نسبی)، بگو تا عوضش کنم
- [x] آیکون تقویم از اموجی به یونیکد مینیمال خطی عوض شد + کادر دکمه‌ی بسته دیگه به‌زور تمام عرض نیست، فیت خودشه
- [x] «اسکرول روی عدد فعلی برای تغییر سریع سال» (مثل price picker چرخشی) — **✅ اضافه شد ⚫ (Ref)** (`JalaliDatePicker.jsx`): خودِ عدد سال توی هدر تقویم (حالت «روزها») الان قابل کشیدنه — کشیدن انگشت/ماوس به بالا روی همون عدد = سال بیشتر، به پایین = سال کمتر (هر ۲۲px کشیدن = ۱ سال، با `pointer events`، کار می‌کنه هم با لمس هم با ماوس)؛ روی دسکتاپ اسکرول ماوس (`wheel`) هم همین کارو می‌کنه. اگه فقط تپ ساده باشه (بدون کشیدن محسوس)، مثل قبل لیست کامل سال‌ها باز می‌شه. بازه‌ی مجاز همون ۱۴۰۰ تا سال جاریه (کلمپ می‌شه، بیرون نمی‌زنه)
- 🔴 لطفاً روی گوشی تست کن: روی فیلد تاریخ بزن، بعد انگشتت رو دقیقاً روی عدد سال (نه جای دیگه‌ی هدر) بذار و بکش بالا/پایین — باید سال سریع عوض بشه بدون باز شدن لیست؛ یه تپ سریع (بدون کشیدن) هم باید مثل قبل لیست رو باز کنه

### انیمیشن‌ها (شبیه تلگرام)
- [x] انیمیشن اسکرول بین تب‌ها (مثل تلگرام) — **✅ بررسی شد، قبلاً حل شده بود 🟡 (Ash):** این دقیقاً همون کاریه که توی بخش «۱۱) اسکرول و ناوبری بین تب‌ها» (پایین همین فایل) با `useTabSlideClass` انجام شده — تکراریه، کد جدیدی لازم نبود
- [x] انیمیشن اینرسی روی دکمه‌ی رفرش (نگه‌داشتن → چرخش؛ رها کردن → ۱-۲ ثانیه با کاهش سرعت طبیعی بچرخه و بایسته) — **✅ اضافه شد** (`GlobalHeader.jsx`): به‌جای انیمیشن CSS ساده‌ی روشن/خاموش، یه چرخش مبتنی بر فیزیک با `requestAnimationFrame` ساخته شد — حین نگه‌داشتن با سرعت ثابت می‌چرخه، موقع رها کردن سرعت طی چند ثانیه با ضریب کاهشی (۰.۹۶۵ در هر فریم) کم می‌شه تا طبیعی بایسته، نه یهو قطع بشه

### پنل مدیریتی مخفی/تکراری (کشف‌شده با اسکرول طرفین از کاتالوگ) — **✅ کاملاً حذف شد (Wall 🟣). جزئیات و توضیح کامل توی «بخش ۱ (Refresh)» بالا و توی «فاکتور/پرینت» زیرِ آیتم «تمپلیت فاکتور بین تب‌ها فرق داره»**
- [x] **جواب سوال «این پنل مخفی از کجا میاد؟»:** پیدا شد — این یه build جدا یا AI Studio نیست، بلکه یه کامپوننت واقعی داخل همین `App.jsx` به اسم `MgmtTabs` (پشت `ManagementPanelModal`، پشت پین)، که یه کپی کامل و موازی از رندر تب‌های مدیریتیه (Gallery, Materials, Products, Accounting, ...) — دقیقاً همون تب‌های اصلی رو دوباره، با کدهای جدا، رندر می‌کنه. این توضیح می‌ده چرا بعضی فیکس‌ها یه‌جا دیده می‌شن یه‌جا نه (مثل باگ QR/فوتر فاکتور — بخش فاکتور/پرینت رو ببینید)
- [x] **ولی خودِ باگ گزارش‌شده (سوایپ از کاتالوگ می‌رسید به تب «فاکتورها» به‌جای «همگام‌سازی») اصلاً از `MgmtTabs` نمی‌اومد** — چون `MgmtTabs` فقط از طریق دکمه‌ی «پنل مدیریت» + پین باز می‌شه، نه با اسکرول. علت واقعی سوایپ این بود که `<main>` (رندر مستقیم و بدون پین، کاملاً جدا از `MgmtTabs`) خودش یه آرایه‌ی سوایپ‌شونده‌ی دیگه داشت (`MAIN_TAB_ORDER`) که «کاتالوگ» هم توش بود، و سوایپ از کاتالوگ به‌راحتی می‌رفت توی «محصولات/متریال/حسابداری/فاکتورها»ی همون `<main>` — بدون هیچ پینی. **این نشست حل شد:** کاتالوگ کاملاً از `MAIN_TAB_ORDER` حذف شد (`App.jsx`)، پس دیگه سوایپ از کاتالوگ به هیچ‌کجا نمی‌ره. این هم‌زمان یه bypass ناخواسته‌ی پین رو بست (چون `activeTab` قبلاً هیچ راه ورود دیگه‌ای جز همین سوایپ نداشت)
- [x] **باقی‌مونده (کم‌فوریت‌تر، جدا از باگ اصلی):** `MgmtTabs` واقعاً یه کپی موازی از تب‌های مدیریتیه — **✅ این نگرانی دیگه صادق نیست (تایید مستقل دوبار، Wall 🟣 و Ash 🟡):** طبق دستور صریح کاربر (بخش امنیت پنل مدیریت، بالاتر توی همین فایل)، مسیر موازیِ بدون‌پین توی `<main>` که این کپی‌کاری رو باعث می‌شد، **کاملاً حذف شد** — `<main>` دیگه هیچ‌وقت Product/Material/WoodCutting/Gallery/Accounting/Invoices رو مستقیم رندر نمی‌کنه، فقط `CatalogTab`. الان هرکدوم از این تب‌ها فقط توی **یه جا** رندر می‌شن (داخل `MgmtTabs`، پشت پین)، نه دو جای موازی — «دوباره‌کاری و باگ‌های پراکنده»ای که این آیتم نگرانش بود دیگه از این جهت وجود نداره. این آیتم بسته می‌شه؛ اگه کاربر یه‌جای دیگه هنوز دو-تایی/ناهماهنگ می‌بینه، لطفاً بگو دقیقاً کجا

### فیلد نام گالری/مشتری در سبد خرید
- [x] فیلد انتخاب نام گالری و مشتری توی سبد خرید باید بیاد بالا؛ دکمه‌های ثبت و پرینت فاکتور اون پایین فیکس (چسبان، از دید خارج نشه) بمونن — **هر دو حل شد** (`ProductTab.jsx`): فیلد نام مشتری/گالری مقصد به بالای سبد (قبل از لیست کالاها) منتقل شد؛ دکمه‌های پرینت/تأیید هم چسبان (`sticky`) شدن — همون فیکسی که چند خط بالاتر (بخش تب گالری) هم ثبت شده

### متریال — نام‌گذاری و کش شدن نام‌ها (باگ مهم)
- [x] وقتی اسم یه متریال/فرش عوض می‌شه، جاهای دیگه (دسته‌بندی توی تب محصولات، ریز هزینه‌ها) به‌روز نمی‌شن — **پیدا و فیکس شد ⚫ (Ref)، با مثال دقیق کاربر (موتور ساعت / فرش سعادت‌آباد)** — توضیح کامل پایین‌تر در «۲) گزارش کاربر با اسکرین‌شات»: مشکل `li.label` بود (کپی یه‌باره‌ی نام موقع لینک‌شدن، نه رفرنس زنده)

### بولک — تخصیص متریال (نکات جدید، همون بخش claimed 🔵 قبلیه)
- [x] وقتی متریال بولک می‌شه، توی هزینه‌ی محصول (و قسمت ویرایش)، باید درصد واقعیِ سهم همون متریال از بولک نوشته بشه (مثلاً ۲ یا ۳ درصد)، نه همیشه «۱۰۰٪» — این «۱۰۰٪» یه اشتباه نوشتاریه، چون بولک به *کل دسته* ۱۰۰٪ بوده، نه به یه محصول خاص — **✅ انجام شد ⚫ (Ref).** بررسی کردم: هیچ‌جا لفظاً «۱۰۰٪» هاردکد نبود (فقط توی همون پاپ‌آپ بولک بود که قبلاً حذفش کردم)؛ مشکل واقعی این بود که برای آیتم‌های قفل‌شده (`li.deductedAt`) اصلاً هیچ درصدی نمایش داده نمی‌شد — نه غلط، فقط نبود. سهم واقعی از قبل توی دیتا (`li.customPct`، همون درصد واقعیِ محاسبه‌شده حین بولک) ذخیره می‌شد ولی جایی نمایش داده نمی‌شد. الان اضافه شد به ۲ جا: (۱) فرم ویرایش محصول، کنار «قفل شده {تاریخ}» → «· سهم: ۲.۳٪»، (۲) کارت محصول، توی «ریزهزینه‌ها» → «نام متریال (۲.۳٪)»
- [x] برای فرش و مساحتی هم دکمه‌ی «پرتی شود» باید عمل کنه: باقیمانده باید به تعداد اقلام وصل‌شده‌ی معلق تقسیم بشه و به هزینه‌ی هرکدوم اضافه بشه — **✅ باگ واقعی پیدا و فیکس شد ⚫ (Ref) — ⚠️ این یکی روی محاسبه‌ی مالی واقعی اثر می‌ذاره، حتماً با دقت تست بشه.** ریشه: توی موتور قفل‌کردن واقعی (`App.jsx`، بخش «۴.۳ بچ‌های مساحتی/فرش»، جایی که با دکمه‌ی رفرش واقعاً deduct می‌شه)، `includeWastage` اصلاً خونده نمی‌شد — باقیمانده‌ی بچ همیشه فقط به *نسبت مساحت خودِ محصولات* بین آیتم‌های معلق تقسیم می‌شد، بدون توجه به این‌که «پرتی شود» تیک خورده یا نه. این دکمه فقط توی *پیش‌نمایش* (`computeAreaBatchCostByProductPure`) واقعاً اثر داشت، نه توی قفل نهایی — یعنی عددی که قبل از قفل نشون داده می‌شد با عددی که واقعاً کسر می‌شد فرق داشت. الان قفل واقعی هم دقیقاً همون منطق پیش‌نمایش رو اجرا می‌کنه: اگه ابعاد بچ (طول×عرض) معتبر باشه، باقیمانده‌ی فیزیکیِ بچ (بعد از کسر مساحت مصرفی قبلی و همین دور) بین آیتم‌های «پرتی‌شود»-دار همین دور تقسیم و به هزینه‌شون اضافه می‌شه؛ اگه بچی ابعاد نداشته باشه (برای امنیتِ داده‌های قدیمی)، به همون روش قبلی برمی‌گرده
- 🔴 **حتماً قبل از استفاده‌ی واقعی تست کن:** یه بچ فرش/مساحتی با ابعاد مشخص بساز، ۲-۳ محصول کوچیک بهش وصل کن، فقط رو بعضی‌هاشون «پرتی شود» بزن، قفل کن (نگه‌داشتن دکمه رفرش)، و مبلغ کسرشده‌ی هرکدوم رو با محاسبه‌ی دستی مقایسه کن
- [x] هر بچ توی متریال حداقل باید تعداد ۱ حساب بشه؛ اگه تعداد واقعی بیشتر بود، همون عدد بیشتر به‌عنوان ورودی حساب بشه (نه اینکه هر بچ صرفاً ۱ فرض بشه) — **✅ باگ واقعی پیدا و فیکس شد 🟠 (Logy).** ریشه: فرم «افزودن بچ» (`MaterialTab.jsx`) یه فیلد «تعداد بچ» داره (مثلاً وقتی ۳ رول فرش کاملاً یکسان با هم خریداری می‌شه)، ولی این عدد فقط برای ثبت تاریخچه‌ی خرید مصرف می‌شد و **هیچ‌وقت روی خودِ آبجکت بچ ذخیره نمی‌شد** (`onAddBatch` اصلاً `qty` رو پاس نمی‌داد). در نتیجه، مساحت کل بچ همه‌جای کد (۴ محل جدا توی `App.jsx`: موتور قفل، پیش‌نمایش هزینه، useMemo زنده) همیشه فقط از «عرض×طول» یه واحد حساب می‌شد و به تعداد واقعی کاری نداشت — دقیقاً مثل این‌که همیشه ۱ عدد فرض بشه، حتی اگه کاربر ۵ تا زده باشه. **فیکس:** `qty` واقعاً به بچ ذخیره می‌شه، هر ۴ محاسبه‌ی مساحت به `عرض×طول×تعداد` تغییر کرد، و یه فیلد «تعداد بچ» هم به فرم *ویرایش* بچ (که قبلاً اصلاً این فیلد رو نداشت) اضافه شد
- 🔴 لطفاً تست کن: یه بچ فرش/مساحتی جدید با تعداد ۲ یا ۳ بساز، مطمئن شو مساحت کل/باقیمانده‌ش به همون نسبت بیشتره (نه فقط عرض×طول یه دونه)

### گزارش‌های متفرقه
- [x] فونت اپ یهو (بدون دخالت کاربر) بهم ریخت — **کاربر گفته انگار دیگه درست شده؛ فعلاً بسته می‌شه**، اگه دوباره دید دوباره گزارش می‌ده
- [x] توی ریز هزینه، قیمتِ نمایش‌داده‌شده با قیمت فرم ویرایش یکی نبود — **✅ حل شد** (`ProductTab.jsx`): با عکس‌های شما (رانر رومیزی #0098: فرم ویرایش می‌گفت فرش مصرفی=۳۲,۵۵۰، لیست/ریز‌هزینه می‌گفت ۹۶,۷۲۷) علتش پیدا شد — **این ربطی به کش‌شدن اسم/داده نداشت**، بلکه دو فرمول محاسبه‌ی جدا برای هزینه‌ی فرش وجود داشت: فرم ویرایش هزینه رو فقط برای همون یه محصول به‌تنهایی حساب می‌کرد (مساحت خودش تقسیم بر مساحت کل فرش)، ولی لیست/قفل‌شدن واقعی از یه فرمول «استخر مشترک» استفاده می‌کنه که وقتی چند محصول با هم به یه فرش وصل باشن، هزینه رو متناسب با سهم مساحت هرکدوم بین‌شون تقسیم می‌کنه (`areaBatchCostByProduct`) — این دومی همونیه که واقعاً موقع قفل شدن پول کم می‌شه، پس فرم ویرایش رو مجبور کردم از همون منبع بخونه تا دقیقاً یکی باشن. فقط برای محصول تازه‌ساز/هنوز-ذخیره‌نشده که هنوز توی محاسبه‌ی استخر نیست، یه تخمین لحظه‌ای موقت نشون داده می‌شه تا سیو بشه

### پیش‌نمایش رنگی اکسل + یکپارچگی فاکتور (فیچر بزرگ، چند بخش)
- [x] پیش‌نمایش اکسل رنگی: کلمات دقیقاً شبیه اکسل نمونه‌ی ارسالی باشه، ولی به‌جای این‌که جزئیات هزینه توی ستون باشه، توی سرِ جدول باشه؛ ریز هزینه به تعداد متریال‌ها باشه — **✅ اضافه شد** (`App.jsx`، `handleExportPreviewExcel`): فایل نمونه (`Book1.xlsx`) رو دیدم؛ ستون‌های ریز-هزینه الان به‌صورت **پویا** از روی متریال‌های واقعاً استفاده‌شده توی محصولات ساخته می‌شن (نه لیست ثابت مثل فایل شما که همیشه همون ۱۲-۱۳ ستون رو داشت) — یعنی هر متریالی که جایی توی محصولات استفاده شده باشه، ستون خودشو توی سرِ جدول می‌گیره، و هر محصول فقط توی ستون‌های متریال‌هایی که واقعاً استفاده کرده عدد داره. اسم هر ستون هم از روی شناسه‌ی زنده‌ی متریال خونده می‌شه (نه از یه نام کش‌شده) — این هم‌زمان به باگ «کش شدن اسم متریال» کمک می‌کنه چون همیشه از منبع تازه می‌خونه. ستون‌های گالری/بدهکار/گروه‌بندی‌رنگی قبلی هم دست‌نخورده موندن، فقط بعد از ستون‌های متریال قرار گرفتن. **این نشست تکمیل شد:** جداکننده‌ی هزارگان (`#,##0`) به همه‌ی ستون‌های پولی اضافه شد (قبلاً رقم خام بدون کاما بود)، و هدر + ۳ ستون اول (کد/نام/ابعاد) الان فریز (freeze pane) هستن تا موقع اسکرول افقی روی ستون‌های زیاد متریال، گم نشن. با دیتای نمونه‌ی شبیه فایل واقعی شما تست شد و ساختارش (گروه‌بندی رنگی، ستون هر متریال، جمع گالری‌ها) درست از آب دراومد
- [x] فاکتور تب حسابداری: باید فیلتر/جستجو بر اساس تاریخ داشته باشه — **✅ اضافه شد** (`AccountingTab.jsx`، `AllInvoicesModal`)
- [x] فاکتور باید قابل‌ویرایش باشه (حذف یا افزودن اقلام بعد از صدور) — **✅ اضافه شد** (`AccountingTab.jsx`، `AllInvoicesModal`): چون «فاکتور» توی این کد یه رکورد مستقل ذخیره‌شده نیست، بلکه یه گروه‌بندی زنده از محصولاته (بر اساس خریدار+تاریخ)، «حذف یه قلم از فاکتور» یعنی همون محصول رو تنها برگردونی به انبار (بدون اینکه بقیه‌ی اقلام فاکتور دست بخوره) — دکمه‌ی 🗑 جلوی هر قلم اضافه شد. «افزودن قلم» هم یعنی یه محصولِ موجودِ دیگه رو به همون گروه خریدار/تاریخِ این فاکتور وصل کنی — یه دکمه‌ی «+ افزودن محصول به این فاکتور» با جستجوی محصولات موجود اضافه شد
- [x] به‌جای sandbox، «پیش‌فاکتور» (Draft) بسازه — بشه سیوش کرد، عکس/PDF ازش گرفت، و اگه خواستی نهایی‌اش کنی — **✅ اضافه شد** (`InvoicesTab.jsx`): بخش «شبیه‌ساز/Sandbox» با دیتای تستی هاردکد کاملاً حذف شد؛ به‌جاش:
  - یه فیلد جستجوی محصول واقعی اضافه شد — با تایپ اسم/کد، از لیست واقعی محصولات پیشنهاد میده و با کلیک، قلم واقعی (اسم/کد/ابعاد/قیمت واقعی) به پیش‌فاکتور اضافه می‌شه (قیمت بعداً هم قابل ویرایش دستیه)
  - هر قلم دکمه‌ی حذف مستقل داره (قبلاً اصلاً نبود)
  - دکمه‌ی «ذخیره پیش‌نویس» — پیش‌فاکتور رو توی آرایه‌ی جدید `data.invoiceDrafts` ذخیره می‌کنه (فیلد جدید توی دیتامدل، `dataModels.js`)
  - لیست «پیش‌فاکتورهای ذخیره‌شده» پایین صفحه — هرکدوم قابل «باز کردن» (بارگذاری دوباره برای ویرایش) یا حذفه
  - «پیش‌نمایش و چاپ A4» همون مکانیزم قبلی (`InvoicePrint.jsx`) رو استفاده می‌کنه، پس عکس/چاپ/دانلود از قبل هم کار می‌کرد
  - ⚠️ **«نهایی‌کردن» خودکار (تبدیل پیش‌فاکتور به فروش واقعی که وضعیت محصولات رو هم عوض کنه) پیاده نشد** — چون این یعنی دست‌زدن به منطق حساس فروش/موجودی؛ فعلاً پیش‌فاکتور فقط برای آماده‌سازی/چاپ/ذخیره‌ست، نهایی‌کردنش دستیه (محصولات رو از همون تب محصولات به‌صورت عادی «فروخته شد» بزنید)
  - ⚠️ **سینک ابری برای `invoiceDrafts` اضافه نشد** — فقط محلی (localStorage) ذخیره می‌شه؛ اگه بین دستگاه‌ها لازمه، نیاز به آپدیت `schema.ts`/`server.ts` داره (مثل بخش‌های قبلی که ستون جدید نیاز داشتن)
- [x] **کشف مهم موقع کار روی فیلتر تاریخ — ✅ حل شد طبق تصمیم شما:** یه کامپوننت کامل و جدا به اسم `InvoicesTab.jsx` (۶۷۰ خط) توی پروژه هست که از قبل فیلتر تاریخ، جستجو، و فیلتر وضعیت‌تسویه داشت. بعد از فیکس امنیتی سوایپ کاتالوگ، این کامپوننت یتیم شده بود. طبق نظر شما، یه دکمه‌ی «نمای پیشرفته» توی هدر مودال «تمامی فاکتورها» (تب حسابداری) اضافه شد که با کلیک، `InvoicesTab.jsx` رو به‌صورت یه صفحه‌ی تمام‌صفحه‌ی جدا (با دکمه‌ی بستن) نشون می‌ده — دیگه هیچ‌جا یتیم نمونده. توجه: بخش «فاکتور فروش (تست)» داخل `InvoicesTab.jsx` (همون sandbox قدیمی) دست‌نخورده موند چون فقط قرار بود قابل‌دسترسی بشه، نه بازطراحی — بازطراحی sandbox→Draft هنوز کار جداییه (خط بعدی)
- [x] کلاً سیستم فیلترها و چاپ فاکتور و خیلی چیزهای دیگه باید یکپارچه بشه (همون چیزی که قبلاً هم گفته بودی: «تمپلیت فاکتور بین تب‌ها فرق داره») ✅ (به دستور مالک — بدون تغییر کد) — خودِ مالک گفته فعلاً «انجام‌شده» در نظر گرفته بشه تا خودش بعداً بررسی کنه؛ **هیچ کدی توی این آیتم تغییر نکرد**، متن اصلی مبهم بود (کدوم فیلترها/تمپلیت‌ها هنوز فرق دارن مشخص نشد) و بررسی/تصمیم واقعی به عهده‌ی خودِ مالک موند. اگه بعداً معلوم شد چیزی هنوز ناهماهنگه، این تیک باید برگرده به `[ ]` با جزئیات دقیق.

⚠️ نکته‌ی مهم درباره‌ی «پنل مدیریتی جدا»: کاربر گفته نصف تغییرات این نشست‌ها توی پنل *خودش* دیده نمی‌شه چون یه پنل جدید/جدا (که با اسکرول طرفین از کاتالوگ کشف کرده) داره باز می‌شه که با این تغییرات هماهنگ نیست. **این نکته حیاتیه** — یعنی ممکنه بخشی از این کدها (این ریپو) روی گوشی کاربر درست دیده نشه چون یه build/پنل دیگه لود می‌شه. لطفاً این رو به همکارها هم بگو که بررسی کنن دقیقاً کدوم build/دامنه/پنل داره لود می‌شه، وگرنه هرچقدر اینجا فیکس بشه ممکنه دیده نشه.

**⚠️ اصلاح این نشست (اشتباه من):** فرض «پنل جعلی» کاملاً غلط بود. با کمک AI Studio مشخص شد این اصلاً باگ نیست — یه فیچر واقعی و عمدیه: `App.jsx` دو تا سیستم تب سوایپ‌شونده‌ی **جدا** داره —
1. `MAIN_TAB_ORDER` (خط ۱۰۷۲): `catalog → products → materials → woodCutting → gallery → accounting → invoices` — بدون پین، مستقیم توی `<main>` رندر می‌شه، با هوک `useSwipeTabNav`
2. `MGMT_TAB_ORDER` (خط ۲۷۹، داخل `ManagementPanelModal`، پشت پین): `products → materials → woodCutting → gallery → accounting → sync`

سوایپ از کاتالوگ که کاربر تجربه می‌کرد، رفتن به تب «فاکتورها»ی همین سیستم اول بود (نه یه پنل جدا) — فقط چون تب آخرش اسم متفاوتی داشت (فاکتورها به‌جای همگام‌سازی، چون این دو آرایه‌ی جدا از هم هستن)، گیج‌کننده به نظر می‌رسید.
**اشتباه من:** برای «حل» یه باگ که اصلاً وجود نداشت، `overscroll-behavior-x: none` رو گلوبال به `html`/`body`/`#root` اضافه کردم — این کار هوک سوایپ واقعی (`useSwipeTabNav`، که خودش با دقت طراحی شده بود تا با اسکرول طبیعی تداخل نکنه) رو در نقاط مرزی (از products به عقب به کاتالوگ، و از آخرین تب به جلو) خراب کرد. **این تغییر کاملاً برگردونده شد** (`index.css`) — دیگه هیچ محدودیت overscroll گلوبالی نیست.

**✅ فیکس واقعی (طبق درخواست صریح شما):** `catalog` کاملاً از `MAIN_TAB_ORDER` حذف شد (`App.jsx`، خط ~۱۰۷۲). حالا:
- توی تب کاتالوگ، سوایپ افقی کاملاً بی‌اثره (چون `activeTab==="catalog"` دیگه توی این آرایه نیست، ایندکسش `-1` می‌شه و هوک سوایپ به‌طور طبیعی هیچ کاری نمی‌کنه)
- «محصولات» الان اولین عضو آرایه‌ست، پس سوایپ به عقب ازش هیچ کاری نمی‌کنه (نمی‌رسه به کاتالوگ)
- «فاکتورها» (آخرین عضو) با سوایپ به جلو هم هیچ کاری نمی‌کنه
- ورود/خروج بین `ManagementPanelModal` (پشت پین) و `<main>` (سوایپ عمومی): این دوتا از اول هم توی DOM «خواهر» هم بودن نه تودرتو (مودال یه overlay کامل `position:fixed` با `zIndex:300` جدا رندر می‌شه)، پس لمسی که از داخل مودال شروع بشه اصلاً به handler سوایپ `<main>` نمی‌رسه و برعکس — این ایزوله‌سازی از قبل هم درست بود، نیازی به تغییر نداشت
- ⚠️ **یه نکته‌ی جانبی مهم که در همین بررسی پیدا شد:** قبل از این فیکس، `activeTab` هیچ راه دیگه‌ای برای عوض‌شدن جز همین سوایپ نداشت (هیچ دکمه‌ای `setActiveTab` رو صدا نمی‌زد) — یعنی از تب عمومی کاتالوگ می‌شد فقط با سوایپ، بدون پین، به «محصولات»، «متریال»، «حسابداری» و «فاکتورها» (همون دیتای کاری واقعی) رسید. با حذف کاتالوگ از این زنجیره، این مسیر هم عملاً بسته شد — یعنی این فیکس یه سوراخ امنیتی ناخواسته (bypass پین) رو هم به‌طور جانبی بست

**✅✅ تصمیم نهایی کاربر و فیکس کامل (این نشست، Wall 🟣):** با اینکه سوایپ بسته شده بود، خودِ سیستم دوم (`MAIN_TAB_ORDER` + رندر مستقیم بدون پین توی `<main>`) هنوز توی کد بود — یعنی اگه یه باگ دیگه (مثل همون که قبلاً سوایپ رو باز کرده بود) دوباره پیش می‌اومد، مسیر بدون‌پین برمی‌گشت. کاربر صریحاً گفت: «پنل مخفی باید کاملاً حذف بشه، بجز با پسورد راهی نباید باشه». پس به‌جای صرفاً «غیرقابل‌دسترس‌کردن»، خودِ کد حذف شد: `<main>` توی `App.jsx` دیگه هیچ‌وقت Product/Material/WoodCutting/Gallery/Accounting/Invoices رو مستقیم رندر نمی‌کنه — همیشه فقط `CatalogTab`. نوار ناوبری‌ای که این تب‌ها رو نشون می‌داد هم از `GlobalHeader.jsx` کاملاً پاک شد. تنها مسیر باقی‌مونده: دکمه‌ی «پنل مدیریت» توی کاتالوگ ← `PinScreen` (رمز ۴رقمی، از قبل موجود بود، دست نخورد) ← `ManagementPanelModal`/`MgmtTabs`. جزئیات کامل توی «بخش ۱ (Refresh)» زیرتر، ذیل موردی که به‌اشتباه یه سیستم PIN جدید و تکراری اول ساختم و بعد برگردوندمش

### دکمه‌ی اسکرول-به-بالا — **✅ تکمیل شد ⚫ (Ref)**
- [x] `ScrollToTopButton` توی پنل اصلی الان دیده نمی‌شه/غیب شده؛ اگه برگردونده بشه، باید دقیقاً هم‌راستا با دکمه‌ی قرمز شناور (+) قرار بگیره، نه یه‌کم جابه‌جا — **علت پیدا و اصلاح شد** (`ScrollToTopButton.jsx`): کامپوننت هر ۳۰۰ میلی‌ثانیه *همه‌ی* divهای صفحه رو اسکن می‌کرد تا حدس بزنه مودالی باز هست یا نه (چک رنگ پس‌زمینه/position/zIndex)، ولی این تشخیص باگ داشت — مقایسه‌ی رنگ فقط فرمت `rgba(...)` رو می‌شناخت، در حالی که رنگ‌های بدون شفافیت (مثل `#0a0a0a` که پس‌زمینه‌ی اصلی پنل مدیریته) به‌صورت `rgb(...)` بدون `a` محاسبه می‌شن و هیچ‌وقت match نمی‌شدن؛ در عوض هر عنصر fixed تمام‌عرض (مثل یه هدر) می‌تونست به‌اشتباه «مودال باز» تشخیص داده بشه. این heuristic ناقص و پرهزینه کامل حذف شد — الان کامپوننت فقط از `hide` (که از `isAnyModalOpen`/`useNestedModalCount` توی `App.jsx` میاد، همون منبع رسمی که سوایپ بین تب‌ها هم استفاده می‌کنه) استفاده می‌کنه. موقعیت هم دقیق‌تر شد: `right` از ۲۶ به ۲۴ تغییر کرد تا دقیقاً هم‌مرکز با دکمه‌ی + (`right:20, width:52`) باشه
- 🔴 لطفاً تست کن: توی چند تب (محصولات، متریال، گالری) اسکرول کن پایین، ببین دکمه ظاهر می‌شه و دقیقاً بالای دکمه‌ی + هم‌راستاست؛ بعد یه مودال (مثل ویرایش محصول) باز کن، ببین دکمه درست مخفی می‌شه

---

## 🆕🆕🆕🆕🆕 دور جدید — گزارش کامل بعد از چک کردن روی AI Studio (تقریباً همه‌چیز بجز ۳-۴ پوش آخر) — این نشست

این لیست عیناً از گزارش خود کاربر ثبت می‌شه (بعد از بررسی مستقیم اپ روی AI Studio). فعلاً همه‌ی موارد **ثبت‌شده، شروع‌نشده** هستن مگر جلوشون علامت زده بشه.

### ۱) دکمه‌ی Refresh (تغییر کامل رفتار) — اولویت بالا — **نصفه‌کامل (Wall 🟣)**
- [x] کلیک ساده: سینک کامل همه‌ی دیتاها — **حل شد** (`App.jsx`، `handleQuickRefresh`): قبلاً فقط از `localStorage` (کش لوکال) دوباره می‌خوند، سینک واقعی با سرور نبود. الان اگه لاگین باشه، اول `performSynchronization` (همون تابعی که تب همگام‌سازی استفاده می‌کنه) رو صدا می‌زنه تا واقعاً با سرور سینک بشه؛ اگه آفلاین/خطا بود، بی‌سروصدا با دیتای لوکال ادامه می‌ده (رفرش لوکال حداقل کار کنه). بعدش قیمت‌های درصدی دوباره ری‌سینک می‌شن (مثل قبل). + بررسی فیلدهای الزامی خالی: اگه محصولی بدون کد/نام، متریالی بدون نام/نوع، یا مشتری‌ای بدون نام پیدا بشه، به‌جای توست معمولی، یه توست هشدار با اسم تب‌های خطادار نشون داده می‌شه (مثلاً «⚠️ فیلد الزامی خالی در: محصولات، متریال»)
- [x] علامت زرد (!) روی خودِ تب‌های خطادار (نه فقط توی متن توست) — **✅ حل شد (Wall 🟣):** یه state جدید (`refreshProblemTabs`، آرایه‌ای از کدهای تب مثل `["products","materials"]`) توی `App.jsx` اضافه شد که `handleQuickRefresh` پرش می‌کنه. **آپدیت بعدی همین نشست:** بعداً که مسیر بدون‌پین (`activeTab`‌محور، توی `GlobalHeader.jsx`) کاملاً حذف شد (پایین‌تر، ذیل «کلاً تمپلیت فاکتور بین تب‌ها فرق داره»)، اون نوار تب هم رفت — پس الان این بج زرد فقط روی نوار تبِ `MgmtTabs` (پشت رمز) دیده می‌شه، که خب دقیقاً همون‌جاییه که باید باشه. **در همین حین یه باگ حیاتی جدا هم پیدا و فیکس شد:** `refreshResetTick` (فیچر ریست فیلتر که Logy اضافه کرده بود) توی `ManagementPanelModal` استفاده می‌شد ولی نه prop گرفته می‌شد نه جای دیگه‌ای تعریف شده بود — یعنی `ReferenceError: refreshResetTick is not defined` و **کل پنل مدیریت کرش می‌کرد.** چون این دقیقاً همون prop-chain ای بود که باید برای `refreshProblemTabs` هم دست می‌زدم، همون‌جا `refreshResetTick` هم به signature و render call اضافه شد — هم کرش رفع شد هم فیچر ریست‌فیلتر Logy واقعاً کار می‌کنه. **⚠️ یه اشتباه جانبی هم همینجا اصلاح شد:** موقع بررسی دسترسی پنل، اول یه سیستم PIN کاملاً جدید و اضافه (`PanelPinGate.jsx`) ساختم، غافل از این‌که یه `PinScreen`/`VALID_PIN_CODES` از قبل کامل وجود داشت — بعد از فهمیدنش، اون فایل و تغییرات مربوطه‌ش کامل حذف/برگردونده شد (تأیید با `git diff` خالی)، و به‌جاش فیکس واقعی (حذف کامل مسیر بدون‌پین) انجام شد
- 🔴 لطفاً تست کن: از کاتالوگ سوایپ/اسکرول کن به هیچ سمتی نباید هیچ اتفاقی بیفته و هیچ راهی جز دکمه‌ی «پنل مدیریت» + رمز نباید به بخش‌های مدیریتی باشه؛ بعد یه محصول بدون کد بساز، از همون دکمه+رمز وارد پنل شو و رفرش بزن، ببین علامت زرد روی تب محصولات میاد یا نه
- [ ] انیمیشن اینرسی چرخش رفرش (نگه‌داشتن=بچرخه، رها کردن=۱-۲ ثانیه کاهش‌سرعت) — **آپدیت 🟡 (Ash):** کاربر گفت این توی محیط پیش‌نمایش/شبیه‌ساز کار می‌کرد ولی روی اپ واقعی نصب‌شده روی گوشی نه. منطق/کد (`startContinuousSpin`/`startDeceleratingSpin`) کاملاً درست و به رویدادهای `onPointerDown`/`onPointerUp` وصل بود — ولی دکمه‌ی رفرش هیچ `touch-action` نداشت. روی گوشی واقعی (برخلاف دسکتاپ/پیش‌نمایش)، بدون `touch-action`، مرورگر می‌تونه رویداد لمس رو با تأخیر ۳۰۰ میلی‌ثانیه‌ای پردازش کنه (چون منتظره ببینه لمس یه اسکرول/زوم می‌شه یا نه) که دقیقاً می‌تونه زمان‌بندی نگه‌داشتن (۵۵۰ms) رو به‌هم بریزه و باعث بشه چرخش اصلاً حس نشه. `touchAction: "manipulation"` به دکمه اضافه شد. **آپدیت کاربر (نشست جدید):** بازم روی گوشی واقعی کار نمی‌کنه؛ کاربر گفت الان مهم نیست، فعلاً بی‌خیالش بشیم — اولویت پایین، بعداً برمی‌گردیم سراغش
- [x] هولد: پاپ‌آپ با ۴ دکمه‌ی مربعی (راست‌بالا=قفل، راست‌پایین=قفل‌باز، چپ‌بالا=رفرش، چپ‌پایین=Undo) — **✅ شروع و پیاده‌سازی شد ⚫ (Ref)، طبق درخواست مستقیم کاربر.** به‌جای نوشتن از صفر، `runLockUnlockPass` (موتور خالص محاسبات) یه پارامتر `mode` گرفت (`"both"` پیش‌فرض، `"lock"`، `"unlock"`) که فقط بخش قفل یا فقط بخش آزادسازی رو اجرا می‌کنه — بدون تغییر رفتار پیش‌فرض (پس فیکس مالی تازه‌ی «پرتی‌شدن» دست‌نخورده موند، فقط قابل‌فراخوانی جدا هم شد). `RefreshLockButton` الان با نگه‌داشتن، به‌جای اجرای مستقیم پاس ترکیبی، یه `FilterPopup` وسط‌چین (هم‌شکل بقیه‌ی پاپ‌آپ‌های اپ) با ۴ دکمه باز می‌کنه: قفل / قفل‌باز / رفرش / Undo. کلیک ساده و دابل‌کلیک-Undو (میان‌بر قدیمی) هم دست‌نخورده موندن
- 🔴 **حتماً تست کن:** چون این مستقیماً به موتور قفل/آزادسازی مالی وصله، لطفاً «قفل» تنها و «قفل‌باز» تنها رو جدا از هم امتحان کن، مطمئن شو هرکدوم فقط کار خودشو می‌کنن
- [x] هولد روی Undo: تاریخچه‌ی کامل قفل/آزادسازی‌ها، امکان برگشت به هر مرحله — **✅ باگ اصلی پیدا و فیکس شد ⚫ (Ref)، حالا که پاپ‌آپ ۴دکمه‌ای پیاده شده بود.** پیدا شد: `undoOperationsRef` هر بار با یه پاس تازه *جایگزین* می‌شد (نه اضافه)، پس عملیات‌های هر پاسِ قبلی (وقتی هولدِ دیگه‌ای انجام می‌شد) کاملاً از بین می‌رفت — فقط آخرین پاس قابل‌بازگشت بود، نه چند پاس قبل‌تر. الان به یه پشته‌ی واقعی تجمیعی تبدیل شد (سقف ۵۰ عملیات)، پس واقعاً می‌شه چند قدم Undo رفت، حتی بین چند بار هولد جدا. یه «مرورگر تاریخچه» بصری (لیست ۴تایی که بشه هرکدوم رو مستقیم انتخاب کرد) اضافه نشد — دابل‌کلیک/دکمه‌ی Undo فعلی یکی‌یکی به عقب برمی‌گرده که برای اکثر موارد کافیه؛ اگه بعداً لازم شد یه UI مرور کامل جدا اضافه می‌شه
- 🔴 لطفاً تست کن: چند بار پشت‌سرهم هولد بزن (قفل/آزادسازی جدا)، بعد چندبار Undo کن، ببین درست تا انتها برمی‌گرده

### 🆕 لایه‌ی دوم همون باگ Undo — گزارش کاربر با عکس (سود از ۱۴ میلیون به ۶۰۰ هزار افتاد) — این نشست 🟠 (Logy)
کاربر گزارش داد: قبل از بازطراحی دکمه‌ی ۴حالته، وقتی قفل می‌زد و بعدش Undo می‌زد، (۱) سود خالص حسابداری به‌طرز عجیبی از ۱۴٬۲۲۸٬۴۶۳ به ۶۴۷٬۷۱۸ تومان سقوط می‌کرد، و (۲) متریال‌هایی که توی همون آخرین پاس قفل شده بودن، آزاد نمی‌شدن.
- [x] **✅ باگ دقیق پیدا و فیکس شد.** فیکس بالای Ref (پشته‌ی تجمیعی) یه لایه از مشکل رو حل کرد، ولی یه لایه‌ی عمیق‌تر هنوز مونده بود: `handleUndoRefresh` طبق کامنت خودِ کد («فقط آخرین عملیات ثبت‌شده رو برمی‌گردونه، نه کل پاس رو با هم») **همیشه فقط یه عملیات تکی** (یه محصول/یه خط) رو برمی‌گردوند، نه همه‌ی چیزی که با یه بار «نگه‌داشتن» قفل شده بود. اگه یه پاس ۵ محصول رو باهم قفل می‌کرد، یه کلیک Undo فقط یکی‌شونو برمی‌گردوند و ۴ تای دیگه قفل می‌موندن — یه حالت بینابینی/ناسازگار که باعث می‌شد محاسبه‌ی سود کلاً به‌هم بریزه (چون بخشی از هزینه‌ها برگشته بود ولی بقیه نه)
- [x] **فیکس:** هر عملیات با یه `passId` مشترک (یکی برای کل همون پاس) تگ می‌شه. الان `handleUndoRefresh` همه‌ی عملیات‌های همون پاس آخر رو (نه فقط یکی) با هم و به‌ترتیب معکوس برمی‌گردونه — یه کلیک Undo دقیقاً معادل خنثی‌کردن کامل آخرین «نگه‌داشتن» می‌شه
- 🔴 لطفاً دوباره امتحان کن: چندتا محصول رو باهم قفل کن (یا از یه بچ)، Undo بزن، مطمئن شو *همه‌شون* آزاد می‌شن و سود درست برمی‌گرده به قبل


- [x] دکمه‌های لغو/ذخیره فقط بالا (چسبیده)؛ نسخه‌ی تکراری پایین حذف بشه — دکمه‌های پایینِ «تایید/لغو» فقط برای «کارت من» حذف شدن (چون اونجا دقیقاً همون کار دکمه‌های بالای مودالن)؛ برای کارت‌های همکاران نگه داشته شدن چون کار واقعی متفاوتی دارن (بستن ویرایش این‌یکی کارت و برگشت به لیست همکاران، بدون بستن کل مودال)
- [x] اسم پیش‌فرض کارت من: «استودیو فرش و دکور ریفرش» — توی `dataModels.js` اضافه شد (فقط برای «کارت من»، نه کارت‌های همکار)
- [x] دکمه‌ی X کنار فیلد نام (اگه ویرایش شد) برای پاک‌کردن و برگشت به پیش‌فرض — اضافه شد (فقط وقتی اسم فعلی با پیش‌فرض فرق داره نشون داده می‌شه)

### ۳) تأیید خروج از ویرایش/افزودن (فیچر جدید) — **✅ کامل شد (Wall 🟣، قبلاً به اسم Logy 🟠 شروع شده بود)**
- [x] موقع ویرایش/افزودن (کارت‌ویزیت/محصول/متریال/گالری)، اگه با X ببندی و فیلدی پر شده، پاپ‌آپ «لغو کنید؟» با دو گزینه‌ی لغو/ادامه — **هر ۴ فرم انجام شد:**
  - [x] گالری/مشتری (`CustomerEditor` در `GalleryTab.jsx`): اسنپ‌شات اولیه با `useRef` گرفته می‌شه، اگه `customer` نسبت به لحظه‌ی بازشدن فرق کرده باشه، دکمه‌ی X و کلیک بیرون به‌جای بستن مستقیم، `ConfirmDialog` («لغو کنید؟» / «ادامه ویرایش» یا «لغو کن») نشون می‌دن
  - [x] کارت‌ویزیت (`BusinessCardModal.jsx`): همون الگو — اسنپ‌شات اولیه‌ی `{myCard, cards}`، هم دکمه‌ی X بالای مودال هم دکمه‌ی «لغو» بالای مودال (ویرایش «کارت من») گارد شدن؛ تایید کردن پاپ‌آپ = واقعاً ریست به مقدار قبلی (`doCancelEdit`) + بستن اگه لازم بود
  - [x] تب محصولات (`ProductTab.jsx`): همون الگو روی `ProductEditor`؛ + یه باگ اضافه پیدا/فیکس شد: دکمه‌ی Back سخت‌افزاری اندروید مستقیم `setEditingProduct(null)` صدا می‌زد و کامل از گارد رد می‌شد — با `closeRequestRef` (که `ProductEditor` هر رندر خودش رو توش می‌ذاره) حل شد
  - [x] تب متریال (`MaterialTab.jsx`): همون الگو روی `MaterialEditor` (که برای هم افزودن هم ویرایش استفاده می‌شه)؛ همون فیکس Back هم اینجا لازم بود و اضافه شد (`materialEditorCloseRef`، هم برای `showAdd` هم `editingMat`)

### ۴) تب کاتالوگ — دکمه‌ی اضافی کارت‌ویزیت — **✅ حل شد**
- [x] کلیک روی لوگو، کارت‌ویزیت باز می‌شه؛ یه دکمه‌ی تکراری کنار دکمه‌ی پنل ظاهر می‌شه که باید حذف بشه — علتش پیدا شد: توی `GlobalHeader.jsx` یه دکمه‌ی جدای QR-کد بود که دقیقاً همون کاری که کلیک روی لوگو می‌کرد (باز کردن کارت‌ویزیت) رو تکرار می‌کرد و درست کنار دکمه‌ی «پنل مدیریت» قرار داشت. کامل حذف شد

### ۵) متریال — بولک/پرتی/کش/قفل (بخشی همون قلمروی claimed 🔵 قبلیه، هماهنگ باشید)
- [x] کش‌شدن نام متریال هنوز نیاز به اسکرین‌شات دقیق داره — **کاربر اسکرین‌شات/مثال دقیق داد، فیکس شد ⚫ (Ref)** — جزئیات در «۲) گزارش کاربر با اسکرین‌شات»
  - 🔍 **پیگیری بعدی 🟠 (Logy):** کاربر بازم گزارش داد که بعد از rename متریال، هدر دسته‌بندی («فرش گل و مرغ» توی تب محصولات) اسم قدیمی نشون می‌ده. کد `groupedProducts` (`App.jsx`) رو دوباره خوندم — دقیقاً مثل قبل، کاملاً زنده از `data.materials` می‌خونه (`data.materials.find((m) => m.id === li.materialId)?.name`)، نه یه مقدار ذخیره‌شده. با تحلیل Ref بالا هم می‌خونه (همون‌جا هم گفته بود گروه‌بندی از قبل زنده بود، مشکل واقعی جای دیگه‌ای/`li.label` توی ریزهزینه بود که فیکس شد). **کاربر گفت فعلاً فقط مستند بشه، بعداً خودش تست می‌زنه** — قبل از کار بیشتر منتظر نتیجه‌ی تست واقعی‌ش می‌مونیم؛ اگه بازم دید مشکل هست، نیاز به اسکرین‌شات دقیق (کجا دقیقاً، کدوم محصول/متریال) داریم چون خود کد قانع‌کننده به‌نظر می‌رسه
- 🔍 **بررسی جدید: آزادسازی متریال موقع حذف محصول 🟠 (Logy):** کاربر گفت وقتی محصولی حذف می‌شه، متریال‌های تخصیص‌یافته‌ش باید آزاد بشن ولی الان فقط می‌سوزن. تابع `deleteProduct` (`App.jsx`) رو کامل خوندم — این منطق (برگردوندن `deductedCost` به `remainingCost`، برگردوندن چوب‌های قفل‌شده به `sticks`، جدا کردن از `linkedProductIds` بچ) از **همون اولین کامیت پروژه** وجود داشته، هیچ‌وقت تغییر نکرده، و از نظر کد کامل به‌نظر می‌رسه. **کاربر گفت فقط مستند بشه، تست فیزیکی بعداً** — قبل از تغییر کد منتظر نتیجه‌ی تستش می‌مونیم
- [x] هر بچ حداقل تعداد۱ — **✅ باگ واقعی پیدا و فیکس شد ⚫ (Ref).** توی فرم «+ افزودن بچ» (`MaterialTab.jsx`)، کاربر تعداد واقعی (`bQty`) رو تایپ و اعتبارسنجی می‌شد (باید >۰ باشه)، ولی موقع صداکردن `onAddPurchase` این عدد اصلاً استفاده نمی‌شد — مقدار `1` هاردکد شده بود، صرف‌نظر از این‌که کاربر چه عددی وارد کرده باشه. یعنی هر بچ همیشه فقط ۱ به `totalQty`/`remainingQty` متریال اضافه می‌کرد، even اگه مثلاً ۵۰ عدد پیچ خریده بودی. الان `bQty` واقعی پاس داده می‌شه. (مسیر مشابه برای خرید عادی/غیر-بچی از قبل درست بود و همیشه عدد واقعی رو می‌فرستاد)
- [x] تعداد در بولک قابل تایپ نیست + جاش باید اول خط، قبل نوار درصد باشه — **انجام شد ⚫ (Ref)، توضیح کامل بالاتر در «بخش بولک»**
- [x] چیدمان محصولات لینک‌شده در بولک از ۲خطی به ۱خطی (کد+نام+ابعاد+نوار+عدد+ضربدر) — **انجام شد ⚫ (Ref)**
- [x] دکمه‌های پرتی‌شود/باقی‌بماند کوچیک‌تر و بالای درصد — **انجام شد ⚫ (Ref)**
- [x] نوشته‌ی پیش‌فرض ۱۰۰٪ حذف بشه — **انجام شد ⚫ (Ref)**
- [x] درصد واقعی سهم متریال توی هزینه‌ی محصول نشون داده بشه (نه همیشه ۱۰۰٪) — **انجام شد ⚫ (Ref)، توضیح کامل بالاتر در «بولک — تخصیص متریال (نکات جدید)»**

### ۶) فاکتور/پرینت — اولویت بالا
- [x] QR کارت‌ویزیت ۳برابر بزرگ‌تر + متن زیرش حذف — **حل شد** (`InvoiceTemplate.jsx`): از ۵۵px به ۱۶۵px، نوشته‌ی «اسکن کارت ویزیت» زیرش حذف شد
- [x] فوتر: شماره واتساپ و تماس حتی اگه یکی بودن جدا نمایش داده بشن — **حل شد**: شرطی که وقتی شماره‌ها یکی بودن واتساپ رو مخفی می‌کرد حذف شد؛ الان همیشه هردو (اگه پر باشن) جدا نشون داده می‌شن
- [x] مجموع تخفیف‌ها/هدیه‌ها یه ردیف جدا پایین فاکتور — **حل شد**: هدیه‌ها (آیتم‌هایی که قیمت نهایی‌شون صفره) از تخفیف‌های عادی جدا شدن؛ الان ۲ ردیف مجزا («مجموع تخفیف‌ها» قرمز، «مجموع هدیه‌ها» بنفش با تعداد قلم) هم توی حالت فاکتور گالری هم حالت عادی نشون داده می‌شه — قبلاً توی حالت گالری اصلاً این ردیف‌ها نبودن، و توی حالت عادی هم هدیه با تخفیف قاطی حساب می‌شد
- [x] افزودن/حذف اقلام بعد از صدور — **بررسی شد، از قبل کامل بود (Logy 🟠):** `handleAddItemToInvoice`/`handleRemoveItemFromInvoice` توی `AccountingTab.jsx` و پیکر مشابهش توی `InvoicesTab.jsx` (افزودن محصول + دکمه‌ی «حذف قلم») هردو موجودن و کار می‌کنن؛ ارجاعِ قبلی توی این فایل به یه بخش با اسم «فاکتورها: افزودن/حذف تک‌تک اقلام» رو پیدا نکردم (شاید توی merge افتاده)، ولی خودِ فیچر توی کد هست؛ کد تغییری نکرد
- [x] یکپارچه‌سازی تمپلیت فاکتور: تفاوت بین `InvoiceTemplate`/`InvoicePrint`/رندر مستقیم/`MgmtTabs` — **✅ بررسی شد، از قبل یکپارچه بود 🟠 (Logy):** هر ۵ نقطه‌ای که فاکتور نشون داده می‌شه (`AccountingTab.jsx`، `GalleryTab.jsx` دوبار، `InvoicesTab.jsx`، `ProductTab.jsx`) همگی مستقیماً از یک `InvoicePrint.jsx` استفاده می‌کنن، و خودِ `InvoicePrint.jsx` هم فقط یک‌بار و یکسان `InvoiceTemplate.jsx` رو رندر می‌کنه (خط ۴۸۳). `MgmtTabs` (توی `App.jsx`) خودش هیچ رندر مستقیم فاکتوری نداره — فقط تب‌های بالا رو به‌عنوان children نشون می‌ده که خودشون از همین مسیر یکتا استفاده می‌کنن. هیچ رندر جداگانه/موازی دیگه‌ای برای فاکتور توی کل پروژه پیدا نشد؛ کد تغییری نکرد
- [x] پیش‌نمایش/چاپ فاکتور نیاز به بازبینی کامل داره («درست نیست، همه موارد باید بازبینی بشه») — **🟡 (Ash) کاربر حق داشت، ۲ ناهماهنگی واقعی پیدا شد:**
  - قالب خودِ فاکتور (`InvoiceTemplate.jsx`) فقط یه جا تعریف شده و همه‌جا (`InvoicePrint.jsx`) ازش استفاده می‌کنن — این تکراری نیست. ولی **رفتار** چاپ بین تب‌ها فرق داشت: `AccountingTab`/`InvoicesTab` وقتی دکمه‌ی «چاپ» می‌زدی خودکار `window.print()` باز می‌شد (`autoPrint`)، ولی همون دکمه توی `GalleryTab` (۲ جا: چاپ فاکتور تک‌مشتری + چاپ سبد انتخابی) این prop رو اصلاً نمی‌فرستاد — یعنی از گالری چاپ می‌گرفتی ولی پنجره‌ی چاپ خودکار باز نمی‌شد، باید یه دکمه‌ی دیگه پیدا می‌کردی. **✅ فیکس شد** (`autoPrint={true}` به هردو اضافه شد). (توی `ProductTab` این prop عمداً نیست چون اون دکمه صراحتاً «پیش‌نمایش فاکتور»ه، نه «چاپ» — درسته، دست نزدم.)
  - یه چیز دیگه هم پیدا شد که شاید توضیح بخشی از حس «فاکتورها هیچ‌وقت درست نمی‌شن» باشه: توی `GalleryTab`، دکمه‌ی «چاپ فاکتور» روی هر گالری/مشتری، یه فاکتور **موقت و ذخیره‌نشده** با شناسه‌ی تصادفی (`Math.random()`) می‌سازه (خلاصه‌ی حساب اون مشتری، نه یه فاکتور واقعی ثبت‌شده در `data.invoices`) — برخلاف بقیه‌ی جاها (حسابداری، تب فاکتورها، …) که همیشه یه فاکتور **واقعی و ذخیره‌شده** رو چاپ می‌کنن. از نظر ظاهری قالب یکیه، ولی از نظر داده دو مسیر مفهومی جداست. اگه این عمدیه (یه «صورت‌حساب خلاصه» جدا از فاکتور رسمی)، مشکلی نیست؛ ولی اگه قراره همیشه فاکتور واقعی چاپ بشه، این باید یکی‌سازی بشه. **لطفاً بگو کدوم رفتار درسته** تا اگه لازم بود یکی‌سازیش کنم.
- [x] بک‌گراند فاکتور: فایل `/assets/images/invoice-header.jpg` توی گیت نیست، باید اضافه بشه — **✅ حل شد 🟡 Ash (آپدیت):** کاربر بعداً فایل سربرگ خام و درست (تمام‌صفحه‌ی A4 واقعی، ۲۴۸۰×۳۵۰۸، ۳۰۰dpi) رو فرستاد که علاوه بر سربرگ بالا (الگو+لوگو+خط)، یه واترمارک بزرگ و کم‌رنگ گوشه‌ی پایین‌راست هم داشت که توی نسخه‌ی قبلی (crop از روی اسکرین‌شات نمونه) نبود. الان با نسبت‌های واقعی این فایل: ۲ تا عکس جدا crop شد — `invoice-header.jpg` (سربرگ بالا) و `invoice-watermark.jpg` (واترمارک، با کادر سفید طبیعی دورش که توی خود فایل حفظ شده) — و با CSS چندلایه (`background-image` با دو تا `url(...)` کاما جدا) هم‌زمان اعمال می‌شن: لایه‌ی اول `top center`/عرض‌کامل مثل قبل، لایه‌ی دوم `bottom right`/۳۳.۵٪ عرض. برای فاکتورهای کوتاه (یه صفحه) دقیقاً مثل نمونه دیده می‌شه؛ برای فاکتورهای بلندتر، واترمارک همیشه ته همون یه container (نه هر صفحه‌ی فیزیکی جدا، چون این اپ صفحه‌بندی واقعی چندصفحه‌ای نداره) میفته. `padding-top` هم با نسبت واقعی سربرگ این فایل هماهنگ شد: چاپ `36mm`، پیش‌نمایش صفحه `130px` (خیلی کمتر از حدس اولیه‌ی من از روی اسکرین‌شات، چون سربرگ واقعی نسبتاً کوتاه‌تر از چیزیه که فکر می‌کردم)
- 🔴 لطفاً تست کن: یه فاکتور رو هم توی پیش‌نمایش (صفحه) هم با گزینه‌ی Print/PDF واقعی چاپ بگیر، ببین سربرگ درست بالای صفحه میاد و روی متن نمی‌افته

### ۷) تب برش
- [x] برش ۱D مایتر: چوب اول (یک‌طرف‌مایتر سبز) جهت ترسیمش برعکسه — سمت کج رو به صاف چوبه، ۲ تا ۲.۵ سانت هدررفت — **✅ حل شد 🟡 (Ash):** بعد از تحلیل عمیق (پایین همین خط) و یه فیکس اشتباه که خودم قبل push کشفش کردم و برگردوندم، فیکس درست و امن رو پیدا کردم: یه استراتژی ترتیب چهارم (`bySingleFirst`) به همون الگوی امن موجود (۳ ترتیب مختلف امتحان می‌شه، هرکدوم هدررفت کمتر داشت انتخاب می‌شه) اضافه شد — توش تکه‌های تک‌مایتر (سبز) قبل از دومایتر (آبی) پردازش می‌شن، تا قبل از این‌که یه تکه‌ی دومایتر یکی از لبه‌های یه سبز رو "قرض" بگیره، سبزها اول با هم زنجیر بشن. چون این دقیقاً همون الگوی «۳ تلاش، بهترین رو نگه‌دار» هست که از قبل توی کدِ خودِ تیم بوده، **نمی‌تونه نتیجه رو بدتر کنه** (تلاش‌های قبلی هم جزو گزینه‌هان) — فقط می‌تونه بهتر بشه یا فرقی نکنه. با داده‌ی کامل همون ۸ چوب عکس تست شد: هدررفت کل از ۲۶۹.۲ به **۲۵۹.۲** سانت رسید (۱۰ سانت کمتر)، و با ۵ تا سناریوی sanity جدا (تک‌تکه، خالی، تکه‌ی خیلی‌بزرگ، همه‌دومایتر، ترکیبی) هم مقایسه شد که نتیجه یا دقیقاً یکسان بود یا بهتر، هیچ‌جا بدتر نشد.
  - با بازسازی دقیق همه‌ی ۸ چوب عکس (۲×۴۳د+۱۲×۵۲د+۱۲×۴۹د+۴×۱۹.۵ت+۶×۹۵ب+۵×۴۳ب) و اجرای مستقیم `optimizeCutting` واقعی، **مجموع هدررفت دقیقاً همون ۲۶۹.۲ سانتی شد که توی عکس بود** — یعنی بازسازی داده درست بوده.
  - ولی وقتی فقط قطعات چوب ۵ (۹۵+۴×۱۹.۵+۲×۴۹) رو تنها روی یه چوب تست کردم، الگوریتم واقعی ۳۱.۹ باقی‌مونده داد (بهتر از ۲۹.۴ عکس)، و با brute-force دستی (همه‌ی ۱۰۵ ترتیب ممکن) ثابت شد بهینه‌ی واقعی برای این ترکیب **۳۶.۹** باقی‌مونده‌ست (فرمول درست، نه فرمول ساده‌شده‌ی اولیه‌ام که باگ داشت). یعنی **بله، یه هدررفت واقعی و قابل‌اندازه‌گیری هست** — نه توهم بصری.
  - ⚠️ **یه راه‌حل نوشتم (پاس بعد از اتمام چیدمان، فقط ترتیب/جهت داخل هر چوب رو دوباره امتحان می‌کرد، بدون جابه‌جایی قطعه بین چوب‌ها) ولی با تست مستقیم قبل از push فهمیدم غلطه و pushش نکردم:** وقتی قطعات یه چوب رو کارآمدتر می‌چینی (اتصال مایتر بهتر)، «هزینه»ی مصرف‌شده کم می‌شه ولی چون قطعه‌ی بیشتری برای پرکردن فضای آزادشده اضافه نمی‌کردم (فقط جابه‌جایی داخل همون چوب بود، نه بین چوب‌ها)، «باقی‌مونده» (که معیار هدررفت کل الگوریتمه) بزرگ‌تر می‌شد نه کوچیک‌تر — تست کامل ۸-چوبی نشون داد مجموع هدررفت از ۲۶۹.۲ به ۲۷۴.۲ (بدتر!) می‌رفت. یعنی فیکس واقعی باید توی خودِ فرآیند تخصیص قطعه‌به‌چوب باشه (که می‌تونه قطعات بیشتری از چوب‌های دیگه رو به فضای آزادشده بیاره)، نه یه پاس جدا بعد از اتمام تخصیص. این یه تغییر الگوریتمی بزرگ‌تر و پرریسک‌تره (احتمالاً نیاز به backtracking واقعی داره) — فعلاً برنگردوندم سراغش چون مستقیم رو مصرف واقعی چوب اثر می‌ذاره و می‌خوام مطمئن‌تر باشم.
- [x] ذخیره‌ی تصویر برش ۱D از نمایش خارج شده/دیده نمی‌شه — **حل شد**: این دقیقاً همون دکمه‌ی بالا بود که به شناور منتقل شد؛ دکمه‌های ذخیره‌ی جدا برای هر بخش (فقط ۱D یا فقط ۲D) هم سرجاشون موندن و کار می‌کنن
- [x] دکمه‌ی «ذخیره تصویر» به شناور قرمز پایین صفحه منتقل بشه — **حل شد** (همون فیکس بالا)
- [x] برش ۱D: چوب‌های اصلی هم‌سایز و داخل کادر باشن مگر عدد جا نشه (اونوقت فقط به‌همون‌اندازه بزرگ‌تر و اسکرول‌شونده) — **✅ بررسی شد، از قبل کامل بود 🟡 (Ash):** این دقیقاً همون `plankScale` (`WoodCuttingTab.jsx`) هست که یه نشست قبل اضافه شده بود — یه مقیاس مشترک از روی پرکارترین ردیف حساب می‌شه تا همه‌ی چوب‌ها هم‌مقیاس/داخل کادر بمونن، و فقط وقتی حتی حداقل سایز خوانا (۱۶px) هم جا نشه مقیاس قفل می‌شه و اسکرول افقی وارد عمل می‌شه؛ کد تغییری نکرد

### ۸) فیلترها و پاپ‌آپ‌ها (همه‌ی تب‌ها) — **✅ کامل شد (Wall 🟣)**
- [x] پاپ‌آپ‌های فیلتر (sort/دسته‌بندی/وضعیت/موقعیت/نوع محصول) از کادر بیرون می‌زنن — باید مثل تقویم مدل ثابت/وسط‌چین بشن — **حل شد:** یه کامپوننت مشترک `FilterPopup.jsx` ساخته شد (دقیقاً مدل `JalaliDatePicker`: بک‌گراند نیمه‌شفاف ثابت که با کلیک می‌بنده + خودِ پاپ‌آپ همیشه `fixed`+وسط‌صفحه، مستقل از این‌که دکمه کجای صفحه باشه). هر ۱۲ پاپ‌آپِ فیلتر/سورت/وضعیت/موقعیت/نوع‌محصول توی سه فایل (`GalleryTab.jsx` ۲تا، `MaterialTab.jsx` ۲تا، `ProductTab.jsx` ۷تا) بهش وصل شدن. **۲ مورد عمداً دست‌نخورده موندن:** `NameAutoSuggest` (گالری) و `GenericAutoSuggest` (محصولات) — این‌ها منوی پیشنهاد-حین‌تایپ‌ان (اتوکامپلیت زیر اینپوت)، نه فیلتر؛ وسط‌چین‌کردنشون تجربه‌ی تایپ رو بدتر می‌کرد، پس با طراحی فعلی (چسبیده به اینپوت) موندن
- [x] تب محصولات: فیلتر برگرده به «همه/موجود/تسویه‌شده/تسویه‌نشده» — **بررسی شد، از قبل کامل بود**: توی `ProductTab.jsx` هر ۴ گزینه (`available`/`unsettled`/`settled` + دکمه‌ی «همه» که `filterStatus` رو خالی می‌کنه) موجودن؛ کد تغییری نکرد
- [x] تب کاتالوگ: فیلتر برگرده به «همه/موجود/فروخته‌شده» — **بررسی شد، از قبل کامل بود**: بخش کاتالوگ توی `ProductTab.jsx` همین ۳ چیپ (`all`/`available`/`sold`) رو داره؛ کد تغییری نکرد
- [x] فیلتر موقعیت گالری: چک‌باکس به سلکت دیسکلت تبدیل بشه (مثل تب محصولات) — **✅ بررسی شد، از قبل کامل بود ⚫ (Ref):** کامپوننت `GalleryStatusFilter` (`GalleryTab.jsx`، بالای لیست گالری، کنار جستجو/سورت) دقیقاً همین الگوئه — یه دکمه‌ی کوچیک بسته که برچسب انتخاب فعلی رو نشون می‌ده، با کلیک یه پاپ‌آپ باز می‌شه با گزینه‌های چک‌باکسی چند-انتخابی (بدهکار/دارای موجودی)؛ کد تغییری نکرد
- [x] ابعاد همه‌ی پاپ‌آپ‌ها فیت محتوا باشه، از صفحه بیرون نزنه — همون کار بالا، با `FilterPopup` (`maxWidth: 88vw`, `maxHeight` قابل تنظیم) حل شد

### ۹) تب گالری — **✅ حل شد (Logy 🟠)**
- [x] منطق دکمه‌ی «همه» دقیقاً مثل تب محصولات کپی بشه — طبق توضیح شما، منظور دکمه‌های «همه/موجود/تسویه‌شده/تسویه‌نشده» موقع اکسپند گالری/مشتری بود. **علت واقعی پیدا شد:** توی `CustomerCard` (`GalleryTab.jsx`) این فیلتر چندگزینه‌ای (Set، هر دکمه جدا toggle) بود، نه انحصاری مثل تب محصولات — یعنی از حالت «همه»، زدن روی یه دکمه‌ی تکی (مثلاً «تسویه نشده») به‌جای نشون‌دادن *فقط* همون، اون رو از حالت روشن در می‌آورد و بقیه روشن می‌موندن (چون toggle بود نه select). به یه استیت تک‌مقداری (`statusFilter`: `all`/`available`/`unsettled`/`settled`) تبدیل شد، دقیقاً هم‌رفتار با فیلتر وضعیت تب محصولات — الان «همه» = نمایش هرسه، و هر دکمه‌ی دیگه = فقط همون دسته

### ۱۰) تب حسابداری
- [x] بازطراحی کامل ۴ فیلتر پیشرفته‌ی فاکتورها (تاریخ/وضعیت/نوع فروش/...) — مثل صفحه‌ی جدا و حرفه‌ای — **بخش قابل‌اجرا و مشخصش پیاده شد 🟠 (Logy):** «مثل صفحه‌ی جدا و حرفه‌ای» سلیقه‌ای/بی‌مشخصات بود، پس روی چیزهای عینی که از خودِ متن آیتم (تاریخ/وضعیت/نوع فروش) در می‌اومد تمرکز شد: (۱) فیلتر تاریخ از تک‌روزه (فقط دقیقاً همون روز) به **بازه‌ی از/تا** تبدیل شد، (۲) فیلتر **«نوع فروش»** که اصلاً وجود نداشت اضافه شد (مستقیم/پیش خودم، به مشتری، ارجاع به گالری — بر مبنای `kind` مشتری/گالری)، (۳) فیلتر وضعیت (تسویه‌شده/بدهکار) و جستجو که از قبل بودن دست‌نخورده موندن. همه‌شون توی `InvoicesTab.jsx`. اگه منظور از «صفحه‌ی جدا و حرفه‌ای» یه طراحی/چیدمان خاص مد نظرته (مثلاً یه پنل کشویی جدا به‌جای همین کارت این‌لاین)، بگو دقیق‌تر چی می‌خوای تا اون بخش بصری/چیدمانی رو هم انجام بدم
- [x] نمودار سود ماهانه قابلیت رفتن به سال‌های قبل (با نگهداری داده‌ی سال‌های قبل) — **✅ بررسی شد، از قبل کامل بود ⚫ (Ref):** `MonthlyChart` (`AccountingTab.jsx`) از قبل یه انتخاب‌گر سال با دکمه‌ی ‹ › داره (فقط وقتی بیش از یه سال داده باشه نمایش داده می‌شه)، و لایه‌ی داده (`monthlyProfit`) همه‌ی سال‌های موجود توی تاریخ فروش رو نگه می‌داره، نه فقط سال جاری؛ کد تغییری نکرد

### ۱۱) اسکرول و ناوبری بین تب‌ها 🟡 Ash
- [x] انیمیشن اسکرول بین تب‌ها مثل تلگرام — **✅ انجام شد:** یه هوک سبک جدید (`useTabSlideClass`، توی `utils/swipeTabs.js`، کنار هوک سوایپ موجود) اضافه شد که بر اساس ایندکس تب فعلی نسبت به تب قبلی توی `MAIN_TAB_ORDER`/`MGMT_TAB_ORDER`، یه کلاس CSS (`tab-slide-in-right`/`tab-slide-in-left`) برمی‌گردونه؛ محاسبه مستقیم توی رندر انجام می‌شه (نه `useEffect`) تا همون فریم اول درست باشه. محتوای هر تب (هم توی `<main>` اصلی، هم توی `MgmtTabs`) توی یه `<div key={activeTab}>` با همین کلاس پیچیده شده، پس با هر تغییر تب (چه با سوایپ، چه هر راه دیگه) remount می‌شه و یه فید+اسلاید ملایم ۲۲۰ میلی‌ثانیه‌ای پخش می‌شه (کیفریم‌ها توی `index.css`، بدون کتابخونه‌ی جدا، با `prefers-reduced-motion` هم احترام گذاشته شده). کاتالوگ (که توی `MAIN_TAB_ORDER` نیست) از این انیمیشن مستثناست.
- 🔴 لطفاً تست کن: بین تب‌های محصولات/متریال/برش/گالری/حسابداری/فاکتورها هم با سوایپ هم (اگه دکمه‌ای برای عوض‌کردن هست) با کلیک جابه‌جا شو، ببین جهت اسلاید منطقی به‌نظر می‌رسه (نه برعکس) — اگه جهتش برعکس حس شد بگو تا معکوسش کنم
- [x] 🔴 باگ: سوایپ بین تب‌های مدیریتی باید فقط وقتی هیچ پنجره‌ی دیگه‌ای (سبد خرید/ویرایش/افزودن/بولک) باز نیست کار کنه — **بررسی شد: این قسمت (`nestedModalCount`/`isAnyModalOpen` توی `App.jsx`) از قبل درست بود** برای `MaterialTab.jsx`/`ProductTab.jsx` (هر دو با `useRegisterOpenModal` ثبت می‌شدن)
- [x] همین قانون برای ویرایش/افزودن گالری/مشتری و کارت‌ویزیت — **این نشست اضافه شد (Logy 🟠):** `useRegisterOpenModal(true)` به سه‌جای جامونده وصل شد: `CustomerEditor` و `InvoiceListModal` توی `GalleryTab.jsx`، و کل `BusinessCardModal` (چون خودش یه مودال تمام‌صفحه‌ست). حالا وقتی هرکدوم از این‌ها بازه، سوایپ بین تب‌های اصلی/مدیریتی پشتشون کار نمی‌کنه

### ۱۲) متفرقه
- [x] دکمه‌ی هاید/آنهاید متریال: کار می‌کنه ولی علامتش برعکسه — پیش‌فرض باید چشمِ خط‌خورده (یعنی هاید) باشه، وقتی آنهاید می‌شه دکمه قرمز و علامتش چشمِ بدون‌خط بشه — **✅ حل شد** (`MaterialTab.jsx`، این نشست با تداخل و merge نهایی): `mat.hidden === true` → چشمِ خط‌خورده، بدون قرمز (پیش‌فرض). `mat.hidden === false` (آنهایدشده/دیده‌می‌شه) → چشمِ بدون‌خط + دکمه قرمز. (نکته: توی یه merge با کار همزمان همکار، دو تفسیر متفاوت پیش اومده بود؛ این نسخه‌ی نهایی دقیقاً طبق توضیح خودِ شما تنظیم شد)

### ۱۳) تقویم شمسی
- [x] روی سال کلیک=لیست اسکرولی (نه دکمه‌های جدا)، سال جاری ته لیست، بازه از ۱۴۰۰ (نه ۱۳۹۰) — **حل شد** (`JalaliDatePicker.jsx`): از گرید ۴ستونه به لیست تک‌ستونه‌ی اسکرولی تبدیل شد، بازه از ۱۴۰۰ تا سال جاری (سال جاری همیشه آخر لیست)، و موقع باز شدن خودکار به سال انتخاب‌شده اسکرول می‌کنه

### ۱۴) همگام‌سازی و ابر
- [x] سینک `invoiceDrafts` به ابر (schema.ts/server.ts) — **✅ تکمیل شد ⚫ (Ref).** یه باگ واقعی پیدا شد: `server.ts` توی `/api/sync` فقط چندتا مجموعه‌ی مشخص (products/materials/customers/...) رو پردازش و توی `consolidatedServerData` برمی‌گردوند؛ `invoiceDrafts` اصلاً توش نبود. چون کلاینت بعد هر سینک کل `data` رو با جواب سرور جایگزین می‌کنه (`saveData(migratedServerData)`) و `migrateData` هم برای فیلدهای غایب مقدار پیش‌فرض خالی (`[]`) می‌ذاره، **هر سینک عملاً همه‌ی پیش‌فاکتورهای ذخیره‌شده‌ی محلی رو پاک می‌کرد.** الان درست شد: مجموعه‌ی `invoice_drafts` به `db-layer.ts` (لیست full-wipe) و `server.ts` (آپسرت با conflict-resolution بر اساس `updatedAt`، مثل بقیه‌ی مجموعه‌ها، + برگردوندنش توی جواب سرور) اضافه شد؛ و `invoiceDrafts` به لیست موجودیت‌های ردیابی‌شده‌ی `syncManager.js` هم اضافه شد (تا مهر زمان و تشخیص خودکار حذف/tombstone مثل بقیه‌ی تب‌ها کار کنه — بدون نیاز به تغییر توی `InvoicesTab.jsx`)
- 🔴 لطفاً تست کن: یه پیش‌فاکتور بساز و ذخیره کن، سینک بزن، بعد از یه دستگاه/نشست دیگه چک کن که هنوز هست
- [x] سینک همه‌ی داده‌ها به ابر + توی اکسپورت/ایمپورت هم باشن — **یه باگ واقعی دیگه پیدا شد و فیکس شد (Wall 🟣):** دقیقاً هم‌خانواده‌ی باگ `invoiceDrafts` بالا — `woodCuttingSessions` (نشست‌های ذخیره‌شده‌ی برش چوب) هیچ‌جای `server.ts`/`db-layer.ts` پردازش نمی‌شد، پس **هر سینک با سرور همه‌ی نشست‌های ذخیره‌شده‌ی برش رو پاک می‌کرد.** با همون الگوی دقیق invoiceDrafts فیکس شد: مجموعه‌ی `wood_cutting_sessions` به `server.ts` (upsert با conflict-resolution بر اساس `updatedAt`/`timestamp`، + برگردوندنش توی جواب سرور) و `db-layer.ts` (`clearAllCollections`) اضافه شد؛ `woodCuttingSessions` به لیست موجودیت‌های ردیابی‌شده‌ی `syncManager.js` هم اضافه شد (مهر زمان خودکار + تشخیص حذف، بدون نیاز به تغییر `WoodCuttingTab.jsx`). اکسپورت/ایمپورت (Excel/JSON) رو هم چک کردم (Wall 🟣): **هردو از قبل کامل بودن، نیازی به فیکس نبود.** JSON export کل `data` رو اسپرد می‌کنه (پس `woodCuttingSessions` خودش توشه) و import هم با `mergeById` ادغامش می‌کنه؛ اکسل هم یه شیت جدا («جلسات برش») داره که هم export و هم import پردازشش می‌کنن. باگ فقط توی مسیر سینک ابری بود، نه اکسپورت/ایمپورت
- 🔴 لطفاً تست کن: یه نشست برش چوب ذخیره کن، سینک بزن، بعد از یه دستگاه/نشست دیگه چک کن که هنوز هست
- [x] 🟠 (Logy) راهنمای migration دیتابیس برای کاربر (هر ستون جدید نیاز به `npx drizzle-kit push` داره) — **بررسی شد: این فرضِ خودِ آیتم درست نبود، نه یه کمبود مستندات.** `src/db/schema.ts` (Postgres/Drizzle) الان توی هیچ‌جای پروژه import نمی‌شه — کد سینک واقعی (`src/lib/db-layer.ts`) کلاً Firestore (NoSQL) + fallback به یه فایل JSON محلی (`data/local_db_<uid>.json`) استفاده می‌کنه، هیچ‌کدوم schema/migration ندارن (فیلد جدید فقط با اضافه‌کردنش به آبجکت JS خودش کار می‌کنه، بدون هیچ مرحله‌ی جداگانه). یعنی `drizzle.config.ts`/`schema.ts` احتمالاً از یه معماری قدیمی‌تر (یا برنامه‌ریزی‌شده ولی هیچ‌وقت وصل‌نشده) باقی مونده. **تصمیم نهایی 🟠 (Logy):** به‌جای حذف (ریسک از دست رفتن کار آینده) یا سکوت (سردرگمی)، یه کامنت واضح بالای هر دو فایل (`schema.ts`/`drizzle.config.ts`) اضافه شد که می‌گه فعلاً استفاده نمی‌شن و چرا — اگه یه روز خواستید واقعاً وصلش کنید، این توضیح همون‌جا منتظره

### ۱۵) فیچر جدید: بیومتریک
- [x] اثرانگشت/فیس برای باز کردن پنل مدیریت (`@capawesome-team/capacitor-biometrics`) — **✅ پیاده‌سازی شد 🟠 (Logy)، با چند نکته‌ی مهم:**
  - پکیجی که توی این آیتم نوشته شده بود (`@capawesome-team/capacitor-biometrics`) اصلاً روی npm وجود نداره؛ به‌جاش از پکیج واقعی و فعال معادلش استفاده شد: `@aparajita/capacitor-biometric-auth` (سازگار با Capacitor 8، دقیقاً همون ورژنی که این پروژه داره)
  - توی `PinScreen` (`App.jsx`، صفحه‌ی رمز ۴رقمی پنل مدیریت) اضافه شد: موقع باز شدن صفحه، اگه دستگاه بیومتریک پشتیبانی/ثبت‌شده داشته باشه، خودکار یه‌بار پرامپت اثرانگشت/فیس نشون داده می‌شه؛ اگه کاربر انصراف بده یا شکست بخوره، همون صفحه‌ی رمز عادی می‌مونه (بدون قفل‌شدن اضافه)، و یه دکمه‌ی «ورود با اثرانگشت/فیس» هم زیر صفحه هست برای تلاش مجدد دستی در هر لحظه
  - طراحی عمداً **افزایشی و بی‌خطر** است: بیومتریک فقط یه *میان‌بر جایگزین* برای پین ۴رقمیه، هیچ‌وقت جایگزین اجباریش نمی‌شه و اگه دستگاه پشتیبانی نکنه (یا حتی خودِ پلاگین روی وب/AI Studio خطا بده) کاملاً بی‌صدا نادیده گرفته می‌شه و پین همیشه در دسترسه
  - 🔴 **نکته‌ی حیاتی برای بیلد APK:** این پروژه پوشه‌ی `android/` یا `capacitor.config` رو توی گیت نداره (احتمالاً جدا مدیریت می‌شه یا موقع بیلد ساخته می‌شه) — پس بعد از `npm install`، هرکی داره APK می‌گیره **حتماً باید `npx cap sync android` (یا معادلش) رو هم اجرا کنه** تا پلاگین نیتیو واقعاً به پروژه‌ی اندروید وصل بشه؛ وگرنه دکمه‌ی بیومتریک بی‌اثر می‌مونه (نه کرش، فقط هیچ‌کاری نمی‌کنه چون پلاگین نیتیو رجیستر نشده)
  - 🔴 **کاملاً تست‌نشده روی دستگاه واقعی** — من امکان اجرای اثرانگشت فیزیکی رو ندارم؛ لطفاً بعد از بیلد روی گوشی خودت با اثرانگشت/فیس واقعی امتحان کن (هم مسیر موفق، هم انصراف/شکست) و اگه چیزی جور درنیومد بگو

### 🆕 گزارش کاربر (با اسکرین‌شات) — این نشست
- [x] ابعاد طول×عرض هنوز جاهایی کوچیک×بزرگه، باید بزرگ×کوچیک بشه — **✅ ریشه‌ش پیدا شد ⚫ (Ref):** منطق نرمالایز (بزرگ×کوچیک) از قبل موقع *ذخیره‌ی جدید* هر محصول اجرا می‌شد و درست بود؛ مشکل واقعی این بود که این نرمالایز فقط روی ذخیره‌های *بعد از این تاریخ* اعمال می‌شد، نه روی محصولات قدیمی‌تری که از قبل با ترتیب اشتباه ساخته شده بودن و دوباره ادیت نشده بودن (مثل محصول #0048 توی عکس، ۴۰×۱۷۰). الان یه مهاجرت به همون بخشی که موقع لود اپ dimW/dimH رو پارس می‌کنه اضافه شد که همه‌ی محصولات مستطیلی موجود (نه دایره/نیم‌دایره) رو هم نرمالایز می‌کنه، نه فقط ذخیره‌های تازه
- [x] وقتی متریالی rename می‌شه، جاهای دیگه (مثل ریزهزینه‌ی محصول) نام قدیمی رو نشون می‌دن — **✅ ریشه‌ش پیدا شد و فیکس شد ⚫ (Ref):** مثال‌های دقیق کاربر («موتور ساعت (۲عدد)» و «فرش بافت سعادت آباد») بررسی شد. گروه‌بندی محصولات بر اساس فرش (`groupedProducts`, `App.jsx`) از قبل زنده/درست بود (`data.materials` توی dependency array هست، از روی شناسه می‌خونه). باگ واقعی جای دیگه بود: `li.label` روی هر آیتم متریالِ محصول، فقط *یه‌بار موقع لینک‌شدن* از `material.name` کپی می‌شه و بعدش هیچ‌وقت sync نمی‌شه (چون یه فیلد قابل‌ویرایش دستیه، نه رفرنس زنده). ریزهزینه‌های کارت محصول (`ProductCard`) از همین `li.label` بی‌واسطه استفاده می‌کردن. الان `materials` به `ProductCard` پاس داده می‌شه و ریزهزینه‌ها اول از شناسه‌ی زنده‌ی متریال نام رو می‌خونن، فقط اگه لینک نبود (آیتم دستی بدون متریال) از `li.label` استفاده می‌کنن. فیلد «عنوان» توی فرم ویرایش محصول عمداً دست‌نخورده موند چون قابل‌ویرایش دستیه و کاربر می‌تونه خودش عنوان دلخواه بذاره
- [x] منطق بولک: وقتی متریالی از محصول unlink می‌شه، باید توی نوار متریال به رنگ آبی دربیاد (نه فوری قرمز/زرد بمونه)، با آزادسازی (نگه‌داشتن دکمه رفرش) از محصول جدا بشه و به باقیمانده (خاکستری، قابل لینک مجدد) برگرده — **✅ بررسی شد: این قابلیت از قبل کامل پیاده‌سازی شده بود، چیزی عوض نشد ⚫ (Ref).** توی `MaterialTab.jsx` نوار پیشرفت هر متریال از قبل ۳ رنگ داره: قرمز (`#8B1A1A`) = قفل‌شده/مصرف‌شده، نارنجی (`#f39c12`) = منتظر قفل (پیش از قفل‌شدن)، **آبی (`#4a9eda`) = «در انتظار آزادسازی»** — دقیقاً با کامنت «Blue: about to be freed once refresh confirms». وقتی متریالی (چه از بولک چه از فرم ویرایش محصول) حذف/جدا می‌شه، اگه قبلاً قفل بوده `pendingUnlock:true` می‌شه (آبی)، و با نگه‌داشتن دکمه‌ی رفرش واقعاً آزاد و به `remainingCost` متریال برگردونده می‌شه (`refundToMaterial`) — قابل لینک مجدد به هرجا. دکمه‌ی رفرش هم یه بج قرمز کوچیک (`hasPending`) داره وقتی همچین چیزی در انتظاره
- 🔴 **لطفاً تست کن:** این مورد آخر (بولک/آزادسازی) رو با دقت روی اپ ببین — اگه رفتاری که توضیح دادم (آبی → نگه‌داشتن رفرش → خاکستری/باقیمانده) دقیقاً چیزی نیست که می‌بینی، لطفاً بگو کجاش فرق داره (شاید یه‌جای خاص، مثل بولک بچ‌ای، این مسیر رو درست طی نمی‌کنه)

### 🆕 دو سوال کاربر — این نشست ⚫ (Ref)
- [x] تب برش: وقتی محصولی که تعریفش دایره (O) هست لود می‌شه، باید خودکار سوییچ کنه به تک‌چوب با O فعال (مثل D و قاب ۴چوب که خودکار ست می‌شه) — **✅ بررسی شد: از قبل کامل و درست پیاده‌سازی شده، چیزی عوض نشد.** توی `WoodCuttingTab.jsx` (پاپ‌آپ «انتخاب محصول»، همون‌جایی که برای D هم کار می‌کنه)، هم `isCircle` (از `p.shape === "circle"` یا پسوند `O` توی `p.dims`) هم `isSemiCircle` (که در واقع یعنی «گرد»، برای D و O مشترکه) به‌درستی ست می‌شن و نوع فریم خودکار میره روی `SINGLE` (تک‌چوب) — دقیقاً مثل رفتار D. اگه روی گوشی این‌طور نبود، لطفاً دقیق بگو از کجا محصول رو لود می‌کنی (شاید یه مسیر دیگه‌ای غیر از همین پاپ‌آپ انتخاب محصول باشه که هنوز بهش وصل نشده)
- [x] رفتار جدید Refresh-hold (پاپ‌آپ ۴حالته) هنوز پیاده نشده (Wall توی بخش ۱ داره روش کار می‌کنه) — طبق درخواست کاربر، تا وقتی اون آماده بشه، آزادسازی به همون هولد فعلی وصل موند: **✅ بررسی شد، از قبل دقیقاً همینه.** `handleHoldRefresh`/`runLockUnlockPass` (`App.jsx`) همین الان توی هر بار نگه‌داشتن دکمه، اول قدم ۱ (قفل‌کردن متریال‌های معلق) و بعد قدم ۲ (آزادسازی متریال‌های `pendingUnlock`) رو پشت‌سرهم توی یه پاس واحد اجرا می‌کنه — چیزی برای وصل‌کردن نمونده. وقتی Wall پاپ‌آپ ۴حالته رو تموم کرد، همین منطق (لاک+آزادسازی) باید به دکمه‌ی «راست‌بالا=قفل» و «راست‌پایین=قفل‌باز» تفکیک بشه؛ فعلاً هردو با هم روی هولد ساده‌ان

### 🆕🆕 گزارش کامل کاربر با ۲۰ اسکرین‌شات (تست دقیق روی گوشی) — این نشست ⚫ (Ref)
لیست ۲۱ موردی؛ در حال پردازش یکی‌یکی، هرکدوم که انجام بشه اینجا [x] می‌شه.
1. [x] پاپ‌آپ لغو/ادامه‌ویرایش (موقع زدن ×) وسط صفحه نیست، جای دیگه‌ست — **✅ فیکس شد:** این پاپ‌آپ (توی `ProductTab.jsx` و `MaterialTab.jsx`) استایل `S.overlay` رو عیناً reuse می‌کرد که برای فرم تمام‌صفحه طراحی شده بود (`alignItems`/`justifyContent` نداشت)، نه یه دیالوگ کوچیک وسط‌چین. `alignItems:center, justifyContent:center` اضافه شد
2. [x] تب برش: دکمه‌ی قرمز شناور پایین-چپه، باید مثل بقیه‌ی تب‌ها پایین-راست باشه — **✅ فیکس شد:** `left:20` → `right:20`
3. [x] تب محصولات: ترتیب دکمه‌های فیلتر (موجود/تسویه‌نشده/تسویه‌شده/همه) → باید بشه (همه/موجود/تسویه‌شده/تسویه‌نشده) — **✅ فیکس شد**
4. [x] وقتی «نوع» به محصولات اضافه می‌شه، چیدمان هدر بهم می‌ریزه؛ فیلد جستجو باید خودکار کوچیک بشه که فیت بشه — **✅ فیکس شد:** باگ کلاسیک فلکس‌باکسه — فیلد جستجو `flex:1` داشت ولی `minWidth:0` نداشت، پس به‌جای جمع‌شدن، کل ردیف می‌رفت خط بعد. `minWidth:0` به کانتینر و اینپوت اضافه شد
5. [x] فیلد انتخاب مشتری (سبد خرید/ویرایش محصول) پاپ‌آپ وسط‌صفحه‌ی سراسری نیست (برخلاف انتخاب گالری که هست) — **✅ فیکس شد:** یه کامپوننت جدید `CustomerSmartSelect` ساخته شد که از همون `FilterPopup` مشترکی استفاده می‌کنه که `SmartLocationSelect` (گالری) استفاده می‌کنه؛ قابلیت تایپ «مشتری جدید» (که قبلاً `GenericAutoSuggest` پشتیبانی می‌کرد) با یه گزینه‌ی صریح «+ افزودن مشتری جدید» حفظ شد. جایگزین شد توی سبد خرید (فروش به مشتری) و فرم ویرایش محصول (فیلد خریدار)
6. [x] همون مشکل ۵ برای دکمه‌ی Sort تب گالری هم هست — **✅ بررسی شد: از قبل درست بود ⚫ (Ref).** `SortButton` توی `GalleryTab.jsx` از قبل از همون `FilterPopup` مشترک (وسط‌چین) استفاده می‌کنه — احتمالاً گزارش کاربر مربوط به قبل از تکمیل بخش ۸ (Wall) بوده
7. [x] فیلتر «همه/بدهکار/دارای موجودی» تب گالری: دکمه‌ی «همه» اضافه‌ست، باید فقط سلکت‌دیسلکت چک‌باکسی بمونه — **✅ فیکس شد ⚫ (Ref):** دکمه‌ی جدای «همه» از پاپ‌آپ `GalleryStatusFilter` حذف شد؛ حالا فقط دو گزینه‌ی چک‌باکسی هستن، و «همه» به‌طور ضمنی یعنی هیچ‌کدوم تیک نخورده
8. [x] ایمپورت متریال: قیمت اولیه/قبلی به‌عنوان بچ ثبت نمی‌شه؛ ظاهر/چیدمان بخش بچ‌ها هم یکپارچه و مرتب نیست — **✅ تا حد امکان (بدون ریسک به محاسبات مالی) یکپارچه شد ⚫ (Ref)، طبق تصمیم کاربر که اگه واقعاً باید ۳ سیستم جدا باشه اشکالی نداره.** یکی‌کردن کامل خودِ داده‌ساختارها (`batches`/`sticks`/`procurements`) خیلی پرریسک بود (مستقیم روی محاسبات قفل‌شده‌ی محصولات اثر می‌ذاره)، ولی این‌ها بدون هیچ تغییری توی منطق مالی انجام شد:
   - **کشف شد:** `procurements[]` برای فرش/مساحتی و خطی صرفاً یه *لاگ خودکار موازی* بود — هر بار که بچ (فرش/مساحتی) یا چوب (خطی) اضافه می‌شد، `addMaterialPurchase` هم‌زمان یه ردیف اینجا هم اضافه می‌کرد، بدون این‌که خودش تأثیری روی هزینه/باقیمانده داشته باشه (چون `totalCost`/`remainingCost`/`totalQty`/`remainingQty` مستقیم روی خودِ متریال آپدیت می‌شن، نه از روی این آرایه محاسبه می‌شن). نتیجه: کاربر ۲ تا لیست «تاریخچه» هم‌پوشان و گیج‌کننده می‌دید (یکیش — بچ‌های واقعی/چوب‌های واقعی — تنها چیزیه که واقعاً به محصولات لینک می‌شه)
   - بخش «تاریخچه خرید» تکراری برای فرش/مساحتی/خطی **مخفی شد** (بدون از دست رفتن دیتا — فقط دیگه نمایش داده نمی‌شه)؛ فقط برای نسبتی/ثابت («ابزار») نشون داده می‌شه، جایی که واقعاً تنها منبع تاریخچه‌ست (اسمش هم به «بچ‌های موجود (خریدها)» تغییر کرد، هم‌نام با بخش فرش/مساحتی، برای یکپارچگیِ ادبیات)
   - «قیمت اولیه به‌عنوان بچ ثبت نمی‌شه»: بررسی دقیق نشون داد در واقع *ثبت می‌شد* (هم برای فرش/مساحتی/خطی توی `batches[0]`/`sticks[0]`، هم برای نسبتی/ثابت توی `procurements[0]`) — فقط چون بخش نمایش دوگانه/گم بود، به چشم نمی‌اومد. با یکپارچه‌سازی بالا الان هرجا باید باشه دیده می‌شه
   - **یه باگ واقعی جانبی هم پیدا و فیکس شد:** فرم «خرید سریع» متریال خطی (مبلغ+تعداد)، وقتی مثلاً ۵ چوب می‌خریدی، همیشه فقط ۱ چوب واقعی ثبت می‌کرد (`qty:1` هاردکد بود) — الان تعداد واقعی خریداری‌شده ثبت می‌شه
9. [x] برش ۱D: جهت مایتر چوب‌های سبز (یک‌طرف‌مایتر) برعکسه — **✅ توسط Ash فیکس شد** (علت: ارث‌بری جهت RTL روی ردیف چوب بود، نه خودِ الگوریتم؛ `direction:ltr` اضافه شد)
10. [x] ترسیم چوب‌های بهینه: باید عرض نمایش فیکس/یکسان باشه بین همه، مگراینکه متن‌ها ناخوانا بشه که اونجا کمی shift کنه — **✅ بررسی شد: از قبل دقیقاً همین بود ⚫ (Ref).** `plankScale` (یه useMemo سراسری که روی *همه‌ی* گروه‌های ضخامت با هم اجرا می‌شه، نه جدا-جدا) دقیقاً همین منطق رو داره: عریض‌ترین ردیف رو پیدا می‌کنه، مقیاس مشترک رو طوری حساب می‌کنه که توی عرض کانتینر جا بشه، و فقط اگه به حداقل خوانا (۱۶px) برسه دیگه کوچیک‌تر نمی‌کنه (اونجا اسکرول افقی طبیعی می‌شه). چیزی برای تغییر پیدا نشد
11. [x] بین گروه‌های چوب با طول متفاوت (۱۵۰ در مقابل ۲۰۰) یه خط نازک جداکننده اضافه بشه؛ همین‌طور توی ۲D بین صفحات با ابعاد متفاوت — **✅ اضافه شد ⚫ (Ref):** یه خط نقطه‌چین کم‌رنگ (`#333`) بین ردیف‌های ۱D وقتی `stockLength` عوض می‌شه، و بین صفحات ۲D وقتی `stockW×stockH` عوض می‌شه
12. [x] بولک: دکمه‌های «پرتی شود»/«باقی بماند» باید دقیقاً روبروی (سمت چپ) متن «درصد باقیمانده مورد استفاده» باشن، بدون متن اضافه — **✅ فیکس شد ⚫ (Ref):** کپشن «تکلیف باقیمانده‌ی این تخصیص چی بشه؟» حذف شد؛ دکمه‌ها الان توی همون ردیفِ برچسب «درصد مورد استفاده از باقیمانده فعال» نشستن، سمت چپش
13. [x] فیلد «تعداد» (که این نشست اضافه شد) باید دقیقاً ابتدای همون خط نوار لغزنده‌ی درصد باشه، با یه کادر جمع‌وجور — **✅ فیکس شد ⚫ (Ref):** از یه ردیف جدا به ابتدای همون ردیفِ نوار لغزنده منتقل شد، عرضش هم از ۵۶ به ۴۴px جمع‌وجورتر شد
14. [x] لیست محصولات لینک‌شده در بولک: فاصله‌ی نام-ابعاد خیلی زیاده و نوار لغزنده خیلی کوچیک شده؛ عرض لیبل نام باید بر اساس بلندترین نامِ *انتخاب‌شده* خودکار تنظیم بشه — **✅ فیکس شد ⚫ (Ref).** ریشه: اسپن نام `flex:1` داشت، پس همیشه *همه‌ی* فضای خالی رو می‌قاپید (نه فقط اندازه‌ی متن خودش)، در نتیجه بین متن کوتاه و ابعاد/کنترل‌ها فاصله‌ی الکی می‌افتاد. الان عرض ستون نام از روی بلندترین نامِ *انتخاب‌شده‌ی فعلی* محاسبه می‌شه (`ch` واحد، حداکثر ۲۸ کاراکتر)؛ فضای آزادشده به گروه نوار لغزنده داده شد که حالا هم بزرگ‌تره هم راحت‌تر لمس می‌شه
15. [x] تب متریال: دکمه‌ی هاید/آنهاید هر متریال قرمز/کادردار نباشه؛ دکمه‌ی فیلتر «نمایش پنهان‌شده‌ها» بالا هم منطقش برعکسه — **✅ هر دو فیکس شد ⚫ (Ref):** (۱) دکمه‌ی هاید/آنهاید هر متریال دیگه پس‌زمینه/کادر قرمز نداره، عین دکمه‌ی ادیت/حذف بدون‌کادره. (۲) دکمه‌ی فیلتر بالا دقیقاً برعکس بود — منطقش swap شد: پیش‌فرض (پنهان‌شده‌ها دیده نمی‌شن) = چشمِ خط‌خورده، بدون رنگ قرمز؛ بعد از کلیک (پنهان‌شده‌ها هم نشون داده می‌شن) = چشمِ باز، با رنگ/کادر قرمز
16. [x] وقتی اسم فرش توی تب متریال ادیت می‌شه، اسم دسته‌بندی توی تب محصولات آپدیت نمی‌شه ✅ **تأیید شد توسط کاربر: درست شده.** (نکته‌ی جدا: مسئله‌ی «کالیگرافی نباید نوع محصول رو تغییر بده» که کاربر همزمان مطرح کرد، ربطی به این آیتم نداشت — قبلاً توسط Logy جدا فیکس شده بود؛ توضیح کامل توی بخش «فیدبک کاربر روی لیبل شناور»)
17. [x] هزینه‌ی تمام‌شده توی فرم افزودن محصول با فرم ویرایش محصول یکی نیست — **✅ باگ واقعی پیدا و فیکس شد ⚫ (Ref).** دو تا پیاده‌سازی جدا از «resolve کردن هزینه‌ی خط» وجود داره: یکی توی `App.jsx` (`resolveLineCost`، همونی که «هزینه تمام‌شده»ی روی کارت محصول ازش میاد) و یکی توی خودِ `ProductTab.jsx` (`resolveCost`، برای پیش‌نمایش زنده‌ی «جمع هزینه» حین افزودن/ویرایش). برای متریال‌های نسبتی/ثابت (پورسانتی)، نسخه‌ی معتبر وقتی `remainingCost` موجود باشه از *همون* به‌عنوان مبنای درصد استفاده می‌کنه (چون درصد یعنی سهم از باقیمانده‌ی در دسترس، نه کل اولیه)، ولی نسخه‌ی توی فرم همیشه از `totalCost` (کل اولیه، حتی اگه بخشیش قبلاً مصرف شده) استفاده می‌کرد. الان یکی شدن
18. [x] فاکتور: QRکد خیلی بزرگه (باید استاندارد بشه)؛ مشتری بی‌نام باید «مشتری گرامی» باشه نه «جناب آقای مشتری گرامی»؛ وقتی QR+کارت‌ویزیت توی فوتر میاد بک‌گراند فاکتور ناپدید می‌شه؛ فاکتور تب گالری تخفیف/هدیه نداره (تب حسابداری داره)؛ فاکتور موجودی‌های گالری باید با یه رنگ خاص/برچسب «موجود در گالری» باشه + ردیف جمع‌کل مخصوص موجودی‌ها — **✅ همه‌ی ۵ زیرمورد بررسی/فیکس شد 🟠 (Logy):**
   - QR بزرگ بود (۱۶۵px) → به ۹۰px کاهش پیدا کرد (اندازه‌ی استاندارد و متعادل‌تر برای هدر فاکتور)
   - «جناب آقای مشتری گرامی» برای بی‌نام: بررسی شد، این متن اصلاً هیچ‌جای کد فعلی وجود نداره (سرچ کامل انجام شد) — قبلاً یه‌جا فیکس شده بوده
   - «تخفیف/هدیه در گالری نیست»: بررسی شد، `GalleryTab.jsx` هم دقیقاً مثل `AccountingTab.jsx` فیلدهای `originalPrice`/`discountPct` رو برای هر آیتم می‌سازه — قبلاً فیکس شده بوده
   - «موجودی گالری رنگ/برچسب خاص + ردیف جمع‌کل مخصوص»: بررسی شد، هر دو از قبل توی `InvoiceTemplate.jsx` هستن (برچسب «موجود در گالری» روی ردیف، + ردیف جدا «موجودی در گالری» با پس‌زمینه‌ی خاکستری توی جمع‌بندی)
   - بک‌گراند ناپدید می‌شه — ✅ **علت واقعی پیدا و فیکس شد 🟠 (Logy):** کاربر عکس واقعی فرستاد — فاکتور کوتاه (بدون آیتم) بک‌گراند داشت، فاکتور پرآیتم (گالری، ۱۲+ ردیف) حتی سربرگ بالا هم نداشت (نه فقط واترمارک). این رد فرضیه‌ی «فقط قطع‌شدن روی صفحه‌ی دوم» رو داد و به علت واقعی رسوند: دکمه‌ی «دانلود/پیش‌نمایش» فاکتور از `html2canvas` استفاده می‌کنه (`InvoicePrint.jsx`)، و `html2canvas` پشتیبانی قابل‌اعتمادی از CSS `background-image` **چندلایه** (دو `url(...)` با کاما، که دقیقاً همون چیزی بود که برای سربرگ+واترمارک استفاده شده بود) نداره — خصوصاً روی المنت‌های بلند (فاکتورهای پرآیتم) کامل رندر نمی‌شد. **فیکس:** هر دو تصویر از CSS `background-image` روی `.invoice-paper` به دو تگ `<img>` واقعی (position:absolute، یکی بالا-چپ برای سربرگ، یکی پایین-راست برای واترمارک، پشت محتوا) توی `InvoiceTemplate.jsx` تبدیل شدن؛ `html2canvas` پشتیبانی خیلی بهتر و قابل‌اعتمادتری از تگ `<img>` معمولی داره تا از background-image چندلایه. ظاهر نهایی (اندازه/جای هردو تصویر) دقیقاً همون قبلیه، هم توی پرینت واقعی مرورگر هم توی پیش‌نمایش/دانلود
   - 🔴 لطفاً تست کن: هم یه فاکتور کوتاه هم یه فاکتور پرآیتم (خصوصاً گالری با QR+کارت‌ویزیت پر) رو با دکمه‌ی دانلود/ذخیره PDF امتحان کن، مطمئن شو سربرگ و واترمارک هردو توی خروجی هستن
19. [x] اسم گالری‌ها باید با پرانتز «(با مدیریت جناب‌آقای/سرکارخانم [نام مالک])» نمایش داده بشه — **✅ فیکس شد ⚫ (Ref):** این تقریباً از قبل بود (فرمت کوتاه‌تر «(آقای/خانم X)»)، فقط متن دقیق‌تر شد: «(با مدیریت جناب آقای/سرکار خانم X)»
20. [x] فیلترهای پیشرفته‌ی فاکتور (تب حسابداری) بازطراحی بشه ✅ (Wall 🟣) — **تکراریِ کامل با آیتم بالاتر همین فایل** («بازطراحی کامل ۴ فیلتر پیشرفته‌ی فاکتورها…» که 🟠 Logy قبلاً انجام داده: بازه‌ی تاریخ از/تا، فیلتر «نوع فروش» جدید، وضعیت تسویه، جستجو — همه توی `InvoicesTab.jsx`). چون این آیتم دقیقاً همون درخواستِ بازطراحی فیلترهای فاکتوره، بدون هیچ جزئیات اضافه‌ی جدیدی نسبت به اونی که Logy پیاده کرده، دوباره‌کاری نکردم — فقط تیکش خورد. اگه چیز مشخص دیگه‌ای غیر از کاری که Logy کرده مد نظرته (مثلاً یه فیلتر خاص دیگه، یا چیدمان متفاوت)، دقیق بگو تا جداگونه پیاده کنم.

### 🆕 گزارش تازه‌ی کاربر: چیدمان پاپ‌آپ ۴دکمه‌ای + فاکتور گالری هنوز تخفیف نداره (این نشست) 🟡 Ash
1. [x] **پاپ‌آپ ۴ دکمه (قفل/آزاد/Undo/رفرش) بزرگ و چیدمانش عجیبه** — کاربر چیدمان دقیق خواست: ردیف بالا = قفل (راست) و آزاد (چپ)، ردیف پایین = Undo (راست) و رفرش (چپ). **✅ فیکس شد:** یه باگ واقعی پیدا شد — دکمه‌ی رفرش یه `gridColumn: 1` اضافه و اشتباه داشت که کل auto-placement گرید رو به‌هم می‌ریخت (دکمه‌ها به ردیف/ستون غلط پرت می‌شدن). حذف شد؛ چون کل اپ RTL هست، ترتیب طبیعی DOM (قفل، آزاد، Undo، رفرش) بدون هیچ override دستی خودش دقیقاً همون چیدمان خواسته‌شده رو می‌ده. سایز دکمه‌ها هم از ۸۰×۸۰ به ۵۶×۵۶ کوچیک‌تر و مینیمال‌تر شد.
2. [x] **فاکتور تب گالری هنوز تخفیف/هدیه نشون نمی‌ده** (برخلاف چیزی که قبلاً «بررسی شد، از قبل درست بود» ثبت شده بود — بخش ۱۸ بالا) — **✅ باگ واقعی پیدا شد، این‌بار دقیق‌تر: `GalleryTab.jsx` دو تا مسیر جدا برای ساخت فاکتور داره (یکی برای «چاپ فاکتور تک‌مشتری»، یکی برای «چاپ سبد انتخابی»).** بار قبل فقط مسیر دوم چک شده بود (که درست بود). مسیر اول از یه فیلد اشتباه استفاده می‌کرد: `p.discount` — که اصلاً روی آبجکت محصول وجود نداره (فیلد واقعی `p.discountedPrice`/`p.discountPercent`ه، همونی که همه‌جای دیگه‌ی اپ ازش استفاده می‌شه) — یعنی این شرط همیشه false بود و تخفیف/هدیه هیچ‌وقت محاسبه نمی‌شد. فیکس شد تا دقیقاً همون منطق درستِ مسیر دوم/`AccountingTab.jsx` رو دنبال کنه.
- 🔴 لطفاً هر دو نوع چاپ فاکتور گالری رو تست کن (هم چاپ فاکتور تک‌مشتری، هم چاپ سبد انتخابی) با حداقل یه محصول تخفیف‌دار و یه هدیه، مطمئن شو هردو حالا درست نشون داده می‌شن
3. **بررسی عمیق ۴ تا خواسته‌ی دیگه‌ی همین گزارش (منطق قفل/آزادسازی، Undo، Refresh، عدم‌تغییر قیمت با قفل/ROI زنده، شناسه‌ی یکتای متریال):** با خوندن دقیق کد فعلی (نه فقط روخوانی روادمپ)، همه‌شون از نظر کد **از قبل دقیقاً طبق چیزی که خواسته بودی پیاده‌سازی شده‌ن**:
   - `resolveLineCostPure`: اگه `li.deductedAt` ست باشه (یعنی قفل‌شده)، همیشه همون `deductedCost` منجمد رو برمی‌گردونه (قیمت با قفل/آزادسازی *بعدی* تغییر نمی‌کنه). اگه هنوز قفل نشده، زنده از روی درصد/بچ/نسبت فعلی متریال محاسبه می‌شه
   - `productTotals` (که `totalCostAll`/`netProfit`/`projectedProfit` تب حسابداری ازش میاد) این تابع رو *برای همه‌ی محصولات، همیشه* صدا می‌زنه — یعنی ROI از قبل زنده‌ست، چه قفل‌شده چه معلق
   - شناسه‌ی یکتای متریال از قبل وجود داره: `material.id` + `li.materialId` روی هر آیتم محصول؛ برچسب‌ها/هدر دسته‌بندی‌ها همه از روی همین شناسه زنده می‌خونن (طبق فیکس‌های قبلی Ref روی `li.label`/`groupedProducts`)
   - منطق قفل (نارنجی→قرمز) / آزادسازی (آبی تا Refresh بزنی→خاکستری/باقیمانده) / Undo (دقیقاً آخرین پاس، با `passId`) هم طبق تاریخچه‌ی بالای همین فایل قبلاً پیاده و تست شده
   
   **چون کد از نظر من قانع‌کننده‌ست ولی خودت گفتی باگ داره، لطفاً یه سناریوی دقیق و مشخص بفرست** (مثلاً: «متریال X رو به محصول Y لینک کردم، قفل زدم، قیمتش شد Z؛ بعد آزاد کردم، قیمت باید بمونه Z ولی شد W») تا با دیتای واقعی دنبالش بگردم — چون بدون یه مورد مشخص که واقعاً fail بشه، ریسک دست‌زدن به این کد (که مستقیم رو محاسبات مالی قفل‌شده اثر می‌ذاره) بی‌دلیل بالاست
20b. [x] تب گالری، دکمه‌ی فاکتورها: چیدمان تیتر هر بخش نصفه‌ست؛ چک‌باکس اول حذف بشه، به‌جاش سلکت‌دیسلکت — **✅ هر دو فیکس شد ⚫ (Ref).** ریشه‌ی چیدمان: یه گرید ۴ستونه (`1fr 60px 60px 30px`) بود ولی فقط ۲ فرزند واقعی توش بودن، پس فرزندها فقط توی ستون ۱ و ۲ می‌نشستن و ۲ ستون آخر خالی می‌موند — دکمه‌ی حذف واقعاً ته ردیف نبود. به یه ردیف flex ساده با `justify-content: space-between` تبدیل شد. چک‌باکس هم برداشته شد، جاش آیکون دایره/تیک (`Circle`/`CheckCircle2`) قابل‌کلیک نشست که با کلیک روی کل ردیف (نه فقط خودِ آیکون) هم کار می‌کنه
21. [x] تب گالری، دکمه‌های همه/موجود/تسویه‌شده/تسویه‌نشده باید منطق «همه‌ی هوشمند» داشته باشن — **✅ فیکس شد ⚫ (Ref):** این فیلتر (توی کارت هر گالری/مشتری، تفکیک موجود/تسویه‌شده/تسویه‌نشده) از تک‌انتخابی-انحصاری به چندانتخابی (آرایه) تبدیل شد؛ «همه» فقط وقتی هایلایت می‌شه که همه‌چی انتخاب یا هیچی انتخاب نشده باشه، بقیه مستقل تیک می‌خورن

### 🆕🆕🆕 گزارش جدید کاربر — منطق پرتی‌شود/باقی‌بماند در بولک + فیلتر/اسکرول (این نشست) 🟠 (Logy)
کاربر گفت این‌ها رو یادش رفته بود به Ref بگه (جدا از گزارش ۲۱موردی بالاست، منطق محاسبه‌ست نه فقط چیدمان UI):

1. [x] **باگ: دکمه‌ی «پرتی شود» در بولک برای متریال مساحتی/فرش عمل نمی‌کنه.** منطق دقیق خواسته‌شده توسط کاربر:
   - اگر **«باقی بماند»** انتخاب شده: مساحت هرکدوم از محصولات انتخابی حساب بشه، از کل مساحت متریال کم بشه (مترمربع)؛ اگه نوار درصد هم روی ۱۰۰٪ باشه، بعد از اعمال باقیمانده داریم
   - اگر **«پرتی شود»** انتخاب شده: مساحت هر محصول حساب بشه، جمع مساحت محصولات انتخابی = ۱۰۰٪ فرض بشه، سهم هر محصول = درصدی از این ۱۰۰٪؛ هرچقدر نوار درصد متریال ست شده بود در نظر گرفته بشه؛ به هر محصول به نسبت سهم خودش از مساحت *کل محصولات انتخاب‌شده* (نه کل متریال) هزینه داده بشه؛ باقیمانده‌ی اضافه نداریم — یعنی کل باقیمانده به‌عنوان پرت روی همون محصولات پخش می‌شه، نه اینکه جدا نگه داشته بشه
   - **✅ فیکس شد 🟠 (Logy):** بعد از بررسی معلوم شد باگ واقعی این بود که تقسیم هدررفتِ «پرتی شود» *مساوی* بین محصولات پرتی‌دار بود (`leftoverArea / تعداد`)، نه به‌نسبت مساحت. کاربر بعد از بحث با دو گزینه (مساوی/وزن‌دار به‌نسبت سایز)، **گزینه‌ی وزن‌دار به‌نسبت سایز رو انتخاب کرد** («کوچیک‌ترها کمتر، بزرگ‌ترها بیشتر»، تا هدررفت زیاد قیمت یه محصول کوچیک رو نامتناسب بالا نبره). فرمول جدید در هر ۳ نسخه‌ی این محاسبه (موتور قفل واقعی در `App.jsx`، نسخه‌ی Pure برای رفرش آنی، و نسخه‌ی useMemo زنده) یکسان شد: `wasteShare = (ownArea / totalWastageArea) * leftoverArea`
2. [x] **متریال نوع چوب (خطی):** حالت «باقی بماند» کار نمی‌کنه و فقط درصد می‌گیره. باید: محیط/طول هم محصولات انتخاب‌شده حساب بشه، از طول کل چوب‌های موجود (تعداد موجود × طول هرکدوم) کسر بشه، بعد باقیمانده داریم. حالت «پرتی شود» برای چوب طبق کاربر همین الانش خوبه، دست نخوره — **✅ فیکس شد 🟠 (Logy):** پیدا شد که علتش این بود که خطی (بدون بچ) از یه مسیر کاملاً جدا (بخش ۴.۱ «استخر درصدی مشترک» در `runLockUnlockPass`، دقیقاً مثل نسبتی/ثابت) عبور می‌کرد که فقط بر مبنای درصدِ *هزینه‌ی* باقیمانده کار می‌کرد و هیچ ارتباطی با طول فیزیکی چوب‌های موجود نداشت؛ مسیر «چوب‌های مجزا/sticks» (بخش ۴.۲) که واقعاً طول رو می‌شناسه، فقط برای برش‌های تک‌به‌تک (`woodCuts`) استفاده می‌شه، نه برای بولک. فیکس: توی `bulkApplyMaterial`، وقتی متریال خطی و بدون بچ باشه، `totalAvailableLength` از `مجموع(تعداد×طول هر چوب موجود)` محاسبه می‌شه، هزینه‌ی هر محصول بر مبنای *محیط خودش* (با `getProductPerimeter`) به‌نسبت این طول کل حساب می‌شه، و «پرتی شود» هم از همون فرمول وزن‌دار-به-نسبت-سایزِ بخش مساحتی/فرش استفاده می‌کنه (همون‌کاری که کاربر خواسته بود انجام بشه دقیقاً چیزیه که پیاده شد)
3. [x] متریال **نسبتی** و **ثابت (ابزاری)**: اصلاً نیازی به این دو دکمه (پرتی‌شود/باقی‌بماند) ندارن — باید مخفی بشن برای این دو نوع — **بررسی شد 🟠 (Logy):** تأیید شد که این دو نوع اصلاً وارد سیستم بچ/پرتی نمی‌شن؛ خودشون یه مسیر جدای «استخر درصدی مشترک» (بخش ۴.۱ در `runLockUnlockPass`) دارن که اصلاً مفهوم «باقیمانده‌ی بچ» نداره. **✅ دکمه‌ها مخفی شدن 🟠 (Logy):** شرط نمایش دکمه‌های پرتی/باقی توی `MaterialTab.jsx` عوض شد، «نسبتی» ازش حذف شد (فقط مساحتی/فرش/خطی نشون داده می‌شن)
4. [x] از این به بعد، فیلترهای اعمال‌شده روی تب‌های محصولات/متریال/گالری باید با کلیک روی دکمه‌ی Refresh بازنشانی (reset) بشن — **✅ انجام شد 🟠 (Logy):** یه شمارنده‌ی `refreshResetTick` توی `App.jsx` اضافه شد که هر کلیک ساده‌ی Refresh (`handleQuickRefresh`) یکی زیادش می‌کنه؛ از طریق `ManagementPanelModal` → `MgmtTabs` به هر ۳ تب پاس داده می‌شه. هرکدوم یه `useEffect` روی تغییر این عدد دارن که فیلترهای *فعالِ* همون تب رو خالی می‌کنه: محصولات (`search`، `filterStatus`، `typeFilter`)، متریال (`search`، `stockFilter`)، گالری (`gallerySearch`، `statusFilter`). عمداً تنظیمات ماندگار/ذخیره‌شده (مثل `showZeroBalance` توی متریال که توی localStorage می‌مونه) دست نخورد چون اون یه preference هست نه فیلتر موقت
5. [x] 🔴 **مغایرت با claim قبلی:** بالاتر «دکمه‌ی اسکرول-به-بالا — ✅ تکمیل شد ⚫ (Ref)» نوشته شده، ولی کاربر می‌گه الان توی *هیچ* تبی دکمه‌ی اسکرول-به-بالا اصلاً نمایش داده نمی‌شه — **✅ باگ واقعی پیدا و فیکس شد 🟠 (Logy).** فیکس قبلی Ref درست بود ولی یه باگ *جدید و مستقلِ* دیگه بعدش این رو خراب کرده بود: `isAnyModalOpen` (که برای `hide` کردن دکمه استفاده می‌شه) خودش شامل `showManagementPanel` بود. چون خودِ تب‌های محصولات/متریال/گالری/حسابداری *داخل* همون `ManagementPanelModal` رندر می‌شن، وقتی پنل باز بود (یعنی همیشه، وقتی کاربر داخل هر کدوم از این تب‌هاست) `isAnyModalOpen` همیشه true بود و دکمه‌ی داخل پنل همیشه مخفی می‌موند — یه تناقض خودارجاع (self-referential). یه فلگ جدا (`isAnyModalOpenInsidePanel`، دقیقاً همون لیست بدون خودِ `showManagementPanel`) ساخته شد و به‌عنوان prop جدید (`hideScrollButton`) به `ManagementPanelModal` پاس داده شد؛ دکمه‌ی *بیرون* پنل (صفحه‌ی اصلی کاتالوگ) دست‌نخورده موند چون اونجا این تناقض وجود نداشت
- 🔴 لطفاً تست کن: توی هرکدوم از تب‌های محصولات/متریال/گالری چند بار فیلتر بزن، اسکرول کن پایین (دکمه‌ی بالا-رفتن باید ظاهر بشه)، بعد دکمه‌ی Refresh رو کلیک ساده بزن — فیلترها باید خالی بشن

### 🆕🆕🆕🆕 باگ حیاتی: کرش کامل اپ (Temporal Dead Zone) — این نشست 🟠 (Logy)
کاربر روی AI Studio لود کرده بود، اپ کرش می‌کرد، از Gemini علتش رو پرسیده بود. بررسی کردم و **دقیقاً همین باگ توی خودِ این ریپو هم بود** (نه فقط توی نسخه‌ی AI Studio):
- [x] توی `App.jsx`، `useSwipeTabNav(MAIN_TAB_ORDER, activeTab, setActiveTab, isAnyModalOpen)` قبلاً خط ۱۱۴۲ بود، ولی `isAnyModalOpen` تا خط ۱۲۲۸ (حدود ۸۶ خط پایین‌تر) تعریف نمی‌شد — یعنی متغیر `const` قبل از تعریف خودش استفاده می‌شد (Temporal Dead Zone)، که همیشه و بدون استثنا خطای `Cannot access 'isAnyModalOpen' before initialization` می‌داد و کل اپ رو موقع لود کرش می‌کرد. **✅ فیکس شد:** فراخوانی `useSwipeTabNav` به بعد از تعریف `isAnyModalOpen` منتقل شد (دقیقاً همون کاری که AI Studio/Gemini هم توی نسخه‌ی جداگونه‌ش انجام داده بود، ولی اینجا توی ریپوی مشترک هنوز نبود)
- ⚠️ این یه باگ کاملاً مستقل و بی‌ربط به هر ۴ رنگ بود (به‌احتمال زیاد یه commit قدیمی‌تر که ترتیبش رو بهم زده بوده)؛ چون کل اپ رو غیرقابل‌اجرا می‌کرد، فوراً (بدون منتظرماندن برای اولویت‌بندی claim) فیکس شد
- 🔴 لطفاً تست کن: اپ رو کامل ری‌لود کن (چه روی گوشی، چه روی AI Studio)، مطمئن شو دیگه هیچ کرشی موقع بالا اومدن نداره

### 🆕🆕🆕🆕🆕 ۲ باگ حیاتی دیگه از تست AI Studio — این نشست 🟠 (Logy)
کاربر سه‌تا خطای دیگه از پیش‌نمایش AI Studio فرستاد؛ هرکدوم رو جدا چک کردم که واقعی‌ان یا مخصوص AI Studio:
- [x] **واقعی و حیاتی — تب محصولات کاملاً خراب بود:** توی `ProductTab.jsx`، کامپوننت `ProductCard` توی هر ۲ نقطه‌ی رندرش (چیدمان گروهی و چیدمان تخت) پراپ‌های `onEdit`، `onDelete`، `onImageUpload`، `onOpenLightbox`، و `expanded` اصلاً پاس داده نمی‌شدن! یعنی: کلیک ویرایش/حذف/آپلود عکس روی هر کارت محصول کرش می‌کرد («X is not a function»)، و چون `expanded` هم نمی‌رسید کارت‌ها هیچ‌وقت باز/جمع نمی‌شدن. **✅ فیکس شد:** هر ۵ پراپ به `setEditingProduct`/`onRequestDelete`/`onImageUpload`/`setLightboxId`/`expandedProductId === p.id` وصل شدن، توی هر دو محل رندر
- [x] **واقعی — کرش قطعی تب گالری:** توی `GalleryTab.jsx`، آیکون `Search` استفاده می‌شد (خط ~۱۷۸۰، نوار جستجو) ولی از `lucide-react` ایمپورت نشده بود → `ReferenceError: Search is not defined` هر بار که نوار جستجوی گالری رندر می‌شد. **✅ فیکس شد:** به import اضافه شد
- این دو مورد کاملاً مستقل از هر ۴ رنگ بودن (شبیه باگ TDZ بالا) — چون هر دو کل یه تب رو غیرقابل‌استفاده می‌کردن، فوراً فیکس شدن
- 🔴 لطفاً تست کن: تب محصولات رو باز کن، روی چند کارت کلیک کن ببین باز/بسته می‌شن، دکمه‌ی ویرایش/حذف/آپلود عکس رو امتحان کن؛ تب گالری رو هم باز کن و توی نوار جستجو تایپ کن

### 🆕 نکته: نسخه‌ی AI Studio با ریپو هماهنگ نیست — این نشست 🟠 (Logy)
کاربر عکس فرستاد که توی AI Studio، لیست‌های انتخاب گالری/مشتری (توی سبد خرید) دوباره به مدل قدیمِ غیروسط‌چین (که از کادر بیرون می‌زنه) برگشته بودن. چک شد: توی خودِ این ریپو، `SmartLocationSelect` و `CustomerSmartSelect` (`ProductTab.jsx`) هر دو از قبل کامل از `FilterPopup` (وسط‌چین، مثل بقیه‌ی فیلترها) استفاده می‌کنن — کد این‌جا مشکلی نداره. یعنی چیزی که توی AI Studio دیده می‌شه مربوط به یه نسخه/snapshot قدیمی‌تره که با آخرین push های ریپو sync نشده، نه یه ریگرشن واقعی توی کد مشترک. اگه بازم همچین چیزی دیده شد، اول باید مطمئن بشیم AI Studio داره از آخرین commit ریپو استفاده می‌کنه.

## ✅ دسته‌ی کارهای سطحی جدید — همه‌شون تکمیل شدن (این نشست، 🟣 Wall)
- [x] ۱۳: دکمه‌های سورت گالری/حسابداری از قانون پاپ‌آپ سراسری پیروی نمی‌کردن — **حل شد.** `GalleryTab.jsx`: SortButton از الگوی «نگه‌دار ۵۰۰ms» به کلیک‌ساده تبدیل شد (توی commit قبلی همین نشست انجام شده بود، اینجا فقط یادآوری). `AccountingTab.jsx`: دکمه‌ی سورتِ «تمامی فاکتورها» که فقط آیکون رو با کلیک عوض می‌کرد (بدون پاپ‌آپ)، الان به `FilterPopup` با ۴ گزینه‌ی برچسب‌دار (تاریخ/مبلغ/نام خریدار/تعداد اقلام) تبدیل شد
- [x] ۲: دکمه‌ی فیلتر گالری «همه» نداشت — **حل شد:** یه دکمه‌ی «همه» به‌عنوان اولین گزینه‌ی `GalleryStatusFilter` اضافه شد که با کلیک، فیلتر رو خالی می‌کنه
- [x] ۳: علامت مربعی/چک‌باکسی کنار گزینه‌های فیلتر گالری — **حل شد:** کامل حذف شد، الان فقط با رنگ متن/پس‌زمینه‌ی دکمه مشخص می‌شه کدوم انتخاب شده (مثل بقیه‌ی فیلترهای اپ)
- [x] ۴: سایز نامتناسب دکمه‌ی X جستجو در تب محصولات/متریال — **حل شد:** این دو تب از متن ساده‌ی `✕` (بدون اندازه‌ی دقیق، بزرگ‌تر از گالری) استفاده می‌کردن؛ به آیکون `<X size={13}/>` (دقیقاً مثل گالری) تبدیل شدن — ۴ مورد (۲تا ProductTab، ۲تا MaterialTab)
- [x] ۵ (بزرگ‌ترین آیتم): ناهماهنگی نام‌گذاری همکار/مشتری — **حل شد، با یه سوءتفاهم اولیه که برطرف شد:**
  - تب گالری (کارت خودِ همکار): «با مدیریت» حذف شد، فقط «مدیوال (آقای حسن زیبایی)» می‌مونه (بدون جناب/سرکار)
  - فاکتور چاپی (`InvoiceTemplate.jsx` + نسخه‌ی متنیِ کپی/اشتراک‌گذاری توی `InvoicePrint.jsx`): «با مدیریت» اضافه شد → «همکار گرامی: مدیوال (با مدیریت جناب آقای حسن زیبایی)»
  - فروش مستقیم بدون نام: **علت واقعی پیدا شد** — توی سبد خرید (`ProductTab.jsx`) و تب حسابداری (`AccountingTab.jsx`)، به‌جای گذاشتن `customer.kind = "warehouse"` با نام خالی (که منطق یکپارچه‌ی `InvoiceTemplate.jsx` رو به‌درستی فعال می‌کنه)، یه رشته‌ی متنی هاردکد می‌شد («مشتری گرامی» یا «انبار اصلی» به‌عنوان *نام* مشتری، نه به‌عنوان fallback) که باعث می‌شد کد اون رو «مشتری معمولی به این اسم» تشخیص بده و بنویسه «جناب آقای مشتری گرامی» یا «جناب آقای انبار اصلی». هردو فیکس شدن، الان همه‌جا یکسان «فروش مستقیم: مشتری گرامی» می‌شه
- [x] بک‌گراند فاکتور بعضی‌وقتا نمیومد — **علت واقعی پیدا و فیکس شد، تصادفی نبود:** توی `InvoicePrint.jsx`، برای PDF/عکس‌گرفتن، یه کپی از فاکتور ساخته می‌شه (`cloneNode`) که شامل `<img>` سربرگ/واترمارک هم می‌شه؛ این عکس‌های کپی‌شده باید دوباره لود/دیکود بشن. کد قبلی فقط ۱۵۰ میلی‌ثانیه‌ی ثابت صبر می‌کرد که رو گوشی‌های کندتر یا بار اول (بدون کش) کافی نبود — دقیقاً همون رفتار «بعضی‌وقتا میاد بعضی‌وقتا نه». الان یه تابع (`waitForImagesToLoad`) واقعاً صبر می‌کنه تا همه‌ی img های کپی‌شده finish بشن (حداکثر ۲ ثانیه، که گیر نکنه)
- [x] 🔴 مهم‌ترین: فاکتور تب گالری تخفیف/هدیه/«موجود در گالری» رو درست نشون نمی‌داد — **علت پیدا و فیکس شد:** `GalleryTab.jsx` یه ساختار داده‌ی دستی و ناقص برای فاکتور می‌ساخت (نه از همون مدل مشترکی که `AccountingTab.jsx` درست استفاده می‌کرد) — فیلد `isGift` اصلاً نبود (حتی توی یه نقطه یه کامنت قبلی همین باگ رو دقیق توضیح داده بود ولی فیکس نشده بود!)، و فیلد `isAvailableInGallery` هم ست نمی‌شد (برای همین به‌جاش «بدهکار» می‌افتاد). هر دو نقطه‌ی ساخت فاکتور توی `GalleryTab.jsx` (چاپ گروهی از کارت گالری + چاپ فاکتور تکی/ادغامی توی `InvoiceListModal`) فیکس شدن؛ منبع درست (`AccountingTab.jsx`) دست نخورد
- [x] باکس آبی تخفیف توی فاکتور، هم درصد هم مبلغ نشون می‌داد — **حل شد:** خط مبلغ از داخل باکس آبی (`InvoiceTemplate.jsx`) حذف شد، فقط درصد می‌مونه
- [x] تب متریال: آیکون چشمِ هاید متریال قرمز بود — **حل شد:** به خاکستری (`#555`، هم‌رنگ بقیه) تغییر کرد

### 🆕 محصولات «در دست ساخت» توی کاتالوگ نمایش داده می‌شدن — این نشست 🟠 (Logy)
- [x] محصولات با وضعیت «در دست ساخت» نباید توی تب کاتالوگ (نمای عمومی/مشتری) نشون داده بشن؛ به‌محض این‌که از حالت در-دست-ساخت خارج شد (ساخته/آماده شد)، خودکار توی کاتالوگ ظاهر بشه — **✅ فیکس شد:** فیلتر لیست کاتالوگ (`CatalogTab` توی `ProductTab.jsx`) چک `p.isDraft` رو نداشت (فقط `hiddenFromCatalog`/نوع/جستجو/وضعیت/موقعیت چک می‌شد). یه خط `if (p.isDraft) return false;` اضافه شد. چون این فیلتر یه `useMemo` است که به خودِ `products` وابسته‌ست، وقتی محصولی از در-دست-ساخت خارج بشه (`isDraft: false` ست می‌شه، جایی که از قبل توی کد وجود داشت)، خودکار و بدون کار اضافه توی رندر بعدی تو کاتالوگ ظاهر می‌شه — نیازی به تغییر جای دیگه نبود






### 🆕🆕🆕 گزارش تازه‌ی کاربر (بعد از تست دوباره) — این نشست ⚫ (Ref)
1. [x] نمایش ابعاد دایره‌ی کامل: «22O» با چشم شبیه «220» خونده می‌شه — **✅ فیکس نهایی شد ⚫ (Ref).** اول به «22-O» تبدیل شده بود، ولی طبق تصمیم بعدی‌تر کاربر، فرمت نهایی شد «22o» (حرف کوچیک، بدون خط تیره) — هم توی `ProductTab.jsx` (فرم ویرایش/ذخیره) هم `WoodCuttingTab.jsx` (برچسب پنل‌های ۲D) اعمال شد. تشخیص و پارس‌کردن هردو فرمت قدیمی (O بزرگ، با یا بی‌خط‌تیره) هنوز کار می‌کنه (case-insensitive + خط‌تیره‌ی اختیاری)، برای سازگاری با دیتای قدیمی
2. [x] ⚠️ **تغییر تصمیم قبلی:** ابعاد باید برعکسِ چیزی که این نشست فیکس شد بشه — کوچیک اول، بزرگ دوم — **✅ برعکس شد ⚫ (Ref)** توی هم منطق ذخیره‌ی `ProductTab.jsx` هم مهاجرت داده‌ی موجود در `App.jsx`
3. [x] دکمه‌ی فیلتر «انواع محصول» (تب محصولات و کاتالوگ) باید چند-انتخابی باشه و با کلیک بیرون پاپ‌آپ بسته بشه؛ توی تب کاتالوگ این دکمه اصلاً کار نمی‌کنه؛ گزینه‌ی «همه» توی لیست انواع باید اسمش «همه» باشه — **✅ حل شد (Wall 🟣 و Ref ⚫ هم‌زمان، مستقل از هم روش کار کردن، merge شد).** هر دو نسخه (تب محصولات + کاتالوگ) از تک‌انتخابی به چند-انتخابی (آرایه) تبدیل شدن؛ برچسب شد «همه»؛ کلیک بیرون می‌بنده نه انتخاب گزینه. برای «کار نکردن توی کاتالوگ»: هیچ‌کدوممون یه باگ منطقی مشخص پیدا نکردیم (فیلتر `p.productTypeId` درست وصل بود)؛ کل بلوک بازنویسی شد، امیدواریم مشکل ظریفی که بوده هم برطرف شده باشه. 🔴 لطفاً تست کن
4. [x] تب گالری، دکمه‌ی Sort: نباید با انتخاب یه گزینه بسته بشه، فقط با کلیک بیرون بسته بشه — **✅ حل شد (Wall 🟣 و Ref ⚫ هم‌زمان):** `setShowPopup(false)` از onClick هر گزینه حذف شد
5. [x] تب گالری، دکمه‌ی فیلتر (همه/بدهکار/دارای موجودی): همینطور — با هر انتخاب (نه فقط «همه») نباید خودکار بسته بشه — **✅ حل شد (Wall 🟣):** گزینه‌های «بدهکار»/«دارای موجودی» از قبل popup رو نمی‌بستن (فقط toggle می‌کردن)؛ فقط کلیک روی «همه» می‌بست که فیکس شد
6. [x] از روی دکمه‌ی ورود با اثرانگشت، کلمه‌ی «/فیس» حذف بشه — **✅ فیکس شد ⚫ (Ref)**
7. [x] تب گالری و تب محصولات: بین هدر خودِ تب (که جستجو داره) و هدر بالایی اپ، موقع اسکرول یه گپ می‌افته که لایه‌ی زیری از پشتش معلوم می‌شه — **آپدیت (این نشست، با عکس واقعی از داخل پنل مدیریت/متریال) 🟡 (Ash):** عکس نشون داد این دقیقاً داخل `MgmtTabs` (پنل مدیریت) اتفاق می‌افته، نه توی کاتالوگ. علت پیدا شد: ارتفاع دو هدر بالایی (`--panel-header-height` و `--mgmt-tabs-height`) با `useEffect` اندازه‌گیری و ست می‌شدن، که *بعد از* رنگ‌آمیزی اولیه‌ی مرورگر اجرا می‌شه — یعنی برای یه فریم گذرا، هدرهای sticky بچه‌ها (تب محصولات/متریال/گالری/...) با مقدار fallback (نه ارتفاع واقعی) جا می‌گرفتن، که دقیقاً همون شکاف/لایه‌ی‌زیری‌-معلوم رو توی عکس توضیح می‌ده. هر ۳ تا اندازه‌گیری مشابه توی اپ (`--panel-header-height`، `--mgmt-tabs-height` توی `App.jsx`، و `--global-header-height` توی `GlobalHeader.jsx`) از `useEffect` به `useLayoutEffect` تغییر کردن (که قبل از رنگ‌آمیزی اجرا می‌شه، نه بعدش) تا این فریم گذرا از بین بره. ⚠️ چون این یه باگ زمان‌بندی/رندره که فقط توی مرورگر واقعی و مخصوصاً موقع اسکرول سریع دیده می‌شه، نمی‌تونم صد‌درصد مطمئن باشم کاملاً حلش کرد بدون تست دوباره — لطفاً دوباره امتحان کن (مخصوصاً اسکرول سریع بلافاصله بعد از باز کردن یه تب) — **آپدیت ۲:** کاربر گفت این تو تب محصولات، کاتالوگ، و برش هم هست. بررسی شد: هر ۴ تا (گالری/محصولات/متریال/برش، چون همه از طریق `MgmtTabs` رندر می‌شن) از همون ۲ متغیر (`--panel-header-height` + `--mgmt-tabs-height`) استفاده می‌کنن، و کاتالوگ از `--global-header-height` — یعنی هر ۳ متغیری که هست، توی همین فیکس بالا پوشش داده شدن؛ کد جداگونه‌ی دیگه‌ای برای هیچ‌کدوم از این تب‌ها نبود. پس این گزارش دوم به‌احتمال زیاد یا از قبل از پوش‌شدن فیکس بوده، یا (اگه بعدش هم دیدینش) نشون می‌ده مشکل یه‌جای دیگه‌ست که این فیکس پوشش نداده. **لطفاً یه‌بار با ری‌لود کامل (نه فقط رفرش تب) دوباره تست کن؛ اگه بازم دیدی، لطفاً یه اسکرین‌شات جدید دقیقاً از تب محصولات/کاتالوگ/برش (بعد از این فیکس) بفرست تا مطمئن بشم دقیقاً کجاش هنوز مشکل داره.**

### 🆕🆕 گزارش کاربر از تست فیزیکی روی گوشی (این نشست) 🟡 (Ash)
1. ادامه‌ی بخش ۷ (برش ۱D چوب) — توضیحش بالای همین فایل، بخش ۷.
2. [x] دکمه‌ی چشم (Eye/EyeOff) کنار هر محصول برای مخفی‌کردن از کاتالوگ عمومی گم شده بود — **✅ حل شد:** خودِ UI دکمه (`ProductCard` توی `ProductTab.jsx`) سرجاش بود، ولی handler اصلی‌ش (`onToggleHideFromCatalog`) از `App.jsx`/`ProductTab.jsx` رفته بود (احتمالاً یه‌جای این چند نشست شلوغ اخیر پاک شده). `handleToggleHideFromCatalog` دوباره نوشته شد (دقیقاً شبیه `handleReturnToAvailable` کنارش) و به هر دو جایی که `ProductCard` رندر می‌شه وصل شد.
3. [x] «انتخاب بچ محصول» با گزینه‌ی «خرید اولیه ۱۴۰۵/۰۵/۰۶ · ۱۰۰×۲۰۰» توی تخصیص متریال «۱۱۱» (فرش) — **این باگ نیست، فیچر عمدیه:** طبق تصمیمی که همین چند نشست اخیر گرفته شده (بخش ۱۱، خط بالای همین فایل)، اولین خرید هر متریال مساحتی/فرش خودش یه «بچ» با شناسه می‌گیره («خرید اولیه» + تاریخ + ابعاد رول/تکه‌ی خریداری‌شده)، تا بشه هزینه‌ی هر محصول رو دقیقاً از همون بچ/رول مشخص کسر کرد (نه به‌صورت درصدی و نامشخص). گزینه‌ی «تخصیص درصدی (بدون بچ)» هم برای وقتیه که نمی‌خواین به یه بچ خاص مقید بشین.
4. [x] اسکرول افقی ناخواسته توی بخش فاکتور تب حسابداری (نمای پیشرفته) — **آپدیت ۲ (علت واقعی این بود):** کاربر توضیح داد منظورش سوایپ-تعویض-تب بوده، نه یه اسکرول CSS. علت پیدا شد: یه مودال تمام‌صفحه (`AllInvoicesModal` — همون «تمامی فاکتورها»/«نمای پیشرفته») هیچ‌وقت خودش رو توی رجیستری مودال‌های تودرتو ثبت نمی‌کرد، پس سوایپ بین تب‌های اصلی از پشتش رد می‌شد و کشیدن انگشت روی فیلترها/لیست، به‌جای تعامل با خودش، تب رو عوض می‌کرد. با `useRegisterOpenModal` فیکس شد. همین موقع، همین باگ رو توی ۲ کامپوننت مشترک دیگه هم پیدا کردم و همه‌جا یه‌جا فیکس کردم: `FilterPopup.jsx` و `JalaliDatePicker.jsx` (چون هیچ‌کدوم قبلاً ثبت نمی‌شدن، این باگ توی هر فیلتر تاریخ و هر پاپ‌آپ فیلتر/سورت کل اپ بود، نه فقط فاکتورها) — و یه مودال دیگه هم توی تب برش (`showProductPicker`) که همین مشکل رو داشت. علاوه بر این، `swipeTabs.js` یه مکانیزم جدید هم گرفت (`data-no-swipe="true"`) برای مناطق پرتراکم از دکمه (مثل کارت «۵ فاکتور اخیر») که مودال نیستن ولی کشیدن انگشت روشون راحت به‌اشتباه سوایپ حساب می‌شه. آیتم قبلی (فیکس `flexWrap` برای بیرون‌نزدن دکمه‌های فیلتر) هم سرجاشه، ولی اون یه باگ CSS جدا بود، نه علت اصلی این مورد.

## ✅ تبدیل ردیف‌های فیلتر به دکمه‌ی پاپ‌آپ — تکمیل شد (این نشست، 🟣 Wall)
- [x] تب کاتالوگ: ردیف «همه/موجود/فروخته‌شده» (تک‌انتخابی) → دکمه‌ی چند-انتخابی «همه/موجود/ناموجود» بین جستجو و دکمه‌ی فیلتر موقعیت. `statusFilter` از رشته به آرایه تبدیل شد
- [x] تب محصولات: ردیف «همه/موجود/تسویه‌شده/تسویه‌نشده» → دکمه بین جستجو و دکمه‌ی فیلتر موقعیت. `filterStatus` از قبل آرایه بود (چند-انتخابی)، فقط UI تبدیل شد
- [x] تب متریال: ردیف «همه/فرش/خطی/مساحتی/نسبتی/ابزار» → دکمه بین جستجو و دکمه‌ی «هاید موارد مخفی». `matGroupFilter` از رشته‌ی تک‌انتخابی به آرایه‌ی چند-انتخابی تبدیل شد
- [x] تب گالری: ردیف جستجو+فیلترها الان `sticky` هست — **علت واقعی پیدا شد:** `stickyTop` از `App.jsx`/`MgmtTabs` به `GalleryTab` prop پاس داده می‌شد ولی خودِ `GalleryTab` اصلاً این prop رو destructure یا استفاده نمی‌کرد؛ اضافه شد و ردیف جستجو+فیلتر دقیقاً مثل تب محصولات/متریال (`position:sticky`) چسبید

هر سه فیلتر جدید دقیقاً همون الگوی موجود (`FilterPopup`، چند-انتخابی، «همه» هوشمند که با انتخاب دستیِ همه‌ی گزینه‌ها هم فعال می‌شه، فقط کلیک بیرون می‌بنده) رو دنبال می‌کنن — الگوبرداری مستقیم از `GalleryStatusFilter` که از قبل درست بود.

**آپدیت (بعد از گزارش تازه‌ی Ref در همین فایل، آیتم‌های ۳/۴/۵ بالاتر):**
- [x] آیتم ۳ (فیلتر «انواع محصول» چند-انتخابی + کار نکردن‌ش توی کاتالوگ) — **حل شد:** `typeFilter` (هم توی `CatalogTab` هم تب محصولات مدیریتی) از تک‌مقداری به آرایه تبدیل شد؛ گزینه‌ی اول الان اسمش دقیقاً «همه»ست؛ پاپ‌آپ با هر انتخاب باز می‌مونه (فقط کلیک بیرون می‌بنده). علت دقیق «کار نکردن توی کاتالوگ» رو با بررسی کد پیدا نکردم (ساختار درست بود)؛ اگه بعد این بازنویسی هم مشکل داشت لطفاً بگو
- [x] آیتم ۴ (Sort گالری نباید با انتخاب ببنده) — **حل شد:** همین بخشی که خودم قبل‌تر این نشست ساخته بودم رو اصلاح کردم؛ الان دقیقاً مثل `SortButton` تب متریال، فقط با کلیک بیرون می‌بنده
- [x] آیتم ۵ (فیلتر گالری نباید با هیچ انتخابی، نه فقط «همه»، ببنده) — **حل شد:** `GalleryStatusFilter` هم همینطور اصلاح شد

### 🆕 اکسل خروجی رنگی (پیش‌نمایش) — دو باگ گزارش‌شده با فایل نمونه — این نشست 🟠 (Logy)
کاربر یه اکسل نمونه (`Book1.xlsx`) با چیدمانِ درست فرستاد و مقایسه کردم:
- [x] **گروه‌بندی اشتباه بود:** `handleExportPreviewExcel` (`App.jsx`) قبلاً بر اساس `p.group` (یه فیلد دسته‌بندی دستی محصول که معمولاً خالیه) گروه‌بندی می‌کرد، در حالی که فایل نمونه‌ی کاربر و خودِ تب محصولات بر اساس **نام فرش/متریال متصل** گروه‌بندی می‌کنن (مثل «فرش قرمز:»). این احتمالاً همون چیزی بود که «برعکس» به‌نظر می‌رسید — چون خروجی یا همه زیر «بدون دسته» جمع می‌شد یا با چیزی که توی اپ می‌دیدن یکی نبود. **✅ فیکس شد:** دقیقاً همون منطق گروه‌بندی «حالت فرش» تب محصول (`data.materials.find بر اساس li.materialId که type==="fabric"`) کپی شد
- [x] **قابلیت جمع/باز-شدن (Group/Outline) برای ریزهزینه‌ها نبود:** الان با `ExcelJS` هم ستون‌های ریزهزینه‌ی متریال (بین نام/ابعاد و «هزینه تمام‌شده») و هم ردیف‌های محصول زیر هر هدر گروه، سطح outline گرفتن — توی خودِ اکسل دکمه‌ی +/- بالای ستون‌ها و کنار ردیف‌ها ظاهر می‌شه که می‌شه باهاش ریزهزینه‌ها/محصولات هر گروه رو جمع کرد و فقط جمع‌کل رو دید. یه اسکریپت تست جدا نوشتم و خروجی رو با `openpyxl` چک کردم که outlineLevel/outlineProperties درست نوشته شدن
- 🔴 لطفاً یه اکسل پیش‌نمایش واقعی بگیر و توی Excel/Google Sheets چک کن: هم گروه‌بندی درست باشه، هم دکمه‌های +/- (بالای ستون‌های ریزهزینه، و کنار ردیف‌های هر گروه) کار کنن

### 🆕 باگ حیاتی: سینک با اکانت گوگل اصلاً کار نمی‌کرد — این نشست 🟠 (Logy)
کاربر گزارش داد: لاگین می‌زنه، ولی نه دکمه‌ی لاگین به لاگین‌شده تغییر می‌کنه، نه سینک کار می‌کنه (می‌گه «لطفاً وارد شوید»، انگار اصلاً لاگین نکرده).
- [x] **علت پیدا شد (`AuthContext.jsx`):** `FirebaseAuthentication.signInWithGoogle()` (پلاگین Capacitor) طبق مستندات خودش **فقط لایه‌ی نیتیو** رو لاگین می‌کنه. برای این‌که خودِ Firebase JS SDK (که کل اپ — از جمله دکمه‌ی لاگین و `performSynchronization` — بهش وابسته‌ست، از طریق `auth.onIdTokenChanged`) هم خودکار sync بشه، پلاگین نیاز به یه تنظیمِ صریح `skipNativeAuth: false` توی `capacitor.config` داره. این پروژه پوشه‌ی `android/`/فایل `capacitor.config` رو توی گیت نداره (جدا مدیریت می‌شه)، پس این تنظیم احتمالاً یا اصلاً ست نشده یا هرجایی هست که کد بهش دسترسی/تضمین نداره — نتیجه: لایه‌ی نیتیو لاگین می‌شد ولی JS SDK هیچ‌وقت خبردار نمی‌شد، `token` همیشه `null` می‌موند، هم دکمه هم سینک همیشه فکر می‌کردن کسی لاگین نکرده
- [x] **✅ فیکس شد:** به‌جای وابسته موندن به یه فایل کانفیگ بیرون از دسترس/غیرقابل‌تأیید، الان توی `login()` صریحاً بعد از لاگین نیتیو، با `idToken`ـی که خودِ پلاگین برمی‌گردونه، `signInWithCredential` روی خودِ JS SDK صدا زده می‌شه — این کار مستقل از تنظیمات `capacitor.config` همیشه درست کار می‌کنه. `logout()` هم برای تقارن صریحاً `auth.signOut()` رو هم صدا می‌زنه
- 🔴 لطفاً تست کن: از تب همگام‌سازی با گوگل لاگین کن، مطمئن شو دکمه فوراً به حالت لاگین‌شده تغییر می‌کنه و سینک واقعاً انجام می‌شه (نه فقط پیام موفقیت الکی)

## 🆕 بازطراحی بزرگ ذخیره‌سازی عکس‌ها (فایل‌محور به‌جای base64) — این نشست 🟣 Wall claim کرد
درخواست کاربر: به‌جای ذخیره‌ی base64 مستقیم توی رکورد محصول/کارت‌ویزیت (که باعث سنگینی localStorage/سینک می‌شه)، فقط اسم فایل ذخیره بشه و خودِ عکس از یه پوشه‌ی مشخص لود بشه (`images/`، `qr/`، `cards/`)، با بک‌آپ خودکار کل پوشه موقع سینک گوگل.

**⚠️ محدودیت فنی که با کاربر هماهنگ شد:** اندروید مدرن اجازه‌ی دسترسی به «مسیر دلخواه تایپ‌شده» رو نمی‌ده (scoped storage). به‌جاش: پوشه‌ی مخصوص اپ (`Documents`، بدون پرمیشن خاص) + کاربر فقط **اسم زیرپوشه** رو می‌ده. توی تب همگام‌سازی هم دکمه‌ی «انتخاب پوشه» نیست، فقط یه فیلد متنی برای اسم زیرپوشه + توضیح متنی که کجا باید فایل‌ها رو بریزه/پیدا کنه (طبق تاییدِ صریح کاربر).

**پلن مرحله‌ای:**
- [x] مرحله ۱: `imageStorage.js` (utility هسته‌ای) + تب همگام‌سازی — **✅ تکمیل شد، با یه یافته‌ی امنیتی جدی در همین حین:**
  - `src/utils/imageStorage.js` ساخته شد: `saveImageToFolder`/`getImageSrc`/`imageFileExists`/`deleteImageFile` + هوک `useResolvedImageSrc`. روی اندروید از `@capacitor/filesystem` (پوشه‌ی `Documents/<زیرپوشه>/images|qr|cards`) استفاده می‌کنه؛ روی وب/پیش‌نمایش (که اصلاً دسترسی واقعی به دیسک نداره — محدودیت خودِ مرورگره) از IndexedDB به‌عنوان fallback
  - تب همگام‌سازی: بخش قدیمیِ «مسیر پوشه تصاویر» (که یه دکمه‌ی انتخاب پوشه‌ی `webkitdirectory` غیرکاربردی داشت و مسیر رو به‌عنوان متن دلخواه می‌گرفت) کامل جایگزین شد؛ **آپدیت بعدی (طبق تصمیم تازه‌ی کاربر):** اسم پوشه دیگه قابل‌تغییر هم نیست — همیشه ثابت `Documents/refarsh/` (کاربر خودش هم می‌تونه دستی بسازتش، اپ هم خودکار می‌سازه اگه نبود). فقط یه توضیح متنی نشون می‌ده که کجا (`images`/`qr`/`cards`) دنبالش بگرده
  - 🔴 **یافته‌ی امنیتی جدی، جدا از این فیچر:** موقع بررسی، معلوم شد یه پیاده‌سازی قبلی و ناقص از همین ایده از قبل وجود داشت — آپلود عکس محصول (`ProductTab.jsx`) به یه endpoint سرور (`/api/upload` توی `server.ts`) عکس می‌فرستاد که مسیر مقصدش (`destPath`) رو **مستقیم و بدون هیچ اعتبارسنجی از کلاینت می‌گرفت** و با `fs.mkdirSync`/`fs.copyFileSync` می‌نوشت — یعنی هر کاربر لاگین‌شده‌ای می‌تونست با یه `destPath` دلخواه هر فایلی رو هرجای دیسک سرور بنویسه (path traversal / arbitrary file write). این endpoint **کامل حذف شد**؛ هم چون با معماری جدید (پوشه‌ی محلی روی گوشی کاربر، نه سرور) ناسازگار بود، هم چون خطرناک بود
  - آپلود عکس تب محصولات (`handleImageUpload`) به سیستم محلی جدید وصل شد؛ هر ۷ نقطه‌ی نمایش عکس محصول (کارت گرید، لایت‌باکس، تصویر کوچیک ادیتور، کاتالوگ، ...) به یه کامپوننت مشترک جدید (`ProductImage`) تبدیل شدن که async حل می‌کنه و برای عکس‌های قدیمی base64/URL هم سازگاره (بدون مهاجرت اجباری). فایل ناقص قبلی (`imageHelpers.js`) دیگه استفاده نمی‌شه (پاک نشد، فقط بی‌استفاده‌ست)
  - 🔴 لطفاً روی گوشی واقعی تست کن: یه عکس محصول آپلود کن، ببین ذخیره/نمایش درست کار می‌کنه؛ اسم زیرپوشه رو توی همگام‌سازی عوض کن و ببین توضیحات درست نشون داده می‌شه
- [x] مرحله ۲: کارت‌ویزیت (عکس خودم + QR توی `qr/` + کارت‌های همکاران توی `cards/`) — **✅ تکمیل شد (Wall 🟣):**
  - `BusinessCardModal.jsx`: کامپوننت مشترک `CardImage` ساخته شد (مثل `ProductImage`)؛ آپلود QR/عکس آرشیو دیگه به سیستم محلی وصله (`saveImageToFolder`) — QR کارت خودم → `qr/`، QR/عکس کارت‌های همکار و تامین‌کننده → `cards/`. حذف عکس هم فایل واقعی رو پاک می‌کنه (`deleteImageFile`)، نه فقط رفرنس آرایه رو
  - `InvoiceTemplate.jsx`: نمایش QR روی فاکتور چاپی هم به همین سیستم وصل شد (async، با سازگاری کامل به عکس‌های قدیمی base64)
  - همه‌جا سازگاری به عقب حفظ شده (عکس‌های قدیمی که هنوز data URL/URL مستقیم هستن هم درست نمایش داده می‌شن، مهاجرت اجباری لازم نیست)
- [x] مرحله ۳: اکسپورت/ایمپورت — بررسی این‌که الان واقعاً کار می‌کنه یا نه + شامل‌کردن تنظیمات پوشه/فیلدهای کارت‌ویزیت من و همکاران و تامین‌کننده‌ها ✅ (Wall 🟣) — **متادیتا کامل رفت‌وبرگشت داره، ولی یه محدودیت واقعی و مهم پیدا شد:**
  - «تنظیمات پوشه» دیگه اصلاً چیزی برای اکسپورت نداره چون طبق تصمیم قبلی خودت اسم پوشه دیگه قابل‌تغییر نیست (همیشه ثابت `Documents/refarsh/`) — این بخش از درخواست خودکار منتفی شد.
  - فیلدهای کارت‌ویزیت (کارت خودم + همکاران/تامین‌کننده‌ها — این دوتا یه ساختار داده‌ی مشترکن، جدا نیستن): هم توی اکسل (`wsBcRows`/شیت «کارت‌ویزیت»، همه‌ی فیلدها + ستون Raw JSON) هم توی JSON کامل (`handleExportJson`/`handleImportJson`، `myBusinessCard`+`businessCards`) کامل و درست export/import و merge-by-id می‌شن — چک شد، باگی نیست.
  - 🔴 **محدودیت واقعی (نه باگ، یه سوال معماریه):** خودِ فایل عکس QR/کارت (`card.qrCode`، فقط یه *اسم فایل*) روی دیسک گوشی (`Documents/refarsh/qr|cards`، یا IndexedDB روی وب) ذخیره می‌شه — هیچ‌کدوم از دو فرمت اکسپورت (اکسل، JSON) خودِ بایت‌های عکس رو نمی‌برن، فقط اسم فایل رو. یعنی اگه بکاپ رو روی یه گوشی دیگه یا بعد از پاک‌کردن دیتای اپ ایمپورت کنی، متن/شماره‌ها همه برمی‌گردن ولی عکس QR/کارت باید دوباره دستی آپلود بشه (تصویر شکسته می‌مونه). برای فیکس واقعی باید یا عکس‌ها base64 بشن و توی JSON جا بگیرن (حجم فایل بکاپ خیلی بزرگ‌تر می‌شه)، یا فرمت اکسپورت از یه فایل تکی به یه ZIP (JSON + پوشه‌ی عکس‌ها) تغییر کنه (تغییر بزرگ‌تر). عمداً این تصمیم رو خودم نگرفتم چون trade-off واقعی داره.
  - ✅ **تصمیم مالک:** همینجوری خوبه، دست نزنم — چون QR ممکنه مدام عوض بشه یا یهو حذفش کنه، پس عمداً این رفتار (عکس‌ها فقط لوکال، توی بکاپ نمیان) دست‌نخورده می‌مونه.
- [x] مرحله ۴: علامت (!) زرد کنار عکس اگه فایل توی پوشه نبود، یا کپی خودکار — ✅ (Logy) `ProductImage` از قبل داشت (طبق «فیکس‌های Ref»)، ولی `CardImage` (کارت‌ویزیت/QR توی `BusinessCardModal.jsx`) هنوز فرق loading/missing رو نمی‌ذاشت و همیشه فقط یه باکس خالی خاکستری نشون می‌داد. الان دقیقاً همون منطق `ProductImage` (تشخیص `resolvedSrc===undefined` به‌عنوان loading در برابر falsy بعد از resolve به‌عنوان missing + دایره‌ی زرد `!`) روی `CardImage` هم پیاده شد. عمداً روی `InvoiceTemplate.jsx` (QR روی فاکتور چاپی) دست نزدم چون اونجا مقصد یه سند چاپی/PDF برای مشتریه، نه UI مدیریتی — نشون‌دادن نشان زرد اونجا بی‌معنی/بدنماست
- [x] مرحله ۵: بک‌آپ/بازیابی کل پوشه موقع سینک گوگل — هنوز باقی مونده، نیاز به تصمیم جدا (چون Firestore برای فایل باینری مناسب نیست) ✅ (به دستور مالک — بدون تغییر کد) — خودِ مالک گفته فعلاً «انجام‌شده» در نظر گرفته بشه تا خودش بعداً بررسی کنه؛ **هیچ کدی توی این آیتم تغییر نکرد**، تصمیم معماری (فایل‌های بک‌آپ کجا ذخیره بشن، خودکار یا دستی) هنوز گرفته نشده. اگه بعداً خواستی واقعاً پیاده بشه، این تیک باید برگرده به `[ ]`.

### 🆕🆕 گزارش جامع کاربر — برش ۱D (عرض/گپ/کرف/سایز-متن) + رفتار انتخاب فیلدهای عددی + باگ ذخیره‌ی عکس ۱D 🟠 (Logy) — **✅ هر ۶ مورد انجام شد**
کاربر یه گزارش خیلی مفصل داد. تشخیص ریشه‌ای: `drawWoodGrain` (تابعی که پایین‌تر توی همین فایل تعریف شده بود) هیچ‌جا صدا زده نمی‌شد — یعنی یه نسخه‌ی SVG قدیمی‌تر از این کامپوننت وجود داشته که با یه نسخه‌ی CSS `clip-path`+flexbox جایگزین شده بوده؛ همون جایگزینی احتمالاً همون «آپدیتی» بوده که کاربر گفت گپ‌ها رو خراب کرده. **راه‌حل: کل `PlankRow`/`computePlankRowLayout` به SVG واقعی (مثل ۲D) بازنویسی شد** — این یه‌جا هم مشکل تناسب/گپ رو حل کرد، هم مشکل عکس رو (چون دیگه نیازی به هیچ hackی نیست).
1. [x] عرض چوب توی برش ۱D باید ۱۰۰٪ عرض صفحه باشه (بدون اسکرول افقی) — **فیکس شد:** قبلاً یه مقیاس *مشترک* بر مبنای پرکارترین چوب استفاده می‌شد که چوب‌های کوتاه‌تر رو باریک‌تر از ۱۰۰٪ نشون می‌داد. الان هر ردیف مستقل، `pxPerCm = عرض‌کادر ÷ طول‌واقعیِ‌همون‌چوب` — یعنی هر چوب (کوتاه یا بلند) دقیقاً ۱۰۰٪ عرض کادر رو پر می‌کنه
2. [x] اگه لیبل قطعه‌ای جا نشه، همه‌ی چوب‌ها با هم کمی بزرگ‌تر بشن — **فیکس شد:** یه `plankGlobalScale` جدید (دوپاسه) قبل از رندر همه‌ی چوب‌ها رو با مقیاس ۱ چک می‌کنه، کوچیک‌ترین نسبتِ لازم برای جا شدن بدترین لیبل رو پیدا می‌کنه، و همون یه مقیاس (سقف ×۴ برای جلوگیری از کشیدگی مسخره) رو یکسان روی *همه‌ی* ردیف‌ها اعمال می‌کنه — فقط همون‌جا که مقیاس >۱ بشه از ۱۰۰٪ بیرون می‌زنه و اسکرول افقی طبیعی وارد عمل می‌شه
3. [x] گپ بین انواع برش (کج-کج/کج-صاف/صاف-صاف) یکسان — **فیکس شد:** حذف کامل شاخه‌بندی قبلی (که flat-flat=۲px ثابت، مایتردار=۲+ضخامت بود، هیچ‌کدوم به kerf واقعی کاری نداشت)؛ الان یه فرمول واحد برای همه، فقط بر مبنای kerf واقعی (فقط اتصال‌های overlap>0 که واقعاً توی هم قفل می‌شن یه استثنای کوچیک دارن، چون اونا واقعاً یه واقعیت فیزیکی متفاوتن نه یه گپ)
4. [x] گپ دینامیک به کرف مثل ۲D — **فیکس شد:** `kerf <= 0.3` → گپ پیکسلی ثابت (۳px، فقط بصری)؛ `kerf > 0.3` → گپ = `kerf × pxPerCm` (دقیقاً متناسب با طول واقعی چوب، عین منطق ۲D)
5. [x] نسبت واقعی هر قطعه از کل طول چوب — **فیکس شد و تست شد:** با یه اسکریپت شبیه‌سازی جدا تأیید شد که مثال دقیق خودِ کاربر (چوب ۱۰۰سانتی، قطعه‌ی ۴۰+۵۲، کرف ۰.۳) دقیقاً ۴۰٪/۵۲٪ عرض واقعی رو می‌گیره
6. [x] باگ لیبل توی عکس ذخیره‌شده — **فیکس شد:** کل هکِ قبلی (که موقع export، تکه‌های مایتردار رو موقتاً از clip-path به SVG سواپ می‌کرد و لیبل رو positon:relative می‌کرد — دقیقاً منشأ جابه‌جایی) کاملاً حذف شد؛ چون رندر همیشگی حالا SVG خالصه، `html2canvas` بدون هیچ hackی درست می‌گیرتش
- 🔴 **کاملاً نیاز به تست چشمی واقعی داره** (من امکان رندر واقعی مرورگر ندارم) — لطفاً چند حالت مختلف رو توی برش ۱D امتحان کن: چوب‌های با طول خیلی متفاوت (کوتاه/بلند)، قطعه‌ی خیلی کوچیک وسط قطعات بزرگ، کرف‌های مختلف (۰.۲ و ۱.۵ مثلاً)، و ذخیره‌ی عکس نهایی

### 🆕🆕 فیچر جدید: رفتار «انتخاب کامل با فوکوس» برای همه‌ی فیلدهای عددی — سراسر اپ 🟠 (Logy) — **✅ انجام شد**
- [x] کاربر خواست: کلیک روی هر فیلد عددی (درصد فرش، ابعاد، قیمت، تعداد، ...) کل مقدار رو انتخاب کنه (نه این‌که مجبور بشه تک‌تک کاراکتر پاک کنه)؛ اگه دوباره روی فیلدِ از قبل فوکوس‌شده کلیک بشه، فقط cursor جابه‌جا بشه (انتخاب برداشته بشه). باید روی همه‌ی اینپوت‌های عددی/جستجوی هر ۴ تب (محصولات، متریال، برش، گالری) + سبد خرید اعمال بشه — **به‌جز کد محصول** — **✅ انجام شد:** `onFocus={(e) => e.target.select()}` روی ~۸۰ اینپوت توی ۶ فایل (`ProductTab.jsx`, `MaterialTab.jsx`, `WoodCuttingTab.jsx`, `GalleryTab.jsx`, `AccountingTab.jsx`, `InvoicesTab.jsx`) اضافه شد — چک‌باکس/رادیو/فایل/رنگ/رنج (اسلایدر) و فیلد کد محصول عمداً مستثنا شدن (چون `.select()` روی این نوع اینپوت‌ها بی‌معنیه). این الگو (`onFocus` که فوکوس بشه select همه، دوباره کلیک تغییری نمی‌ده چون رویداد onFocus فقط یک‌بار در ورود فوکوس فایر می‌شه) همون رفتاری‌یه که کاربر دقیقاً خواسته بود؛ چند مورد که از قبل یه `onFocus` دیگه (برای باز کردن دراپ‌داون autocomplete) داشتن، دستی merge شدن تا هم select هم اون رفتار قبلی حفظ بشه
- 🔴 لطفاً چندتا فیلد مختلف (درصد، ابعاد، قیمت، جستجو) رو تست کن — روی هرکدوم اولین کلیک باید کل مقدار رو آبی/انتخاب‌شده نشون بده




### 🆕 اسپک کامل بازطراحی «نمای پیشرفته‌ی فاکتور» — ✅ هر ۵ زیرمورد بسته شد (Wall بررسی کرد، Ref بقیه رو کامل کرد)
کاربر مشخصات کامل این بازطراحی رو داد؛ خیلی از زیرموارد از قبل (توسط نشست‌های دیگه، بدون این‌که این چک‌لیست خاص آپدیت بشه) پیاده شده بودن:
1. [x] چیدمان کلی — ✅ (Wall) چیدمان از گرید ثابت به یه ستون ساده تبدیل شده، اسکرول عرضی نداره
2. [x] بخش «پیش‌فاکتور» — ✅ کامل شد (Ref، + یه تکمیل کوچیک Wall): علاوه بر جابه‌جایی به پایین صفحه (Wall) و انتخاب محصول به‌صورت لیست باز/بسته‌شونده (که معلوم شد از قبل پیاده بود)، این‌ها هم اضافه شد:
   - تاگل «تسویه‌شده / بدهکار» روی خودِ فرم پیش‌فاکتور
   - وقتی بدهکاره: اینپوت «مبلغ ودیعه» + نمایش زنده‌ی «مبلغ قابل پرداخت» (= جمع اقلام − ودیعه)
   - **تکمیل Wall:** همین مبلغ ودیعه/تسویه‌نشده به خودِ چاپ/پیش‌نمایش A4 هم وصل شد — `InvoiceTemplate.jsx` یه prop اختیاری جدید (`depositAmount`) گرفت که هم از مبلغ نهایی کم می‌شه هم یه ردیف «مبلغ ودیعه (پیش‌پرداخت)» نشون می‌ده؛ قبلش `isSettled` هر قلم پیش‌فاکتور توی خروجی چاپ همیشه هاردکد/نامشخص بود، الان از وضعیت واقعی تاگل فرم میاد
3. [x] «نهایی‌کردن» پیش‌فاکتور — ✅ (Ref) `handleFinalizeDraft` در `InvoicesTab.jsx`: دکمه‌ی سبز «نهایی‌کردن» کنار هر پیش‌فاکتور ذخیره‌شده — دقیقاً همون قرارداد `confirmPurchase` (چک‌اوت سبد خرید در `ProductTab.jsx`) رو برای مشتری تکرار می‌کنه: اگه اسم خریدار با یه مشتری موجود مطابقت نداشت یه رکورد مشتری جدید ساخته می‌شه؛ محصولات لینک‌شده (`productId`) به `status:"sold"` با تاریخ/تسویه/ودیعه‌ی پیش‌فاکتور تبدیل می‌شن، قیمت ویرایش‌شده‌ی هر قلم به‌عنوان `discountedPrice` واقعیِ همون محصول می‌شینه (نه فقط لوکال)، و خودِ پیش‌فاکتور از لیست حذف می‌شه — از اون لحظه یه فاکتور رسمی عادیه که همه‌جا (InvoicesTab/حسابداری/گالری) می‌بینن
4. [x] لیست فاکتورهای رسمی/ثبت‌شده مثل بخش اکسپندشونده‌ی فاکتورهای تب حسابداری، با علامت حذف کنار هر قلم — ✅ (Ref) کامل پیاده شده — همون کاری که در آیتم «نمای پیشرفته فاکتور» جدا انجام شد (حذف تک‌قلم + افزودن محصول + تخفیف تک‌تک، `InvoicesTab.jsx`)، Wall هم تأیید کرد
5. [x] ⚠️ یکپارچگی بین ۳ محل — ✅ **روشن شد و تصمیم گرفته شد (مالک):** دکمه‌ی «چاپ فاکتور» تب گالری (که بر مبنای فیلترهای available/unsettled/settled خلاصه‌ی چندتاریخه چاپ می‌کنه، بدون شماره فاکتور واقعی) **عمداً همین‌طور می‌مونه — این رفتارش درسته و مشکلی نیست.** مشکل واقعی جای دیگه‌ای بود: دکمه‌ی جدای «فاکتورها» توی همون تب گالری (`InvoiceListModal` در `GalleryTab.jsx`) که لیست فاکتورهای واقعیِ تاریخ‌بندی‌شده‌ی همون گالری رو نشون می‌ده، سطح ویرایشش با `InvoicesTab.jsx` یکی نبود (نه تخفیف تک‌تک داشت، نه افزودن محصول). **رفع شد (Ref):** `handleItemDiscountChange` (تخفیف مستقیم روی رکورد محصول) + `handleAddItemToInvoice` (افزودن محصول موجود به فاکتور با `location: stat.id`) به `InvoiceListModal` اضافه شد — الان هر ۳ محل (InvoicesTab، تب حسابداری، فاکتورهای تب گالری) از یه سطح قابلیت ویرایش یکسان برخوردارن، همه هم روی همون منبع دادهٔ زنده (`d.products`) می‌نویسن.
- 🔴 نیاز به تست واقعی: پیش‌فاکتور رو با ودیعه بساز، پیش‌نمایش/چاپ بگیر (مبلغ قابل‌پرداخت باید توی چاپ هم درست کم شده باشه) و نهایی‌ش کن (باید مشتری جدید ساخته بشه اگه نبود، و محصولات sold بشن)؛ توی گالری از دکمه‌ی «فاکتورها» یه محصول به یه فاکتور موجود اضافه کن و تخفیف بذار، بعد برو تب محصولات ببین همون‌جا هم اومده

### 🆕 دکمه‌ی Sort در ۴ تب (کاتالوگ/محصولات/متریال/گالری) — ✅ بازطراحی شد ⚫ (Ref)
- [x] حذف علامت عمومی ⇅ + برچسب + فلش جدا (۳ نماد روی هم)؛ الان خودِ متن/آیکون جهت رو نشون می‌ده: حالت‌های متنی با چرخوندن ترتیب («Az⇅»/«Za⇅»، «۱۲۳⇅»/«۳۲۱⇅»)، حالت‌های آیکونی با پیکان جهت («🕒↑»/«🕒↓»، «🛒↑»/«🛒↓»)
- [x] ایموجی‌های 🕒 و 🛒 با آیکون مینیمال lucide-react (`Clock`, `ShoppingBag` — همون آیکونی که سبد خرید جای دیگه‌ی اپ هم استفاده می‌شه) جایگزین شدن، هم‌شکل بقیه‌ی آیکون‌ها



### 🆕 گزارش کاربر: دابل‌کلیک رفرش + قیمت‌های دستی بعد از Undo قاطی می‌شن — این نشست 🟡 Ash
1. [x] **دابل‌کلیک روی رفرش نباید کاری بکنه (الان Undo می‌کنه، باید فقط از پاپ‌آپ باشه)** — **✅ فیکس شد.** یه چیز مهم‌تر هم توی همین بررسی پیدا شد: کد قبلی، هر ضربه‌ی ساده (چه اول چه دوم) رو بی‌قیدوشرط `onQuickRefresh` صدا می‌زد؛ ضربه‌ی دوم علاوه‌براون `onUndoRefresh` رو هم روش استک می‌کرد. یعنی «دابل‌کلیک» واقعاً یه Refresh کامل + یه Undo پشت‌سرهم بود، نه فقط Undo. منطق دابل‌کلیک کاملاً حذف شد؛ الان هر ضربه‌ی ساده فقط `onQuickRefresh`ه، Undo فقط از پاپ‌آپ نگه‌داشتن در دسترسه.
2. [x] **علت واقعی «قیمت دستی/درصدی همه رو می‌کنه یه‌جور» پیدا شد — این ریشه‌ای‌تر از خودِ Undo بود:** وقتی کاربر قیمت رو مستقیم تایپ می‌کرد (`handleSalePriceChange` توی `ProductTab.jsx`)، کد به‌جای این‌که این محصول رو «کاملاً دستی» علامت بزنه (`profitPct: null`)، یه درصد سود معادل رو *محاسبه و ذخیره* می‌کرد. یعنی از دید سیستم، تقریباً هیچ محصولی واقعاً «دستی» نبود — همه یه `profitPct` ذخیره‌شده داشتن، حتی اگه کاربر فکر می‌کرد قیمتش رو مستقیم و ثابت تعیین کرده. تابع `syncPercentPricedProducts` (که هر Refresh، چه ساده چه به‌خاطر همون باگ دابل‌کلیک بالا، اجرا می‌شه) قیمت *هر* محصولی که `profitPct` غیر-null داشته باشه رو با هزینه‌ی روزِ متریال دوباره بازمحاسبه می‌کنه — یعنی قیمت «دستی» کاربر هیچ‌وقت واقعاً ثابت نمی‌موند، با هر Refresh (و مخصوصاً با تغییر هزینه‌ی متریال بعد از Undo/قفل/آزادسازی) دوباره از صفر حساب می‌شد.
   - **✅ فیکس شد:** حالا تایپ مستقیم قیمت، `profitPct: null` ذخیره می‌کنه (نه یه عدد مشتق‌شده) — یعنی این محصول واقعاً و برای همیشه دستی می‌مونه و `syncPercentPricedProducts` دیگه هیچ‌وقت دست بهش نمی‌زنه. کلیک روی پیش‌فرض‌های درصد (۷۰٪/۱۰۰٪/...) یا فیلد «درصد سود» دست‌نخورده موند (اون‌ها *باید* درصدی بمونن، این عمدیه).
   - ⚠️ **محدودیت مهم:** این فیکس فقط رو ویرایش‌های *بعد از این* اثر می‌ذاره. محصولاتی که از قبل (با کد قدیمی) قیمت‌دستی‌شون ذخیره شده، همچنان یه `profitPct` مشتق‌شده‌ی قدیمی روی داده‌شون دارن و تا وقتی یه‌بار قیمتشون رو دوباره لمس/ذخیره نکنین، هنوز مستعد همون رفتارن. عمداً هیچ migration خودکاری برای پاک‌کردن `profitPct` محصولات قدیمی نزدم — چون سیستم فعلی هیچ فلگ صریحی نداره که بگه کدوم محصول *واقعاً* قرار بوده دستی باشه و کدوم درصدی، و حدس‌زدن اشتباه می‌تونه محصولاتی که عمداً درصدی‌ان رو خراب کنه. اگه یه فلگ صریح «حالت قیمت‌گذاری: دستی/درصدی» (به‌جای استنتاج از `profitPct == null`) بخواید، اون یه کار جدا و دقیق‌تره.
- 🔴 لطفاً تست کن: یه محصول جدید بساز، قیمتش رو دستی تایپ کن، بعد یه قفل/آزادسازی/رفرش بزن، ببین قیمتش دست‌نخورده می‌مونه یا نه

### 🆕 ریزفیکس‌های ظاهری فیلترها — ✅ انجام شد ⚫ (Ref)
- [x] تب محصولات/کاتالوگ: دکمه‌ی «موقعیت محصول» وقتی روی «همه» بود، متن اضافه نشون می‌داد (باید فقط آیکون باشه) — الان مثل دکمه‌ی وضعیت/نوع، فقط وقتی موقعیتی انتخاب شده متن نشون می‌ده
- [x] تب متریال: دکمه‌ی «وضعیت موجودی» و دکمه‌ی «نوع متریال» جاشون عوض شده بود — وضعیت‌موجودی همیشه متن «همه» نشون می‌داد (باید فقط پیش‌فرض آیکون باشه، الان فقط وقتی غیرپیش‌فرضه متن نشون می‌ده)، نوع‌متریال هیچ‌وقت متن نداشت (باید همیشه «همه»/«فرش»/... نشون بده، الان داره) — همچنین آیکونشون یکی بود (`Package`)، آیکون نوع‌متریال به `Tag` تغییر کرد که دیگه تکراری نباشه
- [x] تب متریال: ردیف هدر از قاب صفحه بیرون می‌زد — باگ کلاسیک فلکس‌باکس بود (فیلد جستجو `minWidth:0` نداشت)، مثل فیکس مشابه قبلی توی بقیه‌ی تب‌ها حل شد
- [x] تب گالری: هدر موقع اسکرول جابجا می‌شد (استیکی نبود مثل تب محصولات) — ریشه‌ش پیدا شد: کانتینر بیرونی `padding-top` اضافه داشت که با هدر استیکیِ تب محصولات (که یه `margin-top` منفی برای جبران داره) هماهنگ نبود؛ الان دقیقاً هم‌الگوی تب محصولات شد

### 🆕 هماهنگ‌سازی با فیکس Ash (حذف دابل‌کلیک=Undo)
- [x] Ash دابل‌کلیک=Undo رو از `GlobalHeader.jsx` حذف کرد (طبق درخواست کاربر که Undo فقط از پاپ‌آپ ۴دکمه‌ای در دسترس باشه). متوجه شدم `RefreshLockButton` توی `App.jsx` (دکمه‌ی رفرش داخل خودِ پنل مدیریت — نقطه‌ی ورودی دومی که به همون توابع `handleQuickRefresh`/`handleHoldRefresh`/`handleUndoRefresh` وصله) هنوز همون میان‌بر قدیمی رو داشت — ناهماهنگ با تصمیم تازه‌ی کاربر. حذف شد؛ الان هر ۲ نقطه‌ی ورودی (GlobalHeader و پنل مدیریت) یکسان رفتار می‌کنن ⚫ Ref

### 🆕 گزارش کاربر: Refresh هم قیمت‌ها رو به‌هم می‌ریزه + گپ هدر (margin -16px) — این نشست 🟡 Ash
1. [x] **باگ مالی حیاتی و ریشه‌ای‌تر از چیزی که فکر می‌کردم — سمت سرور بود:** فیکس قبلی من (`profitPct: null` برای قیمت دستی) کافی نبود چون هر بار سینک ابری (که خودِ Refresh هم صداش می‌زنه، از طریق `performSynchronization`) اجرا می‌شد، `server.ts` موقع خوندن دیتا از دیتابیس و برگردوندنش به کلاینت، از یه شرط truthy (`p.profitPct ? Number(p.profitPct) : 0`) استفاده می‌کرد — که نمی‌تونه بین «واقعاً null» (یعنی «دستی، دست نزن») و «واقعاً صفر» فرق بذاره؛ هر دو حالت به ۰ تبدیل می‌شدن. یعنی علامت «دستی‌بودن» که سمت کلاینت گذاشته بودم، توی هر سینک ابری گم می‌شد و دوباره `syncPercentPricedProducts` قیمتش رو بازمحاسبه می‌کرد.
   - **✅ فیکس شد:** یه تابع `numOrNull` اضافه شد که فقط null/undefined/رشته‌ی خالی رو null نگه می‌داره، نه صفر واقعی رو. این الگو برای `profitPct` محصولات، و همین‌طور فیلدهای مشابه‌ی متریال (`ratioValue`, `fixedQty`, `defaultPct`, `totalCost`, `remainingCost`) و تجهیزات (`purchasePrice`, `depreciationYears`, ...) هم اعمال شد — چون همه‌شون دقیقاً همین باگ رو داشتن (این می‌تونه توضیح‌دهنده‌ی باگ‌های قبلی «تعداد بچ متریال هاردکد ۱» هم باشه که یه همکار جدا فیکس کرده بود؛ شاید ریشه‌ش همینجا بوده).
   - جهت ذخیره‌سازی (سمت کلاینت → سرور) از قبل درست بود (`!== undefined && !== null` چک می‌کرد)؛ فقط جهت خوندن (سرور → کلاینت) باگ داشت.
   - ⚠️ همچنان محدودیت قبلی پابرجاست: این فیکس رو دیتای *جدید* (بعد از این پوش) اثر داره. اگه محصولی همین چند روز اخیر (قبل این فیکس سروری) یه‌بار سینک شده باشه، ممکنه دیتای سمت سرورش همچنان `profitPct: 0` باشه (نه null) و بعد از این هم دستکاری بشه — تا وقتی قیمتش رو یه‌بار دیگه دستی لمس/ذخیره نکنین. لطفاً یه محصول *کاملاً تازه* بسازی و تست کنی.
2. [x] **گپ هدر داخلی/اصلی، این‌بار با راه‌حل مستقیم کاربر** — همون `marginTop: "-16px"` که قبلاً پیدا کرده بودم ولی به‌جای حذفش رفته بودم سراغ فیکس تایمینگ (`useLayoutEffect`)، این‌بار مستقیم حذف شد — از هر ۵ جایی که بود (`GalleryTab.jsx`, `MaterialTab.jsx`, `ProductTab.jsx` ×۲, `WoodCuttingTab.jsx`). فیکس تایمینگ قبلی هم سرجاشه (هردو با هم مکمل هم بودن، نه جایگزین).
- 🔴 لطفاً دوباره تست کن: هم گپ هدر (اسکرول سریع توی هر تب)، هم قیمت محصول تازه بعد از رفرش/قفل/آزادسازی

### 🆕 گزارش کاربر با عکس — فیلد description + بگ‌های لایت‌باکس/سینک — این نشست 🟠 (Logy)
1. [x] فیلد «توضیحات دلخواه» چندخطی زیر ابعاد، توی فرم افزودن/ویرایش محصول — **✅ اضافه شد:** یه `<textarea>` (Enter برای خط جدید طبیعتاً کار می‌کنه) به فرم اضافه شد، مقدارش روی `product.description` ذخیره می‌شه (به `emptyProduct()` هم اضافه شد)
2. [x] نمایش description توی لایت‌باکس (بزرگ‌نمایی محصول در تب محصولات/کاتالوگ)، بالای خط ابعاد، بدون نوشتن کلمه‌ی description — **✅ اضافه شد:** با `white-space:pre-wrap` که چندخطی بودن حفظ بشه؛ فقط وقتی description داشته باشه نمایش داده می‌شه
3. [x] سوایپ کناری برای زوم عکس، خودِ تب رو اسکرول می‌کنه — **✅ باگ واقعی پیدا و فیکس شد:** لایت‌باکس (`ImageLightbox` توی `ProductTab.jsx`، مشترک بین کاتالوگ و تب محصولات) اصلاً `data-no-swipe="true"` نداشت — یعنی همون لحظه که کاربر داخل لایت‌باکس داشت با انگشتش برای پن/زوم/تعویض عکس می‌کشید، مکانیزم سراسری سوایپ-بین-تب‌ها (که جدا و هم‌زمان روی همون لمس گوش می‌ده) هم داشت به‌اشتباه به‌عنوان سوایپ-تعویض-تب حساب می‌کرد. اضافه شدن `data-no-swipe="true"` این تداخل رو قطع می‌کنه
4. [x] موقع باز شدن لایت‌باکس، دکمه‌ی scroll-to-up باید هاید بشه — **✅ فیکس شد:** لایت‌باکس هیچ‌وقت `useRegisterOpenModal(true)` صدا نمی‌زد (که مکانیزم هاید-کردن دکمه‌ی اسکرول روش سوار بود) — الان صدا زده می‌شه
5. [x] تب گالری، قسمت بالای صفحه می‌ره زیر هدر و overlap می‌شه ✅ **تأیید شد توسط کاربر: درست شده** (احتمالاً همون فیکس گپ هدر گالری همین مشکل رو هم حل کرده بود)
6. [ ] خطای سینک با گوگل (عکس فرستاده شده) — **بررسی شد، احتمالاً مشکل زیرساخت/دیپلویه، نه کد:** همه‌ی فراخوانی‌های API (`fetch("/api/sync/status")` و مشابه توی `SyncTab.jsx`/`syncManager.js`) از آدرس *نسبی* استفاده می‌کنن. این توی مرورگر/AI Studio خوبه (چون فرانت‌اند و سرور از یه دامنه سرو می‌شن)، ولی توی اپ موبایل (Capacitor) اگه WebView فایل‌های استاتیک رو محلی لود کنه (نه از دامنه‌ی واقعی)، این آدرس‌های نسبی هیچ‌وقت به سرور واقعی نمی‌رسن — دقیقاً منطبق با «پاسخ نامعتبر از سرور» + «خطای ارتباط» هم‌زمان. برای فیکس درست (تبدیل به آدرس مطلق وقتی توی اپ نیتیو اجرا می‌شه) نیاز به دونستن دامنه‌ی واقعی دیپلوی‌شده دارم — 🔴 از کاربر پرسیده شد


---

## ✅ فیکس‌های این نشست‌ها (Ref — بدون تغییر چیدمان UI کاربر)

### دور ۱ (منطق حیاتی)
- [x] **رفرش دیگر قیمت فروش را خراب نمی‌کند** — `handleQuickRefresh` دیگر `syncPercentPricedProducts` را صدا نمی‌زند؛ خود تابع `salePriceManual === true` و `profitPct == null` را رد می‌کند (`App.jsx`)
- [x] **لاگین گوگل پایدار** — وب: `signInWithPopup` + `browserLocalPersistence`؛ نیتیو: `signInWithCredential` روی JS SDK + ست فوری user/token (`AuthContext.jsx`)
- [x] **عکس گم‌شده** — `ProductImage` علامت زرد `!` وقتی فایل در پوشه/IDB نیست؛ هوک `useResolvedImageSrc` بین loading (`undefined`) و missing (`null`) فرق می‌گذارد
- [x] **متریال قدیمی بدون بچ** — `ensureInitialStock` در `migrateData` برای fabric/area/linear/ratio/fixed (`dataModels.js`)
- [x] **فیلتر «همه» گالری** — `statusFilter` همیشه آرایه امن؛ بدون کرش (`GalleryTab.jsx`)

### دور ۲ (درخواست کاربر + Book1)
- [x] **علامت زرد فیلد اجباری روی ردیف** — محصول (کد یا نام خالی) و متریال (نام یا نوع خالی)؛ همیشه روی لیست، نه فقط توست رفرش (`ProductTab.jsx`, `MaterialTab.jsx`)
- [x] **همهٔ رکوردهای دیتابیس id دارند** — در `migrateData`: product, lineItem, material, batch, stick, procurement, customer, businessCard, woodCuttingSession, invoiceDraft, productType, equipment
- [x] **مسیر ذخیره خروجی‌ها** — `Documents/refarsh/bills` (فاکتور PDF/عکس)، `Documents/refarsh/1dnesting`، `Documents/refarsh/2dnesting` از طریق `saveFile(..., { subdir })` (`nativeSave.js`, `InvoicePrint.jsx`, `WoodCuttingTab.jsx`)
- [x] **راهنمای تب همگام‌سازی** — مسیرهای bills / 1dnesting / 2dnesting به متن توضیح اضافه شد (`SyncTab.jsx`)
- [x] **اکسل پیش‌نمایش رنگی ≈ Book1** — شیت «خروجی» (شرح، ابعاد، ستون‌های هزینه دسته‌بندی‌شده، جمع، فروش، حسن زیبایی، فروخته‌شده + گروه بر اساس نام فرش زنده) و شیت «متریال» (ساخته/خرید/ابزار)؛ فقط نمایش، بدون ایمپورت (`handleExportPreviewExcel` در `App.jsx`)
- [x] **`resolveProductGroupName` / `resolveProductFabricId`** — اضافه به `mathCore.js` برای نام دسته از id فرش زنده
- [x] **عرض نمایش ۱D/۲D** — کانتینر نتایج `width: 100%` (`WoodCuttingTab.jsx`)
- [ ] **جهت مایتر یک‌طرفه ۱D** — عمداً بدون عکس دقیق کاربر دست نخورده؛ نیاز اسکرین پیش‌نمایش + خروجی Save

**عمداً دست نخورده در این دورها:** چیدمان کلی تب‌ها، دکمه‌های موجود کاربر، انیمیشن رفرش روی گوشی واقعی، بهینه‌سازی MDF بدون سناریوی مشخص.

### 🆕 بکاپ profitPct=30 + بولک بچ (این نشست Ref)
- [x] آنالیز بکاپ: از ۳۸ محصول با درصد سود ۳۰، حدود ۳۶تاش قیمت بازار دستی بودن → در `migrateData` این‌ها `salePriceManual: true` و `profitPct: null`
- [x] بولک: نوار درصد/سه‌گانه همیشه با انتخاب محصول — نه فقط «بدون بچ»
- [x] بولک: حذف «بدون بچ»؛ چندانتخابی بچ با برچسب تعداد/قیمت
- [x] خطی: توزیع بر اساس محیط قاب مستقل از حالت بدون‌بچ
- [x] apply چندبچ همزمان هنوز از اولین بچ انتخاب‌شده استفاده می‌کند ✅ (Ref ⚫) — طبق تصمیم صریح کاربر (چت): بچ‌ها فقط برای تاریخچه‌ی خریدن؛ موقع بولک، بچ‌های تیک‌خورده یه **استخر ترکیبی** حساب می‌شن (جمع باقیمانده‌شون = مخرج درصدها)، و کسر واقعی **پشت‌سرهم** انجام می‌شه: اول بچ اول پر می‌شه، بعد بچ دوم، و... . پیاده‌سازی: `App.jsx` → `bulkApplyMaterial` حالا پارامتر `batchIds` (آرایه، به ترتیب انتخاب کاربر) می‌گیره (کنار `batchId` تکی قدیمی که برای سازگاری نگه داشته شده)، یه `batchFillList` مرتب می‌سازه (باقیمانده‌ی واقعی هر بچ، مستقل)، و هزینه‌ی هر محصول (که قبلاً روی یه بچ تکی حساب می‌شد) الان از استخر ترکیبی (`combinedRemainingForPreview`/`combinedTotalCost`/`combinedPhysArea`) محاسبه و با `splitCostAcrossBatches()` بین بچ‌ها به ترتیب پر می‌شه — اگه هزینه‌ی یه محصول از باقیمانده‌ی یه بچ رد بشه، خودکار چند `lineItem` (یکی برای هر بچ) براش ساخته می‌شه، بدون این‌که موتور قفل/آزادسازی/Undo (که بر مبنای هر لاین‌آیتم دقیقاً یه `batchId` کار می‌کنه) دست بخوره. بخش ۴ (`linkedProductIds`) هم برای همه‌ی بچ‌های انتخابی آپدیت می‌شه، نه فقط اولی. `MaterialTab.jsx`: `batchIds: selectedBatchIds` به payload اضافه شد. با یه شبیه‌سازی node (۳ بچ ۱۰۰تایی، ۵ محصول ۵۰تایی = جمعاً ۲۵۰) تست شد: دقیقاً طبق مثال کاربر، دو بچ اول کامل تموم شدن، سومی نصف موند. جانبی: بچ‌های کاملاً مصرف‌شده (`locked`) دیگه توی لیست چندانتخابی بچ نشون داده نمی‌شن (هم چیپ‌های انتخاب، هم پیش‌فرض اولیه) — طبق درخواست کاربر که «دفعه بعد بچ تموم‌شده قابل‌استفاده نشون داده نشه». با `esbuild` روی هر دو فایل چک شد. 🔴 **حتماً رو گوشی واقعی تست کن:** یه متریال مساحتی با ۲-۳ بچ بساز، چندتاشو با هم تیک بزن، به چند محصول بولک بزن که مجموع نیازشون از یه بچ بیشتر بشه، مطمئن شو بعد از رفرش (قفل واقعی) هزینه‌ها و باقیمانده‌ی هر بچ درست حساب شده — این مستقیم روی پول/موجودی اثر می‌ذاره.

### 🆕 دامپ خام گزارش/درخواست کاربر — ثبت‌شده برای triage — این نشست 🟠 (Logy)
> ثبت مستقیم متن کاربر، هنوز claim/شروع نشده مگر جلوش مشخص شده باشد. هرکس زودتر رسید رنگ خودش رو جلوش بذاره.
>
> **لاگ Logy 🟠 (همین نشست):** از این دامپ، ۳ مورد سبک رو claim و کامل کردم (پایین علامت‌خورده): تغییر نام دکمه‌ی «مستقیم»، جابه‌جایی دکمه‌های تاریخ به خط بعد، و رنگ نوار معلق. بقیه‌ی موارد (خصوصاً برش ۱D، پاپ‌آپ‌های موقعیت/هاید، دکمه‌های Share/Save، منطق حسابداری متریال معلق، Undo با id) سنگین‌ترن و دست‌نخورده موندن — برای نشست بعدی (خودم یا هرکس دیگه) باز هستن.

**نمای پیشرفته‌ی فاکتور (تب حسابداری):**
- [x] هر فاکتور لیست‌شده در حالت ویرایش باید بتونه: (۱) هرکدوم از اقلامش حذف بشه، (۲) محصول جدید بهش اضافه بشه، (۳) تخفیف روی هر محصول جداگانه اعمال بشه — این تخفیف باید مستقیماً فیلد/دیتای تخفیف همون محصول رو در همه‌ی تب‌ها آپدیت کنه (نه فقط لوکال روی همون فاکتور) ✅ (Ref ⚫) — `InvoicesTab.jsx`: **باگ واقعی پیدا شد قبل از هرچیزی:** دکمه‌ی «ویرایش فاکتور» (`handleEditClick`) و تابع ذخیره (`handleSaveInvoiceEdit`) و همه‌ی استیت‌های ادیت از قبل توی کد بودن، ولی هیچ فرم/UIای که این استیت‌ها رو نشون بده وجود نداشت — یعنی دکمه‌ی ویرایش عملاً هیچ‌کاری نمی‌کرد (کد مرده). این بخش کامل ساخته شد:
  - بلوک اکسپندشده‌ی فاکتور حالا شرطی شده: اگه `editingId === inv.id` فرم ویرایش (نام/تلفن خریدار، تاریخ با `JalaliDatePicker`، تاگل تسویه) رو نشون می‌ده، وگرنه همون نمای فقط‌خواندنی قبلی
  - **حذف قلم:** `handleRemoveItemFromInvoice` — محصول رو به `status:"available"`, `location:"warehouse"` برمی‌گردونه (دقیقاً همون فیلدهایی که `handleDeleteInvoice` برای کل فاکتور ریست می‌کنه، ولی فقط برای یک محصول)
  - **افزودن قلم:** `handleAddItemToInvoice` + پیکر جستجو (از همون لیست `availableProductsForDraft` که برای پیش‌فاکتور استفاده می‌شه) — محصول انتخابی `status:"sold"` می‌شه و `buyerCustomerId`/`location`/`saleDate`/`settled` دقیقاً با فاکتور فعلی (یا مقادیر ویرایش‌شده‌ی فرم) ست می‌شه
  - **تخفیف تک‌تک:** `handleItemDiscountChange` — مستقیم `discountPercent`/`discountedPrice` رو روی خودِ رکورد محصول توی `d.products` می‌نویسه (همون فیلدهایی که `ProductTab.jsx` می‌خونه/می‌نویسه)، پس همون‌جا همه‌ی تب‌های دیگه (محصولات، حسابداری، پیش‌نمایش فاکتور) هم آپدیت‌شده می‌بینن؛ فرمول دقیقاً کپی از `handleDiscountChange` در `ProductTab.jsx` (`discountedPrice = round(salePrice * (1 - pct/100))`)
  - با `esbuild` روی فایل چک شد، خطای سینتکسی نیست
  - 🔴 **نیاز به تست واقعی داره:** یه فاکتور رو باز کن → ویرایش → یه قلم رو حذف کن (باید برگرده به تب محصولات با وضعیت موجود) → یه محصول موجود دیگه اضافه کن → روی یه قلم تخفیف بذار و برو تب محصولات ببین همون‌جا هم اعمال شده → ذخیره کن و مطمئن شو مجموع فاکتور/وضعیت تسویه درست آپدیت می‌شه
- [x] دکمه‌های «از تاریخ» / «تا تاریخ» (روبروی جستجو) به خط بعد، کنار هم منتقل بشن ✅ (Logy) — `InvoicesTab.jsx`: جستجو حالا تمام‌عرض خط اول، دو دیت‌پیکر با `flex:1` کنار هم خط دوم
- [x] نام دکمه‌ی «مستقیم (پیش خودم)» → فقط «مستقیم» ✅ (Logy) — `InvoicesTab.jsx` خط فیلتر نوع فروش

**دسته‌بندی محصولات بر اساس فرش (تب محصولات):**
- [x] فرمت نمایش: «فرش (نام فرش)، (طرح (طرح فرش)) (قدمت)ساله» — مثال: «فرش یزد، (طرح کاشان) ۵۰ ساله» ✅ **نسخه‌ی نهایی (Ref، بعد از هماهنگی با Logy):** نسخه‌ی اول Logy از روی متن آزاد اسم متریال با regex «طرح ...» و «...ساله» رو پیدا می‌کرد که چون این دو تا از اول فیلد جدا هستن (`mat.pattern`/`mat.ageYears`)، نه بخشی از خودِ اسم، عملاً هیچ‌وقت با دیتای واقعی match نمی‌شد (تست شد با بک‌آپ واقعی کاربر: هیچ اسم متریالی «(طرح ...)» یا «...ساله» توش نداشت). خودِ Logy این‌رو یه نشست بعد اصلاح کرد و `formatFabricGroupLabel(name, pattern, ageYears)` رو مستقیم از روی همون دو فیلدِ جدا ساخت (تست شد با دیتای واقعی، درست کار کرد). **موازی، Ref هم همین قابلیت رو مستقل برای `groupedProducts` (تب محصولات) پیاده کرده بود** — بعد از merge، هر دو نسخه هم‌زمان توی کد بودن (یکی inline توی `App.jsx`، یکی exported توی `mathCore.js`). یکی‌شون شدن: `App.jsx` الان مستقیم همون `formatFabricGroupLabel` مشترک رو صدا می‌زنه، منطق تکراری inline حذف شد.
- [x] چون کاربر دیگه کلمه‌ی «فرش» رو خودش تایپ نمی‌کنه، سیستم خودکار کلمه‌ی «فرش» رو اول اسم دسته‌بندی اضافه کنه ✅ (Logy) — `mathCore.js` تابع `resolveProductGroupName`: اگه اسم متریال فرش از قبل با «فرش» شروع نشده بود، خودکار `فرش ` جلوش اضافه می‌شه (فقط روی نمایش دسته‌بندی، اسم خودِ متریال دست‌نخورده می‌مونه)

**برش ۱D:**
- [x] همه‌ی چوب‌های بخش ۱D همیشه با یک سایز/مقیاس ترسیم بشن (سایز فعلی متناسب با عرض صفحه درسته و باید مبنا بمونه) ✅ (Ash 🟡 — بررسی شد، از قبل درست پیاده‌سازی بود) — `WoodCuttingTab.jsx` (`plankGlobalScale`, خط ۷۳۳): یه مقیاس مشترک روی همه‌ی ردیف‌ها اعمال می‌شه، نه هرکدوم جدا
- [x] فقط وقتی این بخش باید از عرض صفحه بزرگ‌تر بشه (و اسکرول افقی بخوره) که بقیه‌ی قطعات دیگه نتونن عرضشون کم بشه چون به حداقل سایزِ لازم برای خوانا بودن متن رویشون رسیدن (یعنی تنگ‌ترشدن قطعات اولویت داره، اسکرول افقی آخرین راه‌حله) ✅ (Ash 🟡 — بررسی شد، از قبل درست بود) — `plankGlobalScale` دقیقاً حداقل مقیاس لازم برای جا شدن تنگ‌ترین لیبل رو حساب می‌کنه (`Math.min(PLANK_MAX_GLOBAL_SCALE, maxRequired)`)، یعنی فقط وقتی واقعاً لازمه از ۱× بزرگ‌تر می‌شه، وگرنه همون عرض عادی صفحه می‌مونه
- [x] وقتی سایز ترسیمی یه قطعه از سایز لیبل روش کوچیک‌تره (یعنی متن جا نمی‌شه)، کل بخش ۱D (همه‌ی چوب‌ها) به اندازه‌ای عرضش/اسکرولش باز بشه که متن روی همه‌ی قطعات خونا باشه ✅ (Ash 🟡 — بررسی شد، از قبل درست بود) — همون مکانیزم `plankGlobalScale` + `plankContainerRef` با `overflowX:auto`؛ این دقیقاً همون چیزیه که توی «چک هم‌سایزی دکمه‌ها» چند نشست قبل هم به‌عنوان مورد ۲ تأیید شده بود، فقط اینجا (بخش قدیمی‌تر روادمپ) هنوز تیک نخورده بود
- [x] موقع ذخیره‌ی عکس، بخش ۱D باید کامل (همه‌ی چیزی که با اسکرول افقی دیده می‌شه) توی عکس ذخیره بشه، نه فقط چیزی که توی ویوپورت بوده ✅ (بررسی شد، از قبل یه همکار حلش کرده بود) — `handleSaveAsJpg` (`WoodCuttingTab.jsx` خط ۵۴۲): قبل از عکس‌گیری موقتاً `overflowX:"visible"` و `width:"max-content"` می‌ذاره، عرض/ارتفاع واقعیِ کامل (`scrollWidth`/`scrollHeight`) رو به `html2canvas` می‌ده، بعد استایل اصلی رو برمی‌گردونه — یعنی عکس همیشه کل محتوای اسکرول‌شده رو داره، نه فقط ویوپورت
- [x] فاصله‌ی گپ بین برش‌های کج (مایتر) در ترسیم، نصف مقدار فعلی بشه — منطق را از ۲D کپی بگیر: حداقل ۳میل کرف چقدر گپ ترسیم می‌کنه، و هرچی کرف بیشتر بشه گپ بیشتر می‌شه (همون تناسب رو با ضریب نصف در ۱D پیاده کن) ✅ (Ref ⚫) — `WoodCuttingTab.jsx` (`computePlankRowLayout`): گپ پایه (`visibleGap`، همون فرمول متناسب‌با-کرف قبلی، حداقل ۳ حداکثر ۶پیکسل) دست‌نخورده موند، ولی حالا برای هر اتصال چک می‌شه که مایتردار هست یا نه (`c.miterLeft === true`، دقیقاً همون فلگی که رنگ سبز/آبی رو تعیین می‌کنه)؛ اتصال‌های مایتردار `visibleGap/2` می‌گیرن، اتصال‌های صاف (بدون مایتر، قرمز) گپ کامل رو نگه می‌دارن. چون این داخل تابعیه که بعداً همه‌چیز رو با scale متناسب می‌کنه تا دقیقاً عرض کادر پر بشه، نسبت اندازه‌ها حفظ می‌مونه. با `esbuild` چک شد، خطای سینتکسی نیست. 🔴 لطفاً رو گوشی واقعی یه چوب با ترکیب مایتر+صاف تست کن و مطمئن شو گپ اتصال‌های مایتردار محسوس کوچیک‌تره.

**تب حسابداری:**
- [x] دکمه‌ی سورت بخش فاکتورها به‌جای یونیکد ساعت/چرخ‌خرید، از همون آیکون SVG که در دکمه‌ی Sort تب محصولات استفاده شده کپی بشه (یکدست بشه) ✅ (Logy) — `AccountingTab.jsx`: ایموجی/یونیکد 🕒/🛒 حذف شد، آیکون‌های `Clock`/`ShoppingCart` از `lucide-react` (دقیقاً همون سبک دکمه‌ی SortButton تب محصولات) جایگزین شد، هم روی خود دکمه هم توی لیست پاپ‌آپ
- [x] متریال معلق (منتظر قفل/آزادسازی) و متریال قفل‌شده باید **هردو به‌عنوان هزینه‌شده حساب بشن** در حسابداری (زنده/live) — چون ممکنه هفته‌ها معلق بمونه بدون قفل ولی باید همون لحظه اثرش تو حساب باشه. اتصال/جداشدن متریال از محصول (چه معلق چه قفل، و جداشدن فقط وقتی *آزاد* می‌شه نه وقتی منتظر جداییه) **نباید خودش مستقیماً روی حسابداری اثر بذاره** چون حسابداری همیشه زنده محاسبه می‌شه — این وضعیت فقط برای ردیابیِ چقدر از هر متریال قبلاً برای محصولات دیگه خرج شده لازمه
  - ✅ (Ash 🟡 — بررسی شد، از قبل درست بود) — `App.jsx` (`resolveLineCost`، خط ۱۹۲۰): تنها شرط اول تابع `if (li.deductedAt) return toNum(li.deductedCost)` هست؛ چون `pendingUnlock` (حالت معلقِ در حال آزادسازی) `deductedAt` رو دست‌نخورده نگه می‌داره (`ProductTab.jsx` تابع `removeLine`، فقط `pendingUnlock:true` اضافه می‌کنه، `deductedAt` پاک نمی‌شه) — یعنی متریال قفل‌شده و متریالِ «در حال آزادسازی» هردو از همون مسیر یکسان هزینه‌ی فریزشده‌شون رو برمی‌گردونن. 🔴 لطفاً یه سناریوی واقعی تست کن: یه محصول با متریال معلق (نه قفل‌شده) بساز، مطمئن شو توی حسابداری همون لحظه به‌عنوان هزینه حساب می‌شه؛ بعد آزادش کن و ببین برمی‌گرده به باقیمانده.
  - ✅ (Logy 🟠 — یه سوراخ جدا پیدا شد) — بالا فقط حالت «قبلاً قفل شده بود، الان معلقِ آزادسازی»ه رو پوشش می‌ده. حالت دوم: متریالی که **هنوز هیچ‌وقت قفل نشده** (تازه لینک شده، منتظر اولین قفل) — برای اکثر متریال‌ها (pct/batch/ratio) این از قبل درست بود چون `resolveLineCost` قبل از قفل‌شدن هم زنده تخمین می‌زد، ولی برای متریال نوع **«فرش»** درصد مصرف فقط داخل پاس قفل واقعی از رو مساحت محصول/مساحت فرش محاسبه می‌شد (نه از یه فیلد ذخیره‌شده) — پس تا اولین قفل، حسابداری زنده هزینه‌ی فرش معلق رو **صفر** می‌دید. فرمول دقیق پاس قفل (`productArea×coverage/fabricArea×100`) به `resolveLineCost`/`resolveLineCostPure` اضافه شد تا زنده هم همون تخمین رو بده.

**پیش‌نمایش فاکتور:** ✅ بررسی و فیکس شد (Logy)
> **لاگ Logy 🟠:** دو باگ واقعی توی `nativeSave.js` پیدا شد که هردو دقیقاً با علائم گزارش‌شده (PDF/Copy کار می‌کنن، Image‌ها نه، Save اصلاً کار نمی‌کنه) جور درمیان:
> 1. **تبدیل دیتایوآرال به Blob با `fetch(dataUrl)` بود** — این روش روی بعضی نسخه‌های Android WebView برای دیتایوآرال‌های بزرگ (دقیقاً حالت عکس تمام‌صفحه‌ی فاکتور با `scale:3`) می‌تونه بی‌سروصدا fail کنه، در حالی که PDF چون از اول به‌صورت Blob مستقیم از jsPDF میاد (نه دیتایوآرال) هیچ‌وقت از این مسیر رد نمی‌شه — دقیقاً چرا PDF کار می‌کرد ولی Image نه. جایگزین شد با دیکود دستی base64 (بدون `fetch`).
> 2. **باگ اصلی «Save اصلاً کار نمی‌کنه»:** توی `saveFile`، اگه نوشتن فایل نیتیو (Capacitor Filesystem) fail می‌شد (هر دلیلی، مثلاً پرمیشن)، کد بی‌سروصدا catch می‌کرد و می‌رفت سراغ `downloadInBrowser` — که توی یه اپ نیتیو (WebView) اصلاً کاری نمی‌کنه (نه دانلود منیجری هست نه لینکی جایی می‌ره). یعنی کاربر نه خطا می‌دید نه فایلی ذخیره می‌شد — همون «دکمه کار نمی‌کنه». الان خطای واقعی throw می‌شه تا `alert`ی که خودِ کامپوننت‌ها از قبل داشتن، پیام واقعی رو نشون بده (نه فقط پیام عمومی).
> - همین دو فیکس روی مسیر مشترک Save Image ۱D/۲D (`WoodCuttingTab.jsx`) هم اثر می‌ذاره چون از همون `saveFile` استفاده می‌کنه؛ علاوه‌براون یه `try/catch` + `showToast` هم بهش اضافه شد چون قبلاً اصلاً خطا رو جایی نشون نمی‌داد (نه throw می‌شد نه catch)
> - 🔴 **نیاز به تست واقعی روی گوشی داره** (من دسترسی به رندر/اجرای واقعی Capacitor ندارم): دکمه‌های Save PDF/Save Image فاکتور، Share Image فاکتور، و Save Image ۱D/۲D رو امتحان کن. اگه بازم fail شد، الان باید یه پیام خطای واقعی (نه فقط «خطا در ذخیره») ببینی — همون پیام رو بفرست تا دقیق‌تر بررسی بشه (احتمال زیاد پرمیشن نوشتن روی `Directory.Documents`)
- [x] دکمه‌های Share PDF، Share Image، Save PDF، Save Image (فاکتور)
- [x] دکمه‌های Save Image 1D، Save Image 2D، Save Both 1D&2D (تب برش)

**پاپ‌آپ نوع متریال / نوع محصول (بر اساس دسته‌ی فرش):** ✅ (Logy)
> **لاگ Ref ⚫ — باگ واقعی پیدا شد، لیبل هیچ‌وقت نشون داده نمی‌شد:** کاربر گفت «اصلاح گفتی ولی کلاً نمی‌بینمش» — و علتش این بود: خطی که `headerBottom` رو حساب می‌کرد (`(typeof stickyTop !== "undefined" ? stickyTop : 88) + 96`) فرض کرده بود `stickyTop` یه عدده، ولی مقداری که واقعاً از `App.jsx` پاس داده می‌شه یه رشته‌ی CSS `calc(...)`ه (`"calc(var(--panel-header-height, 52px) + var(--mgmt-tabs-height, 44px) - 1px)"`). رشته + عدد = رشته (`"calc(...)96"`)، و مقایسه‌ی بعدی (`top < headerBottom`) یه عدد رو با یه رشته‌ی غیرقابل‌تبدیل مقایسه می‌کرد که همیشه `NaN`/`false` می‌شد — یعنی `current` هیچ‌وقت ست نمی‌شد و لیبل شناور از همون اول اصلاً امکان نمایش نداشت (نه فقط جاش بد بود، کلاً کد راهی برای true‌شدن نداشت). **همین باگِ دقیقاً یکسان توی `ProductTab.jsx` هم بود** (کپی همون منطق برای گروه‌بندی فرش تب محصولات) — هر دو فیکس شدن: به‌جای این حساب‌کتاب رشته/عدد، الان یه `stickyHeaderRef` مستقیم به خودِ بلوک `position:sticky` هدر وصله و `headerBottom` از `stickyHeaderRef.current.getBoundingClientRect().bottom` خونده می‌شه — همیشه درسته، به هیچ عدد جادویی یا نوع دیتای stickyTop وابسته نیست.
> **لاگ Logy 🟠 (قبلی):** `floatingCatLabel` بجای `position: sticky` جدا (که هم‌سطح هدر می‌شد و روش overlap می‌کرد)، حالا **داخل همون بلوک sticky هدر** رندر می‌شه (بعد از ردیف Sort) — یعنی دیگه معماری‌اش تضمین می‌کنه هیچ‌وقت روی هدر جستجو نمیاد و همیشه دقیقاً زیرِ ردیف Sort/فیلترهاست. منطق نمایش هم عوض شد: قبلاً با `threshold=160` هر گروهی که کمی به سمت بالا اومده بود لیبل رو نشون می‌داد، حتی اگه هدر واقعی گروه هنوز کاملاً روی صفحه دیده می‌شد. الان لیبل فقط وقتی نشون داده می‌شه که هدرِ واقعیِ گروه پشتِ نوار sticky هدر رفته باشه (یعنی دیگه دیده نمی‌شه)، و اگه اسکرول دقیقاً بالای بالا (`scrollY<=0`) باشه اصلاً لیبل خالی می‌مونه. این منطق عیناً برای تب محصولات (گروه‌بندی بر اساس فرش، `GroupHeader`) هم پیاده شد — قبلاً اصلاً لیبل شناوری اونجا وجود نداشت.
- 🔴 نیاز به تست واقعی: توی تب متریال (و تب محصولات، چون همون باگ اونجا هم بود) با نمای گروه‌بندی‌شده اسکرول کن و مطمئن شو لیبل خاکستری کوچیک بالای لیست (اسم دسته‌ای که داری توش اسکرول می‌کنی) دیده می‌شه
- [x] هردو پاپ‌آپ دقیقاً بالا-وسط صفحه، همیشه بیرون از تب داخلی (هیچ‌وقت روی خودِ تب/محتوا نیان) — `MaterialTab.jsx`, `ProductTab.jsx`
- [x] هروقت اسم خودِ دسته‌بندی مشخصه (قابل‌دیدنه) این پاپ‌آپ هاید می‌شه
- [x] وقتی اسکرول در بالای بالا (top) هست هم پاپ‌آپ هاید می‌شه
- [x] همین منطق دقیقاً برای تب متریال (اسم دسته‌بندی) و تب محصولات (دسته‌بندی محصول بر اساس فرش) پیاده شد — تب محصولات قبلاً این فیچر رو اصلاً نداشت
- [x] (تب متریال) موقعیت این پاپ‌آپ روی هدر جستجو نیست — دقیقاً زیر دکمه‌ی Sort ظاهر می‌شه

**تب همگام‌سازی:**
- [x] متن راهنمای مسیر پوشه‌ها اشتباهه: نوشته `/bills` برای فاکتورها ولی کد واقعاً توی پوشه‌ی `factor` سیو می‌کنه — یا متن یا مسیر واقعی رو هماهنگ کن ✅ (Logy) — کد واقعی درست بود (`factor/pdf`، `factor/image`)، متن راهنما (`SyncTab.jsx`) که اشتباه بود اصلاح شد؛ یه ثابت مرده‌ی قدیمی (`BILLS: "bills"` در `nativeSave.js`) هم که منشأ اشتباه بود حذف شد

**فیلترها/دکمه‌های اکسپند (محصولات، گالری، متریال):**
- [x] دکمه‌های فیلتر (تب محصولات، گالری، متریال) باید ارتفاعشون هم‌سایز بشه با اسکرین‌شات مرجع کاربر (پیوست به این چت) ✅ (Logy) — **بررسی شد، از قبل درست بود.** با عکس مرجع (دکمه‌های دایره‌ای سورت/تگ/لوکیشن/چک‌مارک بالای تب محصولات) و کد تطبیق دادم: `S.chip`/`T.chip` توی هر سه تب از قبل یک مقدار مشترک `height:32, minHeight:32` دارن و دکمه‌های فیلتر بالای هر سه تب (سورت، تگ نوع، لوکیشن، وضعیت، و ردیف «همه/موجود/تسویه‌نشده/تسویه‌شده» توی گالری) همگی از همین `chip` استفاده می‌کنن — یعنی از قبل هم‌سایز بودن.
- [x] دکمه‌های اکسپند — گالری: «همه/تسویه‌شده/...»، محصولات: «ویرایش/حذف/چشم/...»، متریال: «ویرایش/حذف/بولک» — ارتفاعشون باید هم‌سایز بشه با دکمه‌ی هاید/آن‌هاید هدر تب متریال (طبق اسکرین‌شات پیوست) ✅ (Logy) — با بررسی دقیق‌تر، «همه/تسویه‌شده/...» و بولک/ویرایش/حذف محصولات و متریال از قبل `chip` (۳۲px) بودن، ولی **دو تا استثنا/باگ واقعی پیدا شد** که با `iconBtn` (بدون ارتفاع ثابت، کوچیک‌تر) رندر می‌شدن و به همین دلیل با بقیه هم‌سایز نبودن: (۱) دکمه‌ی هاید/آن‌هاید بالای هر آیتم متریال (`MaterialTab.jsx`) (۲) دکمه‌های ویرایش/حذف بالای هر مشتری/گالری (`GalleryTab.jsx`). هر دو الان صریحاً `width:32, height:32` گرفتن تا با بقیه یکی بشن.

**دکمه‌ی Undo:**
- [ ] **گزارش جدید کاربر (این نشست):** موقع حذف متریال از بولک، یا حذف متریال از داخل فرم ویرایش محصول، یا حذف کامل خودِ محصول — متریال‌ها به رنگ آبی (آماده‌ی آزادسازی) در نمیان؛ کاربر مطمئن نیست Undo درست کار می‌کنه یا نه — 🔍 **بررسی شد (Wall 🟣):** هر سه مسیر رو به‌دقت خوندم:
  - **تصحیح دقیق کاربر:** منظورش این نبود که «آبی نمی‌شه و همونجا آویزون می‌مونه» — منظورش این بود که «نه‌تنها آبی نمی‌شه، بلکه مستقیم/فوری آزاد می‌شه» (یعنی هزینه‌ش بی‌درنگ به باقیمانده‌ی متریال برمی‌گرده، بدون این‌که از کاربر خواسته بشه دکمه‌ی رفرش رو نگه داره).
  - حذف از بولک (`bulkApplyMaterial`, بخش «A. Remove material if listed in removedIds»، `App.jsx`): اگه `li.deductedAt` (یعنی واقعاً قفل بوده) → `pendingUnlock:true` می‌ذاره (آبی)؛ اگه هنوز قفل نشده بود (فقط معلق/زرد) → کامل حذف می‌شه (که درسته، چون چیزی از متریال واقعاً کسر نشده بود که نیاز به آزادسازی داشته باشه)
  - حذف از فرم ویرایش محصول (`removeLine`, `ProductTab.jsx`): همین منطق رو داره، و `handleSaveProduct` هم `lineItems` رو بدون فیلتر کردن ذخیره می‌کنه — یعنی `pendingUnlock:true` باید سالم به دیتای اصلی برسه
  - حذف کامل محصول (`deleteProduct`, `App.jsx`): برای هر لاین‌آیتم قفل/معلق، یه `pendingReleaseCredit` مستقیم روی خودِ متریال می‌سازه (نه رو لاین‌آیتم، چون لاین‌آیتم با خودِ محصول پاک می‌شه) — این کردیت‌ها هم توی محاسبه‌ی رنگ آبی متریال (`MaterialTab.jsx`, `pendingReleaseCredits`) لحاظ می‌شن
  
  هر سه مسیر روی کاغذ درست به‌نظر می‌رسن — **هیچ باگ مشخصی توی این بررسی پیدا نشد.** چیزی که می‌تونه توضیح بده چرا آبی ندیدی: اگه لاین‌آیتمی که حذف کردی هنوز **قفل نشده بود** (یعنی زرد/معلق بود، نه قرمز/قفل)، طبق طراحی چیزی برای آزاد کردن نیست پس آبی هم نباید نشون بده — این رفتار درسته نه باگ.
  
  **با تصحیح کاربر، این توضیح خیلی محتمل‌تر شد:** طبق کد، اگه لاین‌آیتم هنوز **زرد/معلق** بود (قفل نشده، `deductedAt` نداشت)، حذفش کاملاً و **فوری** انجام می‌شه — چون چیزی از موجودی متریال واقعاً کسر نشده بود که نیاز به «آزادسازی» (آبی) داشته باشه؛ فقط لاین‌آیتم پاک می‌شه. این دقیقاً همون «مستقیم آزاد شد» ایه که توصیف کردی — **رفتار درسته، نه باگ**، به این شرط که قبل از حذف قرمز (قفل‌شده) نبوده باشه.
  
  🔴 **برای اطمینان کامل نیاز به یه تأیید داریم:** لطفاً یه بار دیگه امتحان کن و دقیق بگو: (۱) قبل از حذف، اون متریال روی محصول **قرمز (قفل‌شده)** بود یا **زرد (فقط معلق)**؟ (۲) کدوم مسیر دقیقاً — از تب متریال (بولک) یا از فرم ویرایش خودِ محصول یا حذف کامل محصول؟ اگه واقعاً **قرمز/قفل‌شده** بود و بازم فوری آزاد شد (نه آبی)، اون‌وقت واقعاً یه باگ واقعیه و نیاز به بررسی عمیق‌تر داره.
- 🔴 لطفاً دوباره تست کن: یه متریال رو قفل کن، بلافاصله از پاپ‌آپ نگه‌داشتنِ دکمه‌ی رفرش گزینه‌ی Undo رو بزن، ببین برمی‌گرده یا نه. اگه بازم کار نکرد، دقیقاً بگو از کدوم دکمه (پاپ‌آپ Undo، یا جای دیگه‌ای؟) و چه پیامی (خطا/توست) می‌بینی

**تب متریال:**
- [x] نوار متریال‌های معلق (منتظر قفل) رنگش زیادی زرده — باید یکم به سمت نارنجی متمایل بشه ✅ (Logy) — `MaterialTab.jsx`: `#f2c94c` → `#f2a83f` (نوار پیشرفت + برچسب درصد + statusColor)، برچسب «(زرد)» هم از متن حذف شد چون دیگه دقیق نیست

**تب همگام‌سازی — آدرس سرور SQL قابل‌تغییر از داخل اپ (این نشست، ثبت‌شده، هنوز شروع نشده):**
> کاربر: SQL نگه‌داشته می‌شه (به‌زودی سایت لایو می‌زنه که بهش نیاز داره — پس این جایگزین Drive نیست، اضافه‌ست). چیزی که می‌خواد: `VITE_API_BASE_URL` (و احتمالاً بقیه‌ی مقادیر مرتبط با سرور SQL) دیگه توی `.env` در زمان build ثابت نباشه؛ بشه از همون تب همگام‌سازی داخل اپ تغییرش داد (چون آدرس واقعی سایت لایو بعداً مشخص می‌شه و عوض هم ممکنه بشه، نمی‌خواد هر بار APK رو دوباره build کنه).
- [x] یه فیلد/تنظیمات توی `SyncTab.jsx` اضافه بشه که کاربر بتونه آدرس سرور (base URL) رو مستقیم توی اپ وارد/ویرایش کنه؛ مقدار ذخیره‌شده (نه مقدار build-time از `.env`) باید اولویت داشته باشه و همه‌جایی که الان از `import.meta.env.VITE_API_BASE_URL` مستقیم استفاده می‌شه (`syncManager.js` و هرجای دیگه) بهش رجوع کنه؛ اگه چیزی ذخیره نشده بود، مقدار `.env` به‌عنوان پیش‌فرض اولیه بمونه ✅ (Ref ⚫) — `syncManager.js`: تابع مشترک جدید `getApiBase()` اکسپورت شد که اول `localStorage` (کلید `refarsh_api_base_url_override`) رو چک می‌کنه، اگه خالی بود میفته رو مقدار قبلی env؛ `apiUrl()` داخلی هم به همین تابع وصل شد. `SyncTab.jsx`: تابع محلی تکراری `getApiBase` حذف شد و به‌جاش همین تابع مشترک import شد (هم `testConnection`/`handleSync` هم `handleResetServerStore` که قبلاً جدا از env می‌خوند، الان همه از یه منبع واحد می‌خونن)؛ یه بخش تاشو جدید («آدرس سرور») کنار بخش پوشه‌ی عکس‌ها اضافه شد با یه اینپوت متن + دکمه‌ی ذخیره که مستقیم توی همون کلید localStorage می‌نویسه و بعدش `testConnection()` رو دوباره صدا می‌زنه تا بلافاصله نتیجه معلوم بشه؛ خالی‌کردن و ذخیره یعنی برگشت به مقدار env. با `esbuild` روی هر دو فایل چک شد، خطای سینتکسی نیست. 🔴 لطفاً روی APK نصب‌شده تست کن: تب سینک → آدرس سرور → IP واقعی سرور رو بذار → ذخیره → مطمئن شو پینگ/سینک بدون build دوباره کار می‌کنه.
- 🔵 هنوز شروع نشده — کسی زودتر رسید رنگ خودشو جلوش بذاره

**سوییچ اکانت گوگل (سوال کاربر — راهنمایی، بدون تغییر کد این نشست):**
- روی وب/دسکتاپ: `AuthContext.jsx` از قبل `prompt: "select_account"` رو ست می‌کنه، یعنی «خروج از حساب» و بعد دوباره زدن دکمه‌ی ورود با گوگل باید همیشه انتخابگر اکانت گوگل رو نشون بده — نیازی به تغییر نداره
- روی APK نیتیو: از پلاگین `@capacitor-firebase/authentication` استفاده می‌شه که خودش باید بعد از Logout دوباره انتخابگر اکانت رو نشون بده؛ اگه روی گوشی امتحان کردی و بازم خودکار همون اکانت قبلی رو انتخاب کرد (بدون نشون‌دادن انتخابگر)، بگو تا با یه تنظیم/کد اضافه مجبورش کنیم همیشه انتخابگر رو نشون بده — چون این رفتار فقط رو گوشی واقعی قابل تست/تأییده، دست نزدم

---

## 🆕 دامپ خام درخواست/گزارش بزرگ کاربر (این نشست 🟠 Logy) — همراه اسکرین‌شات‌ها

> ثبت مستقیم متن کاربر برای triage. موارد ✅ همین نشست claim و کامل شدن؛ بقیه باز مونده برای نشست بعدی (هرکی زودتر رسید رنگ خودشو جلوش بذاره).

1. [x] خطای «کمبود چوب» در برش ۱D نباید اینجوری کش بیاد؛ فونت متن خطای کمبود ۱D باید هم‌اندازه‌ی فونت متن خطای کمبود ۲D (کوچیک‌تر) بشه ✅ (Ash 🟡) — `WoodCuttingTab.jsx`: فونت کادر قرمز کمبود ۱D از ۱۰px/آیکون ۱۴px به ۹.۵px/آیکون ۱۲px (دقیقاً هم‌اندازه‌ی ۲D توی `NestingVisualizer.jsx`) کم شد؛ علت واقعی «کش‌اومدن» این بود که متن روی یک خط (بدون wrap) توی کادری با `width:max-content` بود، پس اگه متن بلند می‌شد کل کادر برش ۱D رو با خودش پهن‌تر می‌کرد — الان کادر خطا `maxWidth: plankContainerWidth` گرفته (همون عرض واقعی صفحه که برای مقیاس ردیف‌ها هم استفاده می‌شه) و متن `wordBreak/whiteSpace:normal` گرفته تا داخل همون عرض wrap بشه، نه این‌که کادر رو بکشه
2. [x] وقتی متن ابعاد قطعه‌ی ۱D جا نمی‌شه: عرض قطعه باید به حداقلِ لازم برای خوانا بودن لیبل برسه و کل بخش ۱D قابل اسکرول افقی بشه (حداقل عریض‌شدن ممکن؛ نه کل صفحه کش بیاد نه متن جا نشه) — **بررسی شد (Ash 🟡):** از قبل توی همین فایل پیاده‌سازی شده بود (کامنت بالای `PlankRow`/`plankGlobalScale`، خط ۱۲۴ به بعد) ولی چک‌باکسش خورده نشده بود: هر ردیف مستقل و دقیقاً ۱۰۰٪ عرض کادر رسم می‌شه؛ فقط وقتی لیبل کوچیک‌ترین قطعه‌ی یه ردیف جا نشه، یه «مقیاس سراسری مشترک» (`plankGlobalScale`, سقف ×۴) روی همه‌ی ردیف‌ها اعمال می‌شه تا هم‌اندازه بمونن، و دقیقاً همون‌جا `plankContainerRef` با `overflowX:auto` اسکرول افقی طبیعی رو فعال می‌کنه. منطق درست بود، فقط تیک نخورده بود.
3. [x] ارتفاع چندتا دسته دکمه باید هم‌سایز بشه با دکمه‌ی نمونه (دکمه‌ی چشم هاید/آن‌هاید تب متریال، طبق اسکرین‌شات مرجع کاربر):
   - تب محصولات: دکمه‌های وضعیت موجودی، وضعیت مکانی، نوع؛ و در حالت اکسپند: دکمه‌های ویرایش/حذف/وضعیت تسویه/انتقال‌به‌گالری‌یا‌برگشت (کل خط)
   - تب متریال: دکمه‌ی هاید/آن‌هاید، دکمه‌ی وضعیت موجودی؛ و در اکسپند: دکمه‌های ویرایش/حذف/افزودن‌به‌محصولات (کل خط)
   - تب گالری: دکمه‌های اکسپند «همه/موجود/تسویه‌نشده/تسویه‌شده» (کل خط)؛ و دکمه‌های موجودی/وضعیت مکانی/نوع
   ✅ (Ash 🟡) — **بررسی کامل شد، چیزی برای فیکس پیدا نشد:** هر سه فایل (`ProductTab.jsx`, `MaterialTab.jsx`, `GalleryTab.jsx`) یه استایل مشترک `S.chip`/`T.chip` دارن که صریحاً `height:32, minHeight:32` رو ثابت گرفته (باکس‌سایزینگ border-box)، و همه‌ی دکمه‌های لیست‌شده بالا — فیلترهای وضعیت/مکان/نوع بالای هر تب، هاید/آن‌هاید متریال (خودِ دکمه‌ی مرجع)، ویرایش/حذف/افزودن‌به‌محصولات، انتقال‌به‌گالری/برگشت، و تب‌های همه/موجود/تسویه‌شده/تسویه‌نشده‌ی گالری — از همون `...S.chip`/`...T.chip` استفاده می‌کنن (یا `iconBtn` با `width:32,height:32` صریح، برای دکمه‌های فقط-آیکون مثل ویرایش/حذف گالری). این احتمالاً نتیجه‌ی فیکس قبلی Logy (بخش «دکمه‌های اکسپند» بالاتر توی همین فایل) بوده که دو استثنای واقعی رو پیدا و درست کرده بود؛ الان همه‌چیز یکدست ۳۲px هست. اگه رو گوشی واقعی هنوز جایی نامساوی می‌بینی، بگو دقیقاً کدوم دکمه تا مستقیم برم سراغش.
4. [x] در بزرگ‌نمایی عکس محصول (لایت‌باکس)، یک ردیف خط خالی بین آخرین مشخصات محصول و ردیف «بها» ✅ (Logy) — `ProductTab.jsx` (`ImageLightbox`)
5. [x] متریال‌های مساحتی/فرش، حالت «باقی‌مانده»: اگر درصد تخصیص‌یافته به محصولات از ۱۰۰٪ بیشتر شد، محاسبه به روش «پرتی» تبدیل بشه؛ اگر کمتر از ۱۰۰٪ بود زنده/باقی‌مانده بمونه (منطق فعلیِ باقی‌مانده درسته، دست نخوره) — هیچ‌وقت نباید بیشتر از ۱۰۰٪ متریال مصرف نشون داده بشه ✅ (Logy) — **باگ واقعی توی موتور محاسبه‌ی زنده‌ی هزینه‌ی بچی (`App.jsx`, `areaBatchCostByProduct`) پیدا شد:** قبلاً هر محصول لینک‌شده به یک بچ، هزینه‌اش رو مستقل و بدون سقف حساب می‌کرد (`ownArea × costPerArea`، بدون هیچ محدودیتی روی مجموع)؛ فقط برای آیتم‌های با `includeWastage=true` سهمی از «باقیمانده‌ی زیرِ ظرفیت» (leftover) اضافه می‌شد، ولی هیچ محافظتی برای حالت *بیش‌تخصیص* (که مجموع مساحت محصولات از مساحت واقعی بچ بیشتر بشه) وجود نداشت — یعنی مجموع هزینه‌ی نمایش‌داده‌شده می‌تونست از کل هزینه‌ی بچ بیشتر بشه. فیکس شد: حالا برای هر بچ، مساحت/هزینه‌ی محصولات از قبل قفل‌شده (`deductedAt`) از ظرفیت واقعی کم می‌شه، و اگه مجموع مساحتِ محصولاتِ هنوز-قفل‌نشده از ظرفیت باقیمانده‌ی واقعی بچ بیشتر بشه، خودکار به تقسیم نسبیِ باقیمانده (فرمول «پرتی») سوییچ می‌کنه — همیشه مجموع هزینه‌ها دقیقاً برابر یا کمتر از باقیمانده‌ی بچه؛ اگه زیر ظرفیت بود، دقیقاً همون منطق لایوِ قبلی (سهم فیزیکی + پخش leftover بین آیتم‌های «پرتی شود») دست‌نخورده می‌مونه. `esbuild` روی `App.jsx` چک شد، خطای سینتکسی نیست؛ منطق با چند سناریوی دستی (بیش‌تخصیص با/بدون قفل قبلی، زیرِ ظرفیت) روی کاغذ صحت‌سنجی شد.
6. [x] برش: موقع ذخیره‌ی عکس تکی ۱D/۲D، ذخیره‌ی جفتی، یا دکمه‌ی دایره‌ای پایین — یک toast «ذخیره شد» نشون داده بشه ✅ (Logy) — `WoodCuttingTab.jsx`: `showToast` موفقیت بعد از `saveFile` توی `handleSaveAsJpg` اضافه شد (هم مسیر تکی هم مسیر ترکیبی/دایره‌ای چون هردو همین تابع رو صدا می‌زنن)؛ به‌عنوان بونوس، همین الگو برای ذخیره‌ی PDF/عکس فاکتور هم توی `InvoicePrint.jsx` اضافه شد (قبلاً فقط روی خطا alert داشت، روی موفقیت هیچ فیدبکی نبود)
7. [x] پیش‌نمایش فاکتور: دکمه‌های آبی PDF/عکس باید save-auto-to-Documents باشن؛ دکمه‌ی PDF قرمز و عکس مشکی باید share باشن ✅ (Logy) — **باگ واقعی پیدا شد:** رفتار (منطق save/share) از قبل کاملاً درست بود، ولی رنگ‌ها برعکس بودن (دکمه‌های Save قرمز/مشکی بودن، دکمه‌های Share آبی بودن). رنگ‌ها بازچیده شدن: Save PDF و Save Image الان هردو آبی، Share PDF قرمز، Share Image مشکی — دقیقاً طبق خواسته‌ی کاربر
8. [x] پاپ‌آپ دکمه‌ی Sort (تب‌های کاتالوگ/محصولات/متریال/گالری + بخش فاکتورهای تب حسابداری): کنار هر گزینه بنویسه الان کدوم فعاله؛ برای بخش فاکتورها، اسم گزینه‌ها دقیقاً مثل تب محصولات باشه (مثلاً A-Z بشه «Az») ✅ (Ash 🟡) — `ProductTab.jsx`/`MaterialTab.jsx`/`GalleryTab.jsx` (کاتالوگ از همون `SortButton` تب محصولات استفاده می‌کنه): هر مورد `SORT_MODES` یه `label` فارسی گرفت (مثلاً «وضعیت موجودی»، «تاریخ»، «کد محصول») و داخل لیست پاپ‌آپ کنار آیکون/متن جهت نمایش داده می‌شه؛ گزینه‌ی فعال هم از قبل با پس‌زمینه‌ی قرمز پررنگ مشخص بود، دست‌نخورده موند. `AccountingTab.jsx` (بخش فاکتورها) از قبل برای هر گزینه لیبل فارسی داشت، فقط اسم گزینه‌ی الفبایی رو از «A-Z» به «Az» عوض کردم تا دقیقاً با تب محصولات یکی بشه.
9. [x] بزرگ‌نمایی: ابعاد `xD`/`xO` (مثلاً `64D`, `55O`) به‌جای کد خام، «نیم‌دایره به قطر ۶۴» / «دایره به قطر ۵۵» نمایش داده بشه ✅ (Logy) — `mathCore.js` تابع جدید `formatDimsHuman`، استفاده در `ImageLightbox` (`ProductTab.jsx`)
10. [x] چک بشه همه‌ی فیلدهای description و بقیه‌ی فیلدها توی اکسپورت/ایمپورت (اکسل و JSON) واقعاً رفت‌وبرگشت می‌کنن ✅ (Ref ⚫) — روی شیت محصولات و شیت متریال‌ها ستون‌به‌ستون چک شد (`handleExportExcel`/`handleImportExcel` در `App.jsx`): توی محصولات، ستون‌های آخر (`description`، `productTypeId`، `salePriceManual`، `hiddenFromCatalog`، `qty`، `image`، `fabricMaterialId`) همه دقیقاً با همون ایندکسی که export می‌نویسه import می‌خونه، هیچ افتادگی پیدا نشد. توی متریال‌ها هم فیلد `hidden` (که فیلد جدا از `hiddenFromCatalog` محصولاته — برای متریال اسمش فقط `hidden`) و batches/sticks/procurements/linkedProductIds (Raw JSON) همه رفت‌وبرگشت درست دارن. `handleExportJson` چون از spread کامل `data` می‌سازه خودکار همه‌چیزو پوشش می‌ده، نیاز به چک جدا نداشت.
15. [x] تب متریال: دکمه‌ی هاید/آن‌هاید متریال‌های مخفی‌شده باید پیش‌فرض روی «هاید» باشه ✅ (Ref ⚫) — **تأیید شد، باگی نیست:** `MaterialTab.jsx` خط ۲۸۹۳، `localStorage.getItem("material_show_zero_balance") === "true"` — وقتی هیچ مقداری قبلاً ذخیره نشده، این عبارت `false` برمی‌گرده یعنی حالت پیش‌فرض دقیقاً «هاید» است. اگه رو گوشی هنوز آن‌هاید دیده می‌شه قطعاً یه مقدار قدیمی توی `localStorage` همون گوشیه، نه چیزی که با پوش جدید حل بشه — با پاک‌کردن دیتا/کش اپ باید درست بشه.
11. [x] ترتیب دسته‌بندی‌های تب محصولات: نه بر اساس نام، بلکه دسته‌ای که کمترین کد محصول رو داره بالاتر بیاد؛ «بدون دسته» و بعدش «در دست ساخت» همیشه آخر لیست ✅ (Logy) — `App.jsx` (`groupedProducts`، حالت پیش‌فرض/دسته‌بندی) + `ProductTab.jsx` (کامپراتور نمایش گروه‌ها دیگه این حالت رو الفبایی نمی‌کنه)
12. [x] دکمه‌ی Sort تب محصولات: کنار حالت «دسته‌بندی» یه گزینه‌ی جدید «گروه‌بندی بر اساس نوع محصول» اضافه بشه (`sortMode="type"`؛ توی `App.jsx` باید یه شاخه‌ی گروه‌بندی مشابه fabric/دسته اضافه بشه که بر اساس `productTypeId`/`productTypeName` گروه کنه، با همون منطق «کمترین کد» برای ترتیب گروه‌ها که همین نشست برای حالت دسته‌بندی پیاده شد) ✅ (Ash 🟡) — `App.jsx` (`groupedProducts`): شاخه‌ی جدید `sortMode === "type"` اضافه شد که دقیقاً روی الگوی fabric/دسته کپی شده (گروه بر اساس `data.productTypes.find(t => t.id === productTypeId)?.name`، ترتیب گروه‌ها بر اساس کمترین کد، «بدون نوع» و «در دست ساخت» همیشه آخر)؛ `data.productTypes` به dependency array اضافه شد. `ProductTab.jsx` (`SortButton`): یه دکمه‌ی سوم کنار «دسته‌بندی/یکجا» اضافه شد («گروه‌بندی بر اساس نوع محصول») که با کلیک هم `sortMode` رو `"type"` می‌کنه هم `groupedView` رو روشن می‌کنه؛ گروه ویژه‌ی «بدون نوع» هم به مرتب‌سازی نمایشی گروه‌ها (که «در دست ساخت»/«بدون فرش»/«بدون دسته» رو همیشه آخر می‌ذاره) اضافه شد تا الفبایی جابه‌جا نشه.
13. [x] متریال‌های مساحتی/خطی/فرش، حالت «پرتی»: وقتی درصد مشخصی از متریال دستی انتخاب شده (مثلاً ۸۹٪)، باید همون ۸۹٪ رو معادل ۱۰۰٪ مصرفی حساب کنه (این بخش الان درست کار می‌کنه) — ولی موقع «قفل‌کردن» باید فرمول عوض بشه: مقادیر دقیقاً همون لحظه‌ی قفل، یک‌بار برای همیشه ثابت/فریز بشن و دیگه هیچ‌وقت بازمحاسبه نشن (چه با تغییر بعدی هزینه‌ی متریال، چه رفرش)؛ موقع Undo هم دقیقاً همون مقادیر قفل‌شده برگردن، بدون بازمحاسبه‌ی سیستمی ✅ (Logy، فقط بخش بچ‌های مساحتی/فرش) — دو باگ واقعی پیدا شد و فیکس شد، هردو توی `App.jsx`:
    - **باگ Undo:** بررسی شد، خودِ Undo از قبل درست بود — سیستم `operations`/`prevLi` واقعاً یه snapshot دقیق از line item قبل از قفل نگه می‌داره و `restoreLi` هم مستقیم `{ ...prevLi }` رو جایگزین می‌کنه، هیچ بازمحاسبه‌ای در کار نیست. نیازی به فیکس نبود.
    - **باگ واقعی: خودِ فرمول لحظه‌ی قفل با فرمول لحظه‌ی نمایش زنده یکی نبود.** توی صفِ قفل‌کردن بچ (`pendingBulkChangesList`، اکشن `lock`)، مبلغِ فریز‌شونده با `batch.totalCost × (productArea / batchArea)` حساب می‌شد — این فرمول کاملاً مستقل از حالت «پرتی» (`includeWastage`) بود، به محصولاتی که قبلاً از همون بچ جدا قفل شده بودن توجه نمی‌کرد (همیشه از `batch.totalCost` کامل سهم می‌داد، نه از باقیمانده‌ی واقعی)، و مثل باگ مورد ۵، سقفی هم برای مجموع همزمانِ چند محصول نداشت. الان دقیقاً همون فرمولِ تصحیح‌شده‌ی مورد ۵ (سهم فیزیکی وقتی زیر ظرفیتیم، تقسیم نسبی باقیمانده وقتی بیش‌تخصیص یا `includeWastage` هست) موقع قفل هم اجرا و توی `deductedCost` فریز می‌شه — یعنی چیزی که قفل می‌شه دقیقاً همون چیزیه که لحظه‌ی قبلش زنده نمایش داده می‌شد، نه یه عدد متفاوت. `esbuild` چک شد.
    - 🔴 **دامنه‌ی این فیکس فقط متریال‌های مساحتی/فرشِ متصل به یه بچ مشخصه** (که طبق اسکرین‌شات‌های کاربر رایج‌ترین حالته). متریال‌های فرش بدون بچ (سیستم جداگانه‌ی «استخر/pool» با `pct` که موقع قفل جدا محاسبه می‌شه، پایین‌تر توی همون فایل) و متریال‌های خطی (که فرمول متفاوت طول‌محور دارن، از قبل فیزیکاً محدودن) دست‌نخورده موندن — این‌ها ساختار کد متفاوتی دارن، نیاز به بررسی/فیکس جدا دارن، بدون عجله‌ی بی‌مورد.
14. [x] وقتی متریالی از یه محصول حذف/آن‌لینک بشه یا کل محصول حذف بشه: متریال آزادشده اول به رنگ آبی روشن توی نوار دربیاد (قابل استفاده نباشه)، و فقط بعد از زدن دکمه‌ی «آزادسازی» واقعاً آزاد و به باقی‌مانده برگرده؛ Undo باید مثل دکمه‌ی قفل رفتار کنه ✅ (Ash 🟡 — بررسی کامل شد، از قبل درست پیاده‌سازی شده بود، فقط تیک نخورده بود):
    - **آن‌لینک تکی (`ProductTab.jsx`, `removeLine`):** اگه لاین‌آیتم قفل‌شده (`li.deductedAt`) حذف بشه، به‌جای حذف فوری، `pendingUnlock:true` می‌گیره — دیگه «مصرف فعلی» حساب نمی‌شه؛ `MaterialTab.jsx` (`pendingUnlockCost`) این‌ها رو جدا از قفل قطعی (قرمز) و معلق (نارنجی) با رنگ آبی («آماده‌ی آزادسازی») نشون می‌ده — دقیقاً همون «حالت پنجم بین قفل/باز»
    - **حذف کل محصول (`App.jsx`, `deleteProduct`):** لاین‌آیتم‌های قفل‌شده به `material.pendingReleaseCredits[]` منتقل می‌شن (آبی، غیرقابل‌استفاده) به‌جای آزادسازی فوری
    - **آزادسازی واقعی:** فقط با نگه‌داشتن دکمه‌ی رفرش (`runLockUnlockPass`, مرحله‌ی UNLOCK) اتفاق می‌افته
    - **Undo مشترک:** هر دو مسیر (`unlock` و `creditUnlock`) دقیقاً همون سیستم `operations`/`passId` مشترکِ قفل رو استفاده می‌کنن (`reverseOperation`) — Undo همیشه آخرین پاسِ قفل *یا* آزادسازی رو دقیق برمی‌گردونه
    - کد هیچ فایلی تغییر نکرد؛ فقط این آیتم که قبلاً پیاده‌سازی شده بود ولی هنوز `[ ]` مونده بود، تیک خورد
16. [x] دکمه‌ی Refresh باید دقیقاً مثل خروج از پنل و برگشت، همه‌ی فیلترها/چیدمان‌ها رو به پیش‌فرض ریست کنه (reset کامل صفحه) ✅ (Logy) — مکانیزم `refreshResetTick` از قبل روی هر ۳ تب (محصولات/متریال/گالری) سوار بود ولی ناقص؛ تکمیل شد: تب محصولات حالا `sortMode` و ردیف‌های باز-شده‌ی دسته‌بندی (`collapsedGroups`) رو هم ریست می‌کنه؛ تب متریال حالا فیلتر دسته‌بندی متریال، پاپ‌آپ نوع، ردیف‌های باز-شده، حالت نمایش گروهی، و توگل «نمایش موارد مخفی» رو هم به پیش‌فرض برمی‌گردونه؛ تب گالری ردیف بازشده‌ی کارت گالری رو هم می‌بنده
17. [x] نمای پیشرفته‌ی فاکتور: توی ویرایش فاکتورهای ثبت‌شده، کنار هر قلم دکمه‌ی حذف؛ و یه ردیف آخر با دکمه‌ی «افزودن محصول» (چندانتخابی، فقط از محصولات موجود/فروش‌نرفته) ✅ (Ash 🟡 — تکراری با آیتمِ بالاترِ همین فایل بود، پیدا شد از قبل انجام شده) — این دقیقاً همون کاریه که Ref ⚫ توی بخش «نمای پیشرفته‌ی فاکتور (تب حسابداری)» بالاتر همین فایل انجام داده: `InvoicesTab.jsx` → `handleRemoveItemFromInvoice` (دکمه‌ی حذف کنار هر قلم) و `handleAddItemToInvoice` + فرم انتخاب چندتایی از `availableProductsForDraft` (ردیف افزودن محصول). کد تغییر نکرد، فقط این ردیف تکراری تیک خورد تا triage گیج‌کننده نباشه.

**لاگ Logy 🟠 (ادامه‌ی همین نشست):** مورد ۱۰ (چک export/import) رو برای شیت محصولات بررسی کردم — فیلد `description` (توضیحات) هم توی export (`handleExportExcel`، ستون ۲۹ «توضیحات») هم توی import (`handleImportExcel`، `row[28]`) درست map شده و رفت‌وبرگشت می‌کنه؛ فقط همین یه فیلد رو دقیق چک کردم (چون مشخصاً همین اواخر اضافه شده بود و ریسک فراموش‌شدنش بیشتر از بقیه‌ی فیلدهای قدیمی‌تر بود). بقیه‌ی فیلدها (متریال/تجهیزات/...) رو کامل خط‌به‌خط چک نکردم، اگه فیلد خاصی رو دیدی که بعد از ایمپورت خالی می‌شه بگو تا دقیق پیگیری کنم. مورد ۱۶ (ریست کامل Refresh) هم کامل شد (بالا).

**لاگ Logy 🟠 (این نشست):** از ۱۷ مورد بالا، ۳تا (۴، ۹، ۱۱) رو کامل کردم؛ همه با `esbuild` روی فایل‌های تغییریافته (`App.jsx`, `ProductTab.jsx`, `mathCore.js`) چک شدن، خطای سینتکسی نیست. بقیه یا نیاز به تست/عکس دقیق‌تر روی گوشی واقعی داشتن (۱، ۲، ۶، ۱۶)، یا مستقیم روی منطق حسابداری/قفل زنده اثر می‌ذارن و بدون تست واقعی ریسکشون بالاست (۵، ۱۳، ۱۴)، یا کار UI نسبتاً بزرگ/چندجایی‌ان که نیاز به نشست جدا دارن (۳، ۷، ۸، ۱۲، ۱۷). مورد ۱۰ (چک export/import) و ۱۵ (پیش‌فرض هاید) نیاز به بررسی/تست دقیق‌تر دارن، فعلاً فقط یادداشت شدن.
🔴 لطفاً موارد ✅شده رو تست کن؛ برای بقیه هرکی نشست بعد رسید ادامه بده.

**لاگ نشست Ash 🟡 (این نشست):** موارد ۱ و ۲ (فونت خطای کمبود ۱D + اسکرول افقی/حداقل‌عرض لیبل) کامل شدن — مورد ۲ در واقع از قبل درست پیاده‌سازی شده بود، فقط تیک نخورده بود؛ مورد ۱ واقعاً باگ بود و فیکس شد. هر دو با `esbuild` روی `WoodCuttingTab.jsx` چک شدن، خطای سینتکسی نیست. بقیه‌ی موارد باز (۳، ۵، ۱۰، ۱۲، ۱۳، ۱۴، ۱۵، ۱۷) دست‌نخورده موندن برای نشست بعد — اکثرشون یا نیاز به عکس مرجع دقیق دارن (۳) یا مستقیم روی منطق حسابداری/قفل اثر می‌ذارن و بدون تست واقعی رو گوشی ریسکشون بالاست (۵، ۱۳، ۱۴). 🔴 لطفاً مورد ۱ و ۲ رو رو گوشی واقعی تست کن (یه حالتی که واقعاً کمبود چوب داره بساز و ببین دیگه کادر خطا صفحه رو نمی‌کشه و متن خونا می‌مونه).

**لاگ نشست Ash 🟡 (ادامه‌ی همین نشست):** مورد ۸ (لیبل کنار گزینه‌های پاپ‌آپ Sort) هم کامل شد — چهار فایل (`ProductTab.jsx`, `MaterialTab.jsx`, `GalleryTab.jsx`, `AccountingTab.jsx`) با `esbuild` چک شدن، مشکلی نیست. جزئیات پیاده‌سازی بالای همین آیتم نوشته شده.

**لاگ نشست Ash 🟡 (ادامه‌ی همین نشست):** مورد ۱۲ (گروه‌بندی بر اساس نوع محصول) هم کامل شد — `App.jsx` و `ProductTab.jsx` با `esbuild` چک شدن، مشکلی نیست. جزئیات پیاده‌سازی بالای همین آیتم نوشته شده. 🔴 لطفاً روی گوشی واقعی تست کن: تب محصولات → دکمه‌ی Sort → «گروه‌بندی بر اساس نوع محصول» → مطمئن شو محصولات بر اساس نوعشون گروه می‌شن و «بدون نوع»/«در دست ساخت» آخر لیست می‌مونن.

**لاگ نشست Ash 🟡 (ادامه‌ی همین نشست):** مورد ۳ (هم‌سایزی ارتفاع دکمه‌ها) رو بررسی کردم — چیزی برای فیکس پیدا نشد، همه‌جا از قبل ۳۲px بود (جزئیات بالای آیتم). فقط ROADMAP.md عوض شد، کد دست نخورد.

**لاگ نشست Ref ⚫ (این نشست):** موارد ۱۰ و ۱۵ از دامپ کاربر رو claim و بررسی کردم — هردو صرفاً بررسی/تست بودن نه فیکس، هیچ‌کدوم باگ واقعی نداشتن (جزئیات بالای هرکدوم). کد هیچ فایلی دست نخورد، فقط ROADMAP.md آپدیت شد. عمداً سراغ موارد ریسک‌دار روی حسابداری/قفل زنده (۵، ۱۳، ۱۴) و کارهای UI بزرگ/چندجایی (۳، ۷، ۱۷ — که ۳ هم توسط Ash کامل شد) نرفتم، چون طبق یادداشت‌های قبلی این‌ها یا نیاز به تست واقعی رو گوشی دارن یا نیاز به عکس مرجع. 🔴 اگه می‌خوای نشست بعد سراغ یکی از موارد ریسک‌دار برم، مشخص کن کدوم اولویت داره.

**لاگ نشست Ref ⚫ (ادامه‌ی همین نشست):** گپ مایتر بخش ۱D (بالای همین بخش «برش ۱D») رو claim و فیکس کردم — جزئیات بالای همون آیتم. بقیه‌ی موارد باز همون بخش (۱۱۱۱-۱۱۱۴، مقیاس یکسان همه‌ی چوب‌ها/اسکرول/ذخیره‌ی کامل عکس) دست‌نخورده موند چون هرکدوم نیاز به تست واقعی رو گوشی با چند سناریوی واقعی داره تا مطمئن بشم چیزی خراب نمی‌کنم — این‌ها منطق چیدمان اصلی همون تابعین که تازه یه بازنویسی بزرگ خورده (کامنت بالای فایل)، ریسک شکستنشون بدون تست بالاست.

**لاگ نشست Ash 🟡 (نشست جدید — چک‌کردن دامپ کامل کاربر که دوباره پیام شد):** کاربر یه پیام کامل با ۱۷ آیتم فرستاد (همون چیزی که پایین همین فایل چندبار triage شده). همه‌ی ۱۷ مورد رو تک‌به‌تک با کد فعلی مقابله کردم:
- ۱۵ مورد از قبل توسط Ash/Logy/Ref کامل و تیک‌خورده بودن (فونت خطای ۱D، اسکرول/حداقل‌عرض لیبل، ارتفاع دکمه‌ها، خط خالی لایت‌باکس، فرمول پرتی>۱۰۰٪، toast ذخیره، رنگ دکمه‌های Save/Share فاکتور، لیبل پاپ‌آپ Sort، نمایش ابعاد نیم‌دایره/دایره، چک export/import، ترتیب دسته‌بندی بر اساس کد، گروه‌بندی نوع محصول، فرمول قفل پرتی، پیش‌فرض هاید متریال، ریست کامل Refresh)
- ۲ مورد (۱۴ و ۱۷) کد/فیچرشون از قبل کامل پیاده‌سازی شده بود ولی چک‌باکسشون یادش رفته بود بخوره — همین نشست تیک خوردن، جزئیات دقیق بالای هرکدوم نوشته شده
- **نتیجه: هیچ کد جدیدی این نشست نیاز نبود بنویسم** — همه‌ی درخواست‌های این دامپ از قبل جواب داده شده بودن. فقط `ROADMAP.md` آپدیت شد.
🔴 اگه بعد از این پیام هنوز موردی از این لیست رو روی گوشی/APK می‌بینی که درست نیست، لطفاً دقیق بگو کدوم شماره + یه اسکرین‌شات تازه، چون طبق کد فعلی همه‌شون باید حل شده باشن.

**لاگ نشست Ref ⚫ (ادامه‌ی همین نشست):** آیتم آدرس دستی سرور (پایین همین فایل، بخش «تنظیمات سرور») رو claim و پیاده کردم — جزئیات بالای همون آیتم. این ربط مستقیم به مورد ۶ (بخش ۱۰۶۳، خطای سینک گوگل روی APK) هم داره: حالا کاربر می‌تونه بدون build دوباره یه آدرس واقعی امتحان کنه تا بفهمیم اون خطا واقعاً مسیر نسبی/دامنه بود یا نه.

**لاگ نشست Ref ⚫ (ادامه‌ی همین نشست — ریسکی، با تأیید صریح کاربر):** آیتم «apply چندبچ همزمان» (بالای همین بخش) رو، بعد از یه رفت‌وبرگشت کامل با کاربر برای فهمیدن دقیق رفتار مطلوب (استخر ترکیبی + پرشدن پشت‌سرهم)، claim و پیاده کردم. جزئیات کامل بالای همون آیتم. این تنها تغییری بود که مستقیم موتور حسابداری/تخصیص متریال رو لمس کرد — چون کاربر صریحاً درخواست و تأیید کرد، ولی همچنان **باید رو گوشی واقعی تست بشه** قبل از هرگونه استفاده‌ی جدی، دقیقاً طبق همون احتیاطی که خودِ این رودمپ همه‌جا تأکید کرده.

**لاگ Logy 🟠 (ادامه‌ی نشست، بعد از هماهنگی با Ash/Ref):** رفتم سراغ سه مورد پرریسک باقی‌مونده (۵، ۱۳، ۱۴) که قبلاً همه (من، Ash، Ref) عمداً بهشون دست نزده بودیم. مورد ۵ و ۱۳ رو با دقت بررسی کردم و در هردو یه باگ واقعی و مشخص پیدا شد (جزئیات کامل بالای هرکدوم) — هردو ریشه‌شون یکی بود: فرمول محاسبه‌ی هزینه برای محصولات مساحتی/فرشِ متصل به یه بچ، سقفی برای «مجموع بیش‌تخصیص» نداشت، هم توی نمایش زنده (مورد ۵) هم توی لحظه‌ی قفل‌کردن (مورد ۱۳). چون این دو تا عملاً یه بحرانِ مشترک بودن، هردو رو با یه فرمول یکسان (پرتی خودکار وقتی بیش‌تخصیص/`includeWastage`، لایو وقتی زیر ظرفیت) فیکس کردم تا لحظه‌ی نمایش و لحظه‌ی قفل همیشه یک عدد بدن. `esbuild` روی `App.jsx` چک شد، خطای سینتکسی نیست؛ چند سناریوی دستی (بیش‌تخصیص، با/بدون قفل قبلی، زیر ظرفیت) روی کاغذ صحت‌سنجی شد. مورد ۱۴ (حالت پنجم «آزادسازی معلق» + Undo مشترک قفل/آزادسازی) هنوز باز مونده — یه فیچر UI/state جدیده (نه فقط فیکس فرمول)، نیاز به یه نشست جداگونه با تمرکز کامل داره. 🔴 **حتماً قبل از استفاده‌ی واقعی رو گوشی تست کن:** چندتا محصول مساحتی/فرش رو به یه بچ مشترک وصل کن جوری که مجموع مساحتشون از مساحت بچ بیشتر بشه، هم موقع نمایش (قبل قفل) هم بعد از زدن دکمه‌ی قفل، مطمئن شو مجموع هزینه از هزینه‌ی کل بچ بیشتر نمی‌شه.

**لاگ نشست Ash 🟡 (ادامه‌ی همین نشست — merge با کار همزمان Ref/Logy):** موقع push مورد ۱۷ (که مستقل و بدون خبر از کار Ref پیاده کرده بودم)، فهمیدم Ref هم زمان تقریباً هم‌زمان دقیقاً همین فیچر رو (حذف تک‌قلم + افزودن محصول به فاکتور) توی `InvoicesTab.jsx` پیاده کرده بود — و نسخه‌ی اون کامل‌تر بود (تاریخ با `JalaliDatePicker`، + تخفیف درصدی تک‌قلمی که من اصلاً نداشتم). موقع merge، پیاده‌سازی خودم رو کامل کنار گذاشتم و نسخه‌ی Ref رو نگه داشتم (توابع تکراری `handleRemoveItemFromInvoice`/state تکراری حذف شدن)، فقط تا مطمئن بشم دو نسخه با هم قاطی نشن. `esbuild` روی `InvoicesTab.jsx` بعد از merge چک شد، تمیزه. ROADMAP.md هم به نسخه‌ی به‌روزتر (که شامل کار Ref/Logy روی موارد ۱۳/۱۴ هم بود) merge شد.

**اصلاح هویت — Wall 🟣 (نه Ref ⚫):** تمام لاگ‌های بالا که این نشست با امضای «Ref ⚫» ثبت کردم (گپ مایتر ۱D، تنظیم آدرس سرور، و apply چندبچی) اشتباه امضا شدن — طبق دستور اولیه‌ی کاربر («تو بنام wall هستی») باید «Wall 🟣» می‌بودن. یه نشست واقعیِ دیگه («Ref ⚫» واقعی، همونی که فاکتور رو ویرایش کرده و توی لاگ Ash بالا اومده) مستقل و هم‌زمان کار می‌کرده — با استفاده‌ی اشتباهیِ من از همون اسم، شانسی به‌جز شباهت نام کاری تداخل واقعی توی کد نیفتاد، ولی از این پاراگراف به بعد امضای من «Wall 🟣»‌ه. **merge دستی همین الان:** تغییر «apply چندبچی» (بالا، امضا اشتباهی Ref) با فیکس سقف ۱۰۰٪ Logy (موارد ۵/۱۳) روی دقیقاً همون تابع (`bulkApplyMaterial`) تداخل مستقیم داشت؛ به‌جای merge خودکار git، دستی ترکیب شدن: منطق استخر-ترکیبی/پرشدن-پشت‌سرهمِ چندبچی نگه داشته شد، و چک بیش‌تخصیصِ Logy (`batchOverAllocated`) هم روی همون استخر ترکیبی (نه یه بچ تکی) تعمیم داده شد — یعنی الان هم چندبچی کار می‌کنه هم سقف ۱۰۰٪ رعایت می‌شه. `esbuild` روی `App.jsx` بعد از merge چک شد، تمیزه. 🔴 **این ترکیب هنوز رو گوشی تست نشده — قبل از استفاده‌ی جدی حتماً چک کن.**

**لاگ Wall 🟣 (این نشست، امضای درست از این به بعد):** «نمای پیشرفته‌ی فاکتور» موارد ۱-۴ رو تکمیل کردم — جزئیات کامل بالای همون بخش (بعداً موقع merge با کار هم‌زمان Ref روی همین بخش، نسخه‌ی Ref پایه گرفته شد چون کامل‌تر بود؛ اضافه‌ی من فقط وصل‌کردن مبلغ ودیعه به خروجی چاپ/پیش‌نمایش A4 بود که نگه داشته شد). `esbuild` روی `InvoicesTab.jsx` و `InvoiceTemplate.jsx` چک شد.

## دامپ جدید کاربر (نشست جدید — همراه با اسکرین‌شات‌های خطای Import و دکمه‌ها)

1. [x] ادامه‌ی کاری که Ash قبلاً داشت انجام می‌داد — این خودش اشاره به فیدبک کاربر روی کار Ref/Wall (لیبل شناور دسته‌بندی) بود؛ جزئیات کامل توی بخش جدید پایین همین فایل («فیدبک کاربر روی لیبل شناور + فرمت دسته‌بندی فرش + ترتیب ابعاد») ثبت شده ✅ (Logy)
2. [x] ایمپورت اکسل خطا می‌ده (اسکرین‌شات ضمیمه شد) — باید بررسی بشه مشکل از خودِ import هست یا از export (فایلی که export می‌شه بعداً قابل import نیست؟) ✅ (Ash 🟡 — سخت‌گیرتر شد، ولی ریشه‌ی دقیق بدون فایل واقعی کاربر پیدا نشد) — دو تا کار انجام شد توی `App.jsx` (`handleImportExcel`):
   1. **پیام خطای واقعی:** قبلاً پیام همیشه ثابت بود («خطا در بازخوانی فایل اکسل پشتیبان») و متن دقیق exception فقط توی `console.error` می‌رفت که روی گوشی/APK اصلاً دیده نمی‌شه. الان `err.message` هم توی toast میاد.
   2. **ایزوله‌سازی هر شیت:** قبلاً کل تابع import یه try/catch سراسری داشت — یعنی اگه توی *یه* سلول از *یه* شیت (مثلاً یه JSON خراب یا truncate‌شده) خطا می‌داد، کل ایمپورت (همه‌ی ۱۰ شیت) fail می‌شد با همون پیام کلی. الان هر بخش (مشتریان/متریال/محصولات/جلسات‌برش + یه گروه مشترک برای کارت‌ویزیت/تجهیزات/لینک‌کارگاه/ردپا/انواع‌محصول/پیش‌نویس‌فاکتور) try/catch جدا خودشو داره؛ اگه یکی خراب باشه، بقیه‌ی بخش‌ها همچنان با موفقیت ایمپورت می‌شن و در پایان دقیقاً توی toast گفته می‌شه کدوم بخش(ها) رد شدن (`sectionErrors`)، به‌جای شکست کامل و کور.
   - چک export/import ستون‌به‌ستون (که قبلاً Logy/Ref/Ash انجام داده بودن) هیچ مشکل ساختاری پیدا نکرد؛ همه‌ی `JSON.parse`های داخل تابع از قبل هم try/catch داشتن (پس خودِ JSON خراب هم قبلاً کرش نمی‌کرد). محتمل‌ترین سناریو برای خطای گزارش‌شده: یه سلول اکسل به سقف ۳۲٬۷۶۷ کاراکتری Excel/xlsx رسیده و truncate شده (مثلاً `lineItems`ی محصول با تعداد زیاد قلم، یا `sticks`/`batches`ی یه متریال با تاریخچه‌ی خیلی طولانی) و JSON.parse همون یه سلول fail کرده — که با فیکس امروز دیگه کل ایمپورت رو نمی‌خوابونه، فقط همون یه ردیف/بخش رد می‌شه و توی toast گفته می‌شه. `esbuild` روی `App.jsx` چک شد، مشکلی نیست.
   🔴 **لطفاً دوباره همون فایلی که خطا می‌داد رو import کن** — الان یا پیام خطای دقیق‌تری می‌بینی، یا (اگه مشکل واقعاً truncate یه سلول بود) ایمپورت با یه هشدار مشخص انجام می‌شه. اگه بازم کامل fail شد، لطفاً متن دقیق toast رو بفرست تا مستقیم بشه مشکل رو پیدا کرد.
3. [x] هم‌سایزی ارتفاع دکمه‌ها (نسخه‌ی کامل‌تر و دقیق‌تر از چک قبلی Ash) — لیست کامل: تب محصولات (وضعیت موجودی/وضعیت مکانی/نوع/sort + ردیف اکسپند فروش/تسویه/ارجاع/برگشت/ویرایش/حذف)، تب متریال (هاید‌آنهاید/وضعیت موجودی/sort + ردیف اکسپند افزودن‌به‌محصولات/ویرایش/حذف)، تب گالری (وضعیت تسویه/sort)، تب حسابداری بخش فاکتورها (sort)، تب کاتالوگ (وضعیت موجودی/نوع/وضعیت مکانی/sort) ✅ (Ash 🟡) — **این بار یه باگ واقعی پیدا شد:** `AccountingTab.jsx`'s `T.chip` (استایل پایه‌ی چیپ/دکمه‌های همون فایل) برخلاف چهارتا فایل دیگه (`ProductTab`/`MaterialTab`/`GalleryTab` که `height:32, minHeight:32, boxSizing:border-box` داشتن) فقط `padding:"4px 9px"` داشت بدون هیچ ارتفاع ثابتی — یعنی دکمه‌ی Sort فاکتورها (و همه‌ی چیپ‌های دیگه‌ی همون فایل) واقعاً چندپیکسل کوتاه‌تر از بقیه بودن. `T.chip` دقیقاً هم‌شکل بقیه‌ی فایل‌ها شد (`height:32, minHeight:32, boxSizing:border-box, whiteSpace:nowrap, padding:"6px 9px"`). بقیه‌ی دکمه‌های لیست‌شده (تب محصولات/متریال/گالری/کاتالوگ) رو دوباره تک‌تک چک کردم، همه از قبل `S.chip`/`T.chip` درست (۳۲px) داشتن — فقط همین یکی مونده بود.
4. [x] توی پاپ‌آپ‌های Sort (کاتالوگ/گالری/محصولات/متریال/فاکتور حسابداری) جای آیکون و توضیحش برعکسه ✅ (Ash 🟡) — `ProductTab.jsx`/`MaterialTab.jsx`/`GalleryTab.jsx`: ترتیب رندر از «لیبل سپس آیکون» به «آیکون سپس لیبل» عوض شد تا با ترتیب از‌قبل‌موجودِ `AccountingTab.jsx` (که همیشه آیکون-اول بود) یکی بشه؛ همچنین توی پاپ‌آپ Sort تب محصولات، اسم «دسته‌بندی» به «بر اساس فرش» و «گروه‌بندی بر اساس نوع محصول» به «بر اساس نوع» کوتاه شد.
5. [x] تب محصولات/متریال: پاپ‌آپ فیلتر «نوع» موقع اسکرول باید فلوتینگ بمونه (نه یه ردیف کامل رو اشغال کنه)، درست زیر دکمه‌ی Sort ✅ (Logy) — پاپ‌آپ فیلتر «نوع» قبلاً از `FilterPopup` استفاده می‌کرد که یه مودال ثابتِ وسط‌صفحه با پس‌زمینه‌ی تیره‌ست (خوب برای Sort/دسته‌بندی، ولی نه چیزی که کاربر برای این فیلتر خواسته بود). یه کامپوننت جدید `AnchoredFloatingPopup` (`FilterPopup.jsx`) اضافه شد: با یه `ref` به دکمه‌ی بازکننده، دقیقاً زیر همون دکمه (`getBoundingClientRect().bottom`) می‌شینه، پس‌زمینه‌ش شفافه (نه تیره)، و موقع اسکرول/تغییر اندازه دوباره موقعیتش حساب می‌شه. سه‌جا وصل شد: `ProductTab.jsx` (هم بخش کاتالوگ هم بخش محصولات) و `MaterialTab.jsx`. `esbuild` روی هر سه فایل چک شد، خطای سینتکسی نیست.
🔴 لطفاً تست کن: تب محصولات (هم کاتالوگ هم لیست محصولات) و تب متریال → دکمه‌ی «نوع» رو بزن، باید یه پاپ‌آپ کوچیک دقیقاً زیر همون دکمه باز بشه (نه وسط صفحه با پس‌زمینه‌ی تیره)، و با اسکرول یا کلیک بیرون درست ببنده.
6. [x] موقع خروج از پنل مدیریت، یه پاپ‌آپ وسط صفحه برای تاییدِ خروج بیاد ✅ (Ash 🟡) — `App.jsx` (کامپوننت پنل مدیریت): دکمه‌ی «خروج» دیگه مستقیم `onClose` صدا نمی‌زنه؛ اول `showExitConfirm` باز می‌شه (دقیقاً همون استایل مودال «تأیید خروج»ی که فرم‌های محصول/متریال/گالری برای تغییرات ذخیره‌نشده استفاده می‌کنن)، با دکمه‌ی «انصراف»/«خروج»؛ فقط با تأیید صریح `confirmExit` واقعاً پنل رو قفل و بسته می‌کنه.
7. [x] فرم افزودن/ویرایش محصول: بین نام محصول و کد، یه دکمه‌ی کوچیک select/deselect به اسم «کالیگرافی» اضافه بشه — اگه فعال باشه خودکار توی «نوع محصول» یه نوع «کالیگرافی» در نظر گرفته بشه (هم توی تب محصولات هم کاتالوگ نمایش داده بشه)؛ توی export/import اکسل و JSON هم ذخیره بشه ✅ (Logy، بعداً به تگ مستقل تبدیل شد، export/import کاملش Wall 🟣) — `ProductTab.jsx`: دکمه کار می‌کنه، و بعداً (طبق فیدبک کاربر که کالیگرافی نباید نوع اصلی محصول رو عوض کنه) به یه فیلد کاملاً مستقل (`isCalligraphy`) تبدیل شد که هیچ‌وقت `productTypeId` رو دست نمی‌زنه. **ولی همین جدا شدن یه گپ واقعی درست کرده بود:** ادعای قبلی این آیتم («export/import خودکار پوشش داده می‌شه») فقط برای نسخه‌ی اولیه درست بود (وقتی کالیگرافی مستقیم روی `productTypeId` می‌نشست، همون ستون موجود پوشش می‌داد)؛ بعد از تبدیل به فیلد مستقل، دیگه هیچ ستونی توی اکسل نداشت. **فیکس شد (Wall):** یه ستون جدید «کالیگرافی؟» (آخرین ستون، ایندکس ۳۵) به هدر/ردیف/ایمپورت شیت محصولات اضافه شد؛ JSON از قبل مشکلی نداشت چون `handleExportJson` کل `data` رو spread می‌کنه (فیلدهای محصول رو بدون نیاز به لیست دستی می‌بره). `esbuild` روی `App.jsx` چک شد.
🔴 لطفاً یه محصول با کالیگرافی فعال رو export (اکسل) و دوباره import کن، مطمئن شو تگ کالیگرافی برمی‌گرده (نه فقط نوع محصول).
8. [x] انتخاب تصاویر موقع افزودن/ویرایش محصول باید چندانتخابی باشه (نه یکی‌یکی)؛ عکس‌هایی که اسم فارسی دارن موقع لود فاکتور ذخیره نمی‌شن — به‌جای اسم فارسی، فایل‌ها با فرمت `کد-شماره.پسوند` (مثلاً `000401.jpg`, `000402.jpg`, `001101.jpg`) کپی/ذخیره بشن ✅ (Logy) — دو مسیر آپلود عکس محصول جدا از هم بودن، هر دو فیکس شدن:
    - **فرم ویرایش/افزودن محصول (`ProductTab.jsx`, `handleImageUpload`):** ورودی فایل `multiple` گرفت؛ اسم فایل قبلاً از روی نام محصول (فارسی) + timestamp ساخته می‌شد (`safeName_timestamp.jpg`) — دقیقاً همون چیزی که کاربر گزارش داد مشکل داره. الان اسم فایل همیشه `کد(۴رقمی)+شماره‌ترتیبی(۲رقمی).jpg`ه (مثلاً کد ۴ → `000401.jpg`, `000402.jpg`) — چون کد یکتاست و همیشه لاتین/عددیه، هم مشکل فارسی حل می‌شه هم تضمین می‌شه اسم فایل با محصول دیگه‌ای تداخل نداره؛ شماره‌ی ترتیبی از تعداد عکس‌های موجودِ همون محصول شروع می‌شه (نه همیشه از ۱)، تا عکس‌های قبلی بازنویسی نشن.
    - **افزودن سریع عکس از روی کارت محصول (`App.jsx`, `handleImageUpload`):** این مسیر یه باگ جدا و بزرگ‌تر داشت — اصلاً از سیستم ذخیره‌ی محلی (`saveImageToFolder`) استفاده نمی‌کرد، مستقیم دیتای خام base64 رو توی `p.image`/`p.images` می‌ذاشت (نه اسم فایل)؛ چندانتخابی هم نبود. الان دقیقاً هماهنگ با فرم ویرایش شد: چندانتخابی + همون فرمت اسم‌گذاری کد-محور + ذخیره‌ی واقعی توی پوشه‌ی محلی به‌جای base64 خام توی دیتای محصول (که حجم دیتای ذخیره‌شده رو هم کاهش می‌ده).

`esbuild` روی `App.jsx` و `ProductTab.jsx` چک شد، خطای سینتکسی نیست.
🔴 لطفاً تست کن: (۱) دکمه‌ی «کالیگرافی» رو بزن، مطمئن شو توی «نوع محصول» انتخاب می‌شه و توی فیلتر/گروه‌بندی نوع هم می‌بینیش؛ (۲) موقع افزودن عکس (هم از فرم ویرایش هم از دکمه‌ی روی کارت) چند عکس با هم انتخاب کن، ببین همه اضافه می‌شن؛ (۳) یه فاکتور با محصولی که عکس داره لود کن، مطمئن شو عکس درست نمایش داده می‌شه (اسم فایل دیگه فارسی نیست).

**لاگ نشست Ash 🟡 (این نشست):** موارد ۳، ۴، ۶ کامل شدن (جزئیات بالای هرکدوم). `esbuild` روی `AccountingTab.jsx`, `ProductTab.jsx`, `MaterialTab.jsx`, `GalleryTab.jsx`, `App.jsx` چک شد، مشکلی نیست. مورد ۲ (خطای import اکسل) و بقیه (۵، ۷، ۸) هنوز باز موندن — مورد ۲ نیاز به بررسی دقیق‌تر کد export/import داره تا مشخص بشه ریشه‌ی خطا کجاست، ۷ و ۸ فیچرهای نسبتاً بزرگ‌ترن.
🔴 لطفاً مورد ۳ رو حتماً با یه build تازه تست کن (قبلی‌ها از قبل درست بودن، این یکی واقعاً کد عوض شد) و مورد ۶ رو هم تست کن (دکمه‌ی خروج پنل مدیریت).

## 🆕 فیدبک کاربر روی لیبل شناور + فرمت دسته‌بندی فرش + ترتیب ابعاد (این نشست 🟠 Logy)

> کاربر روی کار قبلی Ref/Wall (لیبل شناور دسته‌بندی) فیدبک داد که پیاده‌سازی اولیه ناقص بود.

1. [x] لیبل شناور دسته‌بندی (تب متریال + تب محصولات): **قرار نبود وسط باشه، سمت چپ صفحه** باشه؛ **زیر هدر** بمونه (این بخش از قبل درست بود)؛ وقتی اسکرول بالای بالاست یا اسم خودِ دسته‌بندی روی صفحه مشخصه باید هاید بشه (این بخش هم از قبل درست پیاده‌سازی شده بود)؛ و روش که بزنیم باید دقیقاً به‌اندازه‌ای اسکرول برگرده بالا که خودش هاید بشه و اسم دسته‌بندی معلوم بشه ✅ (Logy) — `MaterialTab.jsx` + `ProductTab.jsx`:
   - **پوزیشن:** `marginLeft:"auto", marginRight:"auto"` (وسط‌چین) بود، به `marginLeft:0, marginRight:"auto"` تغییر کرد (سمت چپ فیزیکی صفحه، مستقل از `dir="rtl"`)
   - **کلیک روی لیبل:** قبلاً `el.scrollIntoView({block:"start"})` بود که هدر گروه رو دقیقاً به بالای کادر اسکرول‌شونده می‌چسبوند — که چون یه نوار sticky بالای همون کادره، ممکن بود هدر گروه هنوز پشتِ نوار sticky بمونه و لیبل بازم هاید نشه. الان به‌جاش دقیقاً فاصله‌ی لازم (`elTop - headerBottom - 6`) با `scrollBy` حساب و اسکرول می‌شه — تضمین می‌کنه هدر گروه از پشتِ نوار sticky میاد بیرون
2. [x] دسته‌بندی لیستی تب محصولات، بر اساس فرش: فرمت باید «فرش [نام فرش]، (طرح [نام طرح]) [عدد] ساله» باشه (مثلاً «فرش یزد، (طرح کاشان) 50 ساله») ✅ (Logy) — **باگ واقعی پیدا شد:** `formatFabricGroupLabel` (`mathCore.js`) این فرمت رو از قبل داشت، ولی با رجکس سعی می‌کرد «(طرح ...)» و «...ساله» رو از **توی خودِ رشته‌ی اسم متریال** پیدا کنه — در حالی که طرح و قدمت فرش فیلدهای کاملاً جدا هستن (`mat.pattern`, `mat.ageYears`، دقیقاً همونایی که توی فرم ویرایش متریال/بزرگ‌نمایی جدا نمایش داده می‌شن)، نه بخشی از متن اسم. یعنی این رجکس عملاً هیچ‌وقت match نمی‌شد و فرمت همیشه fallback می‌کرد به فقط «فرش [اسم خام]». الان تابع مستقیم `mat.pattern` و `mat.ageYears` رو می‌گیره (نه رجکس‌زدن روی اسم) و همون فرمت خواسته‌شده رو می‌سازه (با فاصله‌ی درست: «۵۰ ساله» نه «۵۰ساله»). `resolveProductGroupName` هم آپدیت شد که این دو فیلد رو پاس بده.
3. [x] ابعاد محصولات موقع ذخیره/نمایش باید خودکار **Big×Small** بشن (عدد بزرگ‌تر اول)؛ کاربر گزارش داد الان برعکسه (Small×Big) ✅ (Logy) — **باگ واقعی، از یه تصمیم اشتباهِ نشست قبلی:** توی کد یه کامنت صریح بود «طبق تصمیم تازه‌ی کاربر... حالا باید کوچیک‌تر اول باشه» — یعنی یه نشست قبلی این منطق رو عمداً برعکس کرده بود، ولی طبق این پیام کاربر اون تصمیم اشتباه بوده. دو جا همین منطق بود، هردو برگردونده شدن به Big×Small: (۱) `ProductTab.jsx` لحظه‌ی ذخیره‌ی فرم محصول (۲) `App.jsx` لحظه‌ی لود دیتا (یه مهاجرت خودکاره که روی *همه‌ی* محصولات موجود هم اجرا می‌شه، نه فقط محصول‌های جدید — یعنی قبلاً هر بار اپ باز می‌شد داشت دوباره به حالت اشتباه (Small×Big) برمی‌گردوند).

`esbuild` روی هر سه فایل (`App.jsx`, `ProductTab.jsx`, `MaterialTab.jsx`) چک شد، خطای سینتکسی نیست.
🔴 لطفاً حتماً تست کن: (۱) توی تب متریال/محصولات اسکرول کن، لیبل خاکستری سمت چپ صفحه دیده بشه، روش بزن ببین دقیقاً به‌اندازه‌ای برمی‌گرده بالا که خودش گم بشه و اسم دسته دیده بشه؛ (۲) یه محصول با فرش دارای طرح+قدمت پر رو ببین دسته‌بندیش الان درست نوشته می‌شه؛ (۳) یه محصول جدید با ابعاد نامرتب (مثلاً عرض بزرگ‌تر از طول) بساز و ببین بعد از ذخیره بزرگ‌تر اول میاد.

> **لاگ Ref ⚫ — هماهنگ‌سازی با کار موازی + یه باگ حیاتی جدا پیدا شد:**
> این ۳ آیتم رو من هم مستقل (موازی با Logy، قبل از دیدن کار همدیگه) کار کرده بودم. بعد از merge:
> - **لیبل شناور:** هر دو نسخه‌ی کلیک تقریباً یکی بودن؛ نسخه‌ی ساده‌تر (بدون شرط اضافی `panel.scrollTop > 0`) نگه داشته شد.
> - **فرمت فرش:** نسخه‌ی من inline توی `App.jsx` بود، نسخه‌ی Logy توی `mathCore.js` (`formatFabricGroupLabel`, exported). یکی‌شون کردم: الان `App.jsx` مستقیم همون تابع مشترک رو صدا می‌زنه، منطق تکراری حذف شد.
> - **ترتیب ابعاد:** فیکس Logy (سطح دیتا: هم لحظه‌ی ذخیره‌ی فرم، هم مهاجرت خودکار موقع لود) از فیکس من (فقط سطح نمایش، دو جایی که خام `product.dims` رو نشون می‌دادن بدون رد شدن از `formatProductDims`: `MaterialTab.jsx` خط ۱۰۵۸/۱۲۴۸ و `WoodCuttingTab.jsx`) کامل‌تره چون ریشه‌ای حلش می‌کنه. هر دو فیکس با هم تداخلی ندارن و هر دو نگه داشته شدن (فیکس نمایش من الان عملاً یه لایه‌ی دفاعیِ اضافیه، ضرری نداره).
>
> **علاوه بر این، یه باگ کاملاً جدا و حیاتی هم پیدا و فیکس شد (کاربر فایل بک‌آپ واقعیش رو فرستاد تا مشکل ایمپورت اکسل رو بررسی کنم):**
> اکسپورت اکسل متریال دو ستون جدید («قدمت (سال)» و «طرح فرش») بین «تاریخ خرید» و «بچ‌های شارژ شده» اضافه کرده بود (برای همین فیچر فرمت فرش)، که همه‌چی بعدش (batches/sticks/procurements/linkedProductIds/updatedAt/remainingCost/totalQty/remainingQty/creditAllowed/isUsableRemaining) رو ۲ ستون به سمت راست هل داده بود. ولی ایمپورت (`App.jsx`, `handleImportExcel`) هنوز با اندیس‌های قدیمی می‌خوند — یعنی داشت مقدار «قدمت» (یه عدد ساده مثل ۵۰) رو به‌جای «بچ‌ها» JSON.parse می‌کرد. `JSON.parse(50)` بدون خطا خودِ عدد ۵۰ رو برمی‌گردوند (نه آرایه)، ولی خط بعدی بدون شرط `.map()` روش صدا می‌زد → `TypeError: batches.map is not a function`، یه throw خام وسط `forEach` که هیچ try/catch بیرونی نداشت و کل عملیات ایمپورت رو (نه فقط همون متریال) بی‌صدا می‌ترکوند. **تأیید شد با بک‌آپ واقعیِ خودِ کاربر** (اکسل ۸۹ متریالی): دقیقاً همون ۱۰ متریال فرش‌داری که «قدمت» پرشده داشتن (یزد، ترکمن، تبریز مرند، ...) باعث کرش می‌شدن. فیکس: اندیس‌های خوندن (batches=۱۹، sticks=۲۰، procurements=۲۱، linkedProductIds=۲۲، updatedAt=۲۳، remainingCost=۲۴، totalQty=۲۵، remainingQty=۲۶، creditAllowed=۲۷، isUsableRemaining=۲۸) با ستون‌های واقعی export یکی شدن؛ قدمت/طرح هم مستقیم از ستون‌های اختصاصی‌شون (۱۷،۱۸) خونده می‌شن به‌جای حدس‌زدن با regex از روی اسم. با همون فایل بک‌آپ واقعی دوباره تست شد: صفر خطا روی هر ۸۹ متریال.
> 🔴 نیاز به تست واقعی: همون فایل بک‌آپ اکسلی که فرستادی رو از تنظیمات → ایمپورت اکسل امتحان کن، باید بدون خطا کامل بشه و متریال‌های فرش (یزد/ترکمن/...) هم قدمت/طرحشون درست بیاد.

---

## 🆕 لاگ نشست Logy 🟠 (نشست جدید — دامپ عکس‌ها + توضیحات کاربر)

از دامپ جدید کاربر (۹ اسکرین‌شات + توضیحات)، ۵ مورد بررسی/فیکس شد:

1. **کرش «getImageUrl is not defined» توی گالری (موقع فاکتور از سبد گالری)** — علت پیدا شد: `GalleryTab.jsx` تابع `getImageUrl` (فایل قدیمی `imageHelpers.js`، اصلاً سازگار با سیستم فعلی ذخیره‌سازی عکس نیست) رو صدا می‌زد بدون این‌که import کرده باشه — همیشه کرش می‌کرد. با یه کامپوننت جدید `GalleryProductThumb` (دقیقاً هم‌الگو با `ProductImage` توی `ProductTab.jsx`) جایگزین شد که از `useResolvedImageSrc` واقعی استفاده می‌کنه.
2. **عکس محصول توی فاکتور چاپی لود نمی‌شد** — `InvoiceTemplate.jsx` مستقیم `item.image` (که توی سیستم جدید فقط اسم فایله، نه یه src قابل‌استفاده) رو توی `<img src>` می‌ذاشت. `InvoiceItemImage` جدید اضافه شد که resolve واقعی انجام می‌ده (دقیقاً هم‌الگو با resolve کد QR که همون فایل از قبل داشت).
3. **باگ کالیگرافی — جایگزین‌شدن نوع اصلی محصول:** قبلاً دکمه‌ی «کالیگرافی» با فعال‌شدن مستقیم `productTypeId` محصول رو عوض می‌کرد (یعنی نوع اصلی مثل تابلوفرش پاک می‌شد). طبق تصمیم درست کاربر: کالیگرافی باید یه **تگ اضافه** باشه، نه جایگزین. یه فیلد مستقل `isCalligraphy` به `emptyProduct()` (`dataModels.js`) اضافه شد؛ دکمه فقط همینو toggle می‌کنه، `productTypeId` دیگه دست‌نخورده می‌مونه. فیلتر «نوع» (هم کاتالوگ هم تب محصولات) یه گزینه‌ی جدا «کالیگرافی» (با شناسه‌ی مصنوعی `__calligraphy__`) گرفته که با OR روی نوع اصلی فیلتر می‌کنه — یعنی محصول هم‌زمان زیر نوع اصلی‌ش و هم زیر کالیگرافی قابل‌دیدنه.
   - ⚠️ **export/import اکسل عمداً دست نزدم:** ستون‌های اکسل محصولات به‌صورت آرایه‌ی positional هستن و قبلاً دقیقاً همین الگو (اضافه‌کردن ستون بدون آپدیت index خوندن) یه باگ بحرانی (column-shift) ایجاد کرده بود که توی همین فایل مستند شده. اگه می‌خوای `isCalligraphy` هم توی اکسل ذخیره/بازیابی بشه، بگو تا با احتیاط کامل (هم export هم import هم‌زمان) اضافه‌ش کنم.
4. **پاپ‌آپ فیلتر «نوع» وسط‌صفحه باز نمی‌شد:** یه نشست قبلی این پاپ‌آپ رو از مدل وسط‌صفحه‌ی `FilterPopup` به `AnchoredFloatingPopup` (فلوتینگ زیر دکمه) تغییر داده بود. طبق دامپ جدید، کاربر دقیقاً برعکسشو می‌خواد (مثل بقیه‌ی فیلترها وسط‌صفحه). هر ۳ نمونه (کاتالوگ، تب محصولات، تب متریال) به `FilterPopup` برگردوندن شد.
5. **تشخیص خودکار عکس‌های موجود در پوشه بر اساس کد:** `imageStorage.js` یه تابع جدید `autoDetectImagesForCode(code)` گرفت که پوشه‌ی عکس‌ها (native: `Filesystem.readdir`؛ وب: کلیدهای IndexedDB) رو می‌گرده و فایل‌هایی با الگوی `{کد۴رقمی}{شماره۲رقمی}.ext` (دقیقاً همون قراردادی که آپلود از داخل اپ از قبل تولید می‌کنه، مثلاً `000401.jpg`) رو تا سقف ۹۹ عکس پیدا می‌کنه. `ProductEditor` (`ProductTab.jsx`) موقع باز شدن فرم یه‌بار خودکار این اسکن رو اجرا می‌کنه (فقط اضافه می‌کنه، چیزی حذف نمی‌کنه) و یه دکمه‌ی دستی «بررسی پوشه» هم کنار دکمه‌ی «افزودن» عکس اضافه شد برای اسکن مجدد در هر لحظه.

همه‌ی فایل‌های تغییریافته (`GalleryTab.jsx`, `InvoiceTemplate.jsx`, `ProductTab.jsx`, `MaterialTab.jsx`, `dataModels.js`, `utils/imageStorage.js`) با `esbuild` چک شدن، خطای سینتکسی نیست.

🔴 **موارد بازِ باقی‌مونده از همین دامپ که دست نزدم:**
- ادعای «ارتفاع دکمه‌های هدر/اکسپند کم نشده» — کد فعلی (`S.chip`/`T.chip`, height:32) طبق چند بار بازبینی مستقل قبلی (بخش‌های بالاتر همین فایل) از قبل یکدست بود؛ به‌نظر می‌رسه اسکرین‌شات مربوط به یه build قدیمی‌تر از گوشی باشه (کاربر خودش هم گفت عکس تب متریالی که فرستاد قدیمیه). بعد از این پوش لطفاً یه بیلد تازه بگیر و اگه بازم دقیقاً همون دکمه فرق داشت، بگو کدوم دکمه‌ی خاص.
- پس‌زمینه‌ی «کادر مشکی زیر پاپ‌آپ نوع» باید با فیکس شماره‌ی ۴ بالا حل شده باشه (چون `FilterPopup` خودش پس‌زمینه‌ی نیمه‌شفاف استاندارد داره)؛ تست چشمی لازم داره.

## 🆕 پیگیری کاربر روی «دکمه‌های اکسپند باریک نیستن» (این نشست، Ash 🟡)

کاربر بعد از پیام قبلی (که گفته بودم کد از قبل همه‌جا `height:32` یکدسته) دوباره پرسید چرا دکمه‌های اکسپندی که اسم برد بازم باریک نیستن. این بار به‌جای تکرار همون جواب، دونه‌دونه‌ی همون دکمه‌ها رو با `grep` دقیق پیدا کردم (نه فقط `S.chip`/`T.chip` رو چک کردم، بلکه هرجا این استایل‌ها بعداً override شدن) و **یه باگ واقعی پیدا شد:**

- **`MaterialTab.jsx` خط ۲۳۱۶ (دکمه‌ی «افزودن به محصولات» / بولک، توی ردیف اکسپند متریال):** برخلاف دکمه‌های ویرایش/حذف کنارش، این دکمه `flex: 1` صریح داشت — یعنی برخلاف بقیه (که باریک/به‌اندازه‌ی محتوا بودن)، این یکی عمداً کل عرض باقی‌مونده‌ی ردیف رو پر می‌کرد. دقیقاً همینه که کاربر می‌دید و می‌گفت «باریک نیست». `flex:1` حذف شد؛ الان این دکمه هم فقط به‌اندازه‌ی متن خودش عرض می‌گیره، و `justifyContent:"space-between"` روی خودِ ردیف باعث می‌شه دکمه‌های ویرایش/حذف سمت راست بمونن (به‌جای این‌که با گپ بزرگ وسط جدا بشن).
- بقیه‌ی جاهایی که `flex:1` روی `S.chip`/`T.chip` پیدا شد (`GalleryTab.jsx` تسویه‌شده/تسویه‌نشده، `ProductTab.jsx` تسویه‌شده/تسویه‌نشده، `WoodCuttingTab.jsx` نوع فریم، `BusinessCardModal.jsx`) همه یه سوییچ/تاگل **دوگزینه‌ای** هستن (نه یه دکمه‌ی تکی کنار چندتا آیکون) — این‌ها عمداً هم‌عرض طراحی شدن (مثل یه tab switch)، جزو چیزی که کاربر بهش اشاره کرد نیستن، دست نخوردن.
- تب محصولات (ردیف اکسپند فروش/تسویه/ارجاع/برگشت/ویرایش/حذف): دوباره خط‌به‌خط چک شد، هیچ `flex:1` روی دکمه‌های تکی پیدا نشد — فقط *گروه* دکمه‌های وضعیت (که می‌تونن چندتایی و متغیر باشن) یه `flex:1` روی *wrapper*شون داره تا بتونن `flexWrap` کنن، ولی خودِ دکمه‌ها تکی تکی به‌اندازه‌ی متنشونن، نه استرچ‌شده.

`esbuild` روی `MaterialTab.jsx` چک شد، مشکلی نیست.

🔴 لطفاً با یه build تازه دوباره چک کن، مخصوصاً ردیف «افزودن به محصولات / ویرایش / حذف» توی اکسپند متریال — الان باید دکمه‌ی افزودن‌به‌محصولات فقط به‌اندازه‌ی متنش عریض باشه، نه کل ردیف.

### 🆕 دسته‌ی فیدبک کاربر (اسکرین‌شات‌های تب متریال/محصولات) — ✅ (Ref ⚫)
- [x] دکمه‌ی «افزودن به محصولات» (بولک) تب متریال — عریض نبود، فضای خالی کنارش می‌موند. نشست قبلی (Ash) به‌اشتباه فکر کرده بود مشکل «باریک‌نبودن دکمه‌های اکسپند» همینه و `flex:1` رو از این دکمه برداشته بود — که برعکس، این دکمه باید عریض بمونه، فقط ویرایش/حذف کناریش باید باریک باشن (که از قبل بودن). `flex:1` برگشت.
- [x] **باگ واقعی:** دکمه‌ی «بر اساس فرش» (گروه‌بندی تب محصولات) هم‌زمان با «بر اساس نوع» روشن می‌شد و بعد از رفتن روی حالت «نوع»، دیگه نمی‌شد باهاش برگشت به «فرش» — چون شرط روشن‌بودنش فقط `groupedView` بود (بدون چک `sortMode`)، و کلیکش هم فقط وقتی کاری می‌کرد که `groupedView` از قبل خاموش بود. الان شرط روشن‌بودن `!groupByTypeActive && groupedView`ه (منحصربه‌فرد با دکمه‌ی نوع) و کلیکش صریحاً `sortMode` رو برمی‌گردونه به `"fabric"`.
- [x] لیبل شناور دسته‌بندی (تب متریال/محصولات) — هنوز یه نوار مشکی عریض زیرش بود چون داخل flow خودِ بلوک sticky هدر بود (پس‌زمینه‌ی تمام‌عرض اون بلوک کش می‌اومد پایین‌تر). الان `position:absolute` (نه توی flow)، کاملاً مستقل شناور، فقط خودِ حباب کوچیک پس‌زمینه داره — مثل دکمه‌ی اسکرول‌به‌بالا/FAB.
- [x] دکمه‌ی جدید «بررسی پوشه‌ی عکس» در تب همگام‌سازی — یکجا همه‌ی محصولات رو با کدشون توی پوشه‌ی عکس می‌گرده و عکس‌های جدید (طبق قرارداد نام‌گذاری کد+شماره) رو خودکار وصل می‌کنه؛ همون منطق تک‌محصولیِ `autoDetectImagesForCode` که از قبل بود، الان بولک هم داره.
- [x] **باگ واقعی — عکس محصول توی فاکتور (Save/Share/Print) لود نمی‌شد:** `InvoiceItemImage` عکس رو async resolve می‌کنه (`useResolvedImageSrc`، از IndexedDB/فایل‌سیستم)، ولی `paperElement.cloneNode(true)` یه اسنپ‌شات لحظه‌ایه. اگه کاربر زود دکمه‌ی ذخیره/اشتراک/چاپ رو می‌زد، عکس‌هایی که هنوز resolve نشده بودن به‌شکل پلاسهولدر «بدون عکس» برای همیشه توی PDF/عکس نهایی ثبت می‌شدن، حتی اگه یه لحظه بعد لود می‌شدن. فیکس: پلاسهولدرِ در-حال-لود با `data-resolving="true"` مشخص شد، و هر ۳ مسیر capture (`handleSaveAsPDF`, `handleSaveAsImage`, `captureCanvas`) قبل از `cloneNode` صبر می‌کنن تا هیچ پلاسهولدر در-حال-لودی نمونده باشه (حداکثر ۳ ثانیه).
- ⏸️ **بررسی نشد (نیاز به مرجع دقیق‌تر):** ادعای «دکمه‌های هدر/اکسپند هنوز باریک نشدن» — سه نشست قبلی (Ash ۲ بار، Logy) این رو با جزئیات بررسی کردن و همه‌جا `S.chip`/`T.chip` رو `height:32,minHeight:32,boxSizing:border-box` صریح پیدا کردن. یه چهارمین بار حدس‌زدن بی‌فایده‌ست — اگه رو گوشی واقعی هنوز فرقی می‌بینی، لطفاً بگو دقیقاً کدوم دکمه (اسکرین‌شات با انگشت گذاشته‌شده روش کافیه) تا مستقیم برم سراغش با اندازه‌گیری واقعی.
- ⏸️ **بررسی نشد:** رنگ نوتیفیکیشن حذف/آزادسازی متریال (قرمز/زرد/آبی) — نیاز به توضیح دقیق‌تر که کدوم عملیات (حذف بچ؟ آزادسازی قفل؟) چه رنگی باید بده و کجا این قرارداد رنگ اول تعریف شده بود.
🔴 نیاز به تست واقعی همه‌ی موارد بالا، مخصوصاً فاکتور با عکس (Save PDF/Image و Share) روی چند محصول که عکس دارن.

### 🆕 ارتفاع دکمه‌ها یک‌سوم کم بشه — ✅ (Ref ⚫، با اسکرین‌شات مرجع دقیق)
بعد از ۳ بار «بررسی شد، همه‌جا ۳۲px بود» (Ash×۲, Logy)، کاربر یه اسکرین‌شات مشخص فرستاد (ردیف آیکون‌های بالای تب محصولات: کیف/سورت/تگ/لوکیشن/چک‌مارک) و گفت اینا هنوز باریک نشدن، ارتفاعشون باید **یک‌سوم کم بشه** — یعنی بحث دیگه سر این نبود که کد چی می‌گه (۳۲px که همیشه هم بوده)، بلکه خواسته این بود که از همون ۳۲px هم کمتر بشه:
- [x] `S.chip`/`T.chip` توی هر ۴ فایل (`ProductTab.jsx`, `MaterialTab.jsx`, `GalleryTab.jsx`, `AccountingTab.jsx`) از `height:32,minHeight:32,padding:"6px 9px"` به `height:22,minHeight:22,padding:"2px 9px"` تغییر کرد (borderRadius هم ۱۲→۱۱ برای تناسب). این یعنی همه‌ی دکمه‌هایی که از این استایل مشترک استفاده می‌کنن — ردیف فیلتر آیکونی بالای هر تب، Sort، ویرایش/حذف/چشم اکسپند محصولات و متریال، افزودن‌به‌محصولات، فروخته‌شد/به‌گالری، تسویه، و همه‌ی چیپ‌های دیگه — یکجا و یکدست ۱۰px کوتاه‌تر شدن.
- [x] دو تا `iconBtn` صریح (`width:32,height:32`) که مستقل از `S.chip`/`T.chip` بودن (هاید/آن‌هاید متریال در `MaterialTab.jsx`، ویرایش/حذف مشتری در `GalleryTab.jsx`) هم به `22` تغییر کردن تا هم‌سایز بمونن.
🔴 نیاز به تست واقعی روی گوشی: اگه بازم بلندتر از انتظارت بود، بگو دقیقاً چند پیکسل (یا نسبت به چی) می‌خوای — الان یه عدد ثابت (۲۲px) داریم که می‌شه راحت باز هم عوضش کرد.

---

## 🔒 WIP رزرو‌شده برای Ref ⚫ — لطفاً کسی دیگه دست نزنه (آیتم‌های ۶، ۱۰، ۱۱ از دامپ اخیر کاربر)

Ref وسط این کار سشنش قطع شد و چون هر نشست توی sandbox جدا/موقتی اجرا می‌شه، ادیت‌های نصفه‌کاره‌ش هیچ‌جا ذخیره نشده بودن (نه push شده بودن، نه قابل بازیابی). طبق دستور مالک، اینجا فقط **مستندسازی همون چیزی که Ref قبلاً توضیح داده بود** می‌کنم (از تراسکریپت رد و بدل‌شده)، تا وقتی Ref برگرده دقیقاً همون مسیر رو ادامه بده — من (Logy) عمداً هیچ کدی برای این ۴ آیتم ننوشتم.

### آیتم ۶ — منطق آزادسازی بولک متریال قفل‌شده (مهم‌ترین، مالک خودش تشخیص داده)
فایل: `MaterialTab.jsx`, تابع `queueSessionRelease` (خط ~۷۲۸ به بعد) داخل `BulkApplyMaterialPage`.

**باگ ریشه‌ای که همین بررسی الان پیدا شد** (برای Ref، تا زودتر برسه به جواب): تابع فعلی `queueSessionRelease`:
1. مستقیم `setData` صدا می‌زنه — یعنی حذف یه محصول از سشن قفل، **بلافاصله و دائمی** روی دیتای اصلی اعمال می‌شه، نه یه چیز staged/موقت. این دقیقاً همون «مستقیم اعلام می‌کنه» ایه که مالک گزارش داد.
2. بعد از `pendingUnlock:true` گذاشتن روی محصول حذف‌شده، یه لوپ (`remaining.forEach`) هزینه‌ی همون محصول رو **بین بقیه‌ی محصولات قفل‌شده‌ی همون سشن بازتقسیم می‌کنه** (`deductedCost`/`customPct` بقیه رو تغییر می‌ده). طبق تشخیص صریح مالک این غلطه: باید فقط سهم محصولِ حذف‌شده آزاد بشه، درصد/هزینه‌ی بقیه‌ی محصولات قفل‌شده‌ی سشن **اصلاً نباید تغییر کنه**.

راه‌حلی که Ref داشت پیاده می‌کرد (طبق پیام‌های رد و بدل‌شده با مالک): یه state لوکال (`pendingReleaseSet` یا مشابه) داخل `BulkApplyMaterialPage` اضافه بشه؛ کلیک روی دکمه‌ی X کنار محصولِ سشنِ قفل، به‌جای صدازدن مستقیم `queueSessionRelease` (که `setData` می‌زنه)، فقط توی همون state لوکال toggle بشه و ردیف محصول آبی/هایلایت بشه (بدون هیچ `setData`ای). دکمه‌ی «ذخیره» بالای پنل (`handleApply`, خط ۶۴۵) باید در کنار کاری که الان می‌کنه، تمام آیتم‌های توی `pendingReleaseSet` رو هم نهایی کنه (این‌جا `pendingUnlock:true` ست بشه) — **بدون** لوپ بازتقسیم روی بقیه‌ی سشن (اون لوپ باید کامل حذف بشه). دکمه‌ی «لغو» باید state لوکال رو دور بریزه و `onClose` صدا بزنه بدون هیچ `setData`ای (یعنی چیزی که الان هست، چون `queueSessionRelease` دیگه مستقیم صدا زده نمی‌شه، «لغو» خودکار درست می‌شه). دکمه‌ی X بالای پنل (خط ۷۷۹، `onClose` مستقیم) باید — فقط اگه `pendingReleaseSet` یا `selectedIds` تغییر داشته باشن نسبت به حالت اولیه — یه کانفرم («مطمئنی می‌خوای خارج بشی؟») نشون بده، شبیه الگوی `closeRequestRef`/`showDiscardConfirm` که توی `MaterialEditor` (همین فایل، خط ۲۴۲۵ به بعد) از قبل پیاده‌سازی شده — همون الگو رو عیناً کپی کن.

### آیتم ۹ — ✅ فیکس شد (Logy) — الان جزئیات پایین‌تر ثبت شده

### آیتم ۱۰ — ✅ فیکس شد (Logy، همراه با گزارش pinch-zoom)

### آیتم ۱۱ — undo برای آزادسازی متریال کار نمی‌کنه
اصلاً بررسی نشده بود. به‌احتمال زیاد بعد از فیکس آیتم ۶ (که ساختار staged/commit درست می‌شه) این خودش راحت‌تر قابل‌اضافه‌کردنه، چون الان که آزادسازی مستقیم و بی‌مرحله‌ست، یه undo واقعی سخته.

---

## آیتم‌های دیگه از همون دامپ اخیر که هنوز باز مونده (کسی هنوز شروع نکرده)
- آیتم ۳: جابجایی متن کادر وضعیت تسویه/هدایا موقع ذخیره‌ی PDF/عکس فاکتور (پیش‌نمایش درسته، خروجی غلطه)
- آیتم ۴ و ۷: کندی شدید ذخیره‌ی PDF/عکس فاکتور و نستینگ ۱D/۲D
- آیتم ۵: دکمه‌ی «بررسی عکس» تب همگام‌سازی محصول بدون‌لینک رو وصل نمی‌کنه؛ لینک‌کردن دستی از تب ویرایش هم کار نمی‌کنه؛ کپی‌کردن عکس باید حتماً از طریق اپ Sync بشه نه فایل‌منیجر مستقیم؛ حذف عکس از اپ باید از پوشه هم پاکش کنه

## آیتم ۹ — ✅ فیکس واقعی (Logy، نشست بعدی)
مالک درست حدس زده بود که Ref ادعای «فیکس و تایید شده» برای این آیتم داشته، ولی چون هیچ‌وقت push نشد، وقتی چک کردم (`src/App.jsx`) دقیقاً همون کد قدیمی/باگ‌دار بود. باگ واقعی و تایید‌شده: یه `useEffect` جدا توی کامپوننت بیرونی (App) با `pushBackHandler(() => setShowManagementPanel(false))` مستقیم پنل رو می‌بست، درحالی‌که خودِ دکمه‌ی «خروج» از `handleExit` → `showExitConfirm` (پاپ‌آپ تایید وسط صفحه) رد می‌شد — این دو مسیر کاملاً جدا از هم بودن.
فیکس: اون افکت بیرونی حذف شد. داخل خودِ `ManagementPanelModal` (جایی که `showExitConfirm`/`handleExit` تعریف شدن) دو تا افکت جدید اضافه شد:
- وقتی پنل باز و پاپ‌آپ تایید بسته‌ست → بک همون کاری رو می‌کنه که دکمه‌ی «خروج» می‌کنه (یعنی اول پاپ‌آپ تایید باز می‌شه، نه بستن مستقیم)
- وقتی پاپ‌آپ تایید بازه → بک فقط همون پاپ‌آپ رو می‌بنده (لغو)، پنل بسته نمی‌شه

`esbuild` روی `App.jsx` چک شد (با external کردن پلاگین‌های native)، خطای سینتکسی نیست.
🔴 تست لازم: از پنل مدیریت، دکمه‌ی بک سخت‌افزاری/سوایپ لبه رو بزن — باید پاپ‌آپ «تایید خروج» باز بشه، نه بستن مستقیم پنل. بعد همون‌جا دوباره بک بزن — باید فقط پاپ‌آپ بسته بشه، پنل بمونه.

## آیتم ۱۰ — ✅ فیکس شد (Logy) + باگ جدید pinch-zoom که همزمان گزارش شد

**آیتم ۱۰ — سه بخش:**
- **description موقع نبودن عکس نمایش داده نمی‌شد:** بررسی Ref درست بود که خودِ `ImageLightbox` (بخش پایین، اطلاعات محصول) هیچ شرطی نداشت که description رو به عکس‌داشتن گره بزنه — همیشه رندر می‌شد. باگ واقعی جای دیگه بود: توی `ProductCard` (ردیف تب محصولات مدیریتی، نه خودِ کاتالوگ عمومی که تایلش از قبل بدون شرط لایت‌باکس رو باز می‌کرد)، کلیک روی عکس کوچیک محصول با `if (p.image) onOpenLightbox(...)` گارد شده بود — یعنی برای محصول بدون عکس، کلیک اصلاً هیچی نمی‌کرد و لایت‌باکس (و در نتیجه description) هیچ‌وقت باز نمی‌شد. شرط حذف شد.
- **نام/کد بالای بزرگ‌نمایی بزرگ‌تر/بولدتر:** `fontSize:12` بدون بولد → `fontSize:14, fontWeight:700`، رنگ هم روشن‌تر شد (`#F5F0EB` به‌جای خاکستری کم‌رنگ `#ccc`).
- **خط خالی قبل از description:** یه `<div style={{height:10}}/>` قبل از بلوک description اضافه شد (فقط وقتی description واقعاً موجوده).

**باگ جدید (همزمان گزارش شد توسط کاربر) — پرش تصویر بعد از pinch-zoom:**
ریشه‌ی واقعی توی `ImageLightbox` (همون کامپوننت زوم کاتالوگ) پیدا شد: `lastPanRef` (مرجع موقعیت برای پن تک‌انگشتی) فقط توی حالت *یک*‌انگشتی آپدیت می‌شد، نه در طول pinch دو‌انگشتی. یعنی وقتی کاربر دو انگشتی زوم می‌کرد و یکی از دو انگشت رو زودتر ول می‌کرد، پوینتر تک باقی‌مونده حرکت بعدیش رو نسبت به یه موقعیت خیلی قدیمی (از قبل شروع pinch) حساب می‌کرد → یه جهش ناگهانی چندسانتی. فیکس: توی `onPointerUp`، وقتی از ۲ انگشت به ۱ انگشت می‌رسیم، `lastPanRef` رو صریحاً به موقعیت *فعلی* همون انگشت باقی‌مونده sync می‌کنیم.

`esbuild` روی `ProductTab.jsx` چک شد، خطای سینتکسی نیست.
🔴 تست لازم: (۱) یه محصول بدون عکس توی تب محصولات (نه کاتالوگ) رو باز کن، مطمئن شو زوم/توضیحات نشون داده می‌شه؛ (۲) روی یه عکس دو‌انگشتی زوم کن، یکی از انگشت‌ها رو زودتر از اون‌یکی ول کن — نباید تصویر جهش بزنه.

## آیتم ۵ (بخشی) — Logy

از ۴ زیرمورد آیتم ۵، یکی رو با اطمینان کامل پیدا و فیکس کردم، و بقیه رو تا حد ممکن (بدون دسترسی به گوشی واقعی) بهتر کردم:

- **✅ حذف عکس از محصول، فایل رو از پوشه پاک نمی‌کرد:** `deleteImageFile` (`imageStorage.js`) از قبل وجود داشت و توی `BusinessCardModal.jsx` استفاده می‌شد، ولی `handleRemoveImage` توی `ProductTab.jsx` (فرم ویرایش محصول) هیچ‌وقت صداش نمی‌زد — فقط اسم فایل از رکورد محصول برداشته می‌شد. الان وصل شد.
- **⚠️ «بررسی عکس» کار نمی‌کنه / لینک‌کردن خطا می‌ده:** بدون دسترسی به گوشی واقعی نتونستم دلیل دقیق native رو پیدا کنم (چون هر خطای `Filesystem.readdir` قبلاً بی‌صدا catch می‌شد و `[]` برمی‌گشت — یعنی «خطای واقعی دسترسی به پوشه» با «هیچ عکسی نبود» از دید کاربر قابل‌تشخیص نبود). این بخش رو بازنویسی کردم: الان اگه پوشه اصلاً وجود نداشته باشه (حالت عادیه، مشکلی نیست) بی‌صدا `[]` برمی‌گرده، ولی هر خطای دیگه (دسترسی رد شده، مسیر اشتباه، ...) واقعاً به کاربر نشون داده می‌شه — هم توی دکمه‌ی تکی «بررسی پوشه» (فرم ویرایش محصول) هم دکمه‌ی بولک تب همگام‌سازی.
🔴 **برای پیدا کردن ریشه‌ی واقعی نیاز به این‌هاست:** یه بار دیگه دکمه‌ی «بررسی عکس» رو بزن (تکی یا بولک، هرکدوم راحت‌تره) و این‌بار اگه خطا داد، **متن دقیق پیام خطا** رو بفرست — چون قبلاً این پیام هیچ‌جا دیده نمی‌شد، الان مستقیم روی تست دیده می‌شه.
- **⏸️ بررسی نشد:** «باید حتماً از طریق اپ سینک بشه» — نامشخصه دقیقاً چه فیچری مد نظره (یه گزینه‌ی import/انتخاب عکس از داخل اپ به‌جای کپی دستی با فایل‌منیجر؟)، نیاز به توضیح بیشتر.

## 🆕 درخواست جدید کاربر (این نشست): چرخش ۳ ثانیه‌ای دکمه‌ی رفرش با ضربه‌ی ساده

- [x] وقتی دکمه‌ی رفرش با یه ضربه‌ی ساده (نه نگه‌داشتن) زده می‌شه، آیکونش ۳ ثانیه دایره‌ای بچرخه ✅ (Ash 🟡) — `App.jsx` (`RefreshLockButton`): این جدا از سیستم قدیمی «نگه‌داشتن=بچرخه» (آیتم خط ۷۳۱ که الان با منوی قفل/آزاد/Undo/رفرش جایگزین شده) است. یه state جدید `isSpinning` اضافه شد که با هر ضربه‌ی ساده (چه از خودِ دکمه‌ی دایره‌ای هدر، چه از گزینه‌ی «رفرش» داخل پاپ‌آپ نگه‌داشتن) سه ثانیه `true` می‌مونه و یه انیمیشن CSS چرخش (`ashRefreshSpin`, یک دور در ثانیه، ۳ بار) روی خودِ آیکون `RotateCcw` اعمال می‌شه، بعد خودکار خاموش می‌شه. چون `isSpinning` روی خودِ کامپوننت `RefreshLockButton` هست (نه داخل پاپ‌آپ)، حتی وقتی از داخل منو زده بشه و پاپ‌آپ فوری بسته بشه، چرخش روی آیکون اصلی هدر (که همیشه دیده می‌شه) ادامه پیدا می‌کنه. `esbuild` چک شد، مشکلی نیست.

## 🆕 دنبال‌کاری چرخش رفرش: دابل‌کلیک باید ۴ ثانیه/سریع‌تر/با اینرسی + پوشش کامل «همه‌ی تب‌ها»

- [x] دابل‌کلیک روی دکمه‌ی رفرش (که فیلترهای صفحه‌ی فعلی و همه‌ی تب‌های دیگه رو ریست می‌کنه) باید مشابه چرخش تک‌کلیک باشه ولی ۴ ثانیه، سریع‌تر، و در صورت امکان با اینرسی (شروع تند، کم‌کم آروم و متوقف) ✅ (Ash 🟡) — `App.jsx` (`RefreshLockButton`): `isSpinning` (boolean) به `spinMode` (`null|"tap"|"doubleTap"`) تبدیل شد. تک‌کلیک همون انیمیشن قبلی رو داره (`ashRefreshSpin`, ۱ دور در ثانیه، ۳ بار تکرار، سرعت ثابت). دابل‌کلیک یه keyframe جدا داره (`ashRefreshSpinFast`) که ۵ دور کامل (۱۸۰۰ درجه) رو توی ۴ ثانیه با `cubic-bezier(0.15,0.65,0.35,1)` (easing کندشونده) طی می‌کنه — یعنی چون فاصله‌ی بیشتری رو توی همون بازه طی می‌کنه شروعش سریع‌تر حس می‌شه، و چون یه انیمیشن پیوسته با easing است (نه چندتا تکرار خطی)، طبیعتاً با اینرسی/کندشدن تدریجی می‌ایسته، بدون نیاز به فیزیک واقعی توی جاوااسکریپت.
- [x] بررسی «همه‌ی تب‌های دیگه» هم واقعاً ریست می‌شن یا نه — یه شکاف واقعی پیدا شد: `refreshResetTick` (که ProductTab/MaterialTab/GalleryTab باهاش فیلترهاشون رو ریست می‌کنن) اصلاً به `AccountingTab.jsx` پاس داده نمی‌شد، پس سرچ/فیلتر تاریخ/سورت فاکتورهای اون تب هیچ‌وقت با دابل‌کلیک ریست نمی‌شد. الان `refreshResetTick` prop اضافه شد و یه `useEffect` مشابه بقیه‌ی تب‌ها (`searchQuery`, `filterDate`, `sortOrder` رو به پیش‌فرض برمی‌گردونه) اضافه شد.
- هردو فایل (`App.jsx`, `AccountingTab.jsx`) با `esbuild` چک شدن، مشکلی نیست.
🔴 لطفاً روی build تازه تست کن: هم سرعت/حس اینرسیِ چرخش دابل‌کلیک، هم اینکه بعد از دابل‌کلیک، تب حسابداری (سرچ/فیلتر تاریخ/سورت فاکتورها) هم واقعاً ریست می‌شه.

## آیتم ۳ — تلاش اول (Logy) — جابجایی متن کادر تسویه/هدیه موقع ذخیره PDF/عکس

**تشخیص:** پیش‌نمایش با موتور متن native مرورگر رندر می‌شه (درست)، ولی دکمه‌ی ذخیره‌ی PDF/عکس از `html2canvas` استفاده می‌کنه که موتور متن/بیدی (RTL) خودش رو داره — یه محدودیت شناخته‌شده‌ی `html2canvas`ه که متن ترکیبی (فارسی + ایموجی، یا کلاس CSS خارجی روی متن کوتاه RTL) رو می‌تونه جابه‌جا رندر کنه، درحالی‌که همون DOM توی مرورگر واقعی کاملاً درسته.

**فیکس اعمال‌شده (فقط برای بج‌های وضعیت تسویه/بدهکار/هدیه توی ستون هر آیتم، `InvoiceTemplate.jsx`):**
- `direction:"rtl", unicodeBidi:"plaintext"` صریح روی هر سه بج اضافه شد (که قبلاً به تشخیص خودکار بیدیِ مرورگر متکی بودن).
- توی بج «هدیه»، ترتیب ایموجی و متن عوض شد (متن اول، ایموجی بعد) تا با `direction:rtl` صریح هم‌خون بشه، نه این‌که موتور رندر مجبور به حدس‌زدن ترتیب بصری بشه.

⚠️ **این یه حدس مبتنی بر شناخته‌شده‌ترین دلیل این نوع باگه، نه یه فیکس تاییدشده** — چون بدون گوشی واقعی نمی‌تونم عکس/PDF واقعی خروجی بگیرم و مقایسه کنم. اگه بعد از تست بازم جابجایی هست، لطفاً یه اسکرین‌شات از خودِ فایل PDF/عکس ذخیره‌شده (نه پیش‌نمایش) بفرست تا دقیق‌تر ببینم متن کجا و چطور جابه‌جا شده (روی هم افتاده؟ برعکس شده؟ بیرون از کادر رفته؟).

## آیتم ۴/۷ — کندی ذخیره PDF/عکس فاکتور و نستینگ ✅ فیکس شد (Logy 🟠، طبق تایید صریح مالک)
هر دو مسیر (`InvoicePrint.jsx` برای فاکتور، `WoodCuttingTab.jsx` برای نستینگ ۱D/۲D) از `html2canvas` استفاده می‌کنن. قبلاً `scale` روی PDF فاکتور `2` بود (سریع)، ولی روی **عکس فاکتور `3`** بود (کند — دقیقاً چیزی که کاربر گزارش داد) و نستینگ `2`. مالک صریحاً تایید کرد این trade-off (کیفیت کمی پایین‌تر در ازای سرعت) رو انجام بدیم:
- `InvoicePrint.jsx`: عکس فاکتور (هم Save هم Share، هردو از `captureCanvas`/تابع مجزا استفاده می‌کردن) از `scale:3` → `scale:2` (۴۴٪ پیکسل کمتر، تقریباً هم‌سطح PDF)
- `WoodCuttingTab.jsx`: نستینگ ۱D/۲D از `scale:2` → `scale:1.5`
- **اسپینر روی دکمه‌ها:** یه `busyAction` state به `InvoicePrint.jsx` اضافه شد؛ از لحظه‌ی کلیک تا نمایش toast نهایی (موفق یا خطا)، همون دکمه آیکونش به `RefreshCw` در حال چرخش (Tailwind `animate-spin`، دقیقاً همون کلاسی که دکمه‌ی سینک قبلاً استفاده می‌کرد) تبدیل می‌شه و غیرفعال می‌مونه. هر ۴ دکمه (Save PDF/Image، Share PDF/Image) پوشش داده شدن.
- **دکمه‌ی سینک شناور قرمز:** بررسی شد — از قبل دقیقاً رفتار درست رو داره (`SyncTab.jsx`: فقط وقتی `syncStatus==="syncing"` می‌چرخه، روی `"error"`/`"success"` نمی‌چرخه)؛ تغییری لازم نبود.
- 🔴 لطفاً روی گوشی واقعی تست کن: (۱) سرعت Save/Share عکس فاکتور محسوس بهتر شده یا نه، (۲) اسپینر درست ظاهر و بعد از toast محو می‌شه، (۳) کیفیت عکس فاکتور با چشم قابل‌قبوله (اگه نه، می‌تونیم `scale` رو یکم بالاتر ببریم، مثلاً ۲.۲۵، وسط راه)
- ⏳ ~~باقی‌مونده: دکمه‌های Save Image ۱D/۲D/Both توی `WoodCuttingTab.jsx` هنوز اسپینر ندارن~~ ✅ **همین نشست تکمیل شد (Logy 🟠):** `savingTarget` state (`"1d"|"2d"|"both"|null`) اضافه شد؛ هر ۳ دکمه (۱D، ۲D، دکمه‌ی شناور Both) از کلیک تا پایان واقعی `RefreshCw` در حال چرخش نشون می‌دن و غیرفعال می‌مونن؛ چون دکمه‌ی Both خودش پشت‌سرهم ۱D و ۲D رو صدا می‌زنه، دکمه‌های تکی هم موقع «Both» می‌چرخن (نه فقط دکمه‌ی شناور) تا گمراه‌کننده نباشه

## آیتم ۳ — بررسی مدرک واقعی (Logy)

مالک عکس ذخیره‌شده (PNG کامل بدون chrome اپ)، فایل PDF واقعی، و اسکرین‌شات پیش‌نمایش زنده رو فرستاد. با `pdftoppm` صفحه‌ی PDF رو رندر کردم و پیکسل‌به‌پیکسل بج‌های «هدیه»/«تسویه شده»/«موجود در گالری»، هدر، و فوتر رو بین هر سه منبع مقایسه کردم — **هیچ جابجایی‌ای پیدا نکردم، هر سه دقیقاً یکی بودن.** به‌احتمال زیاد فیکس قبلی (`direction:rtl` + ترتیب ایموجی) کار کرده و همین PDF/عکسی که فرستاده شد از build جدید بوده. اگه بازم جایی جابجایی می‌بینی، لطفاً دقیقاً بگو کدوم ردیف/کدوم بخش (شماره ردیف کافیه)، چون با چشم خودم رو کل فاکتور نتونستم پیدا کنم.

## آیتم ۵ (تکمیل توضیح مالک) + فیچر جدید ترتیب عکس‌ها — Logy

مالک روشن کرد منظورش از «باید حتماً از طریق اپ سینک بشه» دقیقاً چیزی بود که از قبل پیاده‌سازی شده: دکمه‌ی «بررسی پوشه» توی فرم ویرایش فقط برای همون یه محصول، و دکمه‌ی بولک توی تب همگام‌سازی برای همه‌ی محصولات — هر دو از قبل دقیقاً همین‌طوری کار می‌کنن (نشست‌های قبلی). چیز جدیدی برای پیاده‌سازی نبود، فقط تاییدِ رفتار موجود.

**فیچر جدید:** «ترتیب عکس‌های یه محصول قابل‌جابجایی بود» — هیچ ردی از drag-and-drop قبلی توی کل کدبیس (`ProductTab`, `BusinessCardModal`, `GalleryTab`, `MaterialTab`) پیدا نشد؛ یا هیچ‌وقت push نشده بود، یا اشتباه به یاد مونده. چون drag-and-drop واقعیِ HTML5 روی وب‌ویوی موبایل/لمسی قابل‌اعتماد نیست، به‌جاش دو تا دکمه‌ی کوچیک ‹ › روی هر تصویر اضافه شد که جاش رو با همسایه‌ش عوض می‌کنه (عکسِ اول = آیکون محصول، طبق لیبل از قبل موجود «اول = آیکون»).

`esbuild` چک شد، خطای سینتکسی نیست.
🔴 تست لازم: توی فرم ویرایش یه محصول با چند عکس، دکمه‌های ‹ › رو امتحان کن؛ ترتیب باید عوض بشه و عکس اول («آیکون») درست آپدیت بشه.

## آیتم ۳ — فیکس واقعی پیدا شد (Logy) — ادامه‌ی تحقیق با مدرک جدید

مالک یه فاکتور دیگه فرستاد که این‌بار مشکل توش واضح بود: بج‌های وضعیت (تسویه‌شده/بدهکار/هدیه/درصد تخفیف) متن‌شون به پایین کادر چسبیده (نه وسط‌چین)، و توی ستون ابعاد، «۴عدد» به «۴ع‌دد» شکسته شده بود.

**ریشه‌ی واقعی (این‌بار قطعی، با مدرک):**
1. **بج‌ها:** `.invoice-table td { vertical-align: middle; }` یه محدودیت خیلی شناخته‌شده‌ی `html2canvas`ه — برخلاف مرورگر واقعی، `html2canvas` این مقدار رو قابل‌اعتماد رعایت نمی‌کنه. فیکس: به‌جای تکیه به `vertical-align` روی خودِ `<td>`، یه `<div>` با `display:flex, alignItems:center, justifyContent:center, height:100%` دور محتوای هر دو ستون (قیمت/تخفیف و وضعیت) اضافه شد — این‌جوری وسط‌چین‌شدن به فلکس‌باکس (که html2canvas خیلی بهتر می‌فهمتش) وابسته‌ست، نه vertical-align.
2. **«۴عدد» شکستن:** `.invoice-table td { word-break: break-word; }` قانون قدیمی/تهاجمی‌ای‌‌ه که اجازه می‌ده متن از وسط هر کلمه بشکنه اگه عرض کافی نباشه — و چون `html2canvas` عرض ستون رو گاهی یه‌خرده متفاوت از مرورگر واقعی حساب می‌کنه، دقیقاً وسط کلمات فارسی می‌شکست. تبدیل شد به `overflow-wrap: break-word` (فقط به‌عنوان آخرین راه‌حل می‌شکنه، نه هر جا خواست)، و ستون ابعاد هم مستقیم `white-space: nowrap` گرفت (چون متن ابعاد همیشه کوتاهه، اصلاً نباید بشکنه).

`esbuild` چک شد، خطای سینتکسی نیست.
🔴 این‌بار مطمئن‌ترم چون دلیلش با مدرک واقعی (نه فقط حدس) پیدا شد — ولی بازم لطفاً بعد از بیلد جدید یه فاکتور واقعی بساز و چک کن.

## آیتم ۳ — دور سوم، فیکس تدافعی‌تر (Logy)

مالک بعد از فیکس قبلی (`a676d67`) بازم همون علائم رو با فاکتور تازه نشون داد (بج‌ها هنوز جای درست خودشون نیستن، خط‌تیره‌ی قیمت قبل تخفیف انگار یه Enter اضافه داره رفته بالا). دو تا احتمال: (۱) build تست‌شده هنوز کامیت جدید رو نداشت، (۲) فیکس قبلی ناکافی بود. چون نمی‌تونم مطمئن باشم کدومه، این دور یه لایه فیکس تدافعی‌تر هم اضافه شد:

- **`gap` توی flexbox تو در تو حذف شد** (جایگزین با `margin` مستقیم روی هر child) — پشتیبانی `gap` توی flexbox یه نقطه‌ضعف شناخته‌شده‌ی نسخه‌های قدیمی‌تر `html2canvas`ه؛ اگه نسخه‌ی نصب‌شده‌ی پروژه ازش پشتیبانی نکنه یا اشتباه حساب کنه، دقیقاً می‌تونه باعث فاصله‌ی عجیب («انگار Enter زده») بشه. این فقط توی سلول قیمت/تخفیف/وضعیت انجام شد (بقیه‌ی جاهای فایل که از `gap` استفاده می‌کنن دست نخوردن، چون توی مقایسه‌ی قبلی هدر/فوتر مشکلی توشون دیده نشده بود).
- **`width:"100%", boxSizing:"border-box"` صریح** به هر دو `div` پوششی اضافه شد — احتمال دیگه این بود که `div` فلکس بدون عرض صریح، توی محاسبه‌ی html2canvas به‌جای پرکردن کل سلول، فقط به‌اندازه‌ی محتوا جمع بشه؛ یعنی `justifyContent:center` هیچ فضایی برای وسط‌چین‌کردن نداشته باشه.

`esbuild` چک شد.

⚠️ **قبل از تست بعدی حتماً مطمئن شو که build از commit `a676d67` به بعد (این کامیت هم) رو واقعاً pull کرده** — چون اگه build قدیمی تست بشه، طبیعتاً همون مشکلات قبلی رو دوباره می‌بینیم و نمی‌تونیم بفهمیم فیکس واقعاً جواب داده یا نه.

### 🆕 دسته‌ی فیدبک کاربر (۱۱ آیتم، دامپ عکس‌های متعدد) — ✅ بخش زیادی توسط Ref، بخشی توسط Logy موازی
1. [x] جابه‌جایی عمودی دکمه‌های سورت/فیلتر توی ردیف هدر — ✅ (Ref) چند دکمه (SortButton، فیلتر نوع متریال، فیلتر لوکیشن، در ۴ کپی مختلف بین تب‌ها) هنوز padding عمودی قدیمی (`6px`) از قبل از کاهش ارتفاع کلی داشتن که با ارتفاع ثابت ۲۲px هم‌راستا نبود؛ نرمال شد به `2px` مثل بقیه
2. [x] فیلد جستجوی سریع تب متریال (داخل پیکر تخصیص بولک) موقع تایپ عریض می‌شد و می‌رفت روی دکمه‌های قبلی — ✅ (Ref) `minWidth:0` روی input و container‌ش جا افتاده بود (باگ کلاسیک فلکس‌باکس)، فیکس شد
3. متن جابه‌جاشده توی PDF/عکس فاکتور (وضعیت تسویه/هدیه) — 🔍 Logy چند دور روش کار کرد (بی‌بی‌سی/rtl، vertical-align در html2canvas، flexbox gap در نسخه‌های قدیمی html2canvas) — نیاز به تأیید کاربر با تست واقعی
4/7. کندی زیاد ذخیره عکس/PDF فاکتور و نستینگ ۱D/۲D — 🔍 Logy بررسی کرد: محدودیت ذاتی html2canvas با SVG/چند-عکسه، به نظر باگ خاصی نیست؛ یه گزینه (کاهش scale از ۲ به ۱.۵) رو برای تصمیم مالک پرچم کرده (کیفیت کمتر در ازای سرعت بیشتر)
5. بررسی پوشه عکس (تکی/بولک) کار نمی‌کنه، لینک دستی خطا می‌ده، حذف عکس از پوشه هم پاک نشه — 🔍 Logy تأیید کرد رفتار فعلیِ تکی/بولک با تفسیر مالک یکی‌ست؛ بخش خطای لینک دستی و حذف‌ازپوشه هنوز باز مونده
6. [x] **باگ حیاتی — آزادسازی بولک متریال قفل‌شده** — ✅ (Ref) دکمه‌ی X کنار محصول قفل‌شده (توی «محصولات قفل‌شده، مستقل از تخصیص جدید») مستقیم روی کلیک `setData` می‌زد (نه با «ذخیره»/«لغو» هماهنگ بود) **و** درصدِ آزادشده رو خودکار بین بقیه‌ی اعضای همون سشن قفل بازتقسیم می‌کرد — درحالی‌که «قفل‌شدن» یعنی درصدها فیکس شدن، نباید دست‌کاری بشن. الان: X فقط یه صف لوکاله (هیچی commit نمی‌شه)، ردیف با رنگ آبی/خط‌خورده نشون داده می‌شه، فقط «ذخیره» واقعاً `pendingUnlock` می‌ذاره (بدون تغییر درصد بقیه)، «لغو» بی‌خطر صرف‌نظر می‌کنه، و X بالای پنل اگه تغییر ذخیره‌نشده باشه یه پاپ‌آپ تایید نشون می‌ده
8. [x] دکمه‌ی سبد خرید تب محصولات — ✅ (Ref) استثنا از کاهش کلی ارتفاع، مستقیم ۳۲px شد
9. [x] بک سخت‌افزاری/ژست اندروید موقع باز بودن پنل، بدون تایید می‌رفت کاتالوگ — ✅ (Ref، هماهنگ با فیکس موازی Wall/Ash روی همین آیتم) بک‌هندلر جدا حذف شد، الان از همون پاپ‌آپ «تایید خروج» رد می‌شه که دکمه‌ی خروج توی برنامه هم داره
10. [x] توضیحات محصول بدون‌عکس در بزرگ‌نمایی کاتالوگ / بولدتر شدن اسم‌وکد / خط خالی قبل توضیحات — ✅ (Ref، تکمیل شد) بررسی کامل شد: توضیحات از قبل مستقل از وجود عکس نمایش داده می‌شه (هیچ شرطی روی `currentImg` توی `ImageLightbox` پیدا نشد که نمایش بخش اطلاعات رو مشروط کنه)، و خط خالی قبل توضیحات هم از قبل بود (`<div style={{height:10}} />` قبل بلوک description). تنها چیزی که واقعاً باقی مونده بود رو فیکس کردم: هدر اسم+کد بالای بزرگ‌نمایی از ۱۴px/۷۰۰ به ۱۶px/۸۰۰ رفت (بزرگ‌تر و بولدتر)
11. [x] **باگ — Undo برای آزادسازی متریال کار نمی‌کرد** — ✅ (Ref) `restoreLi` (تابع کمکی reverseOperation) وقتی لاین‌آیتم رو با id پیدا نمی‌کرد بی‌صدا هیچ کاری نمی‌کرد — و دقیقاً بعد از یه «آزادسازی واقعی»، لاین‌آیتم کاملاً از محصول حذف می‌شه (نه فقط فلگ می‌خوره)، پس Undo موجودی متریال رو برمی‌گردوند ولی خودِ محصول هیچ ردی از اون متریال نداشت. الان اگه لاین‌آیتم پیدا نشه، دوباره به محصول اضافه می‌شه.

🔴 نیاز به تست واقعی مخصوصاً آیتم ۶ (آزادسازی بولک — با چند محصول قفل‌شده تست کن: X بزن، لغو کن، دوباره X بزن، ذخیره کن، ببین فقط همون یکی آزاد می‌شه و بقیه دست‌نخورده می‌مونن) و آیتم ۱۱ (یه متریال رو آزاد کن، Undo بزن، ببین محصول واقعاً برمی‌گرده)

> **یادداشت ادغام (Ref):** من هم موازی (بدون دیدن کار Logy) دقیقاً همین ۳ کار رو انجام داده بودم — کاهش `scale` عکس فاکتور از ۳ به ۲، اسپینر روی دکمه‌های PDF/عکس/اشتراک فاکتور (اسمش `savingAction` بود، مال Logy `busyAction`)، و اسپینر دکمه‌های ۱D/۲D/Both (هردومون `savingTarget`). موقع merge یکی‌شون کردم — منطق و نتیجه‌ی نهایی دقیقاً هم‌راستا بود، فقط اسم متغیرها فرق داشت. `WoodCuttingTab.jsx` scale (۲→۱.۵) دست‌نخورده موند چون فقط Logy روش کار کرده بود.

### 🆕 سه فیدبک UI کاتالوگ/رفرش — ✅ (Ref ⚫)
- [x] جهت سوایپ روی عکس بزرگ‌نمایی‌شده برعکس بود — الان سوایپ به راست = بعدی، سوایپ به چپ = قبلی
- [x] دکمه‌های فلش چپ/راست کنار بزرگ‌نمایی — قبلاً اول عکس بعدی/قبلی همین محصول رو نشون می‌دادن و فقط وقتی عکس‌ها تموم می‌شد می‌رفتن سراغ محصول بعدی/قبلی. الان این دکمه‌ها همیشه مستقیم می‌رن محصول بعدی/قبلی (forward/backward واقعی) — چرخیدن بین عکس‌های همون محصول فقط با سوایپ انجام می‌شه
- [x] جهت انیمیشن چرخش آیکون رفرش (کلیک/دابل‌کلیک/نگه‌داشتن) برعکس شد
🔴 نیاز به تست واقعی هر سه مورد

## 🆕 گروه‌بندی تب کاتالوگ (Logy) — فیچر جدید کاربر

مثل تب محصولات، دکمه‌ی Sort تب کاتالوگ الان یه گزینه‌ی گروه‌بندی هم داره:
- **دسته‌بندی (پیش‌فرض):** محصولات بر اساس نوع (`productTypeId`) دسته می‌شن، هر دسته با لیبل اسم نوع + تعداد؛ داخل هر دسته، موجود (`status==="available"`) بالا، ناموجود (فروخته‌شده) پایین.
- **یکجا:** لیست تخت قبلی، بدون تغییر.

پیاده‌سازی: کامپوننت مشترک `SortButton` (که تب محصولات هم استفاده می‌کنه) گسترش پیدا کرد تا وقتی هیچ‌کدوم از `onGroupByFabric`/`onGroupByType` پاس داده نشن، یه گزینه‌ی ساده‌ی «دسته‌بندی» نشون بده (بدون تغییر رفتار تب محصولات که هر دو رو پاس می‌ده). تایل‌های کارت محصول به یه تابع `renderTile` مشترک استخراج شدن تا بین حالت گروهی/تخت تکرار نشه.

`esbuild` چک شد. هم‌زمان با کار Ref (سواپ جهت swipe/دکمه‌های پیکان لایت‌باکس، اسپینر رفرش) merge شد، بدون تداخل.
🔴 تست لازم: تب کاتالوگ رو باز کن، پیش‌فرض باید دسته‌بندی‌شده باشه با موجودها بالا؛ دکمه‌ی sort رو بزن، «یکجا» رو امتحان کن.

## 🆕 نستینگ ۱D/۲D — دور اول (Logy)

- [x] **کادر قرمز دور خطای کمبود ۱D حذف شد** — دیگه بک‌گراند/بردر قرمز نداره، دقیقاً شبیه ۲D (فقط متن+آیکون). محدودیت عرض (`maxWidth: plankContainerWidth`) که قبلاً Ash اضافه کرده بود دست‌نخورده موند، پس همچنان از بخش خودش بزرگ‌تر نمی‌شه و باعث اسکرول نمی‌شه.
- [x] **آیکون دایره‌ضربدر از ۱D به ۲D کپی شد** — ۲D قبلاً از ایموجی ⛔ استفاده می‌کرد؛ الان همون svg دایره+ضربدر ۱D رو داره، با همون رفتار wrap-safe (`flex:1, minWidth:0, whiteSpace:normal, wordBreak:break-word`) که اگه متن جا نشه از خط بعد ادامه پیدا کنه بدون رفتن روی خط قبل.

`esbuild` چک شد.

🔴 **دو مورد باقی‌مونده که نیاز به مدرک بیشتر دارن (بدون گوشی واقعی نمی‌تونم مطمئن شم):**
- **نوار مشکی سمت راست عکس ذخیره‌شده‌ی ۲D:** کد capture (`html2canvas` با `width/height` از `scrollWidth/offsetWidth` محاسبه می‌شه) رو بررسی کردم، هیچ padding/margin نامتقارن آشکاری پیدا نکردم که توضیح بده. برای پیدا کردن دقیق نیاز به یه اسکرین‌شات از خودِ عکس ذخیره‌شده دارم (نه پیش‌نمایش) تا ببینم دقیقاً چقدر عرض نوار مشکیه و کجای عکسه.
- **گم‌شدن لیبل عدد قطعات کوچک ۱D:** فرضیه‌ی مالک (کادر خطای کمبود اندازه‌ی صفحه رو قفل می‌کنه و نمی‌ذاره برای لیبل‌ها جا باز شه) با فیکس بالا (حذف کادر قرمز) ممکنه خودش تا حدی حل شده باشه چون دیگه یه المان اضافه‌ی صلب نیست. ولی فیچر درخواستی («بخش ۱D به اندازه‌ی عرض لیبل قابل‌اسکرول بشه») هنوز پیاده نشده — نیاز به بررسی دقیق‌تر منطق رسم SVG قطعات (که هنوز پیداش نکردم دقیقاً کجاست) داره؛ نشست بعدی.

## 🆕 هم‌راستایی ارتفاع دکمه‌های هدر (Sort/فیلتر) توی چند تب — گزارش با اسکرین‌شات (Logy 🟠)

کاربر با اسکرین‌شات نشون داد دکمه‌های ردیف هدر (Sort، فیلتر نوع، فیلتر موجودی/مخفی، «همه») توی بعضی تب‌ها هم‌ارتفاع نیستن، با اینکه آیتم ۱ (بالاتر توی رودمپ) قبلاً همین موضوع رو در سطح کلی فیکس کرده بود. علتش این بود: `S.chip`/`T.chip` پایه یه ارتفاع ثابت داره (`height:22, minHeight:22, padding:"2px 9px"`, `boxSizing:"border-box"`)، ولی چندجا موقع override کردن استایل، `padding` رو صریح به `"6px ..."` عوض کرده بودن بدون توجه به اینکه با `height:22` ثابت، این padding باعث می‌شه محتوا (آیکون) از باکس ۲۲پیکسلی بزنه بیرون و باکس عملاً بلندتر از بقیه بشه — دقیقاً همون چیزی که تو اسکرین‌شات‌ها معلوم بود.

فیکس شد (۲ مورد پیدا شد، هردو حذف override و برگشت به padding پایه‌ی `S.chip`/`T.chip`):
- [x] `MaterialTab.jsx` — دکمه‌ی چشم (نمایش/پنهان‌کردن متریال‌های مخفی/موجودی صفر) کنار فیلتر نوع متریال: `padding:"6px 8px"` بود
- [x] `GalleryTab.jsx` — دکمه‌ی «همه»ی `GalleryStatusFilter` (فیلتر بدهکار/دارای‌موجودی): `padding:"6px 10px"` بود

`esbuild` هردو فایل چک شد.
🔴 لطفاً دوباره اسکرین‌شات بگیر از همون تب‌هایی که تو عکس‌ها بودن (متریال، محصولات/کاتالوگ) با چند حالت مختلف sort — اگه بازم جایی نامنظم بود دقیقاً بگو کدوم تب/دکمه چون ممکنه یه مورد مشابه‌ی دیگه جا مونده باشه که با گشتن کد پیداش نکردم (چون بدون گوشی واقعی نمی‌تونم چشمی چک کنم).

**آپدیت (Logy 🟠):** طبق پیشنهاد مالک، اضافه‌تر از فیکس padding بالا، ردیف هدر هر ۴ تب (محصولات، گالری، متریال، کاتالوگ) از `alignItems:"center"` به `alignItems:"flex-start"` تغییر کرد — یعنی همه‌ی دکمه‌ها/باکس جستجو از بالا با هم هم‌تراز می‌شن، نه از وسط. این یه فیکس مقاوم‌تره: حتی اگه یه دکمه به هر دلیلی (متن بلندتر، حالت خاص) کمی بلندتر از بقیه بشه، چون از بالا start شدن، خط بالای همه ثابت می‌مونه و نامنظمی کمتر به چشم میاد.

## 🆕 هاید/آنهاید مشتری‌وگالری + یادآوری گالری توی سبد خرید + کد محصول توی ردپای تغییرات (Wall 🟣)

کاربر یه فیچر جدید خواست (بعد از خالی شدن آیتم‌های نستینگ):

- [x] تب گالری: دکمه‌ی هاید/آنهاید کنار هر مشتری/گالری (دقیقاً مثل دکمه‌ی مشابه متریال) — `GalleryTab.jsx`: یه دکمه‌ی Eye/EyeOff بین ویرایش و حذف اضافه شد، `customer.hidden` رو toggle می‌کنه (`setData` مستقیم، چون این کامپوننت از قبل بهش دسترسی داشت)؛ کارت با `opacity:0.45` کم‌رنگ می‌شه وقتی هایده — دقیقاً رفتار متریال.
- [x] وقتی مشتری/گالری هاید شد: توی خودِ تب گالری فقط کم‌رنگ بمونه (نه حذف) ✅ همون `opacity`.
- [x] ولی توی لیست‌های **انتخاب** (نه نمایش) — پیکر گالری روی کارت محصول (اکسپند ارجاع)، `handleGalleryChange`، `CustomerSmartSelect`/`SmartLocationSelect` توی فرم فروش محصول، `buyerList` توی مودال فروش، و لیست گالری‌های فیلتر «موقعیت مکانی» (هم تب محصولات هم کاتالوگ) — همه‌جا `!c.hidden` اضافه شد (۷ نقطه‌ی مجزا توی `ProductTab.jsx`). چون فیلتر «موقعیت مکانی» از همون `galleryCustomers` تغذیه می‌کنه، خودکار هم پوشش داده شد.
- [x] خودِ محصول توی لیست اصلی هاید نشه — دست‌نخورده موند (هیچ فیلتری روی خودِ محصولات اعمال نشد، فقط روی لیست‌های انتخاب مشتری/گالری).
- [x] توی سبد خرید (هم مشتری هم گالری، چون هردو از یه `BasketPanel` مشترک استفاده می‌کنن)، موقع افزودن محصول، اگه محصول از قبل پیش یه گالری باشه (`status:"available"` و `location` = یه گالری)، جلوش نوشته می‌شه «— پیش [نام گالری]» — دقیقاً همون قرارداد «پیش گالری» که فیلتر لوکیشن از قبل استفاده می‌کنه.
- [x] توی «ردپای تغییرات» (تب همگام‌سازی)، جلوی محصولات کدشون هم نوشته بشه — `syncManager.js`: `entityDisplayName()` (تابع مشترکی که متن نمایشیِ هر رکورد رو برای لاگ می‌سازه، برای همه‌ی موجودیت‌ها: محصول/متریال/مشتری/...) الان اگه رکورد فیلد `code` داشته باشه (فقط محصولات دارن، بقیه ندارن، پس تداخلی نیست) با فرمت `#۰۰۰۴ اسم محصول` نشونش می‌ده.

`esbuild` روی `GalleryTab.jsx`، `ProductTab.jsx`، و `syncManager.js` چک شد.
🔴 نیاز به تست واقعی: یه گالری/مشتری رو هاید کن، مطمئن شو (۱) توی تب گالری کم‌رنگ می‌مونه نه حذف، (۲) توی پیکرهای ارجاع/فروش/سبدخرید/فیلتر لوکیشن دیگه نشون داده نمی‌شه، (۳) خودِ محصولات متصل بهش هنوز عادی توی لیست اصلی دیده می‌شن. همچنین یه محصول رو ویرایش کن و ردپای تغییرات رو چک کن ببین کدش جلوش اومده.

## 🆕 فیدبک کاربر: ریگرشن هدر + برعکس‌بودن جهت لایت‌باکس (Wall 🟣)

- [x] دکمه‌های هدر (سورت/نوع/موجودی/چشم/تگ) بعد از تغییر `alignItems` به `flex-start` بهم ریختن — همه به‌جز سورت رفتن بالا ✅ (Wall 🟣) — **ریشه‌ی واقعی:** خودِ باکس جستجو (`padding:"6px 10px"`, بدون ارتفاع ثابت) طبیعتاً ~۲۸px بلند می‌شه، در حالی که دکمه‌های چیپ (`S.chip`/`T.chip`) دقیقاً ۲۲px ثابتن. با `alignItems:center` (قبلی) این ۶px اختلاف با سنتر شدن مخفی می‌موند؛ با `alignItems:flex-start` (فیکس قبلی Logy برای یه مشکل دیگه)، این اختلاف آشکار شد: باکس جستجو (بلندترین عضو ردیف) سرجاش موند ولی چیپ‌های ۲۲px دیگه اون ۳px حاشیه‌ی بالای ناشی از سنتر شدن رو نداشتن، پس ۳px بالاتر از قبل به نظر می‌رسیدن. **فیکس:** باکس جستجو (هر ۴ نمونه‌ش — تب محصولات، تب کاتالوگ، تب متریال، تب گالری) الان `height:22, minHeight:22, boxSizing:border-box` گرفت، دقیقاً هم‌ارتفاع چیپ‌ها — دیگه اصلاً اختلاف ارتفاعی نیست که `flex-start` بخواد آشکارش کنه.
- [x] دکمه‌های forward/backward بزرگ‌نمایی عکس (لایت‌باکس) باید برعکس بشن — چپ/راست جابه‌جا عمل کنن ✅ (Wall 🟣) — `ImageLightbox` توی `ProductTab.jsx` (یه کامپوننت مشترک، هم برای تب محصولات هم کاتالوگ استفاده می‌شه): موقعیت دکمه‌ها (چپ/راست صفحه) دست‌نخورده موند، فقط عملکردشون جابه‌جا شد — دکمه‌ی چپ الان «محصول بعدی» و دکمه‌ی راست الان «محصول قبلی»ه (قبلش برعکس بود).

`esbuild` روی `MaterialTab.jsx`, `ProductTab.jsx`, `GalleryTab.jsx` چک شد.
🔴 لطفاً تست کن: تب متریال/محصولات/کاتالوگ/گالری رو باز کن، ببین همه‌ی دکمه‌های هدر (سورت، جستجو، فیلترها) دقیقاً هم‌ردیف و هم‌ارتفاعن؛ و توی لایت‌باکس عکس محصول، دکمه‌ی چپ بره جلو (محصول بعدی) و راست بره عقب (محصول قبلی).
